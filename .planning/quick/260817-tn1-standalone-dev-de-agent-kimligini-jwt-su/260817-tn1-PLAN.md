---
phase: quick-260817-tn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/grispi-environment.ts
  - src/lib/standalone-dev.ts
  - src/lib/__tests__/standalone-dev.test.ts
  - src/contexts/grispi-context.tsx
  - src/screens/components/__tests__/thread-components.test.tsx
  - README.md
autonomous: true
requirements: [UX-05]
quick: true
closes:
  - "04.2-REVIEW.md CR-02 — kimlik KAYNAĞI yarısı (hardcoded geliştirici kimliği). Köprü-seçim yarısı (grispi-context.tsx:87) kapsam dışıdır."

must_haves:
  truths:
    - "Standalone dev modunda agent e-postası, modu zaten aktive eden token'ın `sub` claim'inden türetilir — depoda hiçbir kişisel e-posta veya kişi adı sabiti kalmaz"
    - "Açık operatör override'ı (`REACT_APP_DEV_AGENT_EMAIL`) `sub`'ı YENER; boş/yalnızca-boşluk bir override `sub`'a düşer, `null`'a DÜŞMEZ (sıralama kanıtlanır)"
    - "Token çözülemiyorsa, `sub` yoksa, boşsa veya string değilse `agentEmail` `null` olur ve hiçbir istisna fırlatılmaz"
    - "`agentName` için token kaynağı YOKTUR: yalnızca `REACT_APP_DEV_AGENT_NAME` veya `null`. `sub` çözülse bile `agentName`'e sızmaz"
    - "`agentEmail` `null` iken compose/chat gönderim kapıları kapalı kalır (fail-closed) — kimlik bilinmiyorsa canlı API'ye `creator` yazılmaz"
    - "`agentName` `null` iken pending/own mesajın etiketi `senderEmail`'e düşer; `thread-message.tsx` üretim kodunda DEĞİŞİKLİK GEREKMEZ ve bu davranış testle kanıtlanır"
    - "Plugin modu (`bundle.context.agent.email` / `.fullName`) baştan sona dokunulmadan kalır"
    - "Silinen sembollerin adları hiçbir kaynak dosya yorumunda kalıntı bırakmaz"
    - "Mevcut 527 testin tamamı yeşil kalır; `tsc --noEmit` exit 0"
  artifacts:
    - "src/lib/grispi-environment.ts — `DecodedJwt`'e `sub?: string` alanı (tek satırlık tip genişletmesi)"
    - "src/lib/standalone-dev.ts — kimlik sabitleri silinmiş, `agentEmail`/`agentName` null-able, `sub` tabanlı çözüm"
    - "src/lib/__tests__/standalone-dev.test.ts — kimlik testleri YERİNDE yeniden yazılmış (ikinci dosya açılmaz)"
    - "src/contexts/grispi-context.tsx — silinmiş sembolü adlandıran doc comment'ler güncellenmiş (yalnızca yorum)"
    - "src/screens/components/__tests__/thread-components.test.tsx — `agentName={null}` bozulmama kanıtı (tek senaryo eklenir)"
    - "README.md §5 — standalone kimlik kaynağını anlatan tek cümle düzeltmesi"
  key_links:
    - "`REACT_APP_DEV_TOKEN` → `parseJwt(token)?.sub` → `StandaloneDevConfig.agentEmail` → `setAgentEmail` → `useGrispi().agentEmail` → `ComposeStore.submit` → `creator: [{ key: \"us.email\" }]` — CR-02'nin bildirdiği zincirin BAŞI artık token'dır, sabit değil"
    - "`StandaloneDevConfig.agentEmail === null` → `compose-screen.tsx:47` / `chat-screen.tsx:240` `!agentEmail` → `sendDisabled` (fail-closed kapı)"
    - "`StandaloneDevConfig.agentName === null` → `ThreadMessage.agentName` → `senderLabel` → `message.senderEmail` (`active-conversation-store.ts` optimistic mesajda bu alan DOLU)"
    - "`grispi-environment.ts` `DecodedJwt.sub` ⟷ `standalone-dev.ts`'in `sub` okuması — tek JWT parser, iki tüketici (`dev` claim + `sub`)"
  prohibitions:
    - statement: "`src/contexts/plugin-bootstrap.ts` DEĞİŞTİRİLMEZ — plugin modu kimliği bundle'dan doğru okuyor"
      status: must-not
      verification: "`git status --porcelain -- src/contexts/plugin-bootstrap.ts` çıktısı BOŞ"
    - statement: "`grispi-context.tsx`'in `const plugin = standaloneConfig ? null : getPluginInstance()` köprü-seçim satırı DEĞİŞMEZ — bu CR-02'nin ayrı ve daha riskli yarısı"
      status: must-not
      verification: "`grep -c 'standaloneConfig ? null : getPluginInstance()' src/contexts/grispi-context.tsx` → `1`"
    - statement: "`!agentEmail` gönderim kapıları kaldırılmaz veya zayıflatılmaz — kimlik bilinmiyorken gönderimin reddedilmesi İSTENEN davranıştır"
      status: must-not
      verification: "`grep -c '!agentEmail' src/screens/compose-screen.tsx` → `2` VE `grep -c '!agentEmail' src/screens/chat-screen.tsx` → `2`"
    - statement: "Canlı API'ye hiçbir istek atılmaz — probe yok, curl yok, `npm start` ile manuel deneme yok"
      status: must-not
      verification: "SUMMARY'de hiçbir canlı çağrı kaydı yoktur; doğrulamanın tamamı jest + tsc + grep'tir"
    - statement: "`DEFAULT_DEV_TENANT_ID`, `DEFAULT_DEV_TICKET_KEY` ve `DEFAULT_DEV_ENVIRONMENT` ile ilgili hiçbir şey değişmez (bunlar ortam/yönlendirme yapılandırmasıdır, kimlik değil)"
      status: must-not
      verification: "`grep -c 'DEFAULT_DEV_TENANT_ID' src/lib/standalone-dev.ts` → `3` VE `grep -c 'DEFAULT_DEV_TICKET_KEY' src/lib/standalone-dev.ts` → `2` VE `grep -c 'DEFAULT_DEV_ENVIRONMENT' src/lib/standalone-dev.ts` → `2` (üçü de planlama anındaki değerleri) VE bu üçünün mevcut testleri değiştirilmeden yeşil"
    - statement: "İkinci bir JWT çözücü YAZILMAZ — `parseJwt` yeniden kullanılır"
      status: must-not
      verification: "`grep -c 'atob(' src/lib/standalone-dev.ts` → `0` VE `grep -c 'grispi-environment' src/lib/standalone-dev.ts` → `1`"
    - statement: "`.planning/phases/04.2-*/` altındaki hiçbir dosyaya dokunulmaz — o faz UAT bekliyor"
      status: must-not
      verification: "`git status --porcelain -- .planning/phases/` çıktısı BOŞ"
---

<objective>
Standalone dev modunda agent kimliğini token'ın kendi `sub` claim'inden türet; `src/lib/standalone-dev.ts`'teki iki hardcoded kimlik sabitini (kişisel e-posta ve kişi adı) tamamen kaldır.

Purpose: 04.2 kod incelemesinin **CR-02** bulgusu bu iki sabiti canlı bir API yazımına kadar izledi: `standalone-dev.ts:79` → `grispi-context.tsx:87` → `compose-store.ts:203` → `creator: [{ key: "us.email", value: agentEmail }]`. Yani genel bir plugin, tek bir geliştiricinin kimliğiyle yan konuşma açabiliyor. Kimlik ya token'dan ya API'den gelmeli; bir sabitten asla.

Bu değişikliğin dayanağı zaten doğrulanmış durumda — YENİDEN TÜRETİLMESİ GEREKMEZ:

1. **JWT `sub` = e-posta.** İki gerçek token üzerinde teyit edildi. Standalone mod token olmadan hiç aktive olmadığı için e-posta her zaman elde edilebilir durumdadır.
2. **JWT'de ad claim'i YOK.** İki örnek payload'da da alanlar `aud, sub, dev, roles, iss, exp, iat, jti`. `name`/`fullName` yok — bu yüzden ad token'dan GELEMEZ.
3. **Plugin modu ikisini de doğru okuyor** (`plugin-bootstrap.ts:106-107`: `bundle.context.agent?.email` / `?.fullName`) ve bu yol olduğu gibi kalır.
4. **API her sunucu-dönüşü mesaj için yazar kimliğini veriyor** (`Comment.creator: User`, `side-conversation-queries.ts:442-443` → `senderName`/`senderEmail`). Ad yalnızca bir mesajın sunucuya gidip dönmesinden ÖNCEKİ kısa optimistic pencerede eksiktir.
5. **`senderLabel` `null` ad ile zaten doğru bozuluyor** (`thread-message.tsx:44-72`): `senderName || pendingOwnFallback || senderEmail || "Gönderen"`. Optimistic own mesajın `senderEmail`'i `active-conversation-store.ts:339`'da dolu. Yine de VARSAYMA — testle kanıtla.
6. **Fail-closed davranış tasarım gereği zaten var** (`compose-screen.tsx:45-52`, `chat-screen.tsx:240`): `!agentEmail` gönderimi kapatır. `null` e-posta gürültülü ve doğru bir ret üretir — kimlik bilinmiyorken olması gereken tam olarak budur.
7. **`parseJwt` zaten mevcut** (`grispi-environment.ts:22`, hâlihazırda `dev` claim'i için kullanılıyor). `DecodedJwt`'in yalnızca tipli bir `sub` üyesine ihtiyacı var.

Output: `sub` tabanlı, null-able ve çökmeyen bir kimlik çözümü; yerinde yeniden yazılmış testler; silinen sembollere atıf bırakmayan güncel doc comment'ler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
</context>

<interface_context>

**Zincirin okunmuş hâli (yeniden okumaya gerek yok, gerekli olan burada):**

- `parseJwt(token: string): DecodedJwt | null` — `src/lib/grispi-environment.ts:22`. Gövdesinin tamamı `try/catch` içinde: bozuk/JWT-olmayan token (`"tok"`, `"a.b"`, `""`) `null`'a düşer, ASLA fırlatmaz (T-04.1-04). `DecodedJwt` **export EDİLMEZ** (modül-içi `interface`), şekli `{ dev?: boolean; [key: string]: unknown }`. Index signature `unknown` olduğu için `sub` şu an tipsizdir.
- `resolveStandaloneDevConfig(env, locationSearch)` — `src/lib/standalone-dev.ts:62`. Saf resolver; `process.env`/`window`'a dokunmaz. Aktivasyon kuralı: `NODE_ENV === "development"` VE `REACT_APP_DEV_TOKEN` boş değil. **Token, fonksiyonun `null` dönmediği her yolda garanti dolu ve trim'lidir** — `sub` okuması için ek bir varlık kontrolü gerekmez.
- `StandaloneDevConfig` alanları: `token`, `tenantId`, `initialTicketKey`, `agentEmail`, `agentName`, `environment`. Return literal'i bu sırayla tek satırdadır (satır 93).
- `grispi-context.tsx` — `agentEmail`/`agentName` React state'leri ZATEN `useState<string | null>(null)` ve context tipinde ZATEN `string | null`. Yani `StandaloneDevConfig`'i null-able yapmak bu dosyada TİP DEĞİŞİKLİĞİ GEREKTİRMEZ; satır 193-194'teki `setAgentEmail`/`setAgentName` çağrıları olduğu gibi derlenir. Bu dosyada tek iş **yorum** güncellemesidir (satır 28-33 ve 36-40).
- `compose-screen.tsx:78` ve `chat-screen.tsx:262` `agentEmail`'i store'lara geçirir, ama her ikisinde de çağrıdan ÖNCE `!agentEmail` guard'ı vardır — daraltma (narrowing) zaten çalışıyor, bu yüzden `null` tip bugün de derleniyor.
- `senderLabel(message, showFullSender, agentEmail, agentName)` — `thread-message.tsx:44`. `agentName` parametresi ZATEN `string | null | undefined` kabul ediyor ve doğruluk (truthiness) üzerinden çalışıyor, yani `null` ile `undefined` ayırt edilmez.

**Test idiom'ları (uy):**

- **JWT fixture'ı hazır bir örneği var:** `src/lib/__tests__/grispi-environment.test.ts:18-22`'deki `makeToken(payload)` yardımcı fonksiyonu — `btoa(JSON.stringify(...))` ile 3 parçalı, imzasız ama yapısal olarak geçerli bir token üretir. **Bu yardımcıyı `standalone-dev.test.ts` içine YEREL olarak kopyala**; test dosyaları birbirinden import etmez (repo idiom'u), ve o dosyadan export etmek için `grispi-environment.test.ts`'i değiştirme. `btoa` standart base64 üretir; `parseJwt`'in `-`→`+` / `_`→`/` dönüşümü bunda etkisizdir ve `atob` padding'i tolere eder — fixture olduğu gibi çalışır.
- Mevcut testler `resolveStandaloneDevConfig({ NODE_ENV: "development", REACT_APP_DEV_TOKEN: "tok" }, "")` düz nesne kurgusunu kullanır; `"tok"` çözülemeyen bir token'dır (`parseJwt` → `null`).
- Test komutu: `npx craco test --watchAll=false --testPathPattern=<pattern>`; tam suite `CI=true npm test -- --watchAll=false`; tip kontrolü `npx tsc --noEmit -p tsconfig.json`.

**Bilinen tüketici, dokunulmayacak:** `src/contexts/__tests__/grispi-context.test.tsx:297` `REACT_APP_DEV_AGENT_NAME = "Test Agent"` set ediyor ve yalnızca `environment` + `agentName`'i assert ediyor. Override yolunu kullandığı için bu değişiklikten ETKİLENMEZ — o dosyaya dokunma, ama tam suite ile yeşil kaldığını doğrula.

</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kimliği token'ın `sub` claim'inden türet, kimlik sabitlerini kaldır</name>

  <files>
    src/lib/grispi-environment.ts,
    src/lib/standalone-dev.ts,
    src/lib/__tests__/standalone-dev.test.ts
  </files>

  <read_first>
    - `src/lib/standalone-dev.ts` — tam dosya (103 satır). Özellikle `StandaloneDevConfig` (20-27), kimlik sabitleri + doc comment'leri (32-45), kimlik çözümü (79-82), return literal'i (93).
    - `src/lib/grispi-environment.ts` satır 8-11 — `DecodedJwt`.
    - `src/lib/__tests__/standalone-dev.test.ts` — tam dosya (250 satır). Kimlik testleri satır 1-8 (import), 38-51 (tam-şekil `toEqual`), 53-102 (üç kimlik testi). Ortam/tenant/ticket testleri (104-249) DEĞİŞMEZ.
    - `src/lib/__tests__/grispi-environment.test.ts` satır 13-22 — kopyalanacak `makeToken` yardımcısı.
  </read_first>

  <behavior>
    İmplementasyondan ÖNCE yazılacak testler (hepsi `resolveStandaloneDevConfig` üzerinde):

    **`agentEmail` çözüm sırası:**
    - Token `makeToken({ sub: "gsocial@api.grispi.com" })`, override YOK → `agentEmail` `"gsocial@api.grispi.com"`. (`sub`'ın gerçekten okunduğunun kanıtı.)
    - Aynı token + `REACT_APP_DEV_AGENT_EMAIL: "  override@firma.test  "` → `"override@firma.test"` (trim'li, override `sub`'ı YENER).
    - Aynı token + `REACT_APP_DEV_AGENT_EMAIL: "   "` → `sub`'a düşer, yani `"gsocial@api.grispi.com"`. **`null` DEĞİL** — bu, sıralamanın "override → sub → null" olduğunu kanıtlayan testtir; boş bir override `sub`'ı gölgelemez.
    - `sub` içermeyen token (`makeToken({ dev: true })`) → `agentEmail` `null`.
    - Çözülemeyen token (`"tok"`) → `agentEmail` `null`, istisna yok.
    - `sub: "   "` (yalnızca boşluk) → `agentEmail` `null`.
    - `sub` string DEĞİL (`makeToken({ sub: 12345 })`) → `agentEmail` `null` VE çağrı fırlatmaz. (JSON payload güvenilmez girdidir; `parseJwt` bilinçli olarak çökmez, bu okuma da çökmemeli.)

    **`agentName` çözümü:**
    - Override yok → `agentName` `null`.
    - `REACT_APP_DEV_AGENT_NAME: "  Someone Else  "` → `"Someone Else"`.
    - `REACT_APP_DEV_AGENT_NAME: "   "` → `null`.
    - `sub`'ı çözülen bir token + ad override'ı YOK → `agentEmail` dolu ama `agentName` `null`. (Anti-karıştırma testi: e-posta kaynağı ada SIZMAZ.)

    **Tam şekil:**
    - Mevcut `toEqual` testi (satır 38-51) güncellenir: `"tok"` ile `agentEmail: null, agentName: null`, diğer üç alan değişmemiş varsayılanlarıyla.
    - `sub`'lı bir token ile ikinci bir tam-şekil testi: `agentEmail` `sub`'tan, `agentName` `null`, `tenantId`/`initialTicketKey`/`environment` değişmemiş varsayılanlar.
  </behavior>

  <action>
<!-- planner-discipline-allow: DEFAULT_DEV_AGENT_EMAIL -->
<!-- planner-discipline-allow: DEFAULT_DEV_AGENT_NAME -->
<!-- planner-discipline-allow: DEFAULT_DEV_AGENT -->

**1) `src/lib/grispi-environment.ts`:** `DecodedJwt` interface'ine `sub?: string;` alanını ekle (`dev?: boolean` ile index signature'ın arasına). Tek satır; başka hiçbir şeye dokunma — `parseJwt`'in gövdesi, `resolveGrispiEnvironment` ve doc comment'ler aynen kalır. Interface export EDİLMEZ, öyle kalsın.

**2) `src/lib/standalone-dev.ts`:**

- `@/lib/grispi-environment`'tan `parseJwt`'i import et (projenin alias idiom'u; `grispi-context.tsx`'in `@/lib/standalone-dev` kullanımıyla aynı biçim). Döngüsel import riski yok: `grispi-environment.ts` yalnızca `@/grispi/client/environment` ve `@/types/grispi.type`'a bakar.
- İki kimlik sabitini — `DEFAULT_DEV_AGENT_EMAIL` ve `DEFAULT_DEV_AGENT_NAME` — **doc comment'leriyle birlikte tamamen SİL**. `DEFAULT_DEV_TENANT_ID`, `DEFAULT_DEV_TICKET_KEY` ve `DEFAULT_DEV_ENVIRONMENT` ile bunların yorumlarına DOKUNMA.
- `StandaloneDevConfig`: `agentEmail: string | null` ve `agentName: string | null`. Diğer dört alan değişmez.
- `agentEmail` çözümü, tam olarak bu sırayla: (a) `env.REACT_APP_DEV_AGENT_EMAIL?.trim()` dolu ise o; (b) değilse `parseJwt(token)` payload'ının `sub` alanı — **yalnızca `typeof` kontrolü string döndürüyorsa** trim'lenir ve doluysa kullanılır; (c) hiçbiri değilse `null`. `typeof` kontrolü zorunludur: JSON payload'ı güvenilmez girdidir ve string olmayan bir `sub` üzerinde doğrudan `.trim()` çağırmak çalışma zamanında fırlatır — bu dosyanın ve `parseJwt`'in çökmeme duruşuna aykırı olur. Tip sisteminin `sub?: string` demesi bu koruma yerine GEÇMEZ.
- `agentName` çözümü: `env.REACT_APP_DEV_AGENT_NAME?.trim()` dolu ise o, değilse `null`. Ad için token kaynağı YOKTUR — icat etme, `sub`'ı ada da yazma.
- İki `REACT_APP_DEV_AGENT_*` override'ını KORU. Bunlar commit edilmeyen operatör yapılandırmasıdır (hardcoded kimlik değil) ve "başka bir agent'ın mesajı" senaryosunu lokal test etmenin tek yolu. Kaldırılan şey varsayılanlardı.
- Dosyanın en üstündeki modül doc comment'ine kimlik kaynağını anlatan **kısa** bir ek yap (proje kuralı: yorumlar kısa): standalone modda kimlik, modu zaten aktive eden token'ın `sub` claim'inden gelir; ad için claim yoktur, bu yüzden ad ya açık override'dan gelir ya `null` kalır ve `null` e-posta gönderimi bilinçli olarak kapatır. Yeni yazdığın hiçbir yoruma **silinen sabitlerin adlarını yazma** (bkz. aşağıdaki grep kapısı) ve o kişisel e-posta/adı hiçbir biçimde tekrar etme; gerekiyorsa "eski hardcoded varsayılan" gibi tarafsız bir ifade kullan. Silinen sembollerin adları yalnızca commit mesajında ve SUMMARY'de geçebilir.
- `getStandaloneDevConfig` sarmalayıcısına ve return literal'inin alan sırasına dokunma.

**3) `src/lib/__tests__/standalone-dev.test.ts`:** Kimlik assertion'larını **YERİNDE** yeniden yaz — ikinci bir test dosyası AÇMA.

- Silinen iki sabitin import'unu kaldır; `DEFAULT_DEV_TENANT_ID`/`DEFAULT_DEV_TICKET_KEY`/`DEFAULT_DEV_ENVIRONMENT` import'ları kalır.
- `grispi-environment.test.ts`'teki `makeToken` yardımcısını bu dosyaya yerel olarak kopyala; neden kopyalandığını (test dosyaları birbirinden import etmez) tek satırla yaz.
- `<behavior>`'daki tüm senaryoları yaz. Kimlik testlerinin `describe`/`it` başlıklarını, artık ne kanıtladıklarını söyleyecek şekilde güncelle — eski başlıklar "varsayılana düşer" diyor ve bu artık yanlıştır.
- Ortam (`REACT_APP_DEV_GRISPI_ENV`), tenant, ticket-key ve prototip-zinciri testlerini (satır 104-249) DEĞİŞTİRMEDEN bırak.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern=standalone-dev</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern=standalone-dev` exit kodu `0`
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `grep -c 'davutkmbr' src/lib/standalone-dev.ts` → `0` (planlama anında `3`)
    - `grep -c 'DEFAULT_DEV_AGENT' src/lib/standalone-dev.ts` → `0`
    - `grep -c 'Davut' src/lib/standalone-dev.ts` → `0`
    - `grep -c 'atob(' src/lib/standalone-dev.ts` → `0` (ikinci JWT çözücü yazılmadı)
    - `grep -c 'grispi-environment' src/lib/standalone-dev.ts` → `1` (tek import satırı — modül adını yorumda tekrar etme, bu kapıyı bozar)
    - `grep -c 'agentEmail: string | null' src/lib/standalone-dev.ts` → `1`
    - `grep -c 'agentName: string | null' src/lib/standalone-dev.ts` → `1`
    - `grep -c 'sub?: string' src/lib/grispi-environment.ts` → `1`
    - `grep -c 'DEFAULT_DEV_ENVIRONMENT' src/lib/standalone-dev.ts` → `2` (ortam yapılandırması dokunulmadı)
    - Davranış: `sub`'ın `agentEmail` olduğunu assert eden yeşil bir test vardır
    - Davranış: boş/yalnızca-boşluk `REACT_APP_DEV_AGENT_EMAIL`'in `null`'a değil `sub`'a düştüğünü assert eden yeşil bir test vardır
    - Davranış: string olmayan bir `sub` ile çağrının fırlatmadığını ve `agentEmail`'in `null` olduğunu assert eden yeşil bir test vardır
    - Davranış: `agentEmail` `sub`'tan çözülürken `agentName`'in `null` kaldığını assert eden yeşil bir test vardır
  </acceptance_criteria>

  <done>
    Standalone dev kimliği token'dan geliyor; kişisel e-posta ve ad sabitleri dosyadan tamamen kalktı; string olmayan/eksik `sub` çökmek yerine `null` üretiyor; ad için tek kaynak açık override; kimlik testleri yerinde yeniden yazıldı, ortam/tenant/ticket testleri değişmedi.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tüketici dokümantasyonu, `null` kimlikle bozulmama kanıtı ve depo geneli kalıntı kapısı</name>

  <files>
    src/contexts/grispi-context.tsx,
    src/screens/components/__tests__/thread-components.test.tsx,
    README.md
  </files>

  <read_first>
    - `src/contexts/grispi-context.tsx` satır 22-58 — `GrispiContextType`; güncellenecek iki doc comment satır 28-33 (`agentEmail`) ve 36-40 (`agentName`). İkincisi silinmiş bir sembolü adlandırıyor.
    - `src/screens/components/__tests__/thread-components.test.tsx` satır 196-250 — `senderLabel` fallback senaryoları. Satır 220'deki test `agentName` prop'unu HİÇ VERMEYEREK (`undefined`) düşüşü kanıtlıyor; eklenecek olan, bağlamın gerçek çalışma-zamanı değeri olan açık `null`'dır.
    - `README.md` satır 105-121 — §5 Local development; düzeltilecek cümle satır 121.
  </read_first>

  <action>
**1) `src/contexts/grispi-context.tsx` — YALNIZCA YORUM.** Kod satırı, state tipi, `plugin` seçim satırı, effect gövdesi: hiçbiri değişmez.

- `agentEmail` doc comment'i (satır 28-33): standalone kaynağını, artık token'ın `sub` claim'i (veya açık operatör override'ı) olarak anlat ve `null` kalabileceğini — bu durumda gönderimin kapalı olduğunu — tek cümleyle ekle.
- `agentName` doc comment'i (satır 36-40): silinmiş sembolün adını çıkar. Standalone modda tek kaynağın `REACT_APP_DEV_AGENT_NAME` olduğunu, token'da ad claim'i BULUNMADIĞINI ve `null` olduğunda etiketin `senderEmail`'e düştüğünü yaz.
- Her iki yorumda da silinen sabitlerin adlarını TEKRAR ETME (aşağıdaki depo-geneli kapı bunu ölçüyor).

**2) `src/screens/components/__tests__/thread-components.test.tsx` — tek senaryo ekle.** Mevcut fallback `describe`'ının içine, `direction: "own"`, `senderName: undefined`, `senderEmail` DOLU, `status: "pending"` bir mesajı **açıkça `agentName={null}`** ile render eden bir test ekle ve DOM'da `senderEmail`'in göründüğünü assert et. Gerekçeyi tek satırla yaz: bağlam standalone modda artık gerçekten `null` geçiyor; mevcut test bu senaryoyu prop'u hiç vermeyerek (`undefined`) kanıtlıyor ve `senderLabel` doğruluk üzerinden çalıştığı için ikisi aynı dala girmeli — ama bu bir varsayım, kanıtlanmalı. `thread-message.tsx` ÜRETİM KODUNA DOKUNMA; bu test yeşil olmak için hiçbir bileşen değişikliği gerektirmemeli. Eğer beklenmedik şekilde KIRMIZI gelirse dur ve durumu SUMMARY'de rapor et — o zaman bu, kapsamı aşan gerçek bir bileşen hatasıdır.

**3) `README.md` §5 — tek cümle.** Satır 121'deki opsiyonel override cümlesini, agent e-postasının artık `REACT_APP_DEV_TOKEN`'ın kendi `sub` claim'inden türetildiğini söyleyecek şekilde düzelt; agent adının token'da karşılığı olmadığını ve yalnızca `REACT_APP_DEV_AGENT_NAME` ile ayarlandığını, ayarlanmazsa gönderenin e-postasıyla gösterildiğini belirt. §5'in geri kalanına, diğer bölümlere veya kod bloklarına dokunma — bir cümle, en fazla iki.

**4) Depo geneli kalıntı kapısı.** `grep -rn "DEFAULT_DEV_AGENT" src` çıktısının BOŞ olduğunu doğrula. Planlama anındaki üç kalıntı yeri: `standalone-dev.ts` (Task 1'de silindi), `standalone-dev.test.ts` (Task 1'de import'u kalktı), `grispi-context.tsx:38` (bu görevde yorum güncellendi). Çıktı boş değilse dördüncü bir kalıntı var — bul ve gider.

**5) Tam suite + tip kontrolü.** `CI=true npm test -- --watchAll=false` ve `npx tsc --noEmit -p tsconfig.json`. `src/contexts/__tests__/grispi-context.test.tsx`'in standalone testi (satır 293-322) `REACT_APP_DEV_AGENT_NAME`'i açıkça set ettiği için etkilenmemeli — yeşil kaldığını teyit et, o dosyaya DOKUNMA.

**6) Commit.** Yalnızca bu planın altı dosyasını içeren açık bir dosya listesiyle commit et; `git add -A` KULLANMA. `git status --porcelain -- .planning/phases/` ve `git status --porcelain -- src/contexts/plugin-bootstrap.ts` çıktılarının boş olduğunu commit'ten önce doğrula.
  </action>

  <verify>
    <automated>CI=true npm test -- --watchAll=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>test -z "$(grep -rn 'DEFAULT_DEV_AGENT' src)"</automated>
    <automated>test -z "$(git status --porcelain -- src/contexts/plugin-bootstrap.ts .planning/phases/)"</automated>
  </verify>

  <acceptance_criteria>
    - `CI=true npm test -- --watchAll=false` exit kodu `0`; mevcut 527 testin hiçbiri kırılmadı (kimlik testleri Task 1'de yeniden yazıldı, sayı artabilir)
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `grep -rn 'DEFAULT_DEV_AGENT' src` çıktısı BOŞ
    - `grep -c 'standaloneConfig ? null : getPluginInstance()' src/contexts/grispi-context.tsx` → `1` (köprü seçimi değişmedi)
    - `grep -c '!agentEmail' src/screens/compose-screen.tsx` → `2` VE `grep -c '!agentEmail' src/screens/chat-screen.tsx` → `2` (fail-closed kapılar duruyor)
    - `git status --porcelain -- src/contexts/plugin-bootstrap.ts` BOŞ
    - `git status --porcelain -- .planning/phases/` BOŞ
    - `git status --porcelain -- src/screens/components/thread-message.tsx` BOŞ (üretim bileşeni değişmedi)
    - `git diff --stat package.json package-lock.json` BOŞ
    - Davranış: `agentName={null}` ile pending/own mesajın `senderEmail`'e düştüğünü assert eden yeşil bir test vardır
  </acceptance_criteria>

  <done>
    Silinen sembollere atıf yapan hiçbir yorum kalmadı; `null` kimlikle bozulmama üretim kodu değiştirilmeden testle kanıtlandı; README standalone kimlik kaynağını doğru anlatıyor; plugin modu, köprü seçimi ve gönderim kapıları dokunulmadan tam suite yeşil.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT payload → uygulama durumu | `sub`, imzası DOĞRULANMAYAN bir token gövdesinden okunur; tipi, varlığı ve biçimi çalışma zamanında garanti değildir |
| Standalone kimlik → canlı API yazımı | `agentEmail`, `creator: [{ key: "us.email" }]` olarak gerçek bir ticket'a yazılır — yanlış değer üçüncü tarafa yanlış gönderen olarak ulaşır |
| Depo → git geçmişi | Kişisel bir e-posta adresi ve ad, kaynak koddan kaldırılıyor; commit'in kalıntı bırakmaması gerekir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-tn1-01 | Spoofing | `standalone-dev.ts` kimlik çözümü → `compose-store.ts` `creator: us.email` | high | mitigate | CR-02'nin ta kendisi. Sabit kimlik siliniyor; e-posta artık yalnızca (a) operatörün kendi `.env.development.local`'ından ya da (b) o operatörün kendi token'ının `sub` claim'inden gelebiliyor. Her iki kaynak da çalıştıran kişinin gerçek kimliğidir. Gate: `grep -c 'davutkmbr' src/lib/standalone-dev.ts` → `0` VE `sub`→`agentEmail` bağını kanıtlayan yeşil test |
| T-Q-tn1-02 | Denial of Service | `parseJwt` payload'ının `sub` alanı üzerinde `.trim()` | medium | mitigate | JSON payload'ı güvenilmez girdidir: string olmayan bir `sub` (sayı, nesne, `null`) doğrudan `.trim()` çağrısında `TypeError` fırlatır ve `resolveStandaloneDevConfig` modül yükleme sırasında (`grispi-context.tsx:67`) çağrıldığı için bu, ağacı komple çökertir — `parseJwt`'in try/catch'i başka bir fonksiyonda olduğu için burada KORUMAZ. Zorunlu `typeof` kontrolü + `sub: 12345` fixture'ıyla fırlatmama testi (Task 1) |
| T-Q-tn1-03 | Repudiation | Kimlik çözülemediğinde sessizce boş `creator` yazılması | high | mitigate | `agentEmail: null` fail-closed davranışı tetikler: `compose-screen.tsx:47` ve `chat-screen.tsx:240` `!agentEmail` guard'ları gönderimi kapatır. `compose-store.ts:203`'teki `agentEmail ?? ""` ifadesi ancak bu kapılar kaldırılırsa devreye girebilir — bu yüzden kapılara dokunmak açıkça yasaklandı. Gate: her iki dosyada `!agentEmail` sayısı planlama anındaki `2` değerinde kalır |
| T-Q-tn1-04 | Information Disclosure | Kişisel e-posta/adın silinmiş sanılıp yorumda kalması | low | mitigate | Task 1 yeni yorumlarda silinen sabit adlarının ve kişisel bilginin tekrarını yasaklar; Task 2 depo geneli `grep -rn 'DEFAULT_DEV_AGENT' src` kapısını boş çıktıya bağlar. Not: aynı adres `plugin-bootstrap.test.ts:58` ve `grispi-context.test.tsx:47`'de plugin-mode bundle fixture'ı olarak duruyor — kapsam çiti gereği bunlar KAPSAM DIŞIDIR, bu yüzden kapı depo geneli değil dosya kapsamlı ölçülüyor |
| T-Q-tn1-05 | Tampering | `grispi-context.tsx`'in köprü-seçim satırının yanlışlıkla değişmesi | medium | mitigate | Bu dosyada Task 2 YALNIZCA yorum değiştirir. Gate: `grep -c 'standaloneConfig ? null : getPluginInstance()'` → `1`. CR-02'nin bu yarısı (gerçek iframe içinde dev yolunun kazanması) ayrı ve daha risklidir; bu düzeltmeden sonra o yol da bir yabancının değil, token'ın kendi doğru kimliğini yazar — yani artık yanlış-atıf hatası değildir |
| T-Q-tn1-SC | Tampering | Paket yöneticisi kurulumları (npm) | low | accept | Bu plan hiçbir paket eklemiyor/kaldırmıyor/yükseltmiyor; legitimacy gate uygulanmaz. Gate: `git diff --stat package.json package-lock.json` BOŞ |
</threat_model>

<verification>
**Otomatik (bu plan tamamlandığında geçmeli):**

1. `npx craco test --watchAll=false --testPathPattern=standalone-dev` → exit 0
2. `CI=true npm test -- --watchAll=false` → exit 0 (527 test tabanı korunur)
3. `npx tsc --noEmit -p tsconfig.json` → exit 0
4. `grep -rn 'DEFAULT_DEV_AGENT' src` → BOŞ
5. `grep -c 'davutkmbr' src/lib/standalone-dev.ts` → `0`
6. `grep -c 'atob(' src/lib/standalone-dev.ts` → `0`
7. `git status --porcelain -- src/contexts/plugin-bootstrap.ts src/screens/components/thread-message.tsx .planning/phases/` → BOŞ
8. `git diff --stat package.json package-lock.json` → BOŞ

**Bu planın kapsamadıkları (bilinçli):**

- **Canlı doğrulama.** Kullanıcı bu görev için probe'u açıkça reddetti. `sub`'ın gerçek bir yan konuşmada `creator` olarak kabul edildiği canlı olarak ölçülmedi; dayanak, iki gerçek token'ın payload'ı üzerindeki teyittir.
- **CR-02'nin köprü-seçim yarısı** (`grispi-context.tsx:87` — gerçek iframe içinde standalone yolunun kazanması). Ayrı ve daha riskli; bu düzeltmeden sonra yanlış-atıf hatası olmaktan çıkıyor.
- **`sub` üzerinde e-posta biçim doğrulaması.** E-posta biçiminde OLMAYAN bir `sub` (örneğin bir kullanıcı id'si) bugün olduğu gibi geçer. Fail-closed'ı bir adım ileri taşıyabilirdi ama `<required_changes>` "trim'li, boş değil" diyor; bunu politika kararı olarak kullanıcıya bırakıyoruz. Bilinen iki gerçek token'da `sub` e-posta biçimindedir.
- **`REACT_APP_DEV_AGENT_*` override'larının kaldırılması.** Bunlar commit edilmeyen operatör yapılandırmasıdır; kaldırılan şey varsayılanlardı.
</verification>

<success_criteria>
- [ ] Standalone dev kimliği token'ın `sub` claim'inden geliyor; iki kimlik sabiti depodan kalktı (CR-02 kimlik-kaynağı yarısı kapandı)
- [ ] Çözüm sırası testle kilitli: açık override → `sub` → `null`; boş bir override `sub`'ı gölgelemiyor
- [ ] Eksik/boş/string-olmayan `sub` çökmek yerine `null` üretiyor
- [ ] `agentName`'in tek kaynağı açık override; token'dan ad türetilmiyor
- [ ] `agentEmail` `null` iken gönderim kapıları kapalı (fail-closed korundu)
- [ ] `agentName` `null` iken etiket `senderEmail`'e düşüyor — üretim bileşeni değiştirilmeden testle kanıtlandı
- [ ] Plugin modu, köprü seçimi ve `.planning/phases/04.2-*` dokunulmadı
- [ ] Silinen sembollere atıf yapan yorum kalmadı; README standalone kimlik kaynağını doğru anlatıyor
- [ ] Tam suite ve `tsc --noEmit` yeşil; paket değişikliği yok
</success_criteria>

<output>
Create `.planning/quick/260817-tn1-standalone-dev-de-agent-kimligini-jwt-su/260817-tn1-SUMMARY.md` when done.

SUMMARY şunları içermeli:
- `agentEmail` / `agentName` çözüm sıralarının son hâli ve her dalı kanıtlayan test adı
- `sub` string değilken uygulanan `typeof` korumasının gerekçesi (T-Q-tn1-02)
- `agentName={null}` bozulmama testinin sonucu ve `thread-message.tsx`'in gerçekten değişmeden kaldığının teyidi
- Kapsam dışı bırakılanlar ve nedenleri: canlı probe yok, köprü-seçim yarısı yok, `sub` üzerinde e-posta biçim doğrulaması yok
</output>
