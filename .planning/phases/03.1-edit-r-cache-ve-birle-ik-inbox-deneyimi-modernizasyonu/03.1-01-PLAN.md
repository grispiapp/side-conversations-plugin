---
phase: 03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/html-sanitizer.ts
  - src/lib/__tests__/html-sanitizer.test.ts
  - src/screens/components/rich-text-composer.tsx
  - src/screens/components/__tests__/rich-text-composer.test.tsx
  - src/screens/components/thread-message.tsx
  - src/screens/components/__tests__/thread-components.test.tsx
autonomous: true
requirements:
  - THRD-01
  - THRD-02
must_haves:
  truths:
    - "Temsilci kalın, italik, güvenli bağlantı, madde/numara listesi, alıntı ve emoji içeren bir yanıtı seçim kaybı veya tarayıcı execCommand davranışına bağlı kalmadan düzenleyebilir."
    - "Enter yeni satır bırakır; Shift+Enter yalnız anlamlı, sanitize edilmiş içerik gönderir; IME composition sırasında gönderim tetiklenmez."
    - "Uzak, yapıştırılmış, geri yüklenmiş ve gönderilecek HTML aynı DOMPurify politikasıyla temizlenir; Phase 3 alıntı ve internal-note semantiği değişmez."
  artifacts:
    - path: "src/lib/html-sanitizer.ts"
      provides: "DOMPurify-backed tek HTML güven sınırı ve korunmuş quote API"
      exports: ["sanitizeHtml", "splitQuotedHtml", "buildQuotedReplyHtml"]
    - path: "src/screens/components/rich-text-composer.tsx"
      provides: "Tiptap 2.27.2 kontrollü rich composer"
      exports: ["RichTextComposer"]
    - path: "src/screens/components/__tests__/rich-text-composer.test.tsx"
      provides: "Editor biçimlendirme, klavye, paste, restore ve focus regresyonları"
  key_links:
    - from: "src/screens/components/rich-text-composer.tsx"
      to: "src/lib/html-sanitizer.ts"
      via: "paste, external restore, onUpdate and submit sanitation"
      pattern: "sanitizeHtml"
    - from: "src/screens/components/thread-message.tsx"
      to: "src/lib/html-sanitizer.ts"
      via: "splitQuotedHtml is the only dangerouslySetInnerHTML source"
      pattern: "splitQuotedHtml"
---

<objective>
Replace the broken hand-rolled rich editor and custom sanitizer walker with the approved Tiptap 2.27.2 and DOMPurify boundary while preserving every Phase 3 HTML, quote, retry, keyboard, and focus contract.

Purpose: Remove browser-dependent selection/command behavior and consolidate untrusted HTML handling behind one reviewed policy without changing the store-owned draft API.
Output: DOMPurify-backed sanitizer helpers, a controlled accessible Tiptap composer, and focused regression suites.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-UI-SPEC.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-04-SUMMARY.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-06-SUMMARY.md
@.planning/quick/260729-4c0-replace-the-broken-rich-editor-and-custo/260729-4c0-RESEARCH.md
@src/lib/html-sanitizer.ts
@src/screens/components/rich-text-composer.tsx
@src/screens/components/thread-message.tsx

<interfaces>
Current public contracts must remain source-compatible:

```typescript
export function sanitizeHtml(input: string): string;
export function splitQuotedHtml(input: string): {
  bodyHtml: string;
  quotedHtml?: string;
};
export function buildQuotedReplyHtml(
  currentHtml: string,
  context: Array<{ html: string; publicVisible: boolean }>
): string;

export interface RichTextComposerProps {
  value: string;
  recipientLabel: string;
  onChange: (html: string) => void;
  onSubmit: (html: string) => void;
  disabled?: boolean;
  autoFocus?: boolean;
  className?: string;
}
```

Exact approved pins already committed: `@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit`, and `@tiptap/extension-link` at `2.27.2`; `dompurify` at `3.4.12`. Do not install, upgrade, or change package files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the custom sanitizer walker with one DOMPurify policy</name>
  <files>src/lib/html-sanitizer.ts, src/lib/__tests__/html-sanitizer.test.ts, src/screens/components/thread-message.tsx, src/screens/components/__tests__/thread-components.test.tsx</files>
  <behavior>
    - Existing Phase 3 allowlisted paragraph, break, emphasis, list, safe anchor and blockquote markup survives deterministically.
    - `script`, `style`, `svg`, `math`, `iframe`, `img`, `table`, `template`, event/style/data/aria attributes, relative/data/javascript links, and their unsafe payloads cannot reach the rendered or outbound HTML.
    - HTTP(S) anchors normalize to safe `target`/`rel`; `mailto:` stays usable; benign unknown wrappers unwrap while the drop-with-content family disappears.
    - The first sanitized blockquote remains the quote boundary, prior nested quotes do not recurse, and internal notes remain excluded by `buildQuotedReplyHtml`.
    - Word/Gmail paste fixtures and obfuscated URI cases produce stable expected HTML.
  </behavior>
  <action>Extend the sanitizer suite first, then replace only the DOMParser/TreeWalker traversal inside `sanitizeHtml` with a configured DOMPurify 3.4.12 instance. Keep the three exported helper signatures and their first-blockquote/public-only semantics unchanged per Phase 3 D-03..D-05. Set an explicit tag allowlist; allow attributes only on anchors; set `ALLOW_DATA_ATTR:false` and `ALLOW_ARIA_ATTR:false`; enforce only canonical `http:`, `https:`, and `mailto:` URIs; and normalize anchor target/rel in the single wrapper/hook. DOMPurify's default child retention differs from Phase 3, so explicitly remove the drop-with-content family before/through sanitation. Keep `ThreadMessage` consuming only `splitQuotedHtml` output and add sink regressions proving no raw remote HTML bypass exists. Do not broaden the schema to tables, inline images, style, or internal-note creation.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --no-watchman --runTestsByPath src/lib/__tests__/html-sanitizer.test.ts src/screens/components/__tests__/thread-components.test.tsx</automated>
  </verify>
  <done>All established quote/render tests plus the hostile, Word/Gmail, protocol, drop-with-content, and recursive-quote corpus pass against the DOMPurify implementation with unchanged public APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace contentEditable commands with a controlled Tiptap composer</name>
  <files>src/screens/components/rich-text-composer.tsx, src/screens/components/__tests__/rich-text-composer.test.tsx, src/screens/components/__tests__/thread-components.test.tsx</files>
  <behavior>
    - Toolbar commands produce bold, italic, safe link, bullet list, ordered list, blockquote and emoji content; active mark/list buttons expose `aria-pressed`.
    - Undo and redo operate through Tiptap history and every toolbar control has an accessible Turkish name and visible focus.
    - Enter creates a newline; Shift+Enter submits meaningful sanitized HTML; Shift+Enter during IME composition does not submit.
    - Unsafe/complex paste is sanitized before Tiptap parses it; external draft restore/clear is sanitized and synchronized without resetting the selection on ordinary `onUpdate`.
    - Disabled mode blocks editing/commands/submission; forwarded focus and `autoFocus` remain usable by ChatScreen and reopen flow.
  </behavior>
  <action>Write the dedicated composer suite, then rebuild `RichTextComposer` with `useEditor`/`EditorContent`, StarterKit and Link at the exact committed `2.27.2` versions. Disable headings, code/codeBlock, strike, horizontal rule and unsupported document output; retain paragraph, hard break, bold, italic, bullet/ordered lists, blockquote, history, and safe links. Implement toolbar/emoji through Tiptap commands, add ordered-list and undo/redo controls, and use an editor keyboard-shortcut/props seam for Shift+Enter that ignores `event.isComposing`. Preserve the existing controlled props and forwarded-focus contract: sanitize genuine external `value` restore/clear before `setContent`, but never call `setContent` in response to the same editor `onUpdate` value. Sanitize paste before parsing, sanitize emitted changes, and sanitize again immediately before `onSubmit`. Keep the 5–6 line self-scrolling editor behavior and immutable recipient summary required by D-07..D-12. Remove every `contentEditable` DOM command, saved `Range`, and `document.execCommand` path rather than leaving a fallback engine.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --no-watchman --runTestsByPath src/screens/components/__tests__/rich-text-composer.test.tsx src/screens/components/__tests__/thread-components.test.tsx &amp;&amp; if rg -n "execCommand|savedRange" src/screens/components/rich-text-composer.tsx; then exit 1; fi &amp;&amp; CI=true npx tsc --noEmit</automated>
  </verify>
  <done>The Tiptap composer passes formatting, accessibility, selection, paste, restore, clear, disabled, Enter/Shift+Enter/IME, focus, and sanitation tests; no legacy execCommand/Range engine remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Clipboard/remote API → DOMPurify/Tiptap | Untrusted HTML enters the editor or renderer. |
| Tiptap draft → Grispi reply payload | User-authored HTML crosses into an external email mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03.1-01 | Tampering / Information disclosure | `html-sanitizer.ts` | mitigate | Explicit tags/attributes/URI policy, drop-with-content handling, deterministic anchor normalization, and hostile-corpus tests. |
| T-03.1-02 | Tampering | Tiptap paste/restore/send | mitigate | Sanitize before parse, on external restore, on emitted change, and immediately before submit. |
| T-03.1-03 | Repudiation | Reply quoting | mitigate | Preserve one public-only chronological blockquote and regression-test internal-note/recursive-quote exclusion. |
| T-03.1-SC | Tampering | npm dependencies | mitigate | No installs in this plan; use only the preflight-approved exact committed pins and fail if package files change. |
</threat_model>

<verification>
Run both focused suites, TypeScript, the zero-execCommand gate, and `git diff --exit-code -- package.json package-lock.json`.
</verification>

<success_criteria>
- Every inbound/outbound/editor HTML path crosses the same DOMPurify wrapper.
- The Tiptap editor preserves the public controlled props/ref contract and all Phase 3 keyboard, quote, draft, disabled, and focus behavior.
- Unsupported tables, inline images, styles, active content, and internal-note creation remain excluded.
- Package files remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu/03.1-01-SUMMARY.md` when done.
</output>
