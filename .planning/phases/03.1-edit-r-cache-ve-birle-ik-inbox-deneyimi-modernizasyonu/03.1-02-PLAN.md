---
phase: 03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/grispi-context.tsx
  - src/contexts/plugin-bootstrap.ts
  - src/contexts/__tests__/plugin-bootstrap.test.ts
  - src/lib/standalone-dev.ts
  - src/lib/__tests__/standalone-dev.test.ts
  - src/query/query-client.ts
  - src/query/query-keys.ts
  - src/query/side-conversation-queries.ts
  - src/query/__tests__/query-keys.test.ts
  - src/query/__tests__/side-conversation-queries.test.tsx
  - src/index.tsx
autonomous: true
requirements:
  - CORE-03
  - LIST-05
  - COMP-02
  - THRD-01
must_haves:
  truths:
    - "Plugin ve standalone yolları aynı doğrulanmış tenant kimliğini React Query anahtarlarına sağlar; tenant yokken hiçbir query çalışmaz."
    - "İki tenant'ın aynı parent/side/customer değerleri hiçbir cache kaydını paylaşmaz."
    - "Liste, detay ve müşteri sorguları finite stale policy kullanır; polling, iframe focus refetch ve önceki ticket verisini yeni key altında taşıma yoktur."
  artifacts:
    - path: "src/query/query-client.ts"
      provides: "Tek module-stable QueryClient ve retry-disabled test client factory"
      exports: ["queryClient", "createTestQueryClient"]
    - path: "src/query/query-keys.ts"
      provides: "Tenant-scoped exact query-key factories"
      exports: ["sideConversationKeys"]
    - path: "src/query/side-conversation-queries.ts"
      provides: "Typed list/detail/customer query option contracts"
    - path: "src/contexts/grispi-context.tsx"
      provides: "Plugin ve standalone için tek tenantId React context source"
      contains: "tenantId"
  key_links:
    - from: "src/contexts/plugin-bootstrap.ts"
      to: "src/contexts/grispi-context.tsx"
      via: "setTenantId in the same successful auth branch"
      pattern: "setTenantId"
    - from: "src/query/side-conversation-queries.ts"
      to: "src/query/query-keys.ts"
      via: "every query option uses an exact tenant-scoped factory"
      pattern: "sideConversationKeys"
    - from: "src/index.tsx"
      to: "src/query/query-client.ts"
      via: "QueryClientProvider above all application consumers"
      pattern: "QueryClientProvider"
---

<objective>
Establish the tenant identity, QueryClient, key factories, and fetch-option contracts that all subsequent React Query migrations must use.

Purpose: Prevent cross-tenant cache disclosure and policy drift before moving canonical GET state out of MobX.
Output: A tested plugin/standalone tenant source, module-stable Query provider, exact tenant-scoped keys, and no-polling typed query contracts.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-04-SUMMARY.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-02-SUMMARY.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-06-SUMMARY.md
@.planning/quick/260729-4c0-replace-the-broken-rich-editor-and-custo/260729-4c0-RESEARCH.md
@src/contexts/grispi-context.tsx
@src/contexts/plugin-bootstrap.ts
@src/lib/standalone-dev.ts
@src/index.tsx
@src/grispi/client/api.ts

<interfaces>
Existing bootstrap contracts to extend, not replace:

```typescript
export interface BootstrapPluginInitDeps {
  plugin: { _init(): Promise<GrispiBundle> };
  authentication: {
    setTenantId(tenantId: string): void;
    setToken(token: string): void;
  };
  setSettings(settings: Settings): void;
  setLoading(loading: boolean): void;
  setAgentEmail(email: string | null): void;
  switchTicket(ticketKey: string): void;
}

export interface StandaloneDevConfig {
  token: string;
  tenantId: string;
  initialTicketKey: string;
  agentEmail: string;
}
```

Required exact keys:

```typescript
["side-conversations", tenantId, parentKey]
["side-conversation", tenantId, sideKey]
["customers", tenantId, normalizedTerm]
```

The committed dependency is `@tanstack/react-query@5.101.4`; do not install or update dependencies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make tenant identity explicit in plugin and standalone bootstrap</name>
  <files>src/contexts/grispi-context.tsx, src/contexts/plugin-bootstrap.ts, src/contexts/__tests__/plugin-bootstrap.test.ts, src/lib/standalone-dev.ts, src/lib/__tests__/standalone-dev.test.ts</files>
  <behavior>
    - Plugin initialization sets React `tenantId` from `bundle.context.tenantId` in the same successful branch that configures authentication and before application queries can run.
    - Failed plugin initialization leaves/clears `tenantId` to null, clears loading through the established safe path, and never exposes a stale prior tenant.
    - Standalone development uses `resolveStandaloneDevConfig().tenantId` as the identical React context source.
    - Ticket switching never changes tenant identity and the existing agent/settings/ticket/bootstrap error behavior remains intact.
  </behavior>
  <action>Add `tenantId: string | null` to `GrispiContextType` and provider state. Extend `BootstrapPluginInitDeps` with `setTenantId`; on successful `_init`, set the context tenant from the same bundle value used by `authentication.setTenantId`, and on failure explicitly withhold/clear it while preserving the existing no-unhandled-rejection behavior. In standalone mode, set the same React state from the already resolved `StandaloneDevConfig.tenantId` immediately alongside authentication. Update focused tests for successful plugin, failed plugin, standalone resolver, and context-facing state order. Do not derive tenant from ticket keys, global authentication internals, defaults outside standalone resolution, or query cache contents.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --no-watchman --runTestsByPath src/contexts/__tests__/plugin-bootstrap.test.ts src/lib/__tests__/standalone-dev.test.ts</automated>
  </verify>
  <done>Both runtime modes expose one explicit tenantId source; failure exposes null; all pre-existing bootstrap, ticket switch, settings, agent and error tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define exact tenant-scoped query keys and stable client policy</name>
  <files>src/query/query-client.ts, src/query/query-keys.ts, src/query/__tests__/query-keys.test.ts, src/index.tsx</files>
  <behavior>
    - Keys equal the three exact tuple shapes and customer terms are trimmed/lowercased once in the key factory.
    - Same resource values under two tenant IDs produce unequal keys and cannot read each other's cached data.
    - The application receives one module-stable QueryClient; tests can create isolated retry-disabled clients.
    - Default query behavior has no polling and does not refetch on iframe focus.
  </behavior>
  <action>Create `sideConversationKeys.list(tenantId,parentKey)`, `.detail(tenantId,sideKey)`, and `.customers(tenantId,term)` returning readonly exact tuples; normalize only customer term via trim plus locale-independent lowercase. Create a module-stable production `queryClient` with no blanket cache invalidation, `refetchOnWindowFocus:false`, `refetchOnReconnect:true`, `refetchInterval:false`, and safe bounded retry behavior; export `createTestQueryClient()` with both query and mutation retry disabled and isolated caches. Mount `QueryClientProvider` in `src/index.tsx` above `App`, keeping React StrictMode. Test tuple equality, tenant separation, normalized search keys, policy defaults, cache separation, and provider stability. Do not include agent email/token in keys and do not create broad root invalidation helpers.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --no-watchman --runTestsByPath src/query/__tests__/query-keys.test.ts &amp;&amp; CI=true npx tsc --noEmit</automated>
  </verify>
  <done>The app has one QueryClient and only the three tenant-scoped factories; isolated tests prove cache separation and the no-polling/no-focus-refetch policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Encode reusable list, detail, and customer query contracts</name>
  <files>src/query/side-conversation-queries.ts, src/query/__tests__/side-conversation-queries.test.tsx</files>
  <behavior>
    - List options use page size 10 and `useInfiniteQuery`-compatible page parameters without carrying previous-parent data into a new key.
    - Detail uses a 15-second staleTime; list uses 30 seconds; both disable focus refetch and intervals and retain reconnect refetch.
    - Customer search uses the normalized tenant key and stays disabled until tenant exists and the established three-character minimum is satisfied.
    - Every option stays disabled while tenant/resource identity is absent and calls only the existing typed Grispi clients.
  </behavior>
  <action>Build typed option factories/hooks over `grispiAPI.tickets.advancedSearch`, `grispiAPI.tickets.getTicket`, and `grispiAPI.customers.search`. Keep list pagination at `size:10` with API page-number/total-pages semantics and never carry a prior parent ticket's data into a new key. Encode list staleTime 30,000ms and detail staleTime 15,000ms; customer search keeps its existing debounce/generation behavior until Plan 03 moves UI ownership, but its query contract must require tenant plus normalized term length of at least three. Explicitly set `refetchInterval:false`, `refetchOnWindowFocus:false`, and `refetchOnReconnect:true`. Use `enabled` gates rather than sentinel keys or empty API calls. Test exact requests/options, absent-identity disablement, no interval, no prior-key data carryover, page progression, and fresh QueryClient isolation. This task defines contracts only; Plan 03 will connect list/customer consumers and Plan 04 will connect detail/mutations.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --no-watchman --runTestsByPath src/query/__tests__/side-conversation-queries.test.tsx &amp;&amp; CI=true npx tsc --noEmit &amp;&amp; git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <done>Typed list/detail/customer contracts are ready for consumers and prove exact keys, API parameters, enabled gates, finite stale times, zero polling, and zero prior-ticket data carryover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Grispi SDK/standalone config → React context | Tenant identity enters the client state domain. |
| React context → Query cache key | Tenant and ticket/search identifiers decide cache isolation. |
| Query option → Grispi API | Enabled gates control whether authenticated reads occur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03.1-04 | Spoofing / Information disclosure | tenant bootstrap | mitigate | One tested bundle/config source; null on failure; no derivation from ticket or cache. |
| T-03.1-05 | Information disclosure | query keys | mitigate | Tenant ID included in every exact key and cross-tenant cache-separation tests. |
| T-03.1-06 | Denial of service | refetch policy | mitigate | No polling/focus refetch, finite stale times, reconnect-only ambient refresh, enabled gates. |
| T-03.1-SC | Tampering | npm dependencies | mitigate | No install task; exact approved React Query pin is already committed and package-file diff is gated. |
</threat_model>

<verification>
Run all new query/bootstrap suites, TypeScript, and package-file no-change gate. Confirm no `refetchInterval` is truthy and no key omits tenantId.
</verification>

<success_criteria>
- Plugin and standalone modes feed one explicit nullable tenant source.
- Every remote-state key is tenant scoped and every absent-identity query is disabled.
- Query policy is stable, finite, no-polling, no-focus-refetch, and never carries previous-ticket data into a new key.
- Downstream Plans 03 and 04 can consume contracts without inventing keys or defaults.
</success_criteria>

<output>
Create `.planning/phases/03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu/03.1-02-SUMMARY.md` when done.
</output>
