---
phase: 03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu
plan: 02
subsystem: data-fetching
tags: [react-query, tenant-isolation, cache, pagination, bootstrap]

# Dependency graph
requires:
  - phase: 01-04
    provides: Resilient shared plugin bootstrap and ticket-switch path
  - phase: 02-02
    provides: Standalone tenant configuration and agent identity context
provides:
  - Explicit nullable tenant identity shared by plugin and standalone React paths
  - Module-stable QueryClient plus isolated retry-disabled test client factory
  - Exact tenant-scoped list, detail, and normalized customer query keys
  - Typed finite-stale no-polling list, detail, and customer query option contracts
affects: [03.1-03, 03.1-04, 03.1-05, list-cache, detail-cache, customer-search]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Tenant identity is authenticated before becoming available to Query consumers
    - Disabled nullable identities remain in exact keys without sentinel strings
    - Query options own finite freshness, reconnect refresh, and zero ambient polling

key-files:
  created:
    - src/query/query-client.ts
    - src/query/query-keys.ts
    - src/query/side-conversation-queries.ts
    - src/query/__tests__/query-keys.test.ts
    - src/query/__tests__/side-conversation-queries.test.tsx
  modified:
    - src/contexts/grispi-context.tsx
    - src/contexts/plugin-bootstrap.ts
    - src/contexts/__tests__/plugin-bootstrap.test.ts
    - src/index.tsx

key-decisions:
  - "Disabled contracts retain nullable identities in their exact tuple positions; they never substitute sentinel tenant or resource strings."
  - "Customer requests reuse the normalized term emitted by the key factory, keeping cache identity and transport input identical."
  - "Production queries retry once, while test clients disable query and mutation retries for deterministic isolation."

patterns-established:
  - "Tenant bootstrap boundary: authentication first, then React tenant publication, then loading completion and ticket switching."
  - "Exact cache contract: list, detail, and customer reads use only the three exported tenant-scoped factories."
  - "Finite read policy: list/customer staleTime 30s, detail staleTime 15s, reconnect refresh enabled, focus refresh and intervals disabled."

requirements-completed: [CORE-03, LIST-05, COMP-02, THRD-01]

# Metrics
duration: 7min
completed: 2026-07-29
---

# Phase 03.1 Plan 02: Tenant-Scoped Query Foundation Summary

**Authenticated plugin/standalone tenant identity now drives exact isolated React Query keys with stable client policy and typed finite-stale list, detail, and customer contracts.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-07-29T08:34:55Z
- **Completed:** 2026-07-29T08:41:36Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Exposed one nullable `tenantId` through `GrispiContext`, sourced from the authenticated plugin bundle or resolved standalone configuration and explicitly cleared when plugin initialization fails.
- Added one module-stable production `QueryClient`, mounted above `App`, with bounded retry, reconnect refresh, no iframe-focus refetch, and no polling; tests receive fresh retry-disabled clients.
- Added exactly three tenant-scoped key factories for parent lists, side-ticket details, and normalized customer searches, with cross-tenant cache-isolation coverage.
- Added typed infinite-list, detail, and customer option factories with exact existing Grispi client calls, page size 10, API page progression, finite stale times, absent-identity gates, and no previous-key placeholder carryover.

## Task Commits

TDD gates and implementation outcomes were committed separately:

1. **Task 1 RED: Tenant bootstrap contract** - `3ae2e13` (test)
2. **Task 1 GREEN: Explicit plugin/standalone tenant identity** - `d779347` (feat)
3. **Task 2 RED: Tenant query-cache contracts** - `2d027d0` (test)
4. **Task 2 GREEN: Stable client and exact key factories** - `0fe0ede` (feat)
5. **Task 3 RED: Typed query-option contracts** - `64232d8` (test)
6. **Task 3 GREEN: Finite list/detail/customer options** - `b5afc1d` (feat)

## Files Created/Modified

- `src/contexts/grispi-context.tsx` - Publishes nullable tenant identity from both runtime modes.
- `src/contexts/plugin-bootstrap.ts` - Publishes the authenticated bundle tenant and clears it on initialization failure.
- `src/contexts/__tests__/plugin-bootstrap.test.ts` - Proves success ordering and failure clearing.
- `src/index.tsx` - Mounts the module-stable `QueryClientProvider` above all consumers.
- `src/query/query-client.ts` - Production client defaults and isolated retry-disabled test factory.
- `src/query/query-keys.ts` - Exact tenant-scoped list, detail, and normalized customer tuples.
- `src/query/side-conversation-queries.ts` - Typed infinite list, detail, and customer option factories.
- `src/query/__tests__/query-keys.test.ts` - Key equality, normalization, policy, stability, and cache-separation coverage.
- `src/query/__tests__/side-conversation-queries.test.tsx` - Request, gate, paging, stale-policy, interval, and carryover coverage.

## Decisions Made

- Kept absent tenant/resource values as `null` in their normal tuple positions while `enabled:false` prevents execution; no empty-string or alternate sentinel key can collide with authenticated data.
- Reused the customer key factory’s normalized tuple value as the API `searchTerm`, so cache identity and request identity cannot drift.
- Set the production query retry bound to one attempt; test query and mutation retries are disabled.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Context7 was unavailable in the environment. TanStack Query v5 behavior was checked against the installed 5.101.4 type declarations and official v5 documentation before implementation.
- CRA continues to emit its existing Babel preset maintenance warning and Jest open-handle advisory after successful focused runs; neither affects test results.

## Known Stubs

None.

## Threat Flags

None. Tenant bootstrap, cache-key isolation, query execution gates, and refetch policy are the trust boundaries already covered by the plan threat model.

## Authentication Gates

None.

## Verification

- Focused bootstrap/query suites with `--no-watchman`: **PASS** - 4 suites / 19 tests.
- Full repository Jest suite with `--no-watchman`: **PASS** - 19 suites / 172 tests.
- `CI=true npx tsc --noEmit`: **PASS**.
- Package integrity from pre-plan `d213c7d`: **PASS** - `package.json` and `package-lock.json` unchanged.
- No-truthy-interval and no-alternate-query-key static gates: **PASS**.
- Prettier and `git diff --check`: **PASS**.

## TDD Gate Compliance

All three tasks followed RED then GREEN commit order. Each RED suite failed because its planned module or tenant publication contract did not yet exist, and each corresponding GREEN commit passed the focused suite.

## User Setup Required

None.

## Next Phase Readiness

- Plan 03.1-03 can consume the exact list/customer option contracts and tenant source without inventing cache keys or defaults.
- Plan 03.1-04 can extend the exact detail contract and use the same stable client for success-only exact refresh.
- No blockers, goal-preventing stubs, polling behavior, or cross-tenant cache sharing remain.

## Self-Check: PASSED

- All nine declared created/modified source and test files exist.
- Commits `3ae2e13`, `d779347`, `2d027d0`, `0fe0ede`, `64232d8`, and `b5afc1d` exist in git history.
- Focused/full Jest, TypeScript, package-integrity, exact-key, no-polling, formatting, and stub/threat scans pass.
- `STATE.md` and `ROADMAP.md` remain unchanged.

---
*Phase: 03.1-edit-r-cache-ve-birle-ik-inbox-deneyimi-modernizasyonu*
*Completed: 2026-07-29*
