---
phase: 04-dosya-ekleri-ve-inline-g-rseller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/sonner.tsx
  - src/app.tsx
  - src/lib/attachment-test-helpers.ts
  - src/lib/__tests__/attachment-test-helpers.test.ts
autonomous: false
requirements: [COMP-05, COMP-08]
user_setup:
  - service: grispi-live-tenant
    why: "Task 1 canlı probe, gerçek gsocial-test tenant'ına 4 upload/patch isteği atar (P1-P5). Faz 1/2'de kurulan .env.development.local yeterli — yeni kurulum yok."
    env_vars:
      - name: REACT_APP_DEV_TOKEN
        source: ".env.development.local (Faz 1'de mevcut) — canlı gsocial-test token'ı"

must_haves:
  truths:
    - "Upload endpoint'inin gerçek yolu (public/v1 mi kök mü), cevap şekli, ?inline=true desteği, gerçek boyut tavanı ve token yetkisi canlı tenant'ta CONFIRMED/CORRECTED olarak kayıtlı (P1-P5)"
    - "Üç SUS-işaretli paket human tarafından onaylanmış ve tam sürüm pinleriyle kurulu (^ veya latest aralığı yok)"
    - "Toast yüzeyi uygulama kökünde bir kez mount edilmiş ve next-themes bağımlılığı içermiyor"
    - "Testlerde File ve drop/paste olayı üretebilme yeteneği mevcut ve jsdom DataTransfer yeteneği kayıt altında"
  artifacts:
    - src/components/ui/sonner.tsx
    - src/lib/attachment-test-helpers.ts
    - src/lib/__tests__/attachment-test-helpers.test.ts
  key_links:
    - "src/app.tsx → <Toaster /> (kökte tek mount, StoreProvider/GrispiProvider ile aynı ağaçta)"
    - "package.json → react-dropzone@19.1.1, @tiptap/extension-file-handler@2.27.2, @tiptap/extension-image@2.27.2, sonner (tam pin)"
---

## Phase Goal (User Story)

> ROADMAP Faz 4 `**Goal:**` satırından türetildi (invented DEĞİL — yeniden biçimlendirildi; 02-01-PLAN.md'nin aynı uygulaması).

**As a** Grispi temsilcisi (talebi çözmek için üçüncü taraflarla yazışan),
**I want to** yan görüşmelerin hem yeni-mesaj hem yanıt kutusuna dosya ekleyip yapıştırdığım görseli gövdeye gömmek ve karşı tarafın gönderdiği ekleri thread'de açabilmek,
**so that** ekran görüntüsü/log/fatura paylaşmak için talepten hiç çıkmayayım ve gelen ekleri kaçırmayayım.

Bu plan (01) o dilimin **kapısını** açar: hiçbir upload kodu yazılmadan önce canlı API'nin gerçek şekli doğrulanır, paketler onaylanıp tam sürümle kurulur ve testlerin dosya/olay üretme yeteneği kurulur. Kullanıcıya görünür ilk yetenek (ataç + chip) Plan 05'te gelir.

<objective>
Faz 4'ün üç kapısını geç: (1) `P1-P5` canlı probe checkpoint'i ile upload yolu, `attachmentIds` kabulü, `?inline=true` desteği, gerçek boyut tavanı ve token yetkisini CONFIRMED/CORRECTED'e çevir (RESEARCH.md Assumptions Log A1-A5); (2) `SUS` işaretli üç paket için blocking-human meşruiyet onayı al ve dört paketi tam sürüm piniyle kur; (3) toast yüzeyini (`sonner`, `next-themes` olmadan) kökte mount et ve testler için `File`/drop/paste olay üretme yeteneğini (Wave 0) kur.

Purpose: RESEARCH.md'nin birincil tavsiyesi — upload istemcisi/tipler **varsayım** üstüne kurulmasın. `public/v1` ön eki ve `comment.attachmentIds` kabulü bu plugin için doğrulanmamış; 02-01-PLAN.md Task 1 deseninin birebir tekrarı. Ayrıca 04-VALIDATION.md'nin Wave 0 maddesi (jsdom `DataTransfer` yeteneği) drop testleri yazılmadan ÖNCE kanıtlanmalı.
Output: Probe bulguları (SUMMARY'de), kurulu ve pinlenmiş paketler, kökte tek `<Toaster />`, paylaşılan test yardımcıları.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-CONTEXT.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-RESEARCH.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-PATTERNS.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-UI-SPEC.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-VALIDATION.md
@.planning/research/attachment-upload-contract.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli, mevcut kod sanmamalı):
- `src/components/ui/sonner.tsx` (YENİ): `Toaster` bileşeni (shadcn `sonner`, `next-themes` çıkarılmış, `theme="light"` sabit)
- `src/app.tsx`: kök render ağacına `<Toaster />` eklenmesi
- `src/lib/attachment-test-helpers.ts` (YENİ, yalnızca test): `makeTestFile`, `makeClipboardPasteEvent`, `makeDropEvent`, `hasNativeDataTransfer`
- `src/lib/__tests__/attachment-test-helpers.test.ts` (YENİ): yardımcıların kendi testleri + jsdom `DataTransfer` yetenek kaydı
- `package.json`: `react-dropzone@19.1.1`, `@tiptap/extension-file-handler@2.27.2`, `@tiptap/extension-image@2.27.2`, `sonner` bağımlılıkları
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Canlı API probe — upload yolu, attachmentIds, inline, boyut tavanı, token (P1-P5)</name>
  <files>.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-RESEARCH.md (§Live-Probe Design — P1-P6 curl komutları hazır; §Assumptions Log A1-A6; §Open Questions #1/#2)
    - .planning/research/attachment-upload-contract.md (§1 gerçek API sözleşmesi: FormData alanı `files`, cevap `UploadFilesResponse[]`, bağlama `comment.attachmentIds`, boşsa alan hiç gönderilmez)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-PLAN.md (Task 1 — bu checkpoint'in birebir deseni: curl'leri executor üretir, human çalıştırır, verdict SUMMARY'ye yazılır)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md (test parent `TICKET-563`, `ts.requester` `:email` biçimi, kapalı ticket'lar 422 döner)
    - src/grispi/client/http-handler.ts (baseUrl `https://api.grispi.net`, varsayılan `Content-Type: application/json`)
    - src/grispi/client/authentication.ts (header adları: `tenantId`, `Authorization: Bearer <token>`)
  </read_first>
  <what-built>
    Bu, "yazma yapılmadan önce gerçeği doğrula" checkpoint'idir — Faz 2 Plan 01 Task 1'in aynısı. Executor beş probe'u tam curl komutu olarak hazırlar; human token'la çalıştırıp ham cevabı yapıştırır. Hedef: A1-A5'i CONFIRMED/CORRECTED'e çevirmek. Plan 04-02'nin HİÇBİR satırı (tipler, `sendMultipart`, `Attachments`) bu doğrulama tamamlanmadan yazılmaz.
  </what-built>
  <how-to-verify>
    Ortak header'lar: `tenantId: gsocial-test`, `Authorization: Bearer $REACT_APP_DEV_TOKEN`. Base URL `https://api.grispi.net`. Executor önce iki yardımcı dosya üretsin: `printf 'probe' > /tmp/probe-test.txt` ve küçük bir PNG (`/tmp/probe-image.png`).

    1. **P1 — Upload yolu prefix'i:** `POST /public/v1/attachments/upload` çağrısını `-F "files=@/tmp/probe-test.txt;type=text/plain"` ve `-w "\nHTTP_STATUS:%{http_code}\n"` ile at. 404 dönerse AYNI çağrıyı kök yola (`/attachments/upload`, `public/v1` olmadan) tekrarla.
       → SORU A1: Hangi yol 2xx dönüyor? Cevap gövdesi dizi mi (tek dosyada bile) ve `id`/`objectUrl`/`objectThumbUrl`/`mimeType`/`size` alanları var mı? Ek olarak `curl -I <objectUrl>` (auth header'sız) 200 dönüyor mu — `objectUrl`'in gerçekten yetkisiz erişilebilir olduğunu doğrula.
    2. **P2 — `attachmentIds` kabulü:** Faz 2'nin `createTicket`'ıyla TAZE bir probe side ticket'ı oluştur (RESEARCH.md Open Questions #1 tavsiyesi — mevcut `TICKET-563` fixture setini kirletme), sonra `PATCH /public/v1/tickets/<KEY>` gövdesi `{"comment":{"body":"[TEST] Faz4 ek probe","publicVisible":true,"creator":[{"key":"us.email","value":"<agent e-postası>"}],"attachmentIds":[<P1'den gelen id>]}}` ile at.
       → SORU A2: 2xx mi? Reddedilirse hata gövdesindeki gerçek alan adı ne?
    3. **P3 — `?inline=true` ve `inline` yankısı:** `POST .../attachments/upload?inline=true` ile PNG yükle, dönen id'yi P2 desenindeki bir PATCH ile bağla, sonra `GET /public/v1/tickets/<KEY>` yapıp `comments[].attachments` dizisini yazdır.
       → SORU A3: `?inline=true` kabul ediliyor mu? `GET` cevabındaki attachment nesnesinde `inline` alanı geri geliyor mu? (Gelmiyorsa D-22 bu API sürümünde no-op'tur — bunu açıkça not et.)
    4. **P4 — Gerçek boyut tavanı / 413:** `head -c 30000000 /dev/urandom > /tmp/probe-30mb.bin` ile ~30MB üret ve upload et; status kodunu ve hata gövdesini kaydet.
       → SORU A4: Gerçek tavan ne? Hata gövdesi `limitKey` benzeri yapılandırılmış bir alan taşıyor mu (chip'in "Yüklenemedi" vs "Bağlantı sorunu" ayrımı buna bakacak)?
    5. **P5 — Token yetkisi:** P1/P3'ün 401/403 dönmemesi bu soruyu dev-token için yanıtlar. Plugin-mode bundle token'ı curl'lenemez → bu, Plan 08'in panel UAT'sinde ikinci kez doğrulanacak ikincil manuel kontrol olarak not edilir.
  </how-to-verify>
  <resume-signal>Beş probe'un ham cevabını (status + gövde) yapıştır ve A1-A5 için CONFIRMED/CORRECTED durumunu belirt. "Onaylandı" ile devam sinyali ver; sapma varsa (ör. yol kök, `inline` yankılanmıyor, gerçek tavan 20MB) açıkça yaz — Plan 02'nin tipleri ve `Attachments.upload` yolu buna göre yazılacak.</resume-signal>
  <action>
    Executor: yukarıdaki beş probe'u tam, kopyala-çalıştır curl komutları hâline getir (header'lar ve `-w "\nHTTP_STATUS:%{http_code}\n"` dahil) ve human'a sun. Human ham cevapları yapıştırdıktan sonra A1-A5 verdict'lerini (CONFIRMED/CORRECTED + gözlenen şekil) ve şu dört kesin çıktıyı SUMMARY'ye "Probe Bulguları" başlığı altında yaz: (a) doğru upload yolu (tam string), (b) `public/v1` PATCH gövdesinin `comment.attachmentIds`'i kabul edip etmediği, (c) `GET` cevabındaki attachment nesnesinin `inline` alanını taşıyıp taşımadığı, (d) gerçek boyut tavanı ve hata gövdesi şekli. Hiçbir istemci/tip kodu bu task'ta yazılmaz.
  </action>
  <verify>
    <human-check>Human beş probe'un ham cevabını yapıştırdı; A1-A5 için CONFIRMED/CORRECTED belirtildi; doğru upload yolu, attachmentIds kabulü, inline yankısı ve boyut tavanı SUMMARY'ye yazıldı.</human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY'de "Probe Bulguları" başlığı altında A1, A2, A3, A4, A5 için ayrı ayrı CONFIRMED veya CORRECTED verdict'i bulunuyor
    - Doğru upload yolu tam string olarak yazılı (Plan 02 `Attachments.upload` bu stringi kullanacak)
    - `comment.attachmentIds`'in `public/v1` PATCH tarafından kabul edilip edilmediği net (kabul edilmiyorsa gerçek alan adı yazılı)
    - `GET /public/v1/tickets/<KEY>` cevabındaki attachment nesnesinin `inline` alanını taşıyıp taşımadığı net (D-22'nin uygulanabilirliği buna bağlı)
    - Gerçek boyut tavanı (status kodu + hata gövdesi şekli) yazılı
    - `objectUrl`'in auth header'sız 200 döndüğü doğrulandı (yetkisiz erişilebilirlik varsayımı, RESEARCH.md "Treating objectUrl as authenticated" tuzağı)
  </acceptance_criteria>
  <done>A1-A5 canlı tenant'ta doğrulandı ve SUMMARY'ye yazıldı; Plan 02 bunlara yaslanacak, varsayıma düşülmeyecek.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Paket meşruiyet onayı — üç SUS-işaretli paket kurulumdan önce</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-RESEARCH.md (§Package Legitimacy Audit — üç paketin `SUS` verdict'i ve `too-new` yanlış-pozitif gerekçesi; §Standard Stack — sürüm pinleri ve `latest` tag'inin neden kullanılamayacağı)
    - .planning/research/attachment-upload-contract.md (§3 Paket kararları — Uppy/FilePond neden reddedildi)
  </read_first>
  <what-built>
    RESEARCH.md'nin meşruiyet kapısı `react-dropzone`, `@tiptap/extension-file-handler` ve `@tiptap/extension-image` için `SUS` döndürdü. Gerekçe her üçünde de aynı: `too-new`, yani paketin yaşı değil son yama sürümünün tarihi. Üçü de resmi org repolarında, milyonlarca haftalık indirmeli paketler. Protokol gereği kurulumdan önce insan onayı gerekir; `workflow.auto_advance` bu checkpoint'i atlayamaz.
  </what-built>
  <how-to-verify>
    Human üç paketi kayıt defterinde tek tek doğrulasın:
    1. `npmjs.com/package/react-dropzone` → repo `github.com/react-dropzone/react-dropzone`, indirme hacmi milyonlar mertebesinde, `19.1.1` sürümü mevcut.
    2. `npmjs.com/package/@tiptap/extension-file-handler` → repo `github.com/ueberdosis/tiptap` (bu projede zaten çalışan `@tiptap/core`/`@tiptap/react`/`@tiptap/starter-kit` ile aynı org), `2.27.2` sürümü mevcut, lisans MIT.
    3. `npmjs.com/package/@tiptap/extension-image` → aynı org/monorepo, `2.27.2` mevcut.
    Ayrıca kritik pin kuralını teyit et: `@tiptap/extension-file-handler` için `latest` tag'i **3.x** çözer ve `@tiptap/core@3.x` peer'i ister — bu projenin `2.27.2` core'uyla uyumsuzdur. Bu yüzden hiçbir paket `^` veya aralıksız yazılmayacak; üçü de tam sürümle kurulacak.
  </how-to-verify>
  <resume-signal>Üç paketi de doğruladıysan "onaylandı" yaz. Şüphen varsa hangi paket ve neden olduğunu yaz — kurulum yapılmaz.</resume-signal>
  <action>
    Executor: human'a yukarıdaki üç kayıt-defteri bağlantısını ve tam sürüm pinlerini (`react-dropzone@19.1.1`, `@tiptap/extension-file-handler@2.27.2`, `@tiptap/extension-image@2.27.2`) tek listede sun; `SUS` verdict'inin `too-new` yanlış-pozitifi olduğunu ve `@tiptap/extension-file-handler` için `latest` tag'inin uyumsuz v3'e çözdüğünü açıkça belirt. Onay alınmadan Task 3'teki hiçbir kurulum komutu çalıştırılmaz. Onay ve gerekçe SUMMARY'ye yazılır.
  </action>
  <verify>
    <human-check>Human üç paketi npm kayıt defterinde doğruladı ve açık onay verdi; onay SUMMARY'ye kaydedildi.</human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY'de üç paketin her biri için human onayı kayıtlı
    - Tam sürüm pinleri (19.1.1 / 2.27.2 / 2.27.2) onay metninde geçiyor
    - `@tiptap/extension-file-handler` için aralıklı sürüm kullanılmayacağı teyit edilmiş
  </acceptance_criteria>
  <done>Üç SUS paket insan tarafından onaylandı; Task 3 kurulumu serbest.</done>
</task>

<task type="auto">
  <name>Task 3: Paket kurulumu + toast yüzeyi + test yeteneği (Wave 0)</name>
  <files>package.json, src/components/ui/sonner.tsx, src/app.tsx, src/lib/attachment-test-helpers.ts, src/lib/__tests__/attachment-test-helpers.test.ts</files>
  <read_first>
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-RESEARCH.md (§sonner Toast Under CRA — shadcn CLI'nin `next-themes` bağımlılığı ve neden çıkarılacağı; §Validation Architecture — jsdom'da `File`/`DataTransfer`/clipboard olayı üretme)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-PATTERNS.md (§`src/components/ui/sonner.tsx` — `badge.tsx` shadcn-primitif konvansiyonu; §`src/lib/attachment-test-helpers.ts` — `Object.defineProperty` idiom'u ve dosya yerleşimi)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-UI-SPEC.md (§7 — toast konumu `top-center`, genişlik `w-[calc(100%-2rem)] max-w-[340px]`, üst offset, destructive renk dili)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-VALIDATION.md (§Wave 0 Requirements — üç madde)
    - src/components/ui/badge.tsx (projenin shadcn-primitif yazım konvansiyonu: `cva`+`cn`, adlandırılmış export, default export yok)
    - src/app.tsx (kök ağaç: `StoreProvider` > `GrispiProvider` > `AppContent`; `<Toaster />` buraya bir kez girer)
    - src/screens/components/__tests__/rich-text-composer.test.tsx (satır ~318-340: mevcut clipboardData mock şekli — yardımcılar bununla uyumlu olmalı)
  </read_first>
  <action>
    (a) Task 2'de onaylanan üç paketi TAM sürümle kur: `react-dropzone@19.1.1`, `@tiptap/extension-file-handler@2.27.2`, `@tiptap/extension-image@2.27.2`. `package.json`'da üçü de aralıksız (caret'siz) yazılmalı. Ardından `npx shadcn@latest add sonner` çalıştır.

    (b) shadcn'in ürettiği `src/components/ui/sonner.tsx`'i projeye uyarla: `next-themes` import'unu ve `useTheme` kullanımını tamamen kaldır, `theme` değerini sabit `"light"` yap (CLAUDE.md kısıtı: her zaman açık tema). CLI `next-themes`'i bağımlılık olarak eklediyse `npm uninstall next-themes` ile kaldır ve `package.json`'da kalmadığını doğrula. Dosya şekli `badge.tsx` konvansiyonuna uysun: adlandırılmış export (`Toaster`), default export yok. `"use client"` yönerge satırı CRA'de zararsızdır ama gereksizdir — sil. `toastOptions.classNames` içine UI-SPEC §7'nin sınıflarını yaz: konteyner genişliği `w-[calc(100%-2rem)] max-w-[340px]`, yüzey `bg-card text-foreground`, hata toast'ı için `border-destructive/30`. `position="top-center"` ve sabit header'ı geçen üst offset (`--panel-header-height` + `--panel-inset` toplamı, ≈64px) bileşenin kendi varsayılanı olsun; çağıranlar bunları tekrar geçmek zorunda kalmasın. Kapatma affordance'ı görünür olsun (`closeButton`).

    (c) `src/app.tsx`'te `<Toaster />`'ı `App` bileşeninin döndürdüğü ağaca, `GrispiProvider` içinde `AppContent`'in kardeşi olarak bir kez ekle (ekran başına DEĞİL). UI-SPEC §7: uygulamada tam olarak bir tane mount edilir.

    (d) `src/lib/attachment-test-helpers.ts` (YENİ, yalnızca testlerden import edilir; hiçbir üretim dosyası bu dosyayı import etmemeli) şu dördünü export etsin: `makeTestFile(name, size, type)` → `new File([new Uint8Array(size)], name, { type })`; `makeClipboardPasteEvent(files, html?)` → `Object.defineProperty` ile `clipboardData` yerleştirilmiş `paste` olayı, `clipboardData.files` ve `getData(type)` (html verilmediyse `""`) taşır; `makeDropEvent(files)` → aynı idiom'la `dataTransfer` taşıyan `drop` olayı; `hasNativeDataTransfer` → boolean, jsdom'un `new DataTransfer()` + `items.add(file)` desteğini çalışma anında sınayan sonuç. `makeDropEvent`, `hasNativeDataTransfer` true ise gerçek `DataTransfer` kullansın, değilse `{ files }` düz-nesne stub'ına düşsün — böylece drop testleri jsdom sürümünden bağımsız çalışır (04-VALIDATION.md Wave 0, RESEARCH.md A11).

    (e) `src/lib/__tests__/attachment-test-helpers.test.ts` (YENİ): `makeTestFile`'ın `name`/`size`/`type` alanlarını doğru kurduğunu; `makeDropEvent([file])` çıktısının `dataTransfer.files.length === 1` ve `files[0].name` eşleşmesi verdiğini (hangi dal çalışırsa çalışsın); `makeClipboardPasteEvent([file])` çıktısının `clipboardData.files.length === 1` ve `getData("text/html")` boş string döndürdüğünü; `makeClipboardPasteEvent([], "<p>x</p>")` çıktısının `getData("text/html")` ile o HTML'i döndürdüğünü doğrula. Ayrıca `hasNativeDataTransfer` değerinin boolean olduğunu assert et ve executor bu değeri (true/false) SUMMARY'ye yazsın — 04-VALIDATION.md'nin yetenek kaydı budur.

    (f) `npm run build`'in bozulmadığını doğrula (sonner + üç yeni paket bundle'a giriyor).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=attachment-test-helpers && CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"react-dropzone": "19.1.1"' package.json` = 1 (caret yok)
    - `grep -c '"@tiptap/extension-file-handler": "2.27.2"' package.json` = 1 (caret yok)
    - `grep -c '"@tiptap/extension-image": "2.27.2"' package.json` = 1 (caret yok)
    - `grep -c '"sonner"' package.json` ≥ 1
    - `grep -c 'next-themes' package.json` = 0 (CLI eklediyse kaldırılmış olmalı)
    - `grep -c 'next-themes' src/components/ui/sonner.tsx` = 0
    - `grep -c 'export' src/components/ui/sonner.tsx` ≥ 1 ve dosya `Toaster` adını export ediyor
    - `grep -c '<Toaster' src/app.tsx` = 1 (kökte tek mount)
    - `src/lib/attachment-test-helpers.ts` `makeTestFile`, `makeClipboardPasteEvent`, `makeDropEvent`, `hasNativeDataTransfer` sembollerini export ediyor
    - Hiçbir üretim dosyası `attachment-test-helpers`'ı import etmiyor (`grep -rln "attachment-test-helpers" src` çıktısındaki her yol ya `__tests__/` altında ya da dosyanın kendisi)
    - `CI=true npx craco test --watchAll=false --testPathPattern=attachment-test-helpers` yeşil
    - `CI=true npx tsc --noEmit` hatasız
    - `npm run build` hatasız (sonner + Tiptap eklentileri derleniyor)
    - `hasNativeDataTransfer` değeri (true/false) SUMMARY'ye yazılmış
  </acceptance_criteria>
  <done>Dört paket tam pinle kurulu, toast yüzeyi kökte tek sefer mount edilmiş ve `next-themes`'ten arınmış, testler artık `File`/drop/paste olayı üretebiliyor ve jsdom yeteneği kayıtlı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm kayıt defteri → build çıktısı | Bu fazın dört yeni bağımlılığı bundle'a giriyor; tedarik zinciri yüzeyi bu planda açılıyor |
| plugin → Grispi Public API (upload) | Task 1 probe'ları bu sınırı canlı tenant'ta ilk kez dosya baytlarıyla geçiyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm installs (`react-dropzone`, `@tiptap/extension-file-handler`, `@tiptap/extension-image`, `sonner`) | high | mitigate | Task 2 blocking-human meşruiyet checkpoint'i (auto-advance atlayamaz) + tam sürüm pini (caret yok) — özellikle `@tiptap/extension-file-handler`'ın `latest` tag'i uyumsuz v3'e çözüyor; dördünde de `postinstall` script'i yok (RESEARCH.md doğruladı) |
| T-04-01 | Tampering | `next-themes` (shadcn CLI'nin gereksiz transitif eklemesi) | low | mitigate | Task 3(b) paketi kaldırıyor ve `package.json`'da kalmadığı grep ile doğrulanıyor — kullanılmayan bağımlılık = gereksiz saldırı yüzeyi |
| T-04-02 | Information Disclosure | Task 1 probe'ları canlı tenant'a gerçek dosya yüklüyor (P4 30MB rastgele bayt dahil) | low | accept | Probe dosyaları anlamsız içerik (`probe` metni, `/dev/urandom`); taze probe ticket'ı kullanılıyor, mevcut fixture seti kirletilmiyor. Yüklenen dosyalar D-17 gereği sunucuda kalır — kabul edilmiş bedel |
| T-04-03 | Information Disclosure | `objectUrl`'in auth header'sız erişilebilir olması | medium | accept | Grispi API tasarımından devralınan davranış; client tarafında azaltılamaz. Task 1 bunu doğrular ve SUMMARY'ye yazar; `Content-Disposition`/ayrı origin sertleştirmesi Grispi ekibinin işi (kapsam dışı) |

Bu planın yüzeyinde tek `high` tehdit T-04-SC ve `mitigate` disposition'ı Task 2'nin blocking-human checkpoint'i ile karşılanıyor → block-on-high tatmin edilmiş.
</threat_model>

<verification>
- Task 1 probe verdict'leri (A1-A5) SUMMARY'de; Plan 02 bunlara yaslanıyor
- Task 2 paket onayı SUMMARY'de
- `CI=true npx craco test --watchAll=false` tam süit yeşil (mevcut 22 süit/191 test bozulmamış)
- `CI=true npx tsc --noEmit` hatasız
- `npm run build` hatasız
</verification>

<success_criteria>
- Upload yolu, `attachmentIds` kabulü, `?inline=true` desteği, boyut tavanı ve token yetkisi canlı tenant'ta doğrulandı (varsayımdan kod inşa edilmedi)
- Dört paket insan onayıyla ve tam sürüm piniyle kurulu; `next-themes` yok
- Kökte tek `<Toaster />` mount edilmiş
- Test tarafı `File`/drop/paste üretebiliyor; jsdom `DataTransfer` yeteneği kayıtlı
</success_criteria>

<output>
Create `.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-01-SUMMARY.md` when done. Probe bulgularını (A1-A5 verdict'leri, doğru upload yolu, `inline` yankısı, boyut tavanı) ve `hasNativeDataTransfer` değerini SUMMARY'ye MUTLAKA yaz — Plan 02 ve Plan 05 bunlara bağımlı.
</output>
