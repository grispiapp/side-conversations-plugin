---
phase: 04-dosya-ekleri-ve-inline-g-rseller
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/store/active-conversation-store.ts
  - src/query/side-conversation-queries.ts
  - src/query/__tests__/side-conversation-queries.test.tsx
  - src/screens/components/thread-message.tsx
  - src/screens/components/__tests__/thread-components.test.tsx
autonomous: true
requirements: [THRD-06]

must_haves:
  truths:
    - "Karşı tarafın gönderdiği ekler ilgili mesajın altında görünüyor (görsel küçük resim satırı ve/veya dosya chip'i satırı)"
    - "Eke tıklayınca dosya yeni sekmede açılıyor ve indirilebiliyor"
    - "Gövdede zaten gömülü olan ekler de listede görünüyor — hiçbir ek görünürlük dışı bırakılmıyor (D-22)"
    - "Gelen mesaj gövdesindeki uzak görseller hâlâ gösterilmiyor (D-21 bozulmadı)"
  artifacts:
    - src/screens/components/thread-message.tsx
  key_links:
    - "Comment.attachments → normalizeComment → MessageVM.attachments → ThreadMessageData.attachments → ThreadMessage render"
    - "ThreadMessage ek bağlantıları → objectUrl (auth header'sız, yeni sekme, noopener noreferrer)"
---

## Phase Goal (User Story)

**As a** Grispi temsilcisi, **I want to** yan görüşme mesajlarına dosya ekleyip yapıştırdığım görseli inline gömmek ve karşı tarafın gönderdiği ekleri thread'de açabilmek, **so that** ekran görüntüsü/log/fatura paylaşmak için talepten çıkmayayım ve gelen ekleri kaçırmayayım.

Bu plan (04) fazın **ilk uçtan uca görünür dilimini** teslim eder: bu plan bittiğinde temsilci, tedarikçinin gönderdiği faturayı thread'de görüp tıklayarak açabilir. Yeni bir ağ çağrısı gerekmez — veri zaten `getTicket` cevabında geliyor, projeksiyonda düşüyordu.

<objective>
THRD-06'yı kapat: `Comment.attachments` alanının `normalizeComment` projeksiyonunda sessizce düşmesini gider, `MessageVM`/`ThreadMessageData` tiplerine taşı ve `ThreadMessage`'a UI-SPEC §9'un iki bölgeli render'ını (görsel küçük resim satırı + dosya chip'i satırı) ekle. D-22 gereği hiçbir ek filtrelenmez.

Purpose: Bu tamamen bir **projeksiyon hatası düzeltmesi**dir; ekler `getTicket()` cevabında zaten mevcut ve `Attachment` tipi zaten tanımlıydı (Plan 02'de export edildi). Yeni fetch, yeni endpoint, yeni store yok. D-21 gereği gelen gövde HTML'i sıkı politikada kaldığı için, karşı tarafın ekran görüntüsü YALNIZCA bu ek listesinden görünür — bu yüzden grispi-ui'nın gövdede gösterdiği ekleri listeden eleme davranışı bilinçli olarak DEVRALINMAZ (D-22).
Output: Ekleri taşıyan mesaj tipi + thread'de açılabilir ek render'ı.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-CONTEXT.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-RESEARCH.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-PATTERNS.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-UI-SPEC.md
@.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-03-SUMMARY.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/store/active-conversation-store.ts`: `MessageVM.attachments?: Attachment[]` alanı
- `src/screens/components/thread-message.tsx`: `ThreadMessageData.attachments?: Attachment[]` alanı + ek render bloğu
- `src/query/side-conversation-queries.ts`: `normalizeComment`'in `attachments` projeksiyonu
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizeComment ek projeksiyonu — düşen alanı MessageVM'e taşı</name>
  <files>src/store/active-conversation-store.ts, src/query/side-conversation-queries.ts, src/query/__tests__/side-conversation-queries.test.tsx</files>
  <read_first>
    - src/query/side-conversation-queries.ts (satır 411-435 `normalizeComment` — dönüş nesnesi `comment.attachments`'ı hiç okumuyor; `senderName: comment.creator?.fullName || undefined` biçimindeki "boşsa tanımsız" idiom'u kopyalanacak)
    - src/store/active-conversation-store.ts (satır 29-41 `MessageVM` — opsiyonel alan yazım stili: `errorKind?`, `senderName?`, `authoredBodyHtml?`)
    - src/types/grispi.type.ts (Plan 02'de export edilen `Attachment` interface'i ve varsa `inline` alanı)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-PATTERNS.md (§`src/query/side-conversation-queries.ts` — ÖNCE/SONRA kod bloğu; eklenecek tek satır ve korunacak idiom)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-CONTEXT.md (D-22 — gövdede gösterilen ekleri listeden eleme davranışı DEVRALINMAZ; D-21 — gelen gövde politikası değişmez)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-02-SUMMARY.md (Plan 01 P3 probe'unun `inline` alanı hakkındaki bulgusu — alan gerçekten dönüyor mu)
    - src/query/__tests__/side-conversation-queries.test.tsx (mevcut `normalizeComment` fixture'ları ve `advanceAndFlush` zamanlayıcı yardımcısı)
  </read_first>
  <behavior>
    - Test: `attachments` taşıyan bir yorum fixture'ı `MessageVM.attachments` içinde AYNI kayıtlarla dönüyor
    - Test: `attachments` alanı boş dizi olan yorum için `MessageVM.attachments` tanımsız oluyor (mevcut "boşsa tanımsız" idiom'u)
    - Test: `attachments` alanı hiç bulunmayan yorum için `MessageVM.attachments` tanımsız oluyor ve fonksiyon çökmüyor
    - Test: gövdede gömülü olduğu işaretlenmiş bir ek (P3'te doğrulanan işaret alanı) çıktıda AYNEN yer alıyor — hiçbir ek elenmiyor (D-22 regresyon kapısı)
    - Test: `attachments` eklendikten sonra mevcut alanların (gövde, yön, tarih, gönderen, alıntı) değerleri değişmiyor
  </behavior>
  <action>
    (a) `src/store/active-conversation-store.ts`: `MessageVM` interface'ine opsiyonel `attachments?: Attachment[]` alanını ekle, mevcut opsiyonel alanların yazım stiliyle. `Attachment` tipini `@/types/grispi.type`'tan import et (Plan 02'de export edildi).

    (b) `src/query/side-conversation-queries.ts`: `normalizeComment`'in dönüş nesnesine `attachments` alanını ekle. Değer, yorumun ek dizisi doluysa dizinin kendisi, aksi hâlde tanımsız olsun — fonksiyonun zaten kullandığı "boşsa tanımsız" idiom'unun aynısı. **Hiçbir eleme uygulanmaz**: D-22'nin tüm amacı, gövdede gösterilmeyen (D-21) eklerin listede görünür kalmasıdır; grispi-ui'nın gövdede-gösterilen-ekleri-listeden-çıkarma davranışı bu plugin'e alınmaz. Bu kuralı fonksiyonun doc-comment'ine D-22 atfıyla yaz.

    (c) `src/query/__tests__/side-conversation-queries.test.tsx`: behavior bloğundaki beş vakayı ekle. D-22 regresyon vakası kritik — gövdede-gömülü işaretli bir ek taşıyan fixture kur ve çıktıda o kaydın bulunduğunu doğrudan assert et. Plan 01 P3 probe'u işaret alanının bu API sürümünde dönmediğini gösterdiyse fixture'ı yine kur (tip alanı opsiyonel) ve testin adına "D-22" atfını koy — davranış sözleşmesi API sürümünden bağımsız kilitlenir.
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=side-conversation-queries && CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "attachments?: Attachment\[\]" src/store/active-conversation-store.ts` = 1
    - `grep -c "attachments" src/query/side-conversation-queries.ts` ≥ 1 ve `normalizeComment` dönüş nesnesi bu alanı taşıyor
    - `side-conversation-queries` testinde gövdede-gömülü işaretli bir ek fixture'ının çıktıda AYNEN yer aldığını doğrulayan, adında D-22 geçen bir test var
    - Boş ek dizisi ve hiç ek alanı olmayan yorum için çıktının tanımsız olduğunu doğrulayan iki test var
    - Mevcut `normalizeComment` testleri değişmeden yeşil (gövde/yön/alıntı projeksiyonu bozulmadı)
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Ekler artık `getTicket` cevabından mesaj görünüm modeline kadar taşınıyor; hiçbir ek elenmiyor.</done>
</task>

<task type="auto">
  <name>Task 2: ThreadMessage ek render'ı — küçük resim satırı + dosya chip'i satırı</name>
  <files>src/screens/components/thread-message.tsx, src/screens/components/__tests__/thread-components.test.tsx</files>
  <read_first>
    - src/screens/components/thread-message.tsx (tamamı: `ThreadMessageData` tipi, gövde sink'i satır 101-104, alıntı bloğu satır 106-124, bekleyen/başarısız durum blokları satır 126-154 — yeni blok gövde/alıntıdan SONRA, durum satırlarından ÖNCE girer)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-UI-SPEC.md (§9 — iki bölge, sıralama, 72px küçük resim ölçüsü ve sınıfları, SVG'nin küçük resimden hariç tutulması, bağlantı semantiği ve `aria-label` kalıbı; Copywriting Contract'taki ek bağlantı metinleri)
    - src/screens/components/attachment-chip.tsx (Plan 03 — salt-okunur `href` modu; dosya chip'i satırı bunu kullanır)
    - src/lib/attachment-format.ts (Plan 02 — `attachmentKind`, `formatFileSize`, `truncateFilename`)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-PATTERNS.md (§`src/screens/components/thread-message.tsx` — yeni bloğun yerleşimi ve `target="_blank"` + `rel="noopener noreferrer"` konvansiyonunun kaynağı)
    - src/screens/components/__tests__/thread-components.test.tsx (mevcut `ThreadMessage` test kurulumu ve fixture şekli)
    - .planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-CONTEXT.md (D-19 grispi-ui örnekli MIME ayrımı; D-20 yeni sekme, lightbox yok; D-21 gövde görselleri gösterilmez; D-22 eleme yok)
  </read_first>
  <action>
    (a) `ThreadMessageData` interface'ine opsiyonel `attachments?: Attachment[]` alanını ekle (mevcut opsiyonel alan stiliyle). Bu tip `MessageVM` ile yapısal olarak uyumlu kalmalı — chat ekranı MessageVM'i doğrudan bu bileşene geçiriyor.

    (b) Ek render bloğunu, alıntı bloğundan SONRA ve bekleyen/başarısız durum satırlarından ÖNCE ekle (UI-SPEC §9'un konumu). Blok yalnızca ek listesi doluyken render edilir ve **hem gelen hem kendi mesajlarımız** için çalışır (temsilci kendi gönderdiği ekin thread'e döndüğünü de görmeli).

    İki bölge, yalnızca içeriği olan bölge render edilir, sıra sabit — önce görsel küçük resim satırı, sonra dosya chip'i satırı:
    - **Görsel küçük resim satırı:** `attachmentKind` sonucu görsel olan ekler (SVG HARİÇ — D-10 tek kural). Her biri UI-SPEC §9'un 72px kare ölçüsünde, sarmalayıcı esnek ve satır kaydıran bir konteynerde. Her küçük resim bir bağlantının içindedir; bağlantı `href` olarak ekin `objectUrl` değerini alır, `target="_blank"` ve `rel="noopener noreferrer"` taşır (D-20 — `window.open` sarmalayıcısı YOK, düz bağlantı; orta tık/ctrl-tık doğal çalışsın). Görsel elemanın `alt` değeri dosya adıdır, bağlantının `title` değeri de dosya adıdır. Küçük resim kaynağı doğrudan ekin URL'idir — hiçbir yetkilendirme başlığı/fetch sarmalayıcısı eklenmez (RESEARCH.md "objectUrl'i yetkilendirilmiş sanmak" tuzağı).
    - **Dosya chip'i satırı:** kalan her şey (pdf, video, genel dosya ve SVG). Her biri Plan 03'ün `AttachmentChip` bileşeninin salt-okunur bağlantı modunda render edilir; `aria-label` UI-SPEC Copywriting Contract'taki "ad, boyut, yeni sekmede açılır" kalıbını kullanır.

    İki bölge arasındaki boşluk UI-SPEC §9'un değeridir.

    (c) Gövde sink'ine (satır 101-104) ve alıntı sink'ine DOKUNMA — D-21 gereği gelen gövde politikası bu planda değişmez; ekler tamamen ayrı bir blokta render edilir, hiçbir zaman gövde HTML'inin içinde değil.

    (d) `src/screens/components/__tests__/thread-components.test.tsx`: görsel eki olan bir mesajın küçük resim bağlantısı ürettiğini ve bağlantının `rel` değerinin hem `noopener` hem `noreferrer` içerdiğini; pdf ekinin chip satırında göründüğünü; SVG ekinin küçük resim DEĞİL chip ürettiğini (D-10); gövdede-gömülü işaretli bir ekin yine render edildiğini (D-22); eki olmayan mesajın hiç ek bloğu üretmediğini; kendi yönündeki bir mesajın da eklerini gösterdiğini; ek listesindeki dosya adının metin olarak (HTML sink'i olmadan) basıldığını doğrula.
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=thread-components</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "attachments?: Attachment\[\]" src/screens/components/thread-message.tsx` = 1
    - `grep -c "noopener noreferrer" src/screens/components/thread-message.tsx` ≥ 1
    - `grep -c "window.open" src/screens/components/thread-message.tsx` = 0 (D-20 düz bağlantı ister)
    - `grep -c "AttachmentChip" src/screens/components/thread-message.tsx` ≥ 1 (chip kabuğu paylaşılıyor, kopyalanmıyor)
    - `thread-components` testinde SVG ekinin küçük resim üretmediğini doğrulayan bir test var (D-10)
    - `thread-components` testinde gövdede-gömülü işaretli ekin render edildiğini doğrulayan, adında D-22 geçen bir test var
    - Mevcut gövde/alıntı render testleri değişmeden yeşil (D-21 bozulmadı — gelen gövdede hâlâ görsel çıkmıyor)
    - `CI=true npx craco test --watchAll=false --testPathPattern=thread-components` yeşil
  </acceptance_criteria>
  <done>Karşı tarafın gönderdiği ekler mesajın altında görünüyor ve tıklanınca yeni sekmede açılıyor; hiçbir ek gizlenmiyor; gelen gövde politikası bozulmadı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| üçüncü tarafın gönderdiği ek → panel DOM'u | Harici alıcının yüklediği dosya adı, MIME tipi ve URL'i ilk kez panelde render ediliyor |
| harici `objectUrl` → `<img src>` ve `href` | Uzak bir kaynak ilk kez thread yüzeyinde yükleniyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering / Elevation of Privilege | Üçüncü tarafın yüklediği SVG'nin thread'de önizlenmesi (stored XSS sınıfı; sözleşmede FreeScout emsali) | high | mitigate | SVG küçük resim bölgesinden ÇIKARILIR, dosya chip'i olarak render edilir ve yalnızca yeni sekmede (Grispi origin'inde) açılır; `<object>`/`<iframe>`/ham `<svg>` bu bileşende hiç kullanılmaz (D-10, UI-SPEC §9). Regresyon testi mevcut |
| T-04-13 | Tampering | Harici dosya adının render edilmesi | medium | mitigate | Ad yalnızca JSX metin düğümü ve `alt`/`title`/`aria-label` değeri olarak kullanılır; ek bloğu hiçbir HTML sink'i içermez ve mevcut gövde sink'inin dışındadır |
| T-04-14 | Information Disclosure | Gelen gövdedeki uzak görsellerin (takip pikseli) yüklenmesi | medium | mitigate | D-21 gereği gelen gövde politikası bu planda DEĞİŞMİYOR; gövde sink'ine dokunulmuyor ve mevcut testleri yeşil kalıyor. Yalnızca ek listesindeki URL'ler yükleniyor — bunlar zaten karşı tarafın bilinçli olarak gönderdiği dosyalar |
| T-04-15 | Information Disclosure | Yeni sekmede açılan bağlantının açan sayfaya erişmesi (tabnabbing) | low | mitigate | Her bağlantı `rel="noopener noreferrer"` taşır (grep kapısı) — projenin mevcut bağlantı sertleştirme konvansiyonunun aynısı |
| T-04-16 | Information Disclosure | Yetkisiz erişilebilir `objectUrl`'in sızması | medium | accept | Grispi API tasarımından devralınan davranış; Plan 01'de doğrulandı. Client tarafında azaltılamaz — `Content-Disposition`/ayrı origin sertleştirmesi Grispi platform ekibinin işi (kapsam dışı) |

`high` severity T-04-12 `mitigate` disposition'ı ve test kapısıyla karşılanıyor → block-on-high tatmin edilmiş.
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false` tam süit yeşil
- `CI=true npx tsc --noEmit` hatasız
- Chat ekranında ek taşıyan bir mesaj açıldığında ek bloğu görünüyor (Plan 08'in panel UAT'sinde gözle de doğrulanacak)
</verification>

<success_criteria>
- THRD-06 karşılandı: gelen ekler mesajla birlikte görünüyor ve açılıp indirilebiliyor
- D-22 uygulandı: hiçbir ek listeden elenmiyor
- D-21 korundu: gelen gövdede hâlâ görsel render edilmiyor
- D-10 uygulandı: SVG hiçbir yüzeyde önizlenmiyor
</success_criteria>

<output>
Create `.planning/phases/04-dosya-ekleri-ve-inline-g-rseller/04-04-SUMMARY.md` when done.
</output>
