---
phase: 04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/grispi.type.ts
  - src/grispi/client/tickets.ts
  - src/grispi/client/__tests__/tickets.test.ts
  - src/lib/side-conversation.ts
  - src/lib/__tests__/side-conversation.test.ts
  - src/query/side-conversation-queries.ts
  - src/query/__tests__/side-conversation-queries.test.tsx
autonomous: true
requirements: [UX-01, UX-04, UX-09]

must_haves:
  truths:
    - "SC1/D-01 — Yeni yan konuşma oluşturulduğunda, create başarılı olduktan SONRA yan ticket'a `publicVisible: false` taşıyan bir iç not PATCH'i atılır; alıcıya e-posta bağlanmaz (`toId`/`toEmail` null)"
    - "SC1/D-04 — İç not PATCH'i başarısız olursa en fazla 1 kez yeniden denenir, sonra yalnızca `console.error` yazılır; `executeCreateMutation` throw ETMEZ, envelope kabul edilmiş (accepted) durumda kalır, agent hiçbir hata görmez"
    - "SC9/D-22 — Aynı PATCH gövdesi `tu.side_conversation_parent` alanını üst talep anahtarıyla yeniden tesis eder; bu alanın başarısızlığı iç not hatasından AYRI bir `console.error` satırı olarak loglanır"
    - "Sıralama (RESEARCH Pitfall #1) — PATCH, `refreshCanonicalAfterMutation`'ın detay refetch'inden ÖNCE await edilir; agent'ın konuşmayı ilk açışında not zaten thread'dedir"
    - "SC4/D-12 — Detay yolunda alıcı, üçüncü taraf henüz hiç yorum yazmamışken bile çözülür: liste yolundaki mevcut `grispiAPI.users.getUser(requesterId)` fallback'i tekrar kullanılır, yeni bir çözümleme yolu icat edilmez"
  artifacts:
    - "src/types/grispi.type.ts — `InternalNotePatchRequest` (kardeş tip, `publicVisible: false` literal)"
    - "src/grispi/client/tickets.ts — `Tickets.addInternalNote`"
    - "src/lib/side-conversation.ts — `formatInternalNoteBody`"
    - "src/query/side-conversation-queries.ts — `assertSideConversationLink`, `resolveDetailRecipientFallback`"
  key_links:
    - "executeCreateMutation → `boundary.activeConversation.mutationAccepted(envelope)` → **await `assertSideConversationLink(...)`** → `refreshCanonicalAfterMutation(...)` — bu sıra bozulursa not/alan ilk görünümde eksik kalır"
    - "`envelope.request.comment.creator[0].value` → iç not gövdesinin `creator` alanı — agent kimliği zaten envelope'ta taşınıyor, yeni bir plumbing gerekmez"
    - "sideConversationDetailOptions.queryFn → `normalizeSideConversationDetail` → `resolveDetailRecipientFallback` → `grispiAPI.users.getUser` — `—` değerinin tek çıkış kapısı"
  prohibitions:
    - statement: "D-05 — Üst (ana) talebe hiçbir not/yorum yazılmaz; bu fazın tek yeni yazma çağrısı yan ticket'a atılan PATCH'tir"
      status: must-not
      verification: "`grep -c 'envelope.parentKey' src/query/side-conversation-queries.ts` çıktısındaki her kullanım okuma amaçlıdır VE `grep -n 'addInternalNote(' src/query/side-conversation-queries.ts` çıktısındaki tek çağrının ilk argümanı `sideKey`'dir, `parentKey` DEĞİLDİR"
    - statement: "`ReplyTicketPatchRequest.comment.publicVisible` literali `boolean`'a genişletilmez ve iç not için `replyTicket` tekrar kullanılmaz (RESEARCH Pitfall #3)"
      status: must-not
      verification: "`grep -c 'publicVisible: true' src/types/grispi.type.ts` ≥ `2` (create + reply literalleri korunmuş) VE `grep -c 'publicVisible: boolean' src/types/grispi.type.ts` → `0`"
    - statement: "İç not gövdesi hiçbir zaman üst talebin konusunu, açıklamasını veya kullanıcı-yazımı bir metni taşımaz — yalnızca üst talep ANAHTARI interpolasyon edilir (T-04.2-05)"
      status: must-not
      verification: "`grep -c 'subject' src/lib/side-conversation.ts` çıktısı yalnızca mevcut `formatPrefillSubject` bağlamında geçer VE `formatInternalNoteBody` tek parametre alır: `grep -c 'export function formatInternalNoteBody(parentKey: string): string' src/lib/side-conversation.ts` → `1`"
    - statement: "İç not gövdesi `sanitizeHtml`/`sanitizeAuthoredHtml` içinden geçirilmez — sabit metindir, yeni bir sanitizasyon yüzeyi açılmaz"
      status: must-not
      verification: "`grep -c 'sanitize' src/lib/side-conversation.ts` → `0`"
---

## Phase Goal

**As a** Grispi temsilcisi, **I want to** açtığım yan konuşmanın Grispi tarafında da üst talebiyle ilişkili doğmasını ve alıcının kim olduğunu ilk andan itibaren görmeyi, **so that** yan konuşma listede kaybolmasın ve panelde hiçbir yerde `—` ile karşılaşmayayım.

> **Not (MVP mode):** ROADMAP'teki `**Goal:**` satırı sonuç-biçiminde yazılmış, kullanıcı-hikâyesi biçiminde değil. Yukarıdaki hikâye ROADMAP hedefi + UX-01/UX-04/UX-09'dan mekanik olarak türetilmiştir; yeni kapsam eklemez.

<objective>
Fazın tek yeni yazma yolunu (D-01 iç not PATCH'i + D-22 parent alanı yeniden tesisi) ve tek veri-katmanı düzeltmesini (D-12 alıcı fallback'i) uçtan uca kurar. Bu plan hiçbir UI dosyasına dokunmaz — üstüne binen tüm ekran işleri (Plan 06, 07) bu katmanın döndürdüğü veriye güvenir.

Purpose: Bugün yeni bir yan konuşma oluşturulduğunda (a) Grispi tarafında ilişkiyi anlatan hiçbir iz kalmıyor, (b) canlı probe `POST /v2/tickets`'ın `fields` dizisinin `tu.side_conversation_parent`'ı taze create'te kaydetmeyebildiğini gösterdi — yani konuşma listede hiç belirmeyebiliyor, (c) üçüncü taraf yanıt yazana kadar alıcı `—` görünüyor. Üçü de veri katmanında, tek dosyada bitiyor.

Output: Bir yeni istek tipi, bir yeni API istemci metodu, bir saf metin biçimlendirici, mutation akışına eklenen sessiz-yeniden-deneme adımı ve detay yolunun alıcı fallback'i — hepsi mevcut test dosyaları genişletilerek kanıtlanır.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-CONTEXT.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-RESEARCH.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-PATTERNS.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-VALIDATION.md
</context>

## Artifacts this phase produces

> Bu bölüm, drift doğrulaması yapan araçların "bu sembol repoda yok, uydurulmuş" yanlış-pozitiflerini elemesi içindir. Aşağıdaki her şey BU faz tarafından yeni oluşturulur.

**Bu planın ürettiği yeni semboller:**

| Sembol | Tür | Dosya |
|--------|-----|-------|
| `InternalNotePatchRequest` | interface (`publicVisible: false` literal + `fields`) | `src/types/grispi.type.ts` |
| `addInternalNote(ticketKey, body)` | `Tickets` sınıfı metodu | `src/grispi/client/tickets.ts` |
| `formatInternalNoteBody(parentKey)` | saf fonksiyon | `src/lib/side-conversation.ts` |
| `assertSideConversationLink(sideKey, parentKey, agentEmail)` | modül-içi async yardımcı (export edilir, test edilebilirlik için) | `src/query/side-conversation-queries.ts` |
| `resolveDetailRecipientFallback(detail, ticket)` | export edilen async yardımcı | `src/query/side-conversation-queries.ts` |

**Fazın diğer planlarında üretilecekler (bu planın değil):** `grispiTld`, `buildAgentTicketUrl`, `TicketKeyLink`, `ParentKeyChip` (Plan 02); `InfoBox`, `getInfoBoxSeen`, `setInfoBoxSeen` (Plan 04); `ParentBanner`, `isHydratedTicket`, `isSideConversationTicket` (Plan 07).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `InternalNotePatchRequest` tipi, `Tickets.addInternalNote` istemcisi ve D-02 gövde biçimlendiricisi</name>

  <files>
    src/types/grispi.type.ts,
    src/grispi/client/tickets.ts,
    src/grispi/client/__tests__/tickets.test.ts,
    src/lib/side-conversation.ts,
    src/lib/__tests__/side-conversation.test.ts
  </files>

  <read_first>
    - `src/types/grispi.type.ts` satır 240-330 — `CreateTicketRequest`, `ReplyTicketPatchRequest`, `StatusTicketPatchRequest` ve `PatchTicketRequest` birleşimi. Yeni tip bunların hemen yanına, aynı doc-comment disiplinini izleyerek eklenir. `publicVisible`'ın neden literal daraltıldığını anlatan mevcut yorum bu görevin gerekçesidir.
    - `src/grispi/client/tickets.ts` — tam dosya (124 satır). `replyTicket` (satır 88-98) kopyalanacak birebir şablon; `createTicket`/`patchTicket` doc-comment'leri `/v2` vs `public/v1` ayrımının NEDEN var olduğunu anlatıyor — yeni metodun doc-comment'i aynı seviyede olmalı.
    - `src/grispi/client/__tests__/tickets.test.ts` — tam dosya (~6 KB). `http.send` mock'unun kurulumu ve URL/method/body assertion deseni; yeni testler bu deseni birebir izler.
    - `src/lib/side-conversation.ts` satır 1-40 — `SIDE_CONVERSATION_PARENT_FIELD_KEY` sabiti ve `formatRequesterField`/`formatPrefillSubject`'in "tek gerçek kaynak olsun diye küçük saf sarmalayıcı" gerekçeli doc-comment stili.
    - `src/lib/__tests__/side-conversation.test.ts` — mevcut saf-fonksiyon test deseni.
    - `.planning/phases/04.2-.../04.2-RESEARCH.md` — P1/P2/P3 probe sonuçları (satır 100-150): `/v2/tickets` PATCH + `publicVisible: false` CANLI DOĞRULANDI, 200 döner, `ts.status` değişmez, `toId`/`toEmail` null kalır. Bu tekrar doğrulanmayacak.
    - `.planning/phases/04.2-.../04.2-PATTERNS.md` — "`src/grispi/client/tickets.ts`" ve "`src/types/grispi.type.ts`" bölümleri (satır 93-167): kopyalanacak hazır şablonlar.
  </read_first>

  <behavior>
    `formatInternalNoteBody(parentKey)` için yazılacak testler (implementasyondan ÖNCE):
    - `formatInternalNoteBody("TICKET-563")` çıktısı, D-02'nin kilitli metnidir: üst talep anahtarı tek interpolasyon noktasıdır, cümleler birebir CONTEXT.md D-02'deki gibidir
    - Aynı girdi iki kez çağrıldığında aynı çıktıyı verir (saf fonksiyon, tarih/rastgelelik yok)
    - Çıktı hiçbir HTML etiketi içermez (düz metin)

    `Tickets.addInternalNote` için yazılacak testler:
    - `v2/tickets/TICKET-601` yoluna `PATCH` atar (`public/v1` DEĞİL)
    - `encodeURIComponent` uygulanır: `addInternalNote("A/B", …)` yolunda `A%2FB` geçer
    - Gövde JSON'unda `comment.publicVisible` `false`'tur ve `comment.channel` `"WEB"`dir
    - Gövde JSON'unda `fields` dizisi `tu.side_conversation_parent` anahtarını üst talep değeriyle taşır
    - `auth.headers` gövde başlıklarına geçer (mevcut `replyTicket` testiyle aynı assertion)
  </behavior>

  <action>
    **`src/lib/side-conversation.ts`:** `formatInternalNoteBody(parentKey: string): string` adında saf bir fonksiyon export et. Dönen metin D-02'de kilitlenmiş iki cümledir; `{PARENT-KEY}` yerine parametre geçer, başka hiçbir interpolasyon noktası yoktur. Kilitli metnin tamamı `04.2-CONTEXT.md`'nin D-02 maddesinden karakteri karakterine kopyalanır (Türkçe karakterler dâhil) — yeniden ifade etme, kısaltma. Doc-comment'te iki şeyi yaz: (1) metnin settings'ten okunMADIĞI ve sabit olduğu (D-02), (2) yalnızca üst talep ANAHTARI'nın gövdeye girdiği, konu/açıklama gibi kullanıcı içeriğinin asla girmediği (T-04.2-05). Bu dosyada sanitizer import etme.

    **`src/types/grispi.type.ts`:** `ReplyTicketPatchRequest`'in hemen ardına `InternalNotePatchRequest` interface'ini ekle. Şekli: `comment` nesnesi (`body: string`, `publicVisible: false` — literal, `boolean` DEĞİL —, `creator: [{ key: "us.email"; value: string }]`, `channel: "WEB"`) artı `fields: Array<{ key: string; value: string }>`. `ReplyTicketPatchRequest`'i genişletme, `publicVisible`'ı `boolean` yapma, iki tipi birleştirme — literal daraltma bu kod tabanında derleme-zamanı güvenlik mekanizmasıdır, dokümantasyon değil (RESEARCH Pitfall #3). Doc-comment'te `ReplyTicketPatchRequest`'in kendi doc-comment'iyle aynı ayrıntı seviyesinde şunları anlat: P1-P3 canlı probe'unun ne kanıtladığı, neden `/v2/tickets`'ta kaldığı, ve `fields` dizisinin neden burada olduğu (D-22 — create'in `fields` dizisi taze ticket'ta kaydetmeyebiliyor, `PATCH` kaydediyor). `PatchTicketRequest` birleşimine EKLEME — o birleşim `patchTicket`/`replyTicket` yanıt tipi ailesi içindir; yeni tip yalnızca `addInternalNote`'un imzasında geçer.

    **`src/grispi/client/tickets.ts`:** `replyTicket`'in hemen ardına `addInternalNote(ticketKey: string, body: InternalNotePatchRequest)` metodunu ekle. Gövdesi `replyTicket` ile birebir aynı şekildedir: `this.http.send<PatchTicketResponse>` çağrısı, yol `v2/tickets/` + `encodeURIComponent(ticketKey)`, `method: "PATCH"`, `cache: "no-cache"`, `headers: this.auth.headers`, `body: JSON.stringify(body)`. `InternalNotePatchRequest`'i mevcut tip import listesine ekle. Doc-comment'te üç şeyi açıkça yaz: (1) neden `replyTicket`'in kardeşi olduğu ve onu ÇAĞIRMADIĞI, (2) neden `public/v1` değil `/v2` (aynı yazma-yolu ayrımı; `patchTicket`'ın D-15 gerekçesi bozulmuyor çünkü bu metod status alanına hiç dokunmuyor), (3) `fields` dizisinin D-22 gerekçesi.

    **`src/grispi/client/__tests__/tickets.test.ts`:** Mevcut `describe` yapısına `addInternalNote` için yeni bir blok ekle; `<behavior>`'daki beş assertion'ı mevcut dosyanın `http.send` mock kurulumunu tekrar kullanarak yaz. Yeni bir mock altyapısı kurma.

    **`src/lib/__tests__/side-conversation.test.ts`:** `formatInternalNoteBody` için yeni bir `describe` bloğu ekle; mevcut dosyadaki saf-fonksiyon test stilini izle.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern="tickets|side-conversation.test"</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern="tickets|side-conversation.test"` exit kodu `0`
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `grep -c "export interface InternalNotePatchRequest" src/types/grispi.type.ts` → `1`
    - `grep -c "publicVisible: false" src/types/grispi.type.ts` → `1` (yalnızca yeni tipte)
    - `grep -c "publicVisible: boolean" src/types/grispi.type.ts` → `0` (mevcut literaller genişletilmemiş)
    - `grep -c "async addInternalNote(" src/grispi/client/tickets.ts` → `1`
    - `grep -c "public/v1" src/grispi/client/tickets.ts` → `3` (getTicket + advancedSearch + patchTicket; `addInternalNote` bu listede DEĞİL)
    - `grep -c "export function formatInternalNoteBody(parentKey: string): string" src/lib/side-conversation.ts` → `1`
    - `grep -c "sanitize" src/lib/side-conversation.ts` → `0`
    - Davranış: `tickets.test.ts`, `addInternalNote`'un `v2/tickets/` ile başlayan bir yola `PATCH` attığını ve gövdesinde `publicVisible` alanının `false` olduğunu assert eden en az bir yeşil test içerir
    - Davranış: `tickets.test.ts`, gövdedeki `fields` dizisinin `tu.side_conversation_parent` anahtarını taşıdığını assert eder
  </acceptance_criteria>

  <done>
    İç not yazma yolunun tip + istemci + saf metin üçlüsü mevcut; hiçbir mevcut tip genişletilmedi; `replyTicket`/`patchTicket` davranışı değişmedi; ilgili testler yeşil.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `executeCreateMutation`'a sessiz iç not + parent alanı adımını doğru sırayla ekle (D-01, D-04, D-22)</name>

  <files>
    src/query/side-conversation-queries.ts,
    src/query/__tests__/side-conversation-queries.test.tsx
  </files>

  <read_first>
    - `src/query/side-conversation-queries.ts` satır 591-625 — `executeCreateMutation`'ın tamamı. Ekleme noktası satır 612 (`mutationAccepted`) ile satır 613 (`try { refreshCanonicalAfterMutation … }`) ARASIDIR; bu, PATTERNS.md'nin doğrudan okumayla teyit ettiği tek doğru yerdir.
    - `src/query/side-conversation-queries.ts` satır 545-589 — `refreshCanonicalAfterMutation`; ilk işi `queryClient.invalidateQueries` çağırmak, ardından detay refetch. Sıralama testi bu iki çağrıdan birine göre yazılacak.
    - `src/store/active-conversation-store.ts` satır 48-78 — `MutationEnvelope`'un `create` varyantı: `parentKey`, `request: CreateTicketRequest`, `sessionKey`, `tenantId` alanları. Satır 154-158 (`envelopeCreator`) agent e-postasının zaten `request.comment.creator[0].value`'da taşındığını gösteriyor — YENİ bir plumbing GEREKMEZ.
    - `src/query/__tests__/side-conversation-queries.test.tsx` — `executeCreateMutation` testlerinin bulunduğu bölüm; `grispiAPI` mock'unun kurulumu, `boundary` sahtesi ve `queryClient` kullanımı. Yeni testler bu kurulumu tekrar kullanır.
    - `.planning/phases/04.2-.../04.2-RESEARCH.md` "Common Pitfalls → Pitfall 1" (satır 377-381) ve "System Architecture Diagram" (satır 206-244) — await'in NEDEN zorunlu olduğunun tam gerekçesi.
    - `.planning/phases/04.2-.../04.2-CONTEXT.md` D-04 ve D-22 maddeleri — hata politikası ve ayrı log kuralı.
    - `src/contexts/plugin-bootstrap.ts` satır 91 — `console.error("grispi-context", …)` konvansiyonu (ilk argüman modül adı).
  </read_first>

  <behavior>
    `executeCreateMutation` için `side-conversation-queries.test.tsx`'e eklenecek testler (implementasyondan ÖNCE):
    - Başarılı create sonrası `grispiAPI.tickets.addInternalNote` TAM OLARAK BİR KEZ çağrılır; ilk argümanı create yanıtından gelen `sideKey`'dir (`envelope.parentKey` DEĞİL — D-05)
    - Gönderilen gövdenin `comment.body` değeri `formatInternalNoteBody(envelope.parentKey)` çıktısına eşittir, `comment.creator[0].value` envelope'un kendi creator e-postasıdır, `comment.publicVisible` `false`'tur
    - Gönderilen gövdenin `fields` dizisi tam olarak bir eleman taşır: `{ key: SIDE_CONVERSATION_PARENT_FIELD_KEY, value: envelope.parentKey }` (D-22)
    - **Sıralama (fazın kalbi):** `addInternalNote.mock.invocationCallOrder[0]` değeri, `refreshCanonicalAfterMutation`'ın ilk gözlemlenebilir çağrısının (`queryClient.invalidateQueries` casusu) `invocationCallOrder[0]` değerinden KÜÇÜKTÜR
    - `addInternalNote` ilk çağrıda reddederse tam olarak İKİ kez çağrılır (1 yeniden deneme), ikinci de reddederse üçüncü çağrı OLMAZ
    - İki deneme de başarısızken: `executeCreateMutation` reject ETMEZ (`await expect(...).resolves.toBeUndefined()`), `mutationFailed` çağrılMAZ, `console.error` en az iki KEZ çağrılır ve bu iki çağrının mesaj argümanları BİRBİRİNDEN FARKLIDIR (biri iç nota, biri parent alanına ait — D-22'nin "aynı satıra karışmaz" kuralı)
    - `addInternalNote` başarısız olsa bile `refreshCanonicalAfterMutation`'ın invalidate/refetch adımları yine çalışır (akış kesilmez)
    - Envelope'un creator e-postası boşsa (`creator[0]?.value` yok) `addInternalNote` HİÇ çağrılmaz ve tek bir `console.error` yazılır; akış yine devam eder
    - `isCurrent(...)` false döndüğünde (agent başka bir konuşmaya geçmiş) `addInternalNote` çağrılmaz
  </behavior>

  <action>
    **`src/query/side-conversation-queries.ts` — yeni yardımcı:** `assertSideConversationLink(sideKey: string, parentKey: string, agentEmail: string | null): Promise<void>` adında, export edilen (test edilebilirlik için) bir async fonksiyon ekle. `executeCreateMutation`'ın hemen ÜSTÜNE koy.

    Gövdesi: `agentEmail` boş/`null` ise tek bir `console.error("side-conversation-queries", "internal note skipped — no agent identity on envelope", sideKey)` yazıp geri döner. Aksi hâlde `formatInternalNoteBody(parentKey)` ile gövdeyi kurar ve `InternalNotePatchRequest` şeklinde bir nesne oluşturur: `comment` (`body`, `publicVisible: false`, `creator: [{ key: "us.email", value: agentEmail }]`, `channel: "WEB"`) ve `fields: [{ key: SIDE_CONVERSATION_PARENT_FIELD_KEY, value: parentKey }]`. `SIDE_CONVERSATION_PARENT_FIELD_KEY` bu dosyada ZATEN import edilmiş durumda (satır 27-30) — ikinci bir import ekleme, string literal yazma.

    Çağrı politikası D-04: `try { await grispiAPI.tickets.addInternalNote(...) } catch { ikinci kez dene }`; ikinci deneme de başarısızsa **iki ayrı** `console.error` satırı yaz — birincisi iç notun kendisi için, ikincisi parent alanı için, ve ikincisinin mesajı bu alanın listeleme için kritik olduğunu söyler. Aralarına gecikme/backoff KOYMA (D-04 "en fazla 1 yeniden deneme" der, gecikmeli kuyruk değil). Bu fonksiyon HİÇBİR koşulda throw etmez — dışarı çıkan tek yol normal `return`'dür; imzası `Promise<void>` olarak kalır ve `try/catch` bloğunun dışına hiçbir reject sızmaz.

    **`executeCreateMutation` düzenlemesi:** `boundary.activeConversation.mutationAccepted(envelope);` satırının HEMEN ARDINA, mevcut `try { await refreshCanonicalAfterMutation(...) }` bloğunun ÖNÜNE `await assertSideConversationLink(sideKey, envelope.parentKey, envelope.request.comment.creator[0]?.value ?? null);` ekle. Bu satır kendi `try/catch`'ine SARILMAZ çünkü fonksiyonun kendisi hiç throw etmiyor; sarmak, D-04 sözleşmesini iki yere dağıtır. Mevcut `refreshCanonicalAfterMutation` çağrısını, onun `try/catch`'ini ve içindeki yorumu DEĞİŞTİRME. `executeReplyMutation` ve `executeStatusMutation`'a DOKUNMA — iç not yalnızca create yolunda atılır.

    Ekleme noktasının üstüne kısa bir yorum yaz: neden `await` (Pitfall #1 — fire-and-forget refetch yarışını kaybeder, not agent'ın ilk görünümünde eksik kalır) ve neden bu satırın hata fırlatmadığı (D-04).

    **`src/query/__tests__/side-conversation-queries.test.tsx`:** `<behavior>`'daki dokuz senaryoyu, dosyanın mevcut `executeCreateMutation` test kurulumunu (grispiAPI mock'u, sahte `boundary`, `QueryClient`) tekrar kullanarak ekle. `addInternalNote`'u mevcut `grispiAPI.tickets` mock nesnesine yeni bir `jest.fn()` olarak ekle. Sıralama assertion'ı için `jest.spyOn(queryClient, "invalidateQueries")` kur ve `invocationCallOrder` karşılaştırmasını `plugin-bootstrap.test.ts`'in mevcut `setEnvironment` vs `switchTicket` desenindeki gibi yaz. `console.error` için `jest.spyOn(console, "error").mockImplementation(() => {})` + `afterEach(() => jest.restoreAllMocks())` kur; iki farklı mesaj assertion'ı `mock.calls`'ın iki ayrı elemanını karşılaştırır.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern=side-conversation-queries</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern=side-conversation-queries` exit kodu `0`
    - `grep -c "export async function assertSideConversationLink(" src/query/side-conversation-queries.ts` → `1`
    - `grep -c "addInternalNote" src/query/side-conversation-queries.ts` → `2` (import + tek çağrı) VEYA `1` (namespace çağrısı) — her hâlde `grep -c "grispiAPI.tickets.addInternalNote(" src/query/side-conversation-queries.ts` → `2` (retry dâhil iki çağrı satırı) ya da tek yardımcı içinden `2`'yi geçmez
    - `grep -c "await assertSideConversationLink(" src/query/side-conversation-queries.ts` → `1`
    - `grep -n "await assertSideConversationLink" src/query/side-conversation-queries.ts` çıktısındaki satır numarası, `grep -n "await refreshCanonicalAfterMutation" src/query/side-conversation-queries.ts` çıktısının İLK satır numarasından KÜÇÜKTÜR
    - `grep -c "console.error" src/query/side-conversation-queries.ts` ≥ `3` (iç not + parent alanı + kimlik-yok dalı)
    - `grep -c '"tu.side_conversation_parent"' src/query/side-conversation-queries.ts` → `0` (sabit import edilmiş, literal yazılmamış)
    - Davranış: test dosyası, `addInternalNote`'un çağrı sırasının `invalidateQueries`'inkinden küçük olduğunu assert eden en az bir yeşil test içerir
    - Davranış: iki başarısız denemeden sonra `executeCreateMutation`'ın resolve ettiğini ve `mutationFailed`'in çağrılmadığını assert eden yeşil bir test vardır
    - Davranış: iki farklı `console.error` mesajı assert edilir (D-22)
  </acceptance_criteria>

  <done>
    Yeni yan konuşma oluşturulduğunda iç not + parent alanı, detay refetch'inden önce, sessizce ve en fazla iki denemeyle yazılır; hiçbir başarısızlık agent'a yansımaz ve konuşmanın kendisini bozmaz.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Detay yolunda alıcı fallback'i — `—` bug'ının kapanışı (D-12)</name>

  <files>
    src/query/side-conversation-queries.ts,
    src/query/__tests__/side-conversation-queries.test.tsx
  </files>

  <read_first>
    - `src/query/side-conversation-queries.ts` satır 452-465 — `resolveRecipientLabel`: bug'ın kaynağı. Alıcıyı YALNIZCA ticket'ta `ROLE_END_USER` yorumu varsa bulabiliyor; yoksa `"—"` dönüyor.
    - `src/query/side-conversation-queries.ts` satır 145-174 — `hydrateSummaries`'in liste-yolu fallback'i: tekrar kullanılacak precedent (`grispiAPI.users.getUser(userId)`, try/catch → `null`, `primaryEmail` okuması).
    - `src/query/side-conversation-queries.ts` satır 250-266 — `sideConversationDetailOptions.queryFn`: `getTicket(...).then(normalizeSideConversationDetail)` zinciri; fallback bu zincire eklenecek.
    - `src/query/side-conversation-queries.ts` satır 480-520 — `normalizeSideConversationDetail`, satır 512'de `resolveRecipientLabel(ticket)` çağrısı.
    - `src/store/side-conversations-store.ts` satır 28 — `RECIPIENT_UNKNOWN_PLACEHOLDER = "—"`; bu dosyada ZATEN import edilmiş (satır 36-41).
    - `src/grispi/client/users.ts` — `getUser` istemcisi ve doc-comment'i (bu fallback'in neden var olduğunu zaten anlatıyor).
    - `src/types/grispi.type.ts` satır 237-240 — `GrispiUserProfile` yalnızca `id` + `primaryEmail | null` taşır; fallback etiketi bu yüzden yalnızca e-postadır, ad değil.
  </read_first>

  <behavior>
    `resolveDetailRecipientFallback` için yazılacak testler:
    - Ticket'ta `ROLE_END_USER` yorumu VARSA `users.getUser` HİÇ çağrılmaz ve etiket değişmez (gereksiz istek yok)
    - Ticket'ta `ROLE_END_USER` yorumu YOKSA ama `ts.requester` sayısal bir id taşıyorsa `users.getUser` o id ile TAM BİR KEZ çağrılır ve dönen `primaryEmail` etiket olur — sonuç `"—"` DEĞİLDİR
    - `users.getUser` reddederse etiket `"—"` olarak kalır ve fonksiyon throw ETMEZ
    - `users.getUser` `primaryEmail: null` dönerse etiket `"—"` olarak kalır
    - `ts.requester` alanı yoksa / sayıya çevrilemiyorsa `users.getUser` çağrılmaz, etiket `"—"` kalır
    - Detay sorgusu (`sideConversationDetailOptions.queryFn`) bu fallback'ten geçer: yorumsuz bir ticket için dönen `SideConversationDetail.recipientLabel` alıcının e-postasıdır
  </behavior>

  <action>
    **`src/query/side-conversation-queries.ts`:** `normalizeSideConversationDetail`'in hemen ardına `export async function resolveDetailRecipientFallback(detail: SideConversationDetail, ticket: Ticket): Promise<SideConversationDetail>` ekle.

    Gövdesi: `detail.recipientLabel` `RECIPIENT_UNKNOWN_PLACEHOLDER`'a EŞİT DEĞİLSE `detail`'i olduğu gibi döndür (erken çıkış — ekstra istek yok). Eşitse `Number(ticket.fieldMap?.["ts.requester"]?.value)` ile requester id'sini oku; `Number.isFinite` değilse `detail`'i döndür. Aksi hâlde `hydrateSummaries`'in birebir aynı savunma desenini uygula: `try { const user = await grispiAPI.users.getUser(requesterId); … } catch { … }`. `user?.primaryEmail` dolu bir string ise `{ ...detail, recipientLabel: user.primaryEmail }` döndür; her diğer durumda (`null`, boş string, hata) `detail`'i DEĞİŞTİRMEDEN döndür. Yeni bir kullanıcı-arama yolu, yeni bir store metodu veya yeni bir cache anahtarı EKLEME — D-12 açıkça "yeni bir çözümleme yolu icat edilmez" diyor.

    `normalizeSideConversationDetail`'i async YAPMA ve imzasını değiştirme — dışarıya export edilmiş ve mevcut testleri var; fallback ayrı bir sarmalayıcıdır.

    **`sideConversationDetailOptions.queryFn` düzenlemesi:** mevcut `.then(normalizeSideConversationDetail)` zincirini, ticket nesnesine de erişebilen bir forma çevir: ticket'ı bir kez al, `normalizeSideConversationDetail(ticket)` ile detayı üret, sonra `resolveDetailRecipientFallback(detail, ticket)` sonucunu döndür. `queryOptions`'ın diğer alanlarına (`queryKey`, `enabled`, `staleTime`, `finiteReadPolicy`) DOKUNMA.

    Fallback'in üstüne kısa bir yorum yaz: kök neden (`resolveRecipientLabel` yalnızca `ROLE_END_USER` yorumu varken çözebiliyor) ve bunun liste yolundaki `hydrateSummaries` fallback'inin detay yoluna taşınmış hâli olduğu (D-12).

    **`src/query/__tests__/side-conversation-queries.test.tsx`:** `<behavior>`'daki altı senaryoyu ekle. `grispiAPI.users.getUser` mevcut mock nesnesinde zaten var (liste testleri kullanıyor) — yeni mock altyapısı kurma, `mockClear()` ile senaryolar arası izolasyonu koru.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern=side-conversation-queries && CI=true npm test -- --watchAll=false</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern=side-conversation-queries` exit kodu `0`
    - `CI=true npm test -- --watchAll=false` exit kodu `0` (dalga birleşim gate'i)
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `grep -c "export async function resolveDetailRecipientFallback(" src/query/side-conversation-queries.ts` → `1`
    - `grep -c "grispiAPI.users.getUser" src/query/side-conversation-queries.ts` → `2` (liste yolundaki mevcut + detay yolundaki yeni)
    - `grep -c "resolveDetailRecipientFallback" src/query/side-conversation-queries.ts` → `2` (tanım + queryFn çağrısı)
    - `grep -c "export function normalizeSideConversationDetail(" src/query/side-conversation-queries.ts` → `1` (senkron imza korunmuş)
    - Davranış: yorumsuz bir ticket fixture'ı için `sideConversationDetailOptions(...).queryFn` sonucunun `recipientLabel` alanının `"—"` OLMADIĞINI assert eden yeşil bir test vardır
    - Davranış: `getUser` reddettiğinde queryFn'in throw etmediğini ve etiketin `"—"` kaldığını assert eden yeşil bir test vardır
  </acceptance_criteria>

  <done>
    Detay yolu, üçüncü taraf hiç yanıt yazmamışken bile alıcıyı çözüyor; liste yolundaki mekanizma birebir tekrar kullanıldı; başarısızlıkta davranış bugünkü güvenli hâline geri düşüyor; tam suite yeşil.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin iframe → Grispi API (`PATCH /v2/tickets/{key}`) | Bu fazın tek yeni yazma yüzeyi. Gövde tamamen istemcide kurulur; alıcıya e-posta gitmemesi tek bir alanın (`publicVisible`) doğruluğuna bağlıdır |
| Grispi API → panel (detay `GET`) | `ts.requester` ve `comments[].creator` güvenilmeyen sunucu verisidir; etiket üretiminde yalnızca okunur, hiçbir yere interpolasyon edilmez |
| Yan ticket anahtarı + üst talep anahtarı → iç not gövdesi | Anahtarlar güvenilir bağlamdan (`envelope`, create yanıtı) gelir; serbest metin hiçbir zaman gövdeye girmez |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-01 | Information Disclosure | `addInternalNote` gövdesinin yanlışlıkla public gitmesi (talep sahibine e-posta) | critical | mitigate | `InternalNotePatchRequest.comment.publicVisible` `false` LİTERALİNE daraltılır; `ReplyTicketPatchRequest`'in `true` literali genişletilmez. Bir çağrının yanlış tarafa geçmesi derleme hatasıdır, çalışma-zamanı riski değildir. Gate (Task 1): `grep -c "publicVisible: boolean" src/types/grispi.type.ts` → `0` ve `tickets.test.ts`'te gövdenin `false` taşıdığını assert eden yeşil test |
| T-04.2-02 | Information Disclosure | İç not gövdesinin üst talebin konusunu/açıklamasını (PII) taşıması | high | mitigate | `formatInternalNoteBody` tek parametre alır ve yalnızca üst talep ANAHTARINI interpolasyon eder (D-02). Talep konusu, açıklaması, alıcı e-postası veya herhangi bir kullanıcı-yazımı metin fonksiyonun imzasına bile giremez. Gate (Task 1): tek-parametreli imza grep'i |
| T-04.2-03 | Tampering | Gelecekte bir çağrının gerçek yanıtı sessizce iç nota çevirmesi (tip karışıklığı) | high | mitigate | Kardeş tip + kardeş metod (`addInternalNote`), `replyTicket`'in yeniden kullanımı YOK. Bu, kod tabanının kendi kurulu savunma deseni (`StatusTicketPatchRequest`'in kilitli tuple'ı). Gate (Task 1): `grep -c "async addInternalNote(" src/grispi/client/tickets.ts` → `1` ve `patchTicket`/`replyTicket` imzaları değişmemiş |
| T-04.2-04 | Denial of Service | İç not PATCH'inin sonsuz yeniden denemeye girmesi / create akışını bloklaması | medium | mitigate | En fazla iki toplam deneme (1 retry), gecikme/kuyruk yok, fonksiyon hiç throw etmiyor, `executeCreateMutation` her hâlükârda resolve ediyor. Gate (Task 2): iki başarısız denemede üçüncü çağrının olmadığını ve promise'in resolve ettiğini assert eden yeşil testler |
| T-04.2-05 | Information Disclosure | `console.error` ile hassas veri loglanması | low | accept | Loglanan alanlar yalnızca ticket anahtarları ve sabit mesajlardır; token, e-posta gövdesi veya `Authorization` başlığı hiçbir log satırına girmez. DevTools teşhisi için gereklidir (D-04/D-22 açıkça bunu istiyor) |
| T-04.2-06 | Denial of Service | Detay yolundaki `users.getUser` fallback'inin her açılışta ek istek üretmesi | low | mitigate | Fallback yalnızca etiket `—` olduğunda tetiklenir (erken çıkış), tek bir id için tek istek atar ve hata hâlinde sessizce bugünkü davranışa döner. React Query'nin mevcut `DETAIL_STALE_TIME` penceresi ek çağrıyı zaten sınırlar |
| T-04.2-SC | Tampering | Paket yöneticisi kurulumları (npm) | high | accept | Bu plan HİÇBİR yeni paket kurmuyor (RESEARCH.md "Package Legitimacy Audit: Not applicable — this phase installs no new external packages"). Tedarik zinciri yüzeyi genişlemiyor. Gate: `package.json` değişmemiş olmalı |
</threat_model>

<verification>
**Otomatik (bu plan tamamlandığında geçmeli):**
1. `npx craco test --watchAll=false --testPathPattern="tickets|side-conversation"` → exit 0
2. `npx craco test --watchAll=false --testPathPattern=side-conversation-queries` → exit 0
3. `CI=true npm test -- --watchAll=false` → exit 0
4. `npx tsc --noEmit -p tsconfig.json` → exit 0
5. Sıra gate'i: `await assertSideConversationLink` satır numarası < ilk `await refreshCanonicalAfterMutation` satır numarası
6. `git diff --stat package.json package-lock.json` → boş (yeni bağımlılık yok)

**Bu planın kapsamadıkları:** iç notun panelde nasıl göründüğü (Plan 03/06), alıcının çözüldü/kapalı blokta gösterimi (Plan 06), e-postanın gerçekten gitmediğinin posta kutusu doğrulaması (Plan 08 — manuel UAT).

**UX-09 için yedek kaldıraç (şimdi UYGULANMAZ, yalnızca kayıt altına alınır):** Plan 08'in canlı UAT'sinde `tu.side_conversation_parent` hâlâ `null` okunursa, canlı probe'un kanıtladığı mekanizma `fields`-only `PATCH public/v1/tickets/{key}`'tir. Bu durumda düzeltme, `assertSideConversationLink`'in `fields` yarısını ayrı bir `public/v1` çağrısına bölmektir (iç not `/v2`'de kalır). Bu kaldıraç önden uygulanmaz — D-22 ve VALIDATION.md tek gövdeyi tarif ediyor.
</verification>

<success_criteria>
- [ ] `InternalNotePatchRequest` + `Tickets.addInternalNote` mevcut; mevcut hiçbir tip genişletilmedi (UX-01)
- [ ] İç not, create başarılı olduktan sonra, detay refetch'inden ÖNCE await edilerek atılıyor (UX-01, RESEARCH Pitfall #1)
- [ ] Başarısızlık: en fazla 1 retry, sonra iki ayrı `console.error`; agent hiçbir hata görmüyor, akış bozulmuyor (D-04)
- [ ] Aynı gövde `tu.side_conversation_parent`'ı üst talep anahtarıyla yeniden tesis ediyor (UX-09, D-22)
- [ ] Üst talebe hiçbir yazma yapılmıyor (D-05)
- [ ] Detay yolu alıcıyı `users.getUser` fallback'iyle çözüyor; `—` yalnızca gerçekten çözülemeyen durumda kalıyor (UX-04, D-12)
- [ ] Tam test suite ve `tsc --noEmit` yeşil; yeni npm bağımlılığı yok
</success_criteria>

<output>
Create `.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-01-SUMMARY.md` when done
</output>
