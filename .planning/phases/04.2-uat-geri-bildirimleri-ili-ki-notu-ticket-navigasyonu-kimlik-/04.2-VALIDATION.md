---
phase: 04.2
slug: uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-17
---

# Phase 04.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `04.2-RESEARCH.md` § Validation Architecture, with its Wave 0 findings
> corrected against the actual test tree (see "Wave 0 Requirements" below).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 27.5.1 + React Testing Library (CRA-bundled, run through `craco test`) |
| **Config file** | none dedicated — inherited from `react-scripts` / `@craco/craco` defaults |
| **Quick run command** | `npm test -- --watchAll=false <target test file>` |
| **Full suite command** | `npm test -- --watchAll=false` |
| **Estimated runtime** | ~35–60 seconds full suite (21 test files) |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --watchAll=false <the test file touching that surface>`
- **After every plan wave:** Run `npm test -- --watchAll=false`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

Task IDs are assigned by the planner (this file is written before PLAN.md exists, per
plan-phase §5.5). Rows are keyed by requirement; the planner MUST attach each row to the
task that implements it.

| Requirement | Behavior under test | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| UX-01 | Internal-note PATCH fires after create succeeds, retries at most once, never surfaces a UI error (D-04) | unit | `npm test -- --watchAll=false src/query/__tests__/side-conversation-queries.test.tsx` | ✅ extend | ⬜ pending |
| UX-01 | Internal note is awaited before the create mutation's detail refetch (research: sequencing risk) | unit | same file | ✅ extend | ⬜ pending |
| UX-01 | Recipient receives NO email for the internal note | manual-only | — | ❌ UAT | ⬜ pending |
| UX-02 | Chat header renders the side-ticket key, not the recipient; `href` is the env-derived `/tickets/{KEY}` URL with `target="_blank"` + `rel="noopener noreferrer"` | unit | `npm test -- --watchAll=false src/screens/__tests__/chat-screen.test.tsx` | ✅ extend | ⬜ pending |
| UX-02 | Parent key renders as a non-link chip (no `href`, no `role="link"`) | unit | same file | ✅ extend | ⬜ pending |
| UX-02 | TLD is derived from `GRISPI_BASE_URLS`, never a new hardcoded host (CORE-04 / T-04.1-01) | unit | `npm test -- --watchAll=false src/lib/__tests__/grispi-environment.test.ts` | ✅ extend | ⬜ pending |
| UX-03 | Parent banner renders when `tu.side_conversation_parent` is set; conversation list is hidden | unit | `npm test -- --watchAll=false src/screens/__tests__/conversations-list-screen.test.tsx` | ✅ extend | ⬜ pending |
| UX-03 | Compose is blocked on a side ticket; guard reads hydrated state only — during the provisional `{key}`-only window the guard must resolve to "not a side conversation" (safe direction), never flash the banner | unit | same file | ✅ extend | ⬜ pending |
| UX-04 | Detail path falls back to `users.getUser(requesterId)`; `—` is never rendered when the requester exists but has authored no comment | unit | `npm test -- --watchAll=false src/query/__tests__/side-conversation-queries.test.tsx` | ✅ extend | ⬜ pending |
| UX-04 | Recipient is present in the solved/closed block where the composer is hidden (D-09) | unit | `npm test -- --watchAll=false src/screens/__tests__/chat-screen.test.tsx` | ✅ extend | ⬜ pending |
| UX-05 | `senderLabel` returns the agent's name (not "Siz"); "Siz" appears as a sibling badge only when the author is the acting agent | unit | `npm test -- --watchAll=false src/screens/components/__tests__/thread-components.test.tsx` | ✅ extend | ⬜ pending |
| UX-05 | Internal-note label is "İç not" with no "Salt okunur" substring anywhere | unit | same file | ✅ extend | ⬜ pending |
| UX-06 | Info box is always present on the compose screen | unit | `npm test -- --watchAll=false src/screens/__tests__/compose-screen.test.tsx` | ✅ extend | ⬜ pending |
| UX-06 | Chat info box shows once, dismisses permanently, and the flag is tenant-scoped in localStorage | unit | new test file mirroring `src/lib/__tests__/last-seen-store.test.ts` | ⚠️ W0 | ⬜ pending |
| UX-07 | Formatting actions live behind one popover trigger; undo/redo and attach remain outside it; popover closes on Escape and outside click and returns focus | unit | `npm test -- --watchAll=false src/screens/components/__tests__/rich-text-composer.test.tsx` | ✅ extend | ⬜ pending |
| UX-07 | Toolbar produces no horizontal scroll at 280px | manual-only | — (jsdom has no layout engine) | ❌ UAT | ⬜ pending |
| UX-08 | Compact tokens apply under `max-height: 620px`; message area ≥20% taller than the ~197px baseline at 1366×587 | manual-only | — | ❌ UAT | ⬜ pending |
| UX-08 | List header is not truncated at ~280px panel width | manual-only | — | ❌ UAT | ⬜ pending |
| UX-09 | The post-create PATCH body carries `tu.side_conversation_parent` with the parent key alongside the internal-note comment (D-22); its failure logs separately from the note's | unit | `npm test -- --watchAll=false src/query/__tests__/side-conversation-queries.test.tsx` | ✅ extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

RESEARCH.md's Wave 0 list was written from an incomplete scan of the test tree. Corrected
against the actual files on disk:

- ~~`rich-text-composer` has no test file~~ — **WRONG.** `src/screens/components/__tests__/rich-text-composer.test.tsx` exists (42 KB). Extend it; do NOT create a second file.
- ~~`thread-message.tsx` has no test file~~ — **WRONG.** `src/screens/components/__tests__/thread-components.test.tsx` exists (16 KB) and already exercises `ThreadMessage`, including the internal-note and own-direction label branches. Extend it.
- [ ] One new test file for UX-06's dismissal persistence, mirroring `src/lib/__tests__/last-seen-store.test.ts` exactly (same key-namespacing and same storage-failure tolerance).

No framework install, no new packages, no config changes. Existing infrastructure covers
every automatable behavior in this phase.

---

## Manual-Only Verifications

Routed to the phase-end live-panel UAT checklist, following this project's established
"code complete, human-verified pending" pattern (Phase 4 P6b, Phase 04.1 M2).

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| Internal note sends no email | UX-01 | No automated mailbox check exists in this repo | Create a side conversation on `gsocial-test`, then confirm the third-party mailbox received exactly one message (the agent's), not two |
| Ticket link opens the right ticket in a new tab | UX-02 | Cross-origin navigation, real tenant | Click the header key; confirm a new tab opens on the correct tenant host at `/tickets/{KEY}` |
| Parent vs side key are not confusable | UX-02 | Visual judgment | Open a conversation with both keys visible; confirm they read as two different kinds of thing at a glance |
| No horizontal scroll in the toolbar at 280px | UX-07 | jsdom has no layout engine | Drag the panel to its narrowest; confirm the toolbar fits with no scrollbar |
| Compact mode gains ≥20% message area | UX-08 | Real layout measurement | At 1366×587, measure the message feed height; baseline is ~197px, target ≥237px. UI-SPEC meets this by 0.6px — if it falls short, the reserve lever is the composer's recipient row (`min-h-9` → `min-h-7`, +8px) |
| Fresh side conversation appears in the list immediately | UX-09 | Requires a real create on a live tenant | Create a side conversation, then re-open the parent ticket's panel and confirm it is listed without waiting |
| List header not truncated at ~280px | UX-08 | Real layout measurement | Drag the panel narrow; confirm the list title renders in full |

---

## Validation Sign-Off

- [ ] All automatable rows above have a passing targeted test
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0's single new test file exists and mirrors `last-seen-store.test.ts`
- [ ] No watch-mode flags in any committed command (`--watchAll=false` everywhere)
- [ ] Feedback latency < 60s
- [ ] Manual-only rows carried into the phase-end UAT checklist, none silently dropped
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
