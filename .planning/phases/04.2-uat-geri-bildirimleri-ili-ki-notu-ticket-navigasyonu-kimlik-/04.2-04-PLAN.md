---
phase: 04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/info-box-store.ts
  - src/lib/__tests__/info-box-store.test.ts
  - src/screens/components/info-box.tsx
  - src/screens/compose-screen.tsx
  - src/screens/__tests__/compose-screen.test.tsx
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "SC6/D-16(a) — Yeni konuşma ekranında bilgi kutusu KALICIDIR: kapatma düğmesi yoktur, her açılışta görünür"
    - "SC6/D-16(b) — Kapatma durumu tenant bazlı localStorage'da tutulur; `last-seen-store.ts`'in birebir aynı savunma deseni (boş girdi guard'ı + okuma ve yazmada try/catch) korunur"
    - "SC6/D-17 — Bilgi kutusu metni kilitlidir ve kelimesi kelimesine CONTEXT.md D-17'deki gibidir"
    - "localStorage erişimi iframe kısıtı yüzünden hata fırlatırsa kutu GÖRÜNÜR kalır (güvenli yön: bilgilendirme kaybolmaz), akış hiç bozulmaz"
  artifacts:
    - "src/lib/info-box-store.ts — `getInfoBoxSeen`, `setInfoBoxSeen`"
    - "src/lib/__tests__/info-box-store.test.ts — VALIDATION.md'nin tek Wave 0 gereksinimi (YENİ dosya)"
    - "src/screens/components/info-box.tsx — `InfoBox` (`dismissible` prop'lu tek bileşen, iki çağrı yeri)"
  key_links:
    - "compose-screen.tsx → `<InfoBox dismissible={false} />` — `ScreenContent`'in iç kolonunun İLK çocuğu, `RecipientField`'ın üstünde"
    - "info-box-store `sc:infoBoxSeen:{tenantId}` anahtarı → Plan 06'daki sohbet ekranı yerleşiminin tek kalıcılık kaynağı"
  prohibitions:
    - statement: "Yeni konuşma ekranındaki kutu kapatılabilir OLMAZ — orada kapatma düğmesi hiç render edilmez (D-16(a))"
      status: must-not
      verification: "`grep -c 'dismissible' src/screens/compose-screen.tsx` → `1` VE bu tek kullanım `false` değeri geçirir: `grep -c 'dismissible={false}' src/screens/compose-screen.tsx` → `1`"
    - statement: "Kapatma durumu ticket bazlı DEĞİL tenant bazlı tutulur — `last-seen-store`'un iki boyutlu anahtarı taklit edilmez"
      status: must-not
      verification: "`grep -c 'ticketKey' src/lib/info-box-store.ts` → `0`"
    - statement: "D-17 metni parçalanmaz, kısaltılmaz veya settings'ten okunmaz"
      status: must-not
      verification: "`grep -c 'settings' src/screens/components/info-box.tsx` → `0` VE `grep -c 'otomatik' src/screens/components/info-box.tsx` → `1` (metin tek parça hâlinde bileşende)"
    - statement: "Kutu bir canlı bölge (live region) DEĞİLDİR — `role=\"status\"`/`role=\"alert\"`/`aria-live` taşımaz (UI-SPEC §5)"
      status: must-not
      verification: "`grep -c 'aria-live\\|role=\"status\"\\|role=\"alert\"' src/screens/components/info-box.tsx` → `0`"
---

## Phase Goal

**As a** Grispi temsilcisi, **I want to** yeni bir yan konuşma açarken bunun aslında yeni bir TALEP oluşturduğunu ve alan/atanan/durum bilgilerinin kendiliğinden dolmayacağını görmeyi, **so that** sonradan "bu talep neden boş geldi" sürprizini yaşamayayım.

<objective>
UX-06'nın kalıcılık altyapısı, tek bileşeni ve iki yerleşiminden birincisi (yeni konuşma ekranı). İkinci yerleşim (sohbet ekranında bir kez, kapatılabilir) Plan 06'nın işidir ve bu planın ürettiği store + bileşeni tüketir.

Purpose: Canlı UAT'de temsilci, yan konuşmanın Grispi tarafında gerçek bir talep olarak doğduğunu ve hiçbir alanının otomatik dolmadığını fark etmedi. Bu bir kod hatası değil, bir bilgilendirme boşluğu — ve iki farklı anda (yazarken ve gönderdikten sonra) verilmesi gerekiyor.

Output: `last-seen-store.ts` desenini birebir izleyen küçük bir localStorage modülü, onun testi (VALIDATION.md'nin tek Wave 0 gereksinimi), tek bir `InfoBox` bileşeni ve compose ekranına yerleşimi.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-CONTEXT.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-UI-SPEC.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-PATTERNS.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-VALIDATION.md
</context>

## Artifacts this phase produces

**Bu planın ürettiği yeni dosyalar:**
- `src/lib/info-box-store.ts`
- `src/lib/__tests__/info-box-store.test.ts`
- `src/screens/components/info-box.tsx`

**Yeni export edilen semboller:**

| Sembol | Tür | Dosya |
|--------|-----|-------|
| `getInfoBoxSeen` | fonksiyon (`(tenantId: string) => boolean`) | `src/lib/info-box-store.ts` |
| `setInfoBoxSeen` | fonksiyon (`(tenantId: string) => boolean`) | `src/lib/info-box-store.ts` |
| `InfoBox` | React bileşeni (`{ dismissible: boolean; onDismiss?: () => void }`) | `src/screens/components/info-box.tsx` |

**Yeni localStorage anahtar alanı:** `sc:infoBoxSeen:{tenantId}` (mevcut `sc:` ad alanı konvansiyonu içinde).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tenant bazlı kapatma kalıcılığı (`info-box-store.ts`) — VALIDATION.md Wave 0 gereksinimi</name>

  <files>
    src/lib/info-box-store.ts (YENİ),
    src/lib/__tests__/info-box-store.test.ts (YENİ)
  </files>

  <read_first>
    - `src/lib/last-seen-store.ts` — TAM dosya (60 satır). Bu, D-16'nın CONTEXT.md'de adıyla işaret ettiği precedent'tir: `KEY_PREFIX` sabiti, `storageKey()` bileşimi, `encodeURIComponent` kullanımı, boş girdi guard'ı ve HEM okuma HEM yazmada try/catch. Üçü de (özellikle okumadaki try/catch — iframe kısıtı erişimde de fırlatabilir) korunacak.
    - `src/lib/__tests__/last-seen-store.test.ts` — MEVCUT dosya (~3 KB). Yeni test dosyası bunun yapısını birebir aynalar: aynı `localStorage` temizleme/kurma yaklaşımı, aynı "fırlatan storage" simülasyonu.
    - `.planning/phases/04.2-.../04.2-CONTEXT.md` D-16 — kapatma durumunun TENANT bazlı olduğu (ticket bazlı DEĞİL) ve `last-seen-store` precedent'inin adıyla anıldığı madde.
    - `.planning/phases/04.2-.../04.2-VALIDATION.md` "Wave 0 Requirements" (satır 71-82) — bu fazın TEK yeni test dosyası bu görevin çıktısıdır; diğer iki "eksik test dosyası" iddiası yanlıştı ve düzeltildi.
    - `.planning/phases/04.2-.../04.2-PATTERNS.md` "`src/lib/info-box-store.ts`" bölümü (satır 440-474) — aynalanacak tam kod deseni ve ticket-key boyutunun neden düşürüldüğü.
  </read_first>

  <behavior>
    `getInfoBoxSeen(tenantId)` için yazılacak testler (implementasyondan ÖNCE):
    - Hiç kayıt yokken `false` döner
    - `setInfoBoxSeen` çağrıldıktan sonra aynı tenant için `true` döner
    - BAŞKA bir tenant için hâlâ `false` döner (tenant izolasyonu)
    - Boş / yalnızca boşluk `tenantId` → `false` döner ve `localStorage`'a hiç dokunmaz
    - `localStorage.getItem` fırlattığında `false` döner ve hata dışarı sızmaz
    - Anahtarda özel karakter taşıyan bir tenant (`a:b`) kaydı, aynı tenant için doğru okunur ve komşu bir anahtara sızmaz (kodlama testi)

    `setInfoBoxSeen(tenantId)` için yazılacak testler:
    - Başarılı yazımda `true` döner
    - Boş / yalnızca boşluk `tenantId` → `false` döner ve `localStorage`'a hiç dokunmaz
    - `localStorage.setItem` fırlattığında `false` döner ve hata dışarı sızmaz
  </behavior>

  <action>
    **`src/lib/info-box-store.ts` (YENİ):** `last-seen-store.ts`'in yapısını birebir aynala, iki bilinçli farkla: (1) anahtar ön eki bu modüle özgüdür ve `sc:` ad alanı içinde `infoBoxSeen` alt alanını kullanır, (2) `storageKey` YALNIZCA `tenantId` alır — ticket boyutu yoktur (D-16 "tenant bazlı"). `tenantId` `encodeURIComponent`'ten geçirilir.

    `getInfoBoxSeen(tenantId: string): boolean` — boş/boşluk girdide erken `false`; okuma `try`/`catch` içindedir ve `catch` dalı `false` döner. Depolanan değer basit bir işaret dizesidir; okuma bu dizeyle birebir karşılaştırılır (parse etme, JSON kullanma).

    `setInfoBoxSeen(tenantId: string): boolean` — boş/boşluk girdide erken `false`; yazma `try`/`catch` içindedir, başarıda `true`, hatada `false` döner.

    Her iki fonksiyonun üstüne, `last-seen-store.ts`'inkiyle aynı ruhta kısa doc-comment yaz: iframe depolama kısıtının ERİŞİMDE de fırlatabildiği, ve hatanın güvenli yönünün "görülmemiş" (yani kutu görünür) olduğu — bilgilendirme kaybolmaktansa fazladan görünsün.

    Bu dosyada React, MobX veya store bağımlılığı KULLANMA — `last-seen-store.ts` gibi saf bir modüldür.

    **`src/lib/__tests__/info-box-store.test.ts` (YENİ):** `last-seen-store.test.ts`'in yapısını aynala. Fırlatan storage senaryosu için aynı simülasyon yaklaşımını kullan (yeni bir yardımcı icat etme). Her testten sonra depolamayı temizle.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern=info-box-store</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern=info-box-store` exit kodu `0` ve en az 9 test çalışır
    - `grep -c "export function getInfoBoxSeen(tenantId: string): boolean" src/lib/info-box-store.ts` → `1`
    - `grep -c "export function setInfoBoxSeen(tenantId: string): boolean" src/lib/info-box-store.ts` → `1`
    - `grep -c "ticketKey" src/lib/info-box-store.ts` → `0` (tenant bazlı, ticket bazlı değil)
    - `grep -c "encodeURIComponent" src/lib/info-box-store.ts` → `1`
    - `grep -c "catch" src/lib/info-box-store.ts` → `2` (okuma + yazma savunması)
    - `grep -c "infoBoxSeen" src/lib/info-box-store.ts` → `1` (anahtar ön eki tek yerde)
    - `grep -c "import" src/lib/info-box-store.ts` → `0` (saf modül, bağımlılığı yok)
    - Davranış: farklı iki tenant'ın birbirinin kaydını görmediğini assert eden yeşil bir test vardır
    - Davranış: fırlatan `localStorage` ile her iki fonksiyonun da throw etmediğini assert eden yeşil testler vardır
  </acceptance_criteria>

  <done>
    Tenant bazlı kalıcılık modülü ve testi mevcut; `last-seen-store`'un savunma deseni birebir korunmuş; VALIDATION.md'nin tek Wave 0 gereksinimi kapandı.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `InfoBox` bileşeni ve yeni konuşma ekranındaki kalıcı yerleşimi (D-16(a), D-17)</name>

  <files>
    src/screens/components/info-box.tsx (YENİ),
    src/screens/compose-screen.tsx,
    src/screens/__tests__/compose-screen.test.tsx
  </files>

  <read_first>
    - `.planning/phases/04.2-.../04.2-UI-SPEC.md` §5 (satır 256-291) — `InfoBox`'ın TAM JSX'i, sınıf dizeleri, `dismissible` prop sözleşmesi, iki yerleşimin farkı, kapatma sonrası odak kuralı ve "canlı bölge değildir" gerekçesi. Bu bölüm bu görevin birincil kaynağıdır.
    - `.planning/phases/04.2-.../04.2-UI-SPEC.md` "Copywriting Contract" (satır 118-119) — D-17 metninin ve kapatma düğmesi `aria-label`'ının kilitli hâlleri.
    - `.planning/phases/04.2-.../04.2-CONTEXT.md` D-17 — metnin karakteri karakterine kaynağı.
    - `src/screens/compose-screen.tsx` — tam dosya (161 satır). Özellikle `ScreenContent` içindeki iç kolon (satır 131-142): `RecipientField` / `SubjectField` / `MessageField` sırası. Kutu bu kolonun İLK çocuğu olur.
    - `src/screens/__tests__/compose-screen.test.tsx` — MEVCUT dosya (~5 KB). `useGrispi` ve alt alan bileşenlerinin mock'landığı kurulum (satır 8-25); yeni assertion'lar bu kuruluma eklenir.
    - `src/screens/components/attachment-chip.tsx` — bu kod tabanının kabul ettiği en küçük etkileşimli hedef boyutu (kaldır düğmesi); `InfoBox`'ın kapatma düğmesi bunun bir tık üstündedir ve UI-SPEC bunu açıkça gerekçelendiriyor.
  </read_first>

  <behavior>
    `InfoBox` için yazılacak testler:
    - `dismissible={false}` ile render edildiğinde D-17 metni DOM'da bulunur ve HİÇBİR düğme render edilmez
    - `dismissible={true}` ile render edildiğinde kilitli `aria-label`'a sahip tek bir düğme bulunur; tıklandığında `onDismiss` TAM BİR KEZ çağrılır
    - Bileşen `aria-live`, `role="status"` veya `role="alert"` taşımaz
    - Bilgi ikonu `aria-hidden` taşır (erişilebilir ada karışmaz)

    `ComposeScreen` için yazılacak testler:
    - Ekran render edildiğinde D-17 metni DOM'da bulunur
    - Bilgi kutusu, alıcı alanının ÜSTÜNDE render edilir (DOM sırası assertion'ı — `compareDocumentPosition` veya kapsayıcı içindeki çocuk sırası)
    - Ekranda bilgi kutusuna ait bir kapatma düğmesi HİÇ bulunmaz (D-16(a): burada kalıcı)
  </behavior>

  <action>
    **`src/screens/components/info-box.tsx` (YENİ):** UI-SPEC §5'teki bileşeni birebir uygula. Props: `dismissible: boolean` ve `onDismiss?: () => void`. Kök öğe UI-SPEC'teki sınıf dizesini taşır (alt kenarlık, soluk `muted` arka plan, küçük punto, `muted-foreground` renk). İçerik üç parçadır: bilgi ikonu (`aria-hidden="true"`), D-17 metnini taşıyan paragraf, ve `dismissible` doğruysa kapatma düğmesi.

    D-17 metnini `04.2-CONTEXT.md`'nin D-17 maddesinden karakteri karakterine kopyala (Türkçe karakterler ve noktalama dâhil); yeniden ifade etme, iki cümleye bölme, settings'ten okuma. Metin bileşenin içinde sabittir.

    Kapatma düğmesi: `type="button"`, UI-SPEC'in kilitli `aria-label`'ı, `size-6` hit alanı, `focus-visible` halka sınıfları ve kapatma ikonu (`aria-hidden`). `onClick` doğrudan `onDismiss`'i çağırır — bileşen kendi görünürlüğünü YÖNETMEZ, kapatma kararını çağıran ekran verir (compose ekranı hiç kapatmaz, sohbet ekranı Plan 06'da kapatır).

    Bileşene `role="status"`, `role="alert"` veya `aria-live` EKLEME — UI-SPEC §5 bunu açıkça gerekçelendiriyor: statik bilgilendirmedir ve kaldırılması kullanıcının kendi tıklamasının sonucudur, duyurulmasına gerek yoktur.

    Kapatma sonrası odak yönetimini bileşene GÖMME — odağın nereye gideceği çağıran ekranın bilgisidir (UI-SPEC §5: sohbet ekranının geri düğmesi); bu, Plan 06'nın sorumluluğudur.

    **`src/screens/compose-screen.tsx`:** `ScreenContent`'in iç kolonundaki (`aria-label="Yeni konuşma e-postası"` taşıyan `div`) İLK çocuk olarak `<InfoBox dismissible={false} />` ekle — `RecipientField`'ın hemen üstüne. `onDismiss` GEÇME (burada kapatma yok). Başka hiçbir şeye dokunma: `submit`, dirty-guard, konu ön doldurma efekti, `ConfirmDialog` aynen kalır.

    **`src/screens/__tests__/compose-screen.test.tsx`:** `<behavior>`'ın son üç senaryosunu ekle. Alt alan bileşenleri zaten mock'lu olduğu için DOM sırası assertion'ı `RecipientField` mock'unun ürettiği metne göre kurulabilir. `InfoBox`'ı MOCK'LAMA — gerçek bileşenin render edildiğini kanıtlamak testin amacıdır. `InfoBox`'ın kendi dört senaryosu için aynı dosyaya ayrı bir `describe` bloğu ekle (yeni bir test dosyası açmaya gerek yok; bileşen yalnızca bu ekranda ve Plan 06'nın ekranında kullanılıyor).
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern="info-box|compose-screen" && CI=true npm test -- --watchAll=false</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern="info-box|compose-screen"` exit kodu `0`
    - `CI=true npm test -- --watchAll=false` exit kodu `0` (dalga birleşim gate'i)
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `grep -c "dismissible={false}" src/screens/compose-screen.tsx` → `1`
    - `grep -c "onDismiss" src/screens/compose-screen.tsx` → `0` (compose ekranında kapatma yok)
    - `grep -c "InfoBox" src/screens/compose-screen.tsx` → `2` (import + kullanım)
    - `grep -c 'aria-live\|role="status"\|role="alert"' src/screens/components/info-box.tsx` → `0`
    - `grep -c "settings" src/screens/components/info-box.tsx` → `0` (metin sabit, D-17)
    - `grep -c "useState" src/screens/components/info-box.tsx` → `0` (görünürlüğü çağıran yönetir)
    - Davranış: `ComposeScreen` render'ında D-17 metninin bulunduğunu VE hiçbir kapatma düğmesinin bulunmadığını assert eden yeşil bir test vardır
    - Davranış: `dismissible={true}` ile kapatma düğmesine tıklandığında `onDismiss`'in bir kez çağrıldığını assert eden yeşil bir test vardır
    - Davranış: bilgi kutusunun alıcı alanından ÖNCE geldiğini assert eden yeşil bir DOM-sırası testi vardır
  </acceptance_criteria>

  <done>
    Yeni konuşma ekranı, kilitli D-17 metnini kapatılamaz biçimde gösteriyor; `InfoBox` iki yerleşimi de karşılayacak tek bileşen olarak hazır; sohbet ekranı yerleşimi Plan 06'ya devredildi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin iframe → tarayıcı localStorage | Üçüncü taraf iframe'de depolama erişimi tarayıcı politikasıyla engellenebilir; erişim denemesi bile fırlatabilir |
| `tenantId` (bundle) → localStorage anahtarı | Güvenilir bağlamdan gelir, ancak anahtar bileşiminde kodlanmadan kullanılırsa komşu anahtarlarla çakışabilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-15 | Denial of Service | Fırlatan `localStorage`'ın compose/sohbet ekranını çökertmesi | medium | mitigate | Hem okuma hem yazma `try`/`catch` içindedir ve güvenli varsayılana (`false` = görülmemiş) düşer; `last-seen-store.ts`'in kanıtlanmış deseni. Gate (Task 1): fırlatan storage ile iki fonksiyonun da throw etmediğini assert eden yeşil testler |
| T-04.2-16 | Tampering | Anahtar çakışması yoluyla başka bir tenant'ın kaydının okunması/yazılması | low | mitigate | `tenantId` `encodeURIComponent`'ten geçirilir ve sabit bir ön ek altında saklanır; tenant izolasyonu testle sabitlenir. Gate (Task 1): iki tenant izolasyon testi yeşil |
| T-04.2-17 | Information Disclosure | Bilgi kutusu metninin kullanıcı/sunucu verisi taşıması | low | mitigate | Metin D-17'de kilitli bir sabittir; hiçbir prop, settings değeri veya sunucu alanı metne girmez. Gate (Task 2): `settings` grep'i `0`, metin tek parça bileşende |
| T-04.2-18 | Repudiation | Kapatma kaydının cihaz-yerel olması nedeniyle başka cihazda tekrar görünmesi | low | accept | D-16 kalıcılığı açıkça cihaz/tarayıcı yerel tanımlıyor (`last-seen-store` ile aynı model). Sunucu tarafı görülme takibi v2'ye bırakıldı (V2-04) |
| T-04.2-SC | Tampering | Paket yöneticisi kurulumları (npm) | high | accept | Bu plan hiçbir yeni paket kurmuyor; ikonlar mevcut `@radix-ui/react-icons` setinden gelir. Gate: `package.json`/`package-lock.json` diff'i boş |
</threat_model>

<verification>
**Otomatik (bu plan tamamlandığında geçmeli):**
1. `npx craco test --watchAll=false --testPathPattern=info-box-store` → exit 0
2. `npx craco test --watchAll=false --testPathPattern="info-box|compose-screen"` → exit 0
3. `CI=true npm test -- --watchAll=false` → exit 0
4. `npx tsc --noEmit -p tsconfig.json` → exit 0
5. `git diff --stat package.json package-lock.json` → boş

**Bu planın kapsamadıkları:** Sohbet ekranındaki bir-kerelik kapatılabilir yerleşim ve kapatma sonrası odak devri (Plan 06).
</verification>

<success_criteria>
- [ ] `getInfoBoxSeen`/`setInfoBoxSeen` tenant bazlı, savunmalı ve testli (UX-06, D-16)
- [ ] VALIDATION.md'nin tek Wave 0 gereksinimi (yeni test dosyası) kapandı
- [ ] `InfoBox` tek bileşen olarak iki yerleşimi de karşılıyor (`dismissible` prop'u)
- [ ] Yeni konuşma ekranında kutu kalıcı; kapatma düğmesi hiç render edilmiyor (D-16(a))
- [ ] Metin D-17'de kilitli hâliyle, tek parça ve sabit (D-17)
- [ ] Kutu canlı bölge değil; ikon `aria-hidden`
- [ ] Tam test suite ve `tsc --noEmit` yeşil
</success_criteria>

<output>
Create `.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-04-SUMMARY.md` when done
</output>
