---
phase: 04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grispi/client/__tests__/environment-static.test.ts
autonomous: true
gap_closure: true
requirements: [UX-01, UX-09]

must_haves:
  truths:
    - "T-04.1-01 gate'i artık otomatik olarak uygulanır: `src/grispi/client/environment.ts` içine kod düzeyinde bir şablon-değişmez (template literal) sızarsa test paketi KIRMIZI olur — bugün olduğu gibi yalnızca üç yorum satırının iyi niyetine bağlı kalmaz"
    - "Gate, kendi açıklamasıyla kırılamaz: doğrulama yorumları ayıklanmış kaynak üzerinde çalışır, bu yüzden dosyanın doc-comment'lerinde geçen ters tırnaklı prose gate'i tetiklemez (bu tam olarak bu fazda bir kez yaşanmış hatadır — commit `07a2cb9`)"
    - "Gate boşa düşemez: ayıklama adımı kaynağın tamamını silerse test bunu fark eder ve yine kırmızı olur"
  artifacts:
    - "src/grispi/client/__tests__/environment-static.test.ts — statik kaynak gate'i (bu fazda tek yeni test dosyası)"
  key_links:
    - "`GRISPI_BASE_URLS` → `HttpHandler.setEnvironment` → `fetch` host'u VE `buildAgentTicketUrl` → `target=\"_blank\"` bağlantısı — `environment.ts`'e sızacak bir interpolasyon her iki yolu da aynı anda zehirler; gate bu tek dosyayı koruduğu için ikisini birden korur"
  prohibitions:
    - statement: "`src/grispi/client/environment.ts` bu planda DEĞİŞTİRİLMEZ — plan yalnızca mevcut değişmezi otomatik kanıta bağlar"
      status: must-not
      verification: "`git diff --stat src/grispi/client/environment.ts` → boş"
    - statement: "Gate, yeni bir lint kuralı, yeni bir npm paketi veya yeni bir CI adımı ile kurulmaz — mevcut Jest paketinin içinde yaşar"
      status: must-not
      verification: "`git diff --stat package.json package-lock.json .eslintrc* craco.config.js` → boş"
---

## Phase Goal

**As a** bu kod tabanına dokunan bir geliştirici, **I want to** `environment.ts`'e yanlışlıkla bir host interpolasyonu eklediğimde bunu bir insanın gözünden değil test paketinden öğrenmeyi, **so that** agent'ın `Authorization` başlığını taşıyan istekler ve yeni sekmede açılan talep bağlantıları asla yanlış bir host'a gitmesin.

> **Not (MVP mode):** Tek görevli, tek dosyalı bir regresyon gate'i. Plan 09 ile hiçbir dosya çakışması yoktur; aynı dalgada paralel çalışır.

<objective>
Faz 04.1'de kurulan T-04.1-01 değişmezini — `src/grispi/client/environment.ts` kod düzeyinde şablon-değişmez (template literal) İÇERMEZ — otomatik bir teste bağlar. Bugün bu kural yalnızca dosyadaki üç yorum satırıyla ifade ediliyor ve HİÇBİR test veya lint kuralı tarafından uygulanmıyor (REVIEW WR-03; `src/` altında kaynak metnini okuyan tek bir test dosyası yok).

Purpose: Kural teoride değil pratikte kırıldı. Faz 04.2 sırasında gate regresyona uğradı ve elle onarıldı (commit `07a2cb9`) — üstelik onarım sonrası, gate'i AÇIKLAYAN bir yorumun kendisi gate'i yeniden kırdı. Bu, kapsam çitinin bu tek ek maddeyi açıkça yetkilendirmesinin gerekçesidir: değişmez, koruduğu şeyin ağırlığına göre (hem `fetch` host'u hem `target="_blank"` bağlantı host'u tek dosyadan türüyor) fazlasıyla kırılgan bir mekanizmayla tutuluyor.

Output: Kaynağı okuyup yorumları ayıkladıktan sonra tek bir değişmezi assert eden, kendi ayıklamasının boşa düşmediğini de kontrol eden bir test dosyası. Üretim kodu değişmez.

**Frontmatter'daki `requirements` alanının okunuşu:** Bu plan SC1/UX-01 veya SC9/UX-09'un işlevselliğini KENDİSİ kapatmaz — o iş Plan 09'undur. Bu plan, bu fazın başarısız kriterleri için açılmış bir gap-closure koruma planıdır: Faz 04.1'den devralınan T-04.1-01 statik gate'inin `environment.ts` üzerindeki regresyonunu önler; o gate bu faz sırasında sessizce kırıldı ve `07a2cb9` commit'inde elle onarılmak zorunda kaldı.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-REVIEW.md
@.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-VALIDATION.md
</context>

## Artifacts this phase produces

**Bu planın ürettiği YENİ semboller:**

| Sembol | Tür | Dosya |
|--------|-----|-------|
| T-04.1-01 statik gate testi | Jest test dosyası (yeni) | `src/grispi/client/__tests__/environment-static.test.ts` |

**Bu planın DEĞİŞTİRMEDİĞİ:** `src/grispi/client/environment.ts` (yalnızca OKUNUR), `GRISPI_BASE_URLS`, `grispiTld`, `buildAgentTicketUrl`, `isGrispiEnvironment` — hiçbirinin davranışına, imzasına veya yorumuna dokunulmaz.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T-04.1-01 için statik kaynak gate testi</name>

  <files>
    src/grispi/client/__tests__/environment-static.test.ts
  </files>

  <read_first>
    - `src/grispi/client/environment.ts` — tam dosya (77 satır). Üç yerde (satır 1-5, 45-48, 59-63) korunması istenen değişmezi anlatan doc-comment'ler var; hepsi blok yorumudur. Kod bölümünde dikkat edilecek iki şey: (a) `GRISPI_BASE_URLS` sabitinin değerleri çift eğik çizgi içeren URL string'leridir — naif bir satır-yorumu ayıklayıcısı bu satırların yarısını yer; (b) `grispiTld` bir düzenli ifade değişmezi taşır. Gate'in ayıklama adımı ikisini de bozmamalıdır.
    - `src/grispi/client/__tests__/tickets.test.ts` satır 1-20 — bu dizindeki test dosyalarının import/`describe` biçimi ve TypeScript stili. Yeni dosya bu stili izler.
    - `.planning/phases/04.2-.../04.2-REVIEW.md` WR-03 (satır 267-286) — bulgunun tamamı, önerilen çözümün taslağı ve `07a2cb9` commit'inin geçmişi.
    - `.planning/phases/04.2-.../04.2-VERIFICATION.md` satır 133 — bugünkü elle çalıştırılan gate ve beklenen sonucu; yeni test bunun otomatik ve daha güçlü hâlidir.
    - `.planning/phases/04.2-.../04.2-VALIDATION.md` "Test Infrastructure" tablosu — çalıştırma komutları ve Jest sürümü (CRA-bundled 27.5.1; `craco test` üzerinden).
  </read_first>

  <behavior>
    `environment-static.test.ts` için yazılacak testler:

    - **Ana gate:** `src/grispi/client/environment.ts` kaynağından yorumlar ayıklandıktan sonra kalan metin, şablon-değişmez başlatan ters tırnak (backtick) karakterini İÇERMEZ. Bu assertion başarısız olduğunda mesaj, T-04.1-01'i ve dosyanın neden bu kısıtı taşıdığını (host interpolasyonu hem `fetch` hedefini hem yeni-sekme bağlantısını zehirler) yazılı olarak söyler.
    - **Ayıklamanın doğruluğu (blok yorumu):** Dosyanın doc-comment'lerinde ters tırnaklı prose bulunmasına RAĞMEN ana gate yeşildir — yani ayıklayıcı blok yorumlarını gerçekten kaldırıyordur. (Bugünkü dosya tam olarak bu durumdadır; testin bugün yeşil olması bu maddenin kanıtıdır.)
    - **Ayıklamanın boşa düşmemesi:** Ayıklama sonrası kalan metin, dosyanın kod bölümünden bilinen ayırt edici bir tanımlayıcıyı (`GRISPI_BASE_URLS`) HÂLÂ içerir. Bu, ayıklayıcının kaynağın tamamını silip gate'i vakum hâline getirmesini yakalar.
    - **URL string'lerinin korunması:** Ayıklama sonrası kalan metin, `GRISPI_BASE_URLS`'in üç ortam anahtarını (`preprod`, `prod`, `prod_tr`) hâlâ içerir — yani satır-yorumu ayıklaması, çift eğik çizgi taşıyan URL değerlerinin bulunduğu satırları kesmemiştir.
  </behavior>

  <action>
    **Yeni dosya `src/grispi/client/__tests__/environment-static.test.ts`:**

    Node'un `fs` ve `path` modüllerini kullanarak `path.join(__dirname, "..", "environment.ts")` yolundaki kaynağı `readFileSync(..., "utf8")` ile oku. `require.resolve` KULLANMA — derlenmiş/çözümlenmiş bir yol döndürebilir; gate'in okuması gereken şey diskteki TypeScript kaynağının kendisidir.

    Ayıklama adımı iki aşamalıdır ve sırası önemlidir: önce blok yorumlarını (yıldızlı açılış/kapanış çifti) tembel eşleşmeyle kaldır; sonra YALNIZCA satır başında (öndeki boşluklar hariç) çift eğik çizgiyle BAŞLAYAN satırları kaldır. İkinci adımı satır içinde herhangi bir yerde geçen çift eğik çizgiye uygulama — `GRISPI_BASE_URLS`'in URL değerleri çift eğik çizgi taşıyor ve naif bir ayıklama o satırların kalanını yer, gate'i sessizce zayıflatır. Ayıklamayı ayrı bir yardımcı fonksiyona koy ki testler onu paylaşsın.

    Ana assertion: ayıklanmış metin ters tırnak karakterini içermemelidir. Assertion'a, başarısızlıkta okunacak açıklayıcı bir mesaj iliştir: değişmezin adı (T-04.1-01), neyi koruduğu (bu dosya hem API `fetch` host'unun hem agent-UI bağlantı host'unun tek kaynağıdır; interpolasyon her ikisine de sızar) ve doğru çözüm (string birleştirme kullan — `buildAgentTicketUrl` zaten bunu yapıyor). Mesajı kısa tut.

    Ardından `<behavior>`'daki iki sağlamlık assertion'ını ekle: ayıklanmış metnin `GRISPI_BASE_URLS` tanımlayıcısını ve üç ortam anahtarını hâlâ içerdiği. Bunlar gate'in vakum hâline düşmesini ve satır-yorumu ayıklamasının fazla agresif olmasını yakalar.

    Testin adı ve `describe` başlığı, ilerideki bir okuyucunun neyi koruduğunu anında görmesini sağlamalıdır (T-04.1-01'i açıkça anmalı).

    **Değiştirilmeyecekler:** `src/grispi/client/environment.ts`'e hiçbir düzenleme yapma — yorum ekleme, yorum silme, biçimlendirme yapma. Yeni bir ESLint kuralı, yeni bir `craco`/Jest yapılandırması veya yeni bir npm paketi EKLEME; gate mevcut test altyapısının içinde yaşar. Başka hiçbir kaynak dosyayı okuyan ikinci bir gate yazma — kapsam yalnızca bu tek dosyadır.
  </action>

  <verify>
    <automated>npx craco test --watchAll=false --testPathPattern=environment-static && CI=true npm test -- --watchAll=false</automated>
  </verify>

  <acceptance_criteria>
    - `npx craco test --watchAll=false --testPathPattern=environment-static` exit kodu `0`
    - `CI=true npm test -- --watchAll=false` exit kodu `0` (tam suite; suite sayısı bir artmış olmalı)
    - `npx tsc --noEmit -p tsconfig.json` exit kodu `0`
    - `test -f src/grispi/client/__tests__/environment-static.test.ts` → doğru
    - `git diff --stat src/grispi/client/environment.ts` → boş (üretim kodu değişmedi)
    - `git diff --stat package.json package-lock.json` → boş
    - Davranış: test dosyası, `environment.ts` kaynağını diskten okuyup yorumları ayıkladıktan sonra şablon-değişmez başlangıç karakterinin bulunmadığını assert eden yeşil bir test içerir
    - Davranış: test dosyası, ayıklama sonrası kaynağın `GRISPI_BASE_URLS` tanımlayıcısını ve üç ortam anahtarını hâlâ içerdiğini assert eden yeşil bir test içerir (gate vakuma düşemez)
    - Elle regresyon kontrolü (yürütücü çalıştırır, sonucu geri alır): `environment.ts`'in kod bölümüne geçici bir şablon-değişmez eklendiğinde `npx craco test --watchAll=false --testPathPattern=environment-static` KIRMIZI olur; aynı metin bir doc-comment'e eklendiğinde YEŞİL kalır. Kontrol sonrası dosya `git checkout` ile eski hâline döndürülür ve commit'e hiçbir üretim değişikliği girmez
  </acceptance_criteria>

  <done>
    T-04.1-01 artık üç yorum satırının iyi niyetiyle değil, her `npm test` çalıştırmasında kanıtlanan bir testle korunuyor; gate kendi açıklamasıyla kırılamıyor ve boşa düşemiyor; üretim kodu bir karakter bile değişmedi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GRISPI_BASE_URLS` → `HttpHandler`'ın `fetch` host'u | Agent'ın `Authorization: Bearer` başlığını taşıyan HER istek bu tek sabitten host alıyor; buraya sızan bir interpolasyon token'ı yabancı bir host'a taşıyabilir (dosyanın kendi doc-comment'i bu senaryoyu zaten kaydediyor) |
| `buildAgentTicketUrl` → `target="_blank"` bağlantısı | Aynı dosya, agent'ın yeni sekmede açtığı talep bağlantısının host'unu da üretiyor; interpolasyon burada bir yönlendirme (phishing) yüzeyi açar |
| Diskteki kaynak metni → test süreci | Gate yalnızca OKUR; hiçbir üretim dosyasını yazmaz, çalıştırmaz veya değerlendirmez (`eval`/`require` yok) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-10-01 | Tampering | `src/grispi/client/environment.ts`'e sızan host interpolasyonu (T-04.1-01 regresyonu) | high | mitigate | Yorumları ayıklanmış kaynak üzerinde çalışan otomatik statik gate; her `npm test` çalıştırmasında uygulanır. Gate: `environment-static` testi yeşil VE elle enjekte edilen bir şablon-değişmezde kırmızı olduğu doğrulanmış |
| T-04.2-10-02 | Tampering | Gate'in kendi açıklamasıyla kırılması (bu fazda bir kez yaşandı) | medium | mitigate | Assertion yorumlar ayıklandıktan SONRA çalışır; doc-comment'lerdeki ters tırnaklı prose gate'i tetikleyemez. Gate: bugünkü dosya (doc-comment'lerinde ters tırnak taşıyor) testi yeşil geçirir |
| T-04.2-10-03 | Tampering | Gate'in sessizce vakuma düşmesi (ayıklayıcı her şeyi siler, assertion boş metinde çalışır) | medium | mitigate | Ayıklanmış metnin `GRISPI_BASE_URLS` tanımlayıcısını ve üç ortam anahtarını hâlâ içerdiğini assert eden ikinci ve üçüncü kontroller |
| T-04.2-10-04 | Elevation of Privilege | Test'in üretim kaynağını çalıştırması/değerlendirmesi | low | accept | Gate yalnızca `readFileSync` ile metin okur; `eval`, dinamik `require` veya alt süreç yok. Yeni bir yürütme yüzeyi açılmaz |
| T-04.2-10-SC | Tampering | Paket yöneticisi kurulumları (npm) | high | accept | Bu plan HİÇBİR yeni paket kurmuyor; `fs` ve `path` Node'un yerleşik modülleridir, Jest ortamında zaten erişilebilirler. Gate: `git diff --stat package.json package-lock.json` → boş |
</threat_model>

<verification>
**Otomatik (bu plan tamamlandığında geçmeli):**

1. `npx craco test --watchAll=false --testPathPattern=environment-static` → exit 0
2. `CI=true npm test -- --watchAll=false` → exit 0 (tam suite, suite sayısı bir artmış)
3. `npx tsc --noEmit -p tsconfig.json` → exit 0
4. `git diff --stat src/grispi/client/environment.ts` → boş
5. `git diff --stat package.json package-lock.json` → boş

**Bu planın kapsamadıkları:** `buildAgentTicketUrl`'ün `tenantId` doğrulaması (WR-01) ve `grispiTld`'nin fail-open davranışı (WR-02) — ikisi de bilinçli olarak ertelendi, aşağıdaki tabloya bakın. Bu plan `environment.ts`'in DAVRANIŞINI değil, yalnızca yazım biçimi değişmezini korur.
</verification>

## Kapsam dışı — bilinçli olarak ertelendi

| Öğe | Kaynak | Neden ertelendi |
|-----|--------|-----------------|
| `buildAgentTicketUrl`'ün `tenantId`'yi kodlamaması/doğrulamaması | REVIEW WR-01 | Kapsam çiti: bugünkü girdiler güvenilir; davranış değişikliği bu gate'in konusu değil |
| `grispiTld`'nin eşleşmeyen base URL'de fail-open `.replace()` davranışı | REVIEW WR-02 | Aynı — yalnızca yeni bir ortam eklenirse tetiklenir |
| Aynı gate'in başka dosyalara (`tickets.ts` vb.) genişletilmesi | — | `environment.ts` dışındaki dosyalarda şablon-değişmez YASAK DEĞİLDİR; genişletme yanlış olurdu |
| ESLint kuralı veya CI adımı olarak uygulama | REVIEW WR-03 alternatifi | Yeni yapılandırma/paket yüzeyi açar; Jest içinde kalmak daha küçük ve daha hızlı geri bildirim veriyor |

<success_criteria>
- [ ] `src/grispi/client/__tests__/environment-static.test.ts` mevcut ve tam suite içinde yeşil
- [ ] Gate, kod bölümüne enjekte edilen bir şablon-değişmezde kırmızı olur (yürütücü elle doğrular ve geri alır)
- [ ] Gate, doc-comment'lerdeki ters tırnaklı prose'tan etkilenmez
- [ ] Gate vakuma düşemez: ayıklanmış kaynağın hâlâ kod içerdiği assert edilir
- [ ] `src/grispi/client/environment.ts` bir karakter bile değişmedi
- [ ] Yeni npm paketi, yeni lint kuralı, yeni yapılandırma yok
</success_criteria>

<output>
Create `.planning/phases/04.2-uat-geri-bildirimleri-ili-ki-notu-ticket-navigasyonu-kimlik-/04.2-10-SUMMARY.md` when done
</output>
