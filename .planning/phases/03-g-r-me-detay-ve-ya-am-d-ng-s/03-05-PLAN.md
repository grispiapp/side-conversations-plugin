---
phase: 03-g-r-me-detay-ve-ya-am-d-ng-s
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/store/active-conversation-store.ts
  - src/store/__tests__/active-conversation-store.test.ts
autonomous: true
requirements: [THRD-01, THRD-02, THRD-03, THRD-04]
must_haves:
  truths:
    - "A selected side ticket loads all comments chronologically with incoming/own/internal classification."
    - "Reply clears the draft immediately, appears pending, retries identically on failure, then refetches active thread and list."
    - "Reply payload appends a probe-confirmed sanitized quote of the existing thread context to the new HTML body."
    - "Solve/reopen never flips optimistically; failure preserves the current view and is retryable."
    - "Scroll targets the first unseen external message while last-seen writes the latest relevant external timestamp after a successful current-generation load."
  artifacts:
    - path: "src/store/active-conversation-store.ts"
      provides: "Real thread load/reply/lifecycle/read-state owner"
    - path: "src/store/__tests__/active-conversation-store.test.ts"
      provides: "Race, normalization, optimistic reply, lifecycle and read-state coverage"
  key_links:
    - from: "src/store/active-conversation-store.ts"
      to: "grispiAPI.tickets.patchTicket"
      via: "narrow reply/status requests"
      pattern: "patchTicket"
    - from: "src/store/active-conversation-store.ts"
      to: "sideConversations.load"
      via: "server-truth refresh after successful mutation"
      pattern: "sideConversations\\.load"
---

<objective>
Turn ActiveConversationStore into the authoritative real-thread state machine.

Purpose: Close the external correspondence loop with race-safe loading, retryable replies, server-truth lifecycle changes, and correct read tracking.
Output: One tested MobX store implementing THRD-01..04 behavior.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-01-probe-findings.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-PATTERNS.md
@src/store/active-conversation-store.ts
@src/store/side-conversations-store.ts
@src/lib/html-sanitizer.ts
@src/lib/last-seen-store.ts

<interfaces>
Plan 03-02 provides probe-backed `patchTicket(key, PatchTicketRequest)`. Plan 03-03 provides `setLastSeenAt` plus `ConversationRowVM.lifecycle/actionBadge/hasUnseen`. Plan 03-04 provides sanitizer/quote extraction consumed during Comment normalization.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load and normalize the real chronological thread</name>
  <files>src/store/active-conversation-store.ts, src/store/__tests__/active-conversation-store.test.ts</files>
  <behavior>
    - load(key,parentKey) sorts comments oldest-first and classifies public incoming/own plus non-public internal note.
    - Incoming/own HTML and quoted parts are sanitized; recipient/subject/solved state derive from the loaded ticket.
    - A stale earlier load cannot overwrite a later selected ticket.
    - Failed load exposes safe Turkish retry state and never writes last-seen.
    - With multiple unseen external messages, scroll target points to the earliest unseen message but lastSeen writes the latest relevant external timestamp.
    - With no unseen external message, scroll target points to the last message and lastSeen never regresses.
  </behavior>
  <action>Extend the existing store without breaking Phase 2 `startNew`. Add real load state, active/parent keys, recipient/subject, solved state, normalized immutable MessageVM fields, quote/internal metadata, and a monotonic generation guard. Compute two deliberately separate values per D-06/D-18: `scrollTargetMessageId` is the first external message newer than the pre-load lastSeen (or the final message if none), while `latestRelevantExternalAt` is the maximum timestamp among loaded relevant external public messages and is the only timestamp eligible for `setLastSeenAt`. Write the latter only after the active generation successfully normalizes and never regress an existing later value; then ask the list store to refresh/rederive. Add a regression fixture with at least two unseen external messages proving the viewport target is the first while storage advances to the latest. Storage failure must not convert load success into failure.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/store/__tests__/active-conversation-store.test.ts</automated></verify>
  <done>Thread normalization, race, errors, scroll target, and post-success-only read marking pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reply and lifecycle mutation state machines</name>
  <files>src/store/active-conversation-store.ts, src/store/__tests__/active-conversation-store.test.ts</files>
  <behavior>
    - sendReply sanitizes HTML, appends pending own message, clears draft immediately, and retains exact retry payload.
    - sendReply constructs new sanitized HTML followed by the probe-confirmed sanitized quote of existing context; retry neither omits nor duplicates it.
    - Success refetches active ticket and parent list; failure preserves failed message with safe category and identical retry.
    - Solve/reopen status is not optimistic; failure preserves UI and exposes retry.
    - SOLVED disables reply intent; reopen success sets a one-shot composer-focus signal.
    - Status bodies never contain comment and reply bodies never contain create-only fields.
  </behavior>
  <action>Add reply-specific retry payloads without reusing create retry data. Construct the outbound comment body from the newly sanitized editor HTML plus the probe-confirmed quoted-history wrapper produced by Plan 03-04, using the loaded existing multi-message thread context per D-03/THRD-02. Add exact-body tests proving existing context is appended as sanitized quote, hostile markup is removed, the new reply stays outside the quote, and retry reuses the identical already-built payload without duplicating the quote. Then background PATCH and server-truth refetch per D-10. Add `setSolved`, `reopen`, pending/error/retry state per D-13..D-17; use exact Plan 03-01 status values, keep thread open after solve, and consume focus as a one-shot flag. Preserve immutable array transitions and safe NetworkError/server categories. Treat automatic external reactivation as server truth observed on subsequent load, not a client-side synthetic flip.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/store/__tests__/active-conversation-store.test.ts &amp;&amp; CI=true npx tsc --noEmit</automated></verify>
  <done>All pending/sent/failed/retry/refetch and solve/reopen/focus/no-optimism cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API comments→MobX state | Remote authorship, visibility, timestamps and HTML are untrusted |
| UI intent→background mutation | Repeated/retried actions can duplicate messages or race navigation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-14 | Spoofing | message direction | mitigate | Derive from publicVisible and creator role authority; fixture matrix |
| T-03-15 | Tampering | retry payload | mitigate | Retain exact sanitized narrow payload per message; never rebuild from current UI |
| T-03-16 | Denial of Service | load races | mitigate | Monotonic generation guard and immutable state replacement |
| T-03-17 | Repudiation | lifecycle mutation | mitigate | Status-only request tests and explicit pending/error state |
| T-03-SC | Tampering | package installs | mitigate | No package install |
</threat_model>

<verification>Run the focused store suite and TypeScript.</verification>
<success_criteria>The store supplies every state needed for the real thread UI without unsafe HTML, stale loads, optimistic lifecycle lies, or conflated read state.</success_criteria>
<output>Create `.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-05-SUMMARY.md` when done.</output>
