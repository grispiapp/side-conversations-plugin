---
phase: 03-g-r-me-detay-ve-ya-am-d-ng-s
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/types/grispi.type.ts
  - src/grispi/client/tickets.ts
  - src/grispi/client/__tests__/tickets.test.ts
autonomous: true
requirements: [THRD-02, THRD-03]
must_haves:
  truths:
    - "The client sends a public reply with the exact live-proven PATCH body."
    - "Solve and reopen send status-only PATCH bodies with no comment."
  artifacts:
    - path: "src/types/grispi.type.ts"
      provides: "Exported thread types and narrow reply/status PATCH contracts"
    - path: "src/grispi/client/tickets.ts"
      provides: "Encoded authenticated patchTicket client"
      exports: ["Tickets"]
    - path: "src/grispi/client/__tests__/tickets.test.ts"
      provides: "Exact request and response contract regression tests"
  key_links:
    - from: "src/grispi/client/tickets.ts"
      to: "public/v1/tickets/{ticketKey}"
      via: "PATCH with encoded key and authenticated JSON body"
      pattern: "method.*PATCH"
---

<objective>
Define and test the narrow API contracts used by replies and lifecycle mutations.

Purpose: Make it structurally impossible to resend create-only fields or attach a comment to a status transition.
Output: Probe-backed types, client method, and transport tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-01-probe-findings.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-PATTERNS.md
@src/types/grispi.type.ts
@src/grispi/client/tickets.ts

<interfaces>
`Ticket.comments` contains body, publicVisible, createdAt, creator.role.authority, and channel. `Tickets.getTicket(key)` uses `public/v1/tickets/${encodeURIComponent(key)}` with auth headers and no-cache. `CreateTicketRequest` must remain separate from the new reply/status request union.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock reply and status-only PATCH types to probe evidence</name>
  <files>src/types/grispi.type.ts, src/grispi/client/__tests__/tickets.test.ts</files>
  <behavior>
    - Reply request permits only the probe-confirmed public comment shape.
    - Solve/reopen request permits only the probe-confirmed ts.status field body and cannot carry comment.
    - PATCH response typing matches the observed full, partial, or empty response.
  </behavior>
  <action>Export the existing Role/User/Comment shapes or exported thread-facing projections, then add distinct probe-backed reply and status request types plus their union. Keep `CreateTicketRequest` separate. Add failing transport/type-oriented tests for the exact reply, SOLVED, and reopen bodies and response shape before implementation. Reference D-14/D-15/D-17 in comments where the status-only invariant is enforced.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --runTestsByPath src/grispi/client/__tests__/tickets.test.ts</automated>
  </verify>
  <done>The compiler and tests reject create fields on PATCH and prove lifecycle requests omit comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the encoded authenticated PATCH client</name>
  <files>src/grispi/client/tickets.ts, src/grispi/client/__tests__/tickets.test.ts</files>
  <behavior>
    - Ticket keys containing reserved characters are URL-encoded.
    - PATCH uses auth headers, no-cache, JSON serialization, and the probe-confirmed response parser.
    - Reply and lifecycle bodies pass through byte-for-byte without merging create fields.
  </behavior>
  <action>Add `patchTicket(ticketKey, body)` beside `getTicket` using the established HttpHandler convention. Use the response type recorded in Plan 03-01, not RESEARCH.md's assumed Ticket return. Do not add separate “solve” request builders that can drift; callers construct the narrow typed union.</action>
  <verify>
    <automated>CI=true npm test -- --watchAll=false --runTestsByPath src/grispi/client/__tests__/tickets.test.ts &amp;&amp; CI=true npx tsc --noEmit</automated>
  </verify>
  <done>All exact-body, URL, auth, and response tests pass and TypeScript is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| store→Tickets client | Mutable UI intent becomes an external email/status mutation |
| API→typed response | Untrusted remote response enters application state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04 | Tampering | PatchTicketRequest | mitigate | Disjoint narrow union; exact-body tests; no CreateTicketRequest reuse |
| T-03-05 | Information Disclosure | client errors | mitigate | Preserve existing safe error handling; never surface raw response bodies |
| T-03-06 | Elevation of Privilege | ticket key path | mitigate | Encode key and retain authenticated client boundary |
| T-03-SC | Tampering | package installs | mitigate | No package install in this plan |
</threat_model>

<verification>
Run targeted client tests and `CI=true npx tsc --noEmit`.
</verification>

<success_criteria>
The only new mutation primitive is probe-backed, narrowly typed, authenticated, encoded, and regression-tested.
</success_criteria>

<output>Create `.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-02-SUMMARY.md` when done.</output>
