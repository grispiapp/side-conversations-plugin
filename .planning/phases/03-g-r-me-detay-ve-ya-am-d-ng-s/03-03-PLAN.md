---
phase: 03-g-r-me-detay-ve-ya-am-d-ng-s
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/last-seen-store.ts
  - src/lib/__tests__/last-seen-store.test.ts
  - src/store/side-conversations-store.ts
  - src/store/__tests__/side-conversations-store.test.ts
  - src/screens/components/conversation-row.tsx
autonomous: true
requirements: [THRD-04, THRD-03]
must_haves:
  truths:
    - "Viewing an external reply removes only the purple unseen accent; Yeni yanıt remains until the agent replies."
    - "Solved rows show only Çözüldü and no unseen/action-required signal."
  artifacts:
    - path: "src/lib/last-seen-store.ts"
      provides: "Non-throwing last-seen writer"
      exports: ["getLastSeenAt", "setLastSeenAt"]
    - path: "src/store/side-conversations-store.ts"
      provides: "Independent lifecycle, action badge, and unseen derivation"
    - path: "src/screens/components/conversation-row.tsx"
      provides: "Independent badge and purple accent rendering"
  key_links:
    - from: "src/store/side-conversations-store.ts"
      to: "src/lib/last-seen-store.ts"
      via: "last seen affects hasUnseen only"
      pattern: "hasUnseen"
---

<objective>
Separate local seen state from the server-derived lifecycle and reply responsibility signals.

Purpose: Fulfill THRD-04 without falsely claiming an external reply has been answered.
Output: Defensive storage write, corrected row view-model, UI mapping, and regression matrix.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-PATTERNS.md
@src/lib/last-seen-store.ts
@src/store/side-conversations-store.ts
@src/screens/components/conversation-row.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resilient per-agent last-seen writes</name>
  <files>src/lib/last-seen-store.ts, src/lib/__tests__/last-seen-store.test.ts</files>
  <behavior>
    - Writes `sc:lastSeenAt:&lt;ticketKey&gt;` with a finite timestamp and returns true.
    - Storage denial/quota exceptions return false and never block opening a thread.
    - Invalid keys/timestamps do not write corrupt state.
  </behavior>
  <action>Add `setLastSeenAt(ticketKey, timestamp): boolean` beside the defensive reader, using the same prefix and try/catch pattern. Per D-18, this is device/browser-local and failure is non-fatal; do not introduce server synchronization (deferred v2).</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/lib/__tests__/last-seen-store.test.ts</automated></verify>
  <done>Correct writes and all throwing/malformed cases are covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split lifecycle, action badge, and unseen emphasis</name>
  <files>src/store/side-conversations-store.ts, src/store/__tests__/side-conversations-store.test.ts, src/screens/components/conversation-row.tsx</files>
  <behavior>
    - Open + last public external => actionBadge yeni-yanit regardless of last-seen; hasUnseen depends only on timestamp.
    - Open + last public agent => actionBadge yanit-bekleniyor.
    - Solved => lifecycle solved, actionBadge null, hasUnseen false.
    - A later external reply returning server status to open restores both action and unseen signals.
  </behavior>
  <action>Replace `badge` as the conflated source with `lifecycle`, `actionBadge`, and `hasUnseen` per D-18/D-19/D-20. Preserve generation guards, hydration behavior, immutable row replacement, and sorting rules. Update ConversationRow copy to `Çözüldü` (never `Kapalı`), render purple rail only from `hasUnseen`, and render the action badge independently. Do not let localStorage choose between `Yeni yanıt` and `Yanıt bekleniyor`.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/store/__tests__/side-conversations-store.test.ts &amp;&amp; CI=true npx tsc --noEmit</automated></verify>
  <done>The full external/seen/agent/solved/reactivated matrix passes and the row uses distinct visual signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| localStorage→badge derivation | Mutable browser state is untrusted and can be absent/corrupt |
| API comments/status→row VM | Remote data determines action and lifecycle labels |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07 | Tampering | last-seen storage | mitigate | Validate finite values; storage influences accent only, never server action state |
| T-03-08 | Denial of Service | localStorage access | mitigate | Catch read/write failures and continue rendering |
| T-03-09 | Spoofing | author classification | mitigate | Reuse creator role authority and publicVisible rules; cover matrix in tests |
| T-03-SC | Tampering | package installs | mitigate | No package install in this plan |
</threat_model>

<verification>Run both targeted suites and TypeScript.</verification>
<success_criteria>Opening a thread can mark content seen locally without altering lifecycle or reply responsibility.</success_criteria>
<output>Create `.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-03-SUMMARY.md` when done.</output>
