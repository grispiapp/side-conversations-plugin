---
phase: 03-g-r-me-detay-ve-ya-am-d-ng-s
plan: 06
type: execute
wave: 4
depends_on: [03-05]
files_modified:
  - src/screens/conversations-list-screen.tsx
  - src/screens/components/conversation-row.tsx
  - src/screens/chat-screen.tsx
  - src/screens/__tests__/chat-screen.test.tsx
  - src/store/panel-navigation-store.ts
  - src/store/__tests__/panel-navigation-store.test.ts
autonomous: false
requirements: [THRD-01, THRD-02, THRD-03, THRD-04]
must_haves:
  truths:
    - "Selecting a row opens and loads its real chronological email thread."
    - "The fixed composer replies to the immutable recipient and draft-loss navigation is confirmed."
    - "The header menu solves/reopens with mandated copy, retry feedback, solved band, disabled composer, and reopen focus."
    - "At approximately 372px the full THRD-01..04 flow works against the live tenant."
  artifacts:
    - path: "src/screens/chat-screen.tsx"
      provides: "Integrated Phase 3 detail screen"
    - path: "src/screens/__tests__/chat-screen.test.tsx"
      provides: "UI interaction and state wiring coverage"
    - path: "src/screens/conversations-list-screen.tsx"
      provides: "Row-to-thread entry path"
  key_links:
    - from: "src/screens/conversations-list-screen.tsx"
      to: "activeConversation.load"
      via: "row selection before chat screen transition"
      pattern: "activeConversation\\.load"
    - from: "src/screens/chat-screen.tsx"
      to: "src/store/active-conversation-store.ts"
      via: "load/reply/retry/solve/reopen state and actions"
      pattern: "sendReply|setSolved|reopen"
---

<objective>
Wire the real thread state machine into the narrow-panel UI and verify the complete phase live.

Purpose: Deliver the user-visible external correspondence lifecycle promised by Phase 3.
Output: Reachable detail screen, interaction tests, full validation, and end-of-phase UAT.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-UI-SPEC.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-VALIDATION.md
@src/screens/conversations-list-screen.tsx
@src/screens/chat-screen.tsx
@src/store/panel-navigation-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make real threads reachable and protect draft navigation</name>
  <files>src/screens/conversations-list-screen.tsx, src/screens/components/conversation-row.tsx, src/store/panel-navigation-store.ts, src/store/__tests__/panel-navigation-store.test.ts</files>
  <behavior>
    - Clicking/keyboard-activating a row starts load with row key + current parent key and transitions to chat.
    - Empty reply draft returns to list immediately.
    - Non-empty draft opens Taslak kaybolacak; cancel stays; confirm clears and returns.
  </behavior>
  <action>Give ConversationRow accessible button semantics/callback and wire selection through a navigation method that invokes `activeConversation.load(row.key,parentKey)` and opens chat. Preserve the selected parent key so a host ticket change cannot silently rebind the thread. Add the D-12 draft-loss gate using the existing ConfirmDialog contract; do not reuse compose state or its parent-change copy accidentally.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/store/__tests__/panel-navigation-store.test.ts</automated></verify>
  <done>Mouse/keyboard selection reaches the correct ticket and all dirty-back branches pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate email thread, composer, solve and reopen UI</name>
  <files>src/screens/chat-screen.tsx, src/screens/__tests__/chat-screen.test.tsx</files>
  <behavior>
    - Loading/error/retry and chronological message states render; opening scrolls first unseen external or last message.
    - Fixed composer shows immutable recipient, submits Shift+Enter, retries failed blocks, and is disabled under Çözüldü.
    - Header ⋯ shows Çözüldü olarak işaretle or Tekrar aç; solve confirmation uses exact copy.
    - Failed status leaves screen unchanged with Turkish error/Tekrar dene; reopen focuses composer.
  </behavior>
  <action>Replace the Phase 2 bubble shell with ThreadMessage + RichTextComposer per D-01..D-11. Implement header menu and ConfirmDialog per D-13/D-14, solved band/disabled composer/focus per D-16, and non-optimistic retry UI per D-17. Keep the thread mounted after solve. Use refs/effects to consume the store scroll target exactly once. Apply UI-SPEC spacing, restrained purple accent, amber internal note, accessible labels and visible focus. No internal-note creation, attachments, tables, inline images, polling, or server-side read sync.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/screens/__tests__/chat-screen.test.tsx src/screens/components/__tests__/thread-components.test.tsx &amp;&amp; CI=true npx tsc --noEmit</automated></verify>
  <done>All integrated screen behaviors and locked copy/disabled/focus/error states pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run Phase 3 live UAT at narrow-panel width</name>
  <files>src/screens/chat-screen.tsx</files>
  <action>After all automated gates pass, pause for the live tenant verification; record any failed numbered item before allowing phase completion.</action>
  <what-built>Complete THRD-01..04 thread, reply, solve/reopen, and local seen flow.</what-built>
  <how-to-verify>First run `CI=true npm test -- --watchAll=false`, `CI=true npx tsc --noEmit`, and `npm run build`. Then at ~372px against the approved tenant: (1) open a thread with at least two unseen external replies; confirm it scrolls to the first unseen while localStorage advances to the latest relevant external timestamp, purple accent clears, and Yeni yanıt remains; (2) verify chronological own/external/internal blocks, first/successive identity, safe HTML and closed quote; (3) paste hostile/complex HTML and inspect rendered DOM plus PATCH body; (4) send via Shift+Enter and inspect PATCH plus delivered email: the new sanitized reply must precede a probe-confirmed quoted/collapsed copy of the existing multi-message thread context, with no context loss or unsafe markup; observe immediate clear/pending, refetched thread/list and Yanıt bekleniyor; (5) force a send failure and retry, confirming the identical body is used and quoted context is not duplicated; (6) solve with exact confirmation, verify no email/comment, same thread + Çözüldü band + disabled composer; (7) force lifecycle failure and verify no optimistic flip + retry; (8) reopen and verify focus; (9) receive an external reply after solve and verify automatic active + unseen/new-reply signals; (10) test dirty-back cancel/confirm and toolbar keyboard/focus behavior.</how-to-verify>
  <verify><automated>CI=true npm test -- --watchAll=false &amp;&amp; CI=true npx tsc --noEmit &amp;&amp; npm run build</automated></verify>
  <done>All automated gates and all ten live UAT items are approved.</done>
  <resume-signal>Type "approved" or describe any failed numbered item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| row selection→active ticket | A user action chooses which external thread mutations target |
| ChatScreen→store | UI keyboard/menu actions trigger email or status mutation |
| sanitized VM→HTML sink | Approved markup is rendered in the iframe |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-18 | Tampering | selected ticket/parent | mitigate | Pin keys at row selection; generation guard; no live-host rebind |
| T-03-19 | Spoofing | recipient summary | mitigate | Read-only server-derived recipient shown before every reply |
| T-03-20 | Repudiation | menu/keyboard mutation | mitigate | Explicit solve confirmation, pending/error UI, exact UAT evidence |
| T-03-21 | Elevation of Privilege | HTML sink | mitigate | Sink accepts only sanitizer output; hostile-DOM test and UAT inspection |
| T-03-SC | Tampering | package installs | mitigate | No package install in this plan |
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status
GOAL | — | Real message view, reply, solve/reopen, seen lifecycle | 03-01..03-06 | COVERED
REQ | THRD-01 | Chronological direction-separated messages | 03-04,03-05,03-06 | COVERED
REQ | THRD-02 | Reply creates email and thread message | 03-01,03-02,03-05,03-06 | COVERED
REQ | THRD-03 | Solve/reopen and external reactivation | 03-01,03-02,03-03,03-05,03-06 | COVERED
REQ | THRD-04 | Per-agent localStorage seen state | 03-03,03-05,03-06 | COVERED
RESEARCH | — | Probe-backed PATCH contract | 03-01,03-02 | COVERED
RESEARCH | — | All Open Questions resolved before dependent execution | 03-01 | COVERED
RESEARCH | — | Shared inbound/outbound sanitizer | 03-04,03-05 | COVERED
RESEARCH | — | Probe-backed outgoing quoted thread preservation | 03-01,03-04,03-05,03-06 | COVERED
RESEARCH | — | Generation guard + server-truth refetch | 03-05 | COVERED
RESEARCH | — | Badge/unseen split | 03-03 | COVERED
CONTEXT | D-01..D-06 | Email thread, identity, outgoing quote preservation, notes, safe HTML, distinct first-unseen scroll target | 03-01,03-04,03-05,03-06 | COVERED
CONTEXT | D-07..D-12 | Fixed rich composer, keyboard, recipient, retry, draft guard | 03-04,03-05,03-06 | COVERED
CONTEXT | D-13..D-17 | Solve/reopen status lifecycle | 03-01,03-02,03-05,03-06 | COVERED
CONTEXT | D-18..D-20 | Seen/action/lifecycle separation | 03-03,03-05,03-06 | COVERED
</source_coverage>

<verification>
Targeted suites after each task; full Jest, TypeScript, build, and ten-item live UAT at phase end.
</verification>

<success_criteria>
All THRD-01..04 criteria work end-to-end against live Grispi, all automated commands pass, and no deferred feature appears.
</success_criteria>

<output>Create `.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-06-SUMMARY.md` when done.</output>
