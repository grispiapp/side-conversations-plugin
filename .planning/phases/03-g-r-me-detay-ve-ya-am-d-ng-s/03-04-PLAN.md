---
phase: 03-g-r-me-detay-ve-ya-am-d-ng-s
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/html-sanitizer.ts
  - src/lib/__tests__/html-sanitizer.test.ts
  - src/screens/components/thread-message.tsx
  - src/screens/components/rich-text-composer.tsx
  - src/screens/components/__tests__/thread-components.test.tsx
autonomous: true
requirements: [THRD-01, THRD-02]
must_haves:
  truths:
    - "Incoming and outbound HTML preserves only approved lightweight formatting."
    - "Probe-confirmed quote markers can be split and rebuilt without losing existing thread context."
    - "Scripts, handlers, styles, unsafe links, images, and tables cannot reach rendered DOM or PATCH payloads."
    - "The composer produces HTML, keeps Enter as newline, and uses Shift+Enter to submit."
  artifacts:
    - path: "src/lib/html-sanitizer.ts"
      provides: "Shared inbound/outbound HTML allowlist and quote splitter"
    - path: "src/screens/components/thread-message.tsx"
      provides: "Safe chronological email block with collapsed quote/internal note states"
    - path: "src/screens/components/rich-text-composer.tsx"
      provides: "Accessible compact controlled contentEditable composer"
  key_links:
    - from: "src/screens/components/thread-message.tsx"
      to: "src/lib/html-sanitizer.ts"
      via: "sanitized HTML before dangerouslySetInnerHTML"
      pattern: "sanitize"
    - from: "src/screens/components/rich-text-composer.tsx"
      to: "src/lib/html-sanitizer.ts"
      via: "paste/input/output boundary"
      pattern: "sanitize"
---

<objective>
Build the secure HTML boundary and reusable email-thread/composer components.

Purpose: Preserve useful email formatting while preventing hostile API or pasted markup from executing or leaking data.
Output: Dependency-free allowlist sanitizer, email message block, rich composer, and hostile-input tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-CONTEXT.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-UI-SPEC.md
@.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-RESEARCH.md
@src/screens/components/message-bubble.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement one shared strict HTML policy</name>
  <files>src/lib/html-sanitizer.ts, src/lib/__tests__/html-sanitizer.test.ts</files>
  <behavior>
    - p/br/strong/b/em/i/ul/ol/li/a/blockquote survive.
    - Only href/target/rel survive on anchors; http/https/mailto are accepted and unsafe protocols are removed.
    - script/style/event attributes/img/table/iframe/svg and pasted inline styles are removed.
    - Links receive safe rel; unknown quote markup remains sanitized body rather than being silently deleted.
  </behavior>
  <action>Implement a deterministic DOMParser/tree-walk allowlist shared by inbound rendering and outbound composer/PATCH boundaries, following the Plan 03-01 RESOLVED compatibility constraints. Do not install a package: RESEARCH.md contains no Package Legitimacy Audit. Extract only probe-confirmed quote markers into `{bodyHtml, quotedHtml?}` and provide a deterministic helper that serializes sanitized prior thread context into the exact probe-confirmed outbound quote wrapper; otherwise preserve sanitized content in body. Ensure anchor normalization prevents javascript/data/vbscript and reverse-tabnabbing. Return safe plain text through React interpolation when markup parsing is unavailable.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/lib/__tests__/html-sanitizer.test.ts</automated></verify>
  <done>Allowlist, hostile corpus, quote split, and outbound quote reserialization tests pass without dropping existing context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build chronological email block and compact rich composer</name>
  <files>src/screens/components/thread-message.tsx, src/screens/components/rich-text-composer.tsx, src/screens/components/__tests__/thread-components.test.tsx</files>
  <behavior>
    - Message is full-width email flow, labels first external sender fully, subsequent identity minimally, own messages as Siz, and internal notes as amber İç not.
    - Quotes start closed behind Önceki e-postayı göster.
    - Toolbar exposes accessible bold/italic/link/list/emoji/quote controls; no table/image/attachment.
    - Enter creates newline; Shift+Enter submits non-empty sanitized HTML; solved/disabled state cannot submit.
  </behavior>
  <action>Create `ThreadMessage` replacing chat-bubble geometry per D-01..D-05, including pending/failed retry state from the existing MessageBubble contract. It may use `dangerouslySetInnerHTML` only with the shared sanitizer result. Create a controlled selection-preserving contentEditable wrapper per D-07..D-11 with always-visible one-line toolbar, 5–6-line max height/internal scroll, readonly recipient summary, sanitized paste/input, accessible names/focus, and no D-09/Faz-4 controls. Keep draft ownership outside the component so navigation can enforce D-12.</action>
  <verify><automated>CI=true npm test -- --watchAll=false --runTestsByPath src/screens/components/__tests__/thread-components.test.tsx src/lib/__tests__/html-sanitizer.test.ts &amp;&amp; CI=true npx tsc --noEmit</automated></verify>
  <done>Component tests prove rendering, disclosure, safety, keyboard, retry, disabled, and accessibility contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Grispi/paste→DOM | Attacker-controlled HTML enters the iframe |
| editor→PATCH | Locally produced markup becomes external email content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-10 | Elevation of Privilege | HTML renderer | mitigate | Strict element/attribute/protocol allowlist before any HTML sink |
| T-03-11 | Information Disclosure | anchor/img/style markup | mitigate | Remove images/styles and unsafe URLs; force noopener noreferrer |
| T-03-12 | Tampering | pasted content | mitigate | Sanitize on paste/input and again before submit |
| T-03-13 | Denial of Service | complex markup | mitigate | Rebuild only allowlisted shallow DOM; discard unsupported nodes/tags safely |
| T-03-SC | Tampering | npm installs | mitigate | No install; missing legitimacy audit blocks adding a sanitizer dependency |
</threat_model>

<verification>Run hostile HTML, component, and TypeScript checks.</verification>
<success_criteria>Only allowlisted HTML crosses either boundary and the locked email/composer interaction contract is reusable by ChatScreen.</success_criteria>
<output>Create `.planning/phases/03-g-r-me-detay-ve-ya-am-d-ng-s/03-04-SUMMARY.md` when done.</output>
