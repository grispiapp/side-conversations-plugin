---
phase: 01-temel-ve-salt-okunur-g-r-me-listesi
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/store/side-conversations-store.ts
  - src/store/__tests__/side-conversations-store.test.ts
  - src/screens/conversations-list-screen.tsx
  - src/screens/components/empty-state.tsx
  - src/screens/components/error-card.tsx
  - src/screens/components/list-footer.tsx
autonomous: false
requirements: [CORE-03, LIST-04, LIST-05, LIST-06]
must_haves:
  truths:
    - "Hiç yan görüşme yoksa gizlilik açıklaması ('Talep sahibi bu yazışmayı görmez') + tek CTA içeren boş durum ekranı görünür (LIST-04, D-13)"
    - "advanced-search düşerse tam alan hata kartı (Türkçe mesaj + 'Yeniden dene') gösterilir; ağ vs HTTP hatası ayrı mesaj alır (CORE-03, D-11)"
    - "Satır-detay isteklerinin bir kısmı düşse liste yine render edilir; etkilenen satırlar soluk/özetsiz gösterilir ve sessizce yeniden denenir (D-11)"
    - "10'dan fazla görüşmede 'Daha fazla yükle' sonraki sayfayı çeker ve listeye ekler; sonsuz kaydırma yok (LIST-06, D-12)"
    - "'Daha fazla yükle' yüklerken buton spinner'a döner ama erişilebilir adı (aria-label 'Daha fazla yükle') korunur (D-14, UI checker notu)"
    - "'+' butonu ve boş durum CTA'sı görünür ama devre dışı, 'Çok yakında' aria/tooltip ile — layout zıplaması olmadan Faz 2'de enable edilir (D-13)"
    - "Aktif talep değişince liste ANINDA boşalır + skeleton görünür, eski talebin verisi bir an bile görünmez; yeni talebin verisi dolar (LIST-05, D-15)"
  artifacts:
    - path: "src/screens/components/empty-state.tsx"
      provides: "EmptyState — gizlilik cümlesi + devre dışı '+' CTA (LIST-04, D-13)"
      contains: "Talep sahibi bu yazışmayı görmez"
    - path: "src/screens/components/error-card.tsx"
      provides: "ErrorCard — network/HTTP Türkçe varyant + Yeniden dene (CORE-03, D-11)"
      contains: "Yeniden dene"
    - path: "src/screens/components/list-footer.tsx"
      provides: "Daha fazla yükle butonu — spinner + korunan aria-label (LIST-06, D-12/D-14)"
      contains: "Daha fazla yükle"
    - path: "src/store/side-conversations-store.ts"
      provides: "loadMore() sayfalama + hasMore + partial-failure sessiz retry + ticket-switch reset"
      contains: "loadMore"
  key_links:
    - from: "src/screens/conversations-list-screen.tsx"
      to: "src/store/side-conversations-store.ts"
      via: "status='empty'→EmptyState, 'error'→ErrorCard(retry=store.load), footer→store.loadMore"
      pattern: "loadMore|store\\.load"
    - from: "src/screens/components/error-card.tsx"
      to: "src/store/side-conversations-store.ts"
      via: "Yeniden dene → store.load(activeKey) (NetworkError vs HttpError copy)"
      pattern: "Yeniden dene"
    - from: "src/store/side-conversations-store.ts"
      to: "src/grispi/client/tickets.ts"
      via: "loadMore → advancedSearch(page+1) append"
      pattern: "advancedSearch"
---

<objective>
Final Phase-1 slice: make the list robust and complete. Adds the empty state (with the feature-identity privacy sentence), the layered error experience with Turkish copy + retry, partial-hydration dimming with silent retry, "Daha fazla yükle" pagination, and rock-solid active-ticket-switch refresh with zero stale flash. After this plan the read-only list satisfies every Phase-1 success criterion.

Purpose: These are the states that make the difference between a demo and a shippable panel — an agent with zero side conversations sees the privacy promise + CTA; an offline agent sees a retry, not a blank screen; an agent with 11+ conversations can page through them; switching tickets never shows the wrong ticket's data.

Output: Store `loadMore`/`hasMore`/partial-retry/ticket-switch reset (tested), plus `EmptyState`, `ErrorCard`, and `ListFooter` components wired into the screen's state machine.
</objective>

## Phase Goal (MVP user story)

**As a** destek temsilcisi, **I want to** boş/hata/çok-sayfa durumlarında da anlamlı bir arayüz görüp talep değiştirdiğimde listenin anında doğru talebe geçmesini, **so that** panel her koşulda güvenilir ve anlaşılır kalsın.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-RESEARCH.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-UI-SPEC.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-01-SUMMARY.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-SUMMARY.md

<interfaces>
<!-- From Plans 01-02. -->
From src/store/side-conversations-store.ts:
```typescript
export type ConversationsListStatus = "loading" | "ready" | "empty" | "error";
export class SideConversationsStore {
  status: ConversationsListStatus; rows: ConversationRowVM[]; page: number;
  error: NetworkError | HttpError | null;
  load(parentKey: string): Promise<void>;   // resets + fetches page 0
  // Plan 03 ADDS: hasMore, loadingMore, loadMore(), and the retry entry point
}
```
From src/grispi/client/http-handler.ts: `NetworkError` (cause), `HttpError` (status, body).
From src/screens/conversations-list-screen.tsx: observer branching on store.status; uses useGrispi().ticket + useStore().sideConversations.
Existing `Button` (src/components/ui/button.tsx): variants outline/secondary, size sm/icon, `disabled`. Spinner icon: `@radix-ui/react-icons` (e.g. ReloadIcon/UpdateIcon with `animate-spin`).
</interfaces>
</context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan:
- Components: `EmptyState` (empty-state.tsx), `ErrorCard` (error-card.tsx), `ListFooter` (list-footer.tsx)
- Store additions: `hasMore`, `loadingMore` fields; `loadMore()` method; partial-hydration silent-retry path; ticket-switch reset already anchored to `load` generation guard
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Empty state + layered error card + partial-hydration retry</name>
  <files>src/screens/components/empty-state.tsx, src/screens/components/error-card.tsx, src/store/side-conversations-store.ts, src/store/__tests__/side-conversations-store.test.ts, src/screens/conversations-list-screen.tsx</files>
  <read_first>
    - src/screens/conversations-list-screen.tsx (status branch skeleton from Plan 01/02)
    - src/store/side-conversations-store.ts (load, error field, toRow hydrationFailed path)
    - src/grispi/client/http-handler.ts (NetworkError/HttpError to switch copy on)
    - src/components/ui/button.tsx (Button variants for CTA + Yeniden dene)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-UI-SPEC.md (Copywriting Contract exact strings; EmptyState/ErrorCard layout; Accent color for disabled CTA)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md (D-11 layered error; D-13 disabled CTA; specifics: exact empty-state copy)
  </read_first>
  <action>
    Create `src/screens/components/empty-state.tsx` exporting `EmptyState`. Centered in ScreenContent (lg padding). Per UI-SPEC Copywriting Contract, EXACT Turkish copy: Display heading "Henüz yan görüşme yok"; body "Tedarikçi ya da başka bir ekiple, talep sahibinin görmediği ayrı bir e-posta akışı başlatın."; then, as its own visually distinct line, the privacy sentence "Talep sahibi bu yazışmayı görmez." (this sentence is the feature's identity — MUST be present, CONTEXT specifics + LIST-04). Below, a SINGLE CTA (LIST-04): a `Button` showing a "+" / "Yeni görüşme başlat" that is `disabled` with `aria-label="Çok yakında"` and a matching `title` (D-13 — visible+disabled now, enabled in Phase 2 with zero layout shift; use the primary/accent styling per UI-SPEC).

    Create `src/screens/components/error-card.tsx` exporting `ErrorCard` (props: `error: NetworkError | HttpError`, `onRetry: () => void`). Full-width card, destructive-tinted icon/accent. Copy switches on error type (CORE-03/D-11): `NetworkError` → "Bağlantı sorunu nedeniyle yan görüşmeler yüklenemedi."; `HttpError` (any non-2xx) → "Yan görüşmeler şu anda yüklenemiyor. Lütfen daha sonra tekrar deneyin." Include a "Yeniden dene" `Button` calling `onRetry`. The card MUST NOT render `error.body`, status codes, stack traces, or any header (V7 Info Disclosure — fixed generic strings only).

    In `side-conversations-store.ts`: add the partial-hydration SILENT retry (D-11): after the initial `Promise.allSettled`, for rows where the getTicket outcome rejected (`hydrationFailed:true`), schedule ONE background re-fetch of just those keys (no backoff, ≤10 rows) guarded by the current generation; on success, replace those rows in place (recompute badge/summary) and re-sort; on repeat failure leave them dimmed. This retry is silent — it never flips `status` to "error" (only the primary advanced-search failure does that). Add a store test asserting: a rejected hydration yields a dimmed row on first settle, and a subsequent successful background retry upgrades that row (mock getTicket to reject once then resolve).

    In `conversations-list-screen.tsx`: replace the Plan-01 minimal empty/error placeholders — `status==="empty"` → `<EmptyState />`; `status==="error"` → `<ErrorCard error={store.error!} onRetry={() => ticket?.key && store.load(ticket.key)} />`.
  </action>
  <verify>
    <automated>CI=true npx craco test --testPathPattern="side-conversations-store" --watchAll=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <human-check>Force an empty ticket → confirm privacy sentence + disabled "+" CTA (tooltip "Çok yakında"). Kill network → confirm the network Türkçe message + "Yeniden dene" restores the list on reconnect.</human-check>
  </verify>
  <acceptance_criteria>
    - empty-state.tsx contains the exact string "Talep sahibi bu yazışmayı görmez" and a `disabled` Button with `aria-label="Çok yakında"`.
    - error-card.tsx renders distinct Turkish copy for NetworkError vs HttpError, a "Yeniden dene" Button, and contains no reference to `error.body`/status/headers.
    - side-conversations-store.ts contains the silent partial-hydration retry gated by the generation counter; store test exits 0 asserting dim→upgrade on retry.
    - conversations-list-screen.tsx renders EmptyState on "empty" and ErrorCard(onRetry=store.load) on "error".
    - `npx tsc --noEmit` exits 0; full suite green.
  </acceptance_criteria>
  <done>Empty state shows the privacy promise + single disabled CTA, the error card gives type-appropriate Turkish copy + working retry, and partially-failed rows self-heal silently without breaking the list.</done>
</task>

<task type="auto">
  <name>Task 2: Pagination "Daha fazla yükle" + ticket-switch reset hardening</name>
  <files>src/store/side-conversations-store.ts, src/store/__tests__/side-conversations-store.test.ts, src/screens/components/list-footer.tsx, src/screens/conversations-list-screen.tsx</files>
  <read_first>
    - src/store/side-conversations-store.ts (load/generation guard/page from Plan 01, silent-retry from Task 1)
    - src/grispi/client/tickets.ts (advancedSearch {size,page})
    - src/components/ui/button.tsx (Button; spinner via @radix-ui/react-icons + animate-spin)
    - src/screens/conversations-list-screen.tsx (ready branch mapping rows)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md (D-12 pagination; D-15 ticket switch)
    - Planning context: UI checker note — pagination button MUST preserve accessible name during loading (aria-label "Daha fazla yükle" stays while spinner replaces visible text).
  </read_first>
  <action>
    In `side-conversations-store.ts`: add observable `hasMore: boolean` and `loadingMore: boolean`. Compute `hasMore` after each fetch from the advanced-search envelope (e.g. `pageNumber + 1 < totalPages`, using the Task-1-confirmed field names and page indexing — if page is 1-indexed adjust accordingly; fall back to `content.length === size`). Add `async loadMore()`: no-op if `loadingMore || !hasMore || status !== "ready"`; set `loadingMore=true`, capture the current generation, call `advancedSearch(sameConditions, { size: 10, page: this.page + 1 })` + `Promise.allSettled(getTicket)` hydrate, and on the SAME generation APPEND the new rows to `this.rows`, increment `this.page`, recompute `hasMore`, re-run `sortConversations` across the full list (D-08 keeps groups correct across pages), then `loadingMore=false`. Discard if a newer generation started (ticket switched mid-page — Pitfall #6). Confirm `load(parentKey)` still resets `page=0`, `rows=[]`, `hasMore=false`, `loadingMore=false`, and bumps generation SYNCHRONOUSLY at entry so a ticket switch clears instantly with zero stale flash (D-15/LIST-05). Add store tests: (a) loadMore appends page-1 rows and toggles hasMore off when the last page is reached; (b) load() called mid-loadMore discards the stale page append (generation guard); (c) after load(), rows/page/hasMore are reset synchronously before the await resolves.

    Create `src/screens/components/list-footer.tsx` exporting `ListFooter` (props: `hasMore`, `loading`, `onLoadMore`). Render nothing when `!hasMore`. Otherwise a `Button` (outline/secondary, sm) with `aria-label="Daha fazla yükle"` ALWAYS set; when `loading`, replace the visible "Daha fazla yükle" text with a spinning icon (`animate-spin`) but KEEP the aria-label so the accessible name never disappears (D-14 + UI checker note) and the button width does not jump; disable the button while `loading`.

    In `conversations-list-screen.tsx`: in the `ready` branch, after the rows list render `<ListFooter hasMore={store.hasMore} loading={store.loadingMore} onLoadMore={() => store.loadMore()} />`.
  </action>
  <verify>
    <automated>CI=true npx craco test --testPathPattern="side-conversations-store" --watchAll=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <human-check>With 11+ test side tickets on the parent (D-17): confirm only 10 load initially, "Daha fazla yükle" fetches the rest and appends in correct group order, the button shows a spinner (not a text swap) while loading, and switching the active ticket instantly clears to skeleton with no flash of the old ticket's rows.</human-check>
  </verify>
  <acceptance_criteria>
    - side-conversations-store.ts exports/holds `hasMore` + `loadingMore` and a `loadMore()` that appends page+1 and re-sorts; load() resets page/rows/hasMore synchronously and bumps the generation.
    - store test exits 0 covering: append + hasMore-off at last page, stale loadMore discard, synchronous reset on load().
    - list-footer.tsx renders a Button with a constant `aria-label="Daha fazla yükle"` and swaps only the visible content to a spinner while loading.
    - conversations-list-screen.tsx renders ListFooter in the ready branch wired to store.loadMore.
    - `npx tsc --noEmit` exits 0; full suite green.
  </acceptance_criteria>
  <done>Pagination fetches and appends subsequent pages in correct order with an accessible, non-jumping loading button, and switching tickets resets the list instantly with no stale data — completing LIST-05 and LIST-06.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-1 UAT in the real Grispi panel</name>
  <files>(none — end-of-phase verification checkpoint; failures route to /gsd-plan-phase --gaps)</files>
  <action>PAUSE for human verification. After Tasks 1-2 are automated and green, run the 7-step real-panel UAT below against Davut's tenant (D-16/D-17/D-18). No code is written in this task; it gates phase completion.</action>
  <what-built>The full read-only side-conversation list: real advanced-search fetch, hydrated rows, badges + grouping/sort + unread rail, empty state with privacy copy + disabled CTA, layered error card + retry, pagination, and instant ticket-switch refresh.</what-built>
  <how-to-verify>
    Preconditions (D-16/D-17): field `tu.side_conversation_parent` exists in the tenant; ≥11 test side tickets on one parent with badge variety (awaiting / new-reply / closed). Per D-18, do the final check in the REAL Grispi panel (not only localhost mock).
    1. Open a parent ticket with side conversations → list shows skeleton×3 then real rows (alıcı · göreli zaman · konu · özet). (LIST-01)
    2. Verify each row's badge is correct and rows are grouped Yeni yanıt → Yanıt bekleniyor → Kapalı, descending by last activity within each group; new-reply rows show the purple left rail and sit on top; closed stay listed. (LIST-02/03)
    3. Open a parent ticket with NO side conversations → empty state shows "Henüz yan görüşme yok", the body copy, the privacy sentence "Talep sahibi bu yazışmayı görmez.", and the single disabled "+" CTA with "Çok yakında" tooltip. (LIST-04/D-13)
    4. Simulate an API failure (e.g. offline) → full-width Türkçe error card + "Yeniden dene"; reconnect + retry restores the list. Confirm partially-failed rows appear dimmed then self-heal. (CORE-03/D-11)
    5. With 11+ conversations → only 10 load first; "Daha fazla yükle" fetches the rest; while loading the button spins without a width jump and keeps its accessible name. (LIST-06/D-12/D-14)
    6. Switch the active ticket in Grispi → list clears to skeleton instantly with no flash of the previous ticket's rows, then loads the new ticket's conversations. (LIST-05/D-15)
    7. Confirm no external comment text renders as HTML (paste an HTML-ish body into a test comment; it appears as literal text). (Security T-01)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 pass, or list the specific step(s) that failed for gap closure.</resume-signal>
  <verify>
    <human-check>All 7 UAT steps pass in the real Grispi panel with ≥11 real test side tickets; failures recorded.</human-check>
  </verify>
  <done>All five ROADMAP Phase-1 success criteria verified live in the real Grispi panel with real test data; any failures are recorded for `/gsd-plan-phase --gaps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grispi API error → displayed error UI | Error responses (bodies, statuses, headers) must not leak into user-facing/logged strings |
| Active-ticket switch event → store | A late response from a previous ticket must never render for the current ticket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ErrorCard copy / logs | high | mitigate | Fixed generic Turkish strings per error class; never render error.body/status/headers (Task 1) |
| T-03-02 | Information Disclosure | Cross-ticket data flash on switch | medium | mitigate | Synchronous reset + generation guard in load(); loadMore discards stale-generation appends (Task 2, D-15) |
| T-03-03 | Denial of Service (self) | Rapid repeated loadMore / retry clicks | low | mitigate | loadMore no-ops while loadingMore/!hasMore; retry re-enters load() which bumps generation (Task 1/2) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase. RESEARCH Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false` green (all store pagination/retry/reset tests).
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- Manual UAT: the 7-step real-panel checklist (Task 3) all pass.
</verification>

<success_criteria>
- Empty/error/pagination/ticket-switch states all behave per D-11/D-12/D-13/D-15 with exact Turkish copy.
- CORE-03 (Türkçe error + retry), LIST-04, LIST-05, LIST-06 satisfied and verified live.
- All five ROADMAP Phase-1 success criteria pass in the real Grispi panel.
</success_criteria>

<output>
Create `.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-03-SUMMARY.md` when done.
</output>
