---
phase: 01-temel-ve-salt-okunur-g-r-me-listesi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grispi/client/http-handler.ts
  - src/grispi/client/__tests__/http-handler.test.ts
  - src/grispi/client/tickets.ts
  - src/types/grispi.type.ts
  - src/contexts/grispi-context.tsx
  - src/lib/side-conversation.ts
  - src/lib/__tests__/side-conversation.test.ts
  - src/lib/relative-time.ts
  - src/lib/__tests__/relative-time.test.ts
  - src/store/side-conversations-store.ts
  - src/store/__tests__/side-conversations-store.test.ts
  - src/store/root-store.ts
  - src/components/ui/skeleton.tsx
  - src/screens/conversations-list-screen.tsx
  - src/screens/components/conversation-row.tsx
  - src/app.tsx
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, LIST-01]
must_haves:
  truths:
    - "Panel dev'de açıldığında aktif talebin yan görüşmeleri gerçek POST /public/v1/tickets/advanced-search çağrısıyla çekilir ve listelenir (Walking Skeleton, LIST-01)"
    - "Her satır alıcı e-postası (mono), göreli zaman, konu ve son mesaj özetini gösterir (LIST-01, D-09)"
    - "İlk yüklemede 3 skeleton satır kartı görünür; veri gelince gerçek satırlarla değişir (D-14)"
    - "HttpHandler ağ hatasını NetworkError, HTTP olmayan-2xx yanıtı HttpError olarak fırlatır; hiçbir hatayı null'a yutmaz (D-11 temeli, CORE-03)"
    - "Göreli zaman ve satır sıralaması kaynağı SON PUBLIC YORUMUN zamanıdır, ticket.updatedAt değil (D-10)"
    - "Yan görüşme sorgusu YALNIZCA tek allConditions koşuluyla çalışır (fieldKey sabiti + EQUAL + aktif talep key'i); ek koşul yok (D-03)"
    - "Talep key'i istek URL'sine encodeURIComponent ile enjekte edilir (V5, güvenlik)"
  artifacts:
    - path: "src/grispi/client/http-handler.ts"
      provides: "NetworkError + HttpError sınıfları; send<T> throws yerine null döner (CORE-03/D-11)"
      contains: "class NetworkError"
    - path: "src/grispi/client/tickets.ts"
      provides: "advancedSearch(body, {size,page}) metodu; getTicket encodeURIComponent'li"
      contains: "advancedSearch"
    - path: "src/lib/side-conversation.ts"
      provides: "SIDE_CONVERSATION_PARENT_FIELD_KEY sabiti (CORE-01, D-01/D-02)"
      contains: "tu.side_conversation_parent"
    - path: "src/lib/relative-time.ts"
      provides: "formatRelativeTime(ms, now) — Türkçe kompakt format (D-10)"
      exports: ["formatRelativeTime"]
    - path: "src/store/side-conversations-store.ts"
      provides: "SideConversationsStore: iki-katmanlı fetch + generation guard + status durum makinesi"
      contains: "class SideConversationsStore"
    - path: "src/screens/conversations-list-screen.tsx"
      provides: "Liste ekranı — skeleton/ready dalları, WelcomeScreen yerine geçer"
      contains: "ConversationsListScreen"
    - path: "src/app.tsx"
      provides: "ConversationsListScreen mount (WelcomeScreen kaldırıldı)"
      contains: "ConversationsListScreen"
  key_links:
    - from: "src/store/side-conversations-store.ts"
      to: "src/grispi/client/tickets.ts"
      via: "grispiAPI.tickets.advancedSearch(allConditions fieldKey=SIDE_CONVERSATION_PARENT_FIELD_KEY) + Promise.allSettled(getTicket)"
      pattern: "advancedSearch|allSettled"
    - from: "src/screens/conversations-list-screen.tsx"
      to: "src/store/side-conversations-store.ts"
      via: "observer + useStore().sideConversations, effect ticket?.key değişince store.load"
      pattern: "useStore\\(\\)\\.sideConversations"
    - from: "src/app.tsx"
      to: "src/screens/conversations-list-screen.tsx"
      via: "JSX mount"
      pattern: "ConversationsListScreen"
---

<objective>
Walking Skeleton for Phase 1: prove the full stack end-to-end through the existing Grispi starter — panel boots, real bundle/token is applied, one real `advanced-search` read fetches the active ticket's side conversations, and real rows render in the ~372px panel (recipient · relative time · subject · summary), replacing the starter `WelcomeScreen`.

This is the thinnest real vertical slice. Badges, grouping/sort, empty/error UI, and pagination are deliberately OUT of this plan (Plans 02–03) — this plan's job is a live, rendered list.

Purpose: De-risk the whole phase by exercising the real fetch→hydrate→render pipeline against Davut's live tenant before layering derivation logic on top. Also hardens the shared HTTP layer (typed errors) that CORE-03's error UI (Plan 03) depends on.

Output: Hardened `HttpHandler`, `Tickets.advancedSearch`, the field-key constant, `formatRelativeTime`, `SideConversationsStore` (happy path), `Skeleton` primitive, `ConversationRow`, `ConversationsListScreen`, and the `app.tsx` swap — with automated unit coverage for the pure/logic layers.

**Scope note (CORE-02 narrowing):** Per RESEARCH Open Question #6 / Pitfall 7, CORE-02's text spans `createTicket`/`patchTicket`/`searchCustomers`/`getDigest` too — those are Phase 2–4 features and are intentionally NOT built here. This plan satisfies the Phase-1 portion of CORE-02: `advancedSearch` + a hardened `getTicket` only.

**API-shape assumption:** The exact live shape of `advanced-search` (envelope fields, page indexing, `ts.status` path, requester location) is UNVERIFIED (RESEARCH Open Questions #1–3). This plan builds against RESEARCH's documented assumed shape, isolating ALL assumptions in `grispi.type.ts` + a single `toRow` mapper so correction is cheap. Plan 02's first task is a human checkpoint that confirms/corrects these against real JSON before badge derivation is built.
</objective>

## Phase Goal (MVP user story)

**As a** destek temsilcisi, **I want to** aktif talebe bağlı yan görüşmeleri panelde gerçek verilerle (alıcı, konu, özet, göreli zaman) tek listede görebilmek, **so that** harici yazışmaların durumunu talepten hiç ayrılmadan takip edebileyim.

> Derived faithfully from the ROADMAP Phase 1 goal + PROJECT core value. The ROADMAP `**Goal:**` line is descriptive prose, not strict "As a / I want to / so that" form — see planning return note.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-RESEARCH.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-PATTERNS.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the current repo this session. -->

Current src/grispi/client/http-handler.ts (to be REPLACED — currently swallows to null):
```typescript
async send<T>(url: string, options: RequestInit): Promise<T | null> {
  const response = await fetch(`${this.baseUrl}/${url}`, { ...options, headers: {...} });
  if (response.ok) return await response.json();
  return null;   // <-- must become typed throws
}
```

Current src/grispi/client/tickets.ts getTicket (analog to copy for advancedSearch; note UNENCODED key):
```typescript
async getTicket(ticketKey: string) {
  return this.http.send<Ticket>(`public/v1/tickets/${ticketKey}`, {
    method: "GET", cache: "no-cache", headers: this.auth.headers,
  });
}
```

Current src/types/grispi.type.ts (Ticket has fieldMap, comments[], NO top-level status/requester):
```typescript
interface Comment { id: number; body: string; publicVisible: boolean; createdAt: number; creator: User; ... }
interface FieldMap { id: any; value: any; serializedValue: string|null; userFriendlyValue: string; type: string; key: string; }
export interface Ticket { key: string; channel: string; createdAt: number; updatedAt: number;
  comments: Comment[]; fieldMap: { [key: string]: FieldMap }; ... }
interface User { role: { authority: string; impliedAuthorities: string[]; teamUser: boolean }; email: string; fullName: string; ... }
```

MobX store analog — src/store/current-user-store.ts + root-store.ts:
```typescript
export class CurrentUserStore { rootStore: RootStore; constructor(rootStore){ makeAutoObservable(this); this.rootStore = rootStore; } }
export class RootStore { currentUser: CurrentUserStore; constructor(){ this.currentUser = new CurrentUserStore(this); } }
```

Store access + API singleton — src/contexts/store-context.tsx exports useStore(); src/grispi/client/api.ts exports `grispiAPI`.
Screen analogs — src/screens/welcome-screen.tsx (observer + Screen/ScreenHeader/ScreenContent), loading-screen.tsx (rocket). Screen primitive at src/components/ui/screen.tsx.
grispi-context.tsx exposes useGrispi() → { ticket, settings, loading }; already refetches on plugin.currentTicketUpdated.
</interfaces>
</context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan (exclude from drift/source-grounding checks — they are new):
- Classes: `NetworkError`, `HttpError` (src/grispi/client/http-handler.ts); `SideConversationsStore` (src/store/side-conversations-store.ts)
- Methods: `Tickets.advancedSearch` (src/grispi/client/tickets.ts)
- Functions: `formatRelativeTime` (src/lib/relative-time.ts); `toRow` (internal mapper, side-conversations-store.ts)
- Constants: `SIDE_CONVERSATION_PARENT_FIELD_KEY` (src/lib/side-conversation.ts)
- Types: `AdvancedSearchRequest`, `AdvancedSearchResponse`, `SideTicketSummary`, extended `Ticket` (optional `status`/`requester` helpers) (src/types/grispi.type.ts); `ConversationRowVM`, `ConversationsListStatus` (side-conversations-store.ts)
- Components: `Skeleton` (src/components/ui/skeleton.tsx); `ConversationRow`, `SkeletonRow` (src/screens/components/conversation-row.tsx + local); `ConversationsListScreen` (src/screens/conversations-list-screen.tsx)
- New files: side-conversation.ts, relative-time.ts, side-conversations-store.ts, and their __tests__ siblings
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden HTTP layer + add advancedSearch + lock assumed API types</name>
  <files>src/grispi/client/http-handler.ts, src/grispi/client/__tests__/http-handler.test.ts, src/grispi/client/tickets.ts, src/types/grispi.type.ts, src/contexts/grispi-context.tsx, src/lib/side-conversation.ts, src/lib/__tests__/side-conversation.test.ts</files>
  <read_first>
    - src/grispi/client/http-handler.ts (current swallow-to-null impl — replacing in place)
    - src/grispi/client/tickets.ts (getTicket analog + unencoded key to fix)
    - src/grispi/client/authentication.ts (this.auth.headers shape)
    - src/types/grispi.type.ts (Ticket/Comment/User/FieldMap — extend, do not rewrite)
    - src/contexts/grispi-context.tsx (two getTicket call sites: initial _init + currentTicketUpdated catch)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-RESEARCH.md (Code Examples #1; Pitfalls #1-3; Security Domain V5)
  </read_first>
  <behavior>
    - http-handler test (mock global.fetch via jest.spyOn(global,"fetch")): response.ok=true resolves parsed JSON body; response.ok=false throws HttpError with .status set to the response status; fetch() rejecting (network TypeError) throws NetworkError with .cause set.
    - side-conversation test: SIDE_CONVERSATION_PARENT_FIELD_KEY strictly equals the string tu.side_conversation_parent (CORE-01 / D-01 / D-02 guard).
  </behavior>
  <action>
    Create `src/lib/side-conversation.ts` exporting `export const SIDE_CONVERSATION_PARENT_FIELD_KEY = "tu.side_conversation_parent";` (the ONE hard-coded field key per D-01/D-02 — never read from settings). Add a guard test asserting exact equality (CORE-01).

    Replace `HttpHandler` per RESEARCH Code Examples #1: export `NetworkError` (carries `cause: unknown`, super("network")) and `HttpError` (carries `status: number`, `body: unknown`, super(`http_${status}`)). `send<T>` signature becomes `Promise<T>` (no `| null`): wrap `fetch` in try/catch → throw `NetworkError(cause)` on rejection; after fetch, if `!response.ok` read body via `response.json().catch(() => null)` and throw `HttpError(response.status, body)`; otherwise return `response.json()`. Do NOT retain the null branch.

    In `tickets.ts`: (a) wrap the getTicket path segment with `encodeURIComponent(ticketKey)` (V5 fix, Security Domain); (b) add `async advancedSearch(body: AdvancedSearchRequest, params: { size: number; page: number })` that POSTs to `public/v1/tickets/advanced-search?size=${params.size}&page=${params.page}` with `method:"POST", cache:"no-cache", headers: this.auth.headers, body: JSON.stringify(body)`, returning `this.http.send<AdvancedSearchResponse>(...)`. Do not add any other CRUD methods (CORE-02 narrowing — createTicket/patchTicket/searchCustomers/getDigest belong to Phases 2-4).

    In `grispi.type.ts`, EXTEND (append, do not rewrite existing interfaces): `AdvancedSearchRequest` = `{ allConditions: Array<{ fieldKey: string; operator: string; value: string }>; anyConditions: Array<{ fieldKey: string; operator: string; value: string }> }`; `SideTicketSummary` = `{ key: string; [extra: string]: unknown }` (lean summary, exact fields UNVERIFIED — Open Q#1); `AdvancedSearchResponse` = `{ content: SideTicketSummary[]; totalPages: number; totalSize: number; pageNumber: number; numberOfElements: number }` (envelope modeled on the public spec's PagedCommentDigest, ASSUMED per Pitfall #1). Add a JSDoc note above these three types marking them ASSUMED / confirm-via-live-probe. Do NOT alter the existing `Ticket` fields; the status/requester access path stays in the store's mapper (Task 2).

    In `grispi-context.tsx`, update BOTH getTicket call sites for the new throwing contract: the initial `_init().then` block and the `currentTicketUpdated` handler. Extend the existing try/catch around `currentTicketUpdated`'s getTicket to branch `if (err instanceof NetworkError) ... else if (err instanceof HttpError) ...` and keep current behavior (console.error with the ticket key, leave `ticket` state unchanged); wrap the initial `_init` getTicket in an equivalent try/catch so a boot-time failure logs and leaves `ticket` null instead of an unhandled rejection. Never surface `error.body` or the Authorization header into any thrown/logged UI string (V7 — Info Disclosure).
  </action>
  <verify>
    <automated>CI=true npx craco test --testPathPattern="http-handler|side-conversation" --watchAll=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/grispi/client/http-handler.ts contains `class NetworkError` and `class HttpError`; `send` return type is `Promise<T>` and the file contains no `return null` branch.
    - http-handler.test.ts exits 0 and covers: ok→json, non-ok→HttpError(status), fetch-throw→NetworkError.
    - tickets.ts contains `advancedSearch` and `public/v1/tickets/advanced-search?size=`; getTicket path uses `encodeURIComponent(`.
    - src/lib/side-conversation.ts exports SIDE_CONVERSATION_PARENT_FIELD_KEY equal to tu.side_conversation_parent; its test exits 0.
    - grispi.type.ts exports AdvancedSearchRequest, AdvancedSearchResponse, SideTicketSummary.
    - grispi-context.tsx references NetworkError/HttpError in a try/catch around getTicket; `npx tsc --noEmit` exits 0 (no callers still treat getTicket as returning null).
  </acceptance_criteria>
  <done>HTTP layer throws typed errors, advancedSearch exists with encoded/POST semantics, assumed API types are declared and isolated, field-key constant is guarded by a test, and both getTicket call sites compile against the throwing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SideConversationsStore (two-tier fetch happy path) + relative-time</name>
  <files>src/store/side-conversations-store.ts, src/store/__tests__/side-conversations-store.test.ts, src/store/root-store.ts, src/lib/relative-time.ts, src/lib/__tests__/relative-time.test.ts</files>
  <read_first>
    - src/store/current-user-store.ts (MobX makeAutoObservable class shape)
    - src/store/root-store.ts (registration pattern)
    - src/grispi/client/api.ts (grispiAPI singleton — import directly, no DI)
    - src/grispi/client/tickets.ts (advancedSearch + getTicket signatures from Task 1)
    - src/types/grispi.type.ts (Ticket.fieldMap, Comment.publicVisible/createdAt/creator, AdvancedSearchResponse)
    - src/lib/side-conversation.ts (field key constant)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-RESEARCH.md (Code Examples #2 + #4; Pitfall #6 generation guard; D-09/D-10)
  </read_first>
  <behavior>
    - relative-time test: <60s → "az önce"; 12*60s → "12 dk"; 3*3600s → "3 sa"; yesterday's timestamp → "Dün"; a date >2 days ago → Intl short form like "12 Tem" (assert via a fixed `now` argument, not real Date.now()).
    - store test (spy on grispiAPI.tickets.advancedSearch + getTicket): after load(parentKey) resolves with 2 summaries + 2 fulfilled getTicket hydrations, status becomes "ready" and rows.length === 2 with recipient/subject/summary/lastPublicCommentAt populated; when advancedSearch resolves with empty content, status becomes "empty"; a stale generation (second load starts before first resolves) does NOT overwrite the newer result.
  </behavior>
  <action>
    Create `src/lib/relative-time.ts` per RESEARCH Code Examples #4: `formatRelativeTime(timestampMs: number, now: number = Date.now()): string` with MINUTE/HOUR/DAY thresholds → "az önce" / "N dk" / "N sa" / "Dün" (yesterday window) / `Intl.DateTimeFormat("tr-TR", { day:"numeric", month:"short" })` fallback. Keep `now` injectable for deterministic tests.

    Create `src/store/side-conversations-store.ts` exporting `class SideConversationsStore` following the `current-user-store.ts` shape (`makeAutoObservable(this)`, holds `rootStore`). State fields: `status: ConversationsListStatus` (`"loading" | "ready" | "empty" | "error"`), `rows: ConversationRowVM[]`, `page: number`, `error: NetworkError | HttpError | null`, plus a private generation counter. Export `ConversationRowVM` = `{ key: string; recipientEmail: string; subject: string; summary: string; lastPublicCommentAt: number | null; hydrationFailed: boolean }`.

    Implement `async load(parentKey: string)`: synchronously set `status="loading"`, `rows=[]`, `page=0`, and capture `const gen = ++this.generation` (D-15 instant-clear + Pitfall #6 guard). Call `grispiAPI.tickets.advancedSearch({ allConditions: [{ fieldKey: SIDE_CONVERSATION_PARENT_FIELD_KEY, operator: "EQUAL", value: parentKey }], anyConditions: [] }, { size: 10, page: 0 })` — the ONLY condition (D-03). Then `Promise.allSettled(response.content.map(row => grispiAPI.tickets.getTicket(row.key)))`. After awaiting, if `gen !== this.generation` return without committing (discard stale). Map each settled outcome via a `toRow` helper: fulfilled → build `ConversationRowVM` from the hydrated `Ticket` (`hydrationFailed:false`); rejected → build a partial row from the summary (`hydrationFailed:true`, `summary:""`). `toRow` derives: `recipientEmail` from the ticket's requester (access path is UNVERIFIED — read from `ticket.fieldMap["ts.requester"]?.userFriendlyValue` OR a top-level requester if present, falling back to `row.key`; centralize this ONE lookup here and comment it as confirm-via-Plan-02-probe); `subject` from the last publicVisible comment's context / ticket form or `row.key` fallback; `summary` from the last `publicVisible` comment `body` (plain string, truncated to a sensible length — NEVER rendered as HTML); `lastPublicCommentAt` = max `createdAt` among `publicVisible` comments (D-10 — comment time, not updatedAt). Set `status = rows.length ? "ready" : "empty"`. Wrap the advancedSearch call in try/catch and on NetworkError/HttpError set `status="error"`, store `error` (error UI arrives in Plan 03; here just reach the state). Do NOT compute badges or sort yet (Plan 02). Use MobX `runInAction` where committing after `await` if the project's strict mode requires it.

    Register in `root-store.ts`: add `sideConversations: SideConversationsStore` and construct it in the constructor exactly as `currentUser` is.
  </action>
  <verify>
    <automated>CI=true npx craco test --testPathPattern="side-conversations-store|relative-time" --watchAll=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - relative-time.ts exports formatRelativeTime; its test exits 0 for all five buckets using an injected `now`.
    - side-conversations-store.ts contains `class SideConversationsStore`, references SIDE_CONVERSATION_PARENT_FIELD_KEY, uses `Promise.allSettled`, and has a generation guard (a stale-load test asserts no overwrite).
    - store test exits 0 covering ready(2 rows), empty, and stale-generation discard.
    - root-store.ts constructs `this.sideConversations = new SideConversationsStore(this)`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The store performs a real two-tier fetch (advancedSearch → allSettled hydrate), maps to row view models sourcing time from the last public comment, guards against stale ticket-switch races, and reaches loading/ready/empty/error states; relative time formats correctly. All logic is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: List screen + row + skeleton primitive, swap into app</name>
  <files>src/components/ui/skeleton.tsx, src/screens/components/conversation-row.tsx, src/screens/conversations-list-screen.tsx, src/app.tsx</files>
  <read_first>
    - src/screens/welcome-screen.tsx (observer + Screen/ScreenHeader/ScreenContent shape to mirror)
    - src/screens/loading-screen.tsx (rocket LoadingScreen — reused for bundle-init only, D-14)
    - src/components/ui/screen.tsx (Screen/ScreenHeader/ScreenTitle/ScreenContent props)
    - src/components/ui/button.tsx (cva/cn convention — Skeleton mirrors the cn styling only)
    - src/contexts/grispi-context.tsx (useGrispi → { ticket, loading })
    - src/contexts/store-context.tsx (useStore)
    - src/store/side-conversations-store.ts (status + ConversationRowVM from Task 2)
    - src/lib/relative-time.ts (formatRelativeTime)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-UI-SPEC.md (Row anatomy; Typography; Color; panel-width truncation)
  </read_first>
  <action>
    Create `src/components/ui/skeleton.tsx` per RESEARCH Code Examples #6: a plain `FC<HTMLAttributes<HTMLDivElement>>` rendering `<div className={cn("animate-pulse rounded-md bg-slate-200", className)} {...props} />`. No cva, no forwardRef.

    Create `src/screens/components/conversation-row.tsx` exporting `ConversationRow` (props: a `ConversationRowVM`) and `SkeletonRow`. Per UI-SPEC "Row anatomy": a white (`bg-card`) `rounded-md` card, `px-4 py-3` padding, stacked in an 8px-gap list. Line 1 (meta): recipient email in `font-mono text-xs` + `·` + relative time (`text-xs text-muted-foreground`), left-aligned. Line 2: subject `text-sm` weight-400, `truncate`. Line 3: summary `text-sm text-muted-foreground truncate` — render summary via React `{row.summary}` text interpolation ONLY (NEVER dangerouslySetInnerHTML — external untrusted email content, Security T-01); when `row.hydrationFailed` is true, dim the row (`opacity-60`) and omit the summary line (no error text on the row, D-11). Rows are NON-interactive this phase (no onClick/hover affordance, Open Q#4). `SkeletonRow` mimics this 3-line structure using `<Skeleton>` blocks. Leave a clearly-marked slot/placeholder comment where Plan 02 will add the Badge + unread rail — do not add badge markup now.

    Create `src/screens/conversations-list-screen.tsx` exporting `ConversationsListScreen = observer(() => { ... })`. Read `const { ticket, loading } = useGrispi()` and `const store = useStore().sideConversations`. While `loading` (bundle init) return the existing rocket `<LoadingScreen />` (D-14). Add `useEffect(() => { if (ticket?.key) store.load(ticket.key); }, [ticket?.key])` to fetch on mount and whenever the active ticket key changes (LIST-05 baseline; hardened in Plan 03). Render `<Screen><ScreenHeader><ScreenTitle>Yan Görüşmeler</ScreenTitle></ScreenHeader><ScreenContent>...</ScreenContent></Screen>`. Inside ScreenContent branch on `store.status`: `"loading"` → three `<SkeletonRow />` (D-14); `"ready"` → map `store.rows` to `<ConversationRow key={row.key} row={row} />`; `"empty"` → a minimal single-line placeholder "Henüz yan görüşme yok" (full empty-state with privacy copy + disabled CTA is Plan 03); `"error"` → a minimal single-line "Yan görüşmeler yüklenemedi" (full layered error card is Plan 03). Keep all copy Turkish. All text truncates (`truncate`/`line-clamp-1`) to fit the 372px panel.

    In `src/app.tsx`: replace the `WelcomeScreen` import and its JSX usage with `ConversationsListScreen`. No other structural change (StoreProvider + GrispiProvider wrappers stay; no router).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>CI=true npx craco test --watchAll=false</automated>
    <human-check>Run `npm start`, open the panel (localhost mock bundle per README, or real Grispi) — confirm skeleton×3 on load then rows render, and app no longer shows the starter "Welcome to starter" screen.</human-check>
  </verify>
  <acceptance_criteria>
    - src/components/ui/skeleton.tsx exports a Skeleton component using `animate-pulse` and `cn(`.
    - src/screens/components/conversation-row.tsx exports ConversationRow and SkeletonRow; ConversationRow renders summary via `{` interpolation and contains NO `dangerouslySetInnerHTML`.
    - conversations-list-screen.tsx exports ConversationsListScreen, calls `store.load` inside a useEffect keyed on `ticket?.key`, and branches on `store.status` for loading/ready/empty/error.
    - app.tsx imports and renders ConversationsListScreen and no longer imports WelcomeScreen.
    - `npx tsc --noEmit` exits 0 and the full test suite is green.
  </acceptance_criteria>
  <done>The panel boots, loads the active ticket's side conversations via the real store pipeline, shows 3 skeletons then real rows (recipient · time · subject · summary), and the starter welcome screen is gone — a rendered Walking Skeleton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grispi parent frame → our iframe | Bundle (token, tenantId, ticketKey) crosses via postMessage; token is untrusted-to-store (in-memory only) |
| Grispi Public API → client | advanced-search / getTicket responses (including externally-authored email comment bodies) are untrusted input |
| External third party → comment body | The "son mesaj özeti" is authored by an external emailer — fully untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering/EoP | ConversationRow summary render | high | mitigate | Render external comment body via React `{}` text interpolation only; no dangerouslySetInnerHTML; plain-text truncate (Task 3) |
| T-01-02 | Tampering | Ticket key in request URL (getTicket) | medium | mitigate | `encodeURIComponent(ticketKey)` before URL interpolation (Task 1) |
| T-01-03 | Information Disclosure | Error logging in grispi-context / store error state | high | mitigate | User/log strings never include `error.body` or Authorization header; NetworkError/HttpError carry status/cause for dev console only (Task 1/2) |
| T-01-04 | DoS (self) / Info Disclosure | Overlapping fetch cycles on rapid ticket switch | medium | mitigate | Generation counter captured per `load`; discard stale results before commit (Task 2) |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false` — full suite green (http-handler, side-conversation, relative-time, side-conversations-store).
- `npx tsc --noEmit -p tsconfig.json` — exits 0.
- Manual (dev): panel boots → skeleton×3 → real rows; WelcomeScreen removed.
</verification>

<success_criteria>
- Real advanced-search read renders real side-conversation rows in the panel (Walking Skeleton proven).
- Typed HTTP errors replace null-swallowing; getTicket key encoded.
- Pure logic (relative-time, field-key constant, store happy path) covered by green automated tests.
- All API-shape assumptions isolated in grispi.type.ts + the store's `toRow` mapper for cheap Plan-02 correction.
</success_criteria>

<output>
Create `.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-01-SUMMARY.md` when done.
</output>
