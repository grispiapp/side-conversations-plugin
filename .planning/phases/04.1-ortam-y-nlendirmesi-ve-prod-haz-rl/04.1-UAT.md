---
status: partial
phase: 04.1-ortam-y-nlendirmesi-ve-prod-haz-rl
source: [04.1-VERIFICATION.md]
started: 2026-08-10T12:00:00Z
updated: 2026-08-10T12:35:00Z
---

## Current Test

[oturum duraklatıldı — Test 2 deploy/build'e bloke]

## Tests

### 1. M2a — standalone-mode varsayılan preprod yönlendirmesi
expected: Yalnızca `REACT_APP_DEV_TOKEN` set edilmiş hâlde (`REACT_APP_DEV_GRISPI_ENV` YOK) istekler `api.grispi.net`'e gider ve `gsocial-test` tenant listesi eskisi gibi yüklenir.
result: [passed]
evidence: |
  2026-08-10, temiz derlenmiş bundle üzerinde doğrulandı (stale bundle tuzağından
  kaçınmak için :3000'deki eski dev server durdurulup yeniden başlatıldı —
  `Compiled successfully! / No issues found.`).

  Konsol: `grispi-context STANDALONE DEV MODE — Grispi SDK bridge bypassed.
  tenant=gsocial-test ticket=TICKET-563` → standalone dal aktif.

  `performance.getEntriesByType('resource')` host dağılımı:
    - `https://api.grispi.net`  → 10 istek
    - `https://grispi.app`      → 1 (SDK script yüklemesi, API host değil)
    - `http://localhost:3000`   → 1 (bundle)
    - `api.grispi.com` / `api.grispi.com.tr` → **0 istek**

  İlk XHR: `https://api.grispi.net/public/v1/tickets/TICKET-563` (preprod).
  Sonraki istekler de preprod: `/public/v1/tickets/advanced-search?size=5&page=0`,
  `/public/v1/tickets/TICKET-593`, `/public/v1/tickets/TICKET-592`.

  Ekranda `gsocial-test` görüşme listesi eskisi gibi yüklendi (5 görüşme +
  "Daha fazla yükle") → SC3'ün "mevcut akış bozulmaz" şartı da karşılandı.

  Not: standalone modda konsolda `Uncaught Error: E1 Origin is empty!` görünüyor —
  bu, iframe dışında yüklenen Grispi plugin SDK script'inden geliyor. Standalone
  modda SDK köprüsü zaten bypass edildiği için beklenen ve zararsız; uygulama
  sorunsuz çalışıyor.

### 2. M2b — plugin-mode canlı preprod ağ kontrolü (gerçek Grispi iframe)
expected: Gerçek Grispi preprod tenant iframe'inde ilk XHR host'u `api.grispi.net`, sıfır `.com` isteği.
result: blocked
blocked_by: release-build
reason: "buna deploy/build sonrası bakacağım"
why_human: |
  Gerçek bundle + gerçek token, Grispi panel iframe'i içinde çalışmalı. Bu yarı
  jsdom'da veya localhost standalone oturumunda üretilemez — bundle'ı yalnızca
  Grispi host sayfası sağlıyor ve `_grispi_env` / JWT `dev` claim çözümlemesi
  ancak orada gerçek veriyle çalışır.
resume: |
  Deploy/build sonrası plugin'i gerçek bir preprod tenant panelinde aç,
  DevTools → Network'te ilk XHR'ın host'una bak, sonra `/gsd-verify-work 04.1`
  ile bu oturumu kaldığı yerden sürdür.

## Summary

total: 2
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

Otomatik doğrulama tarafında boşluk yok — 11/11 must-have doğrulandı
(`04.1-VERIFICATION.md`, re-verification: önceki durum `gaps_found` 9/11).
WR-01 ve WR-02 kapatıldı.

Geriye yalnızca M2b (plugin-mode canlı iframe kontrolü) kaldı — kod boşluğu değil,
önkoşul kapısı (`blocked_by: release-build`). Bu yüzden `04.1-VERIFICATION.md`
bilerek `human_needed` olarak bırakıldı; M2b fiilen yapılmadan `passed`'e
çevrilmesi, fazın çekirdek iddiasını (üretimde doğru host) kanıtlanmamış hâlde
doğrulanmış göstermek olurdu.
