---
status: testing
phase: 04.1-ortam-y-nlendirmesi-ve-prod-haz-rl
source: [04.1-VERIFICATION.md]
started: 2026-08-10T12:00:00Z
updated: 2026-08-10T12:00:00Z
---

## Current Test

number: 1
name: M2 — canlı preprod ağ kontrolü (plugin-mode + standalone-mode)
expected: |
  (i) Plugin-mode: gerçek bir Grispi preprod tenant iframe'inde plugin açıldığında
  DevTools → Network'te ilk XHR'ın host'u `api.grispi.net` (preprod) olmalı; hiçbir
  `.com` isteği görülmemeli.
  (ii) Standalone-mode: yalnızca `REACT_APP_DEV_TOKEN` set edilmiş hâlde (
  `REACT_APP_DEV_GRISPI_ENV` YOK) `npm start` çalıştırıldığında istekler yine
  `api.grispi.net`'e gitmeli ve `gsocial-test` tenant listesi eskisi gibi yüklenmeli.
awaiting: user response

## Tests

### 1. M2 — canlı preprod ağ kontrolü (plugin-mode + standalone-mode)
expected: Plugin-mode ilk XHR host'u `api.grispi.net`, sıfır `.com` isteği. Standalone-mode varsayılan olarak preprod'a gider, `gsocial-test` yüklenir.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

Yok — 11/11 must-have otomatik olarak doğrulandı (`04.1-VERIFICATION.md`,
re-verification: önceki durum `gaps_found` 9/11). WR-01 ve WR-02 kapatıldı;
geriye yalnızca jsdom'da üretilemeyen bu canlı ağ kontrolü kaldı.

**Neden insan gerekiyor:** Gerçek bundle + gerçek token, Grispi panel iframe'i
içinde çalışmalı ve DevTools ile ağ trafiği incelenmeli. Statik analiz veya
jsdom ile üretilemez. `04.1-02-SUMMARY.md` bu maddeyi hâlâ
"AWAITING USER CONFIRMATION" olarak kaydediyor.
