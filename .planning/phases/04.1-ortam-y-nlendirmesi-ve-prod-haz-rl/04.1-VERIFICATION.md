---
phase: 04.1-ortam-y-nlendirmesi-ve-prod-haz-rl
verified: 2026-08-10T12:00:00Z
status: human_needed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/11
  gaps_closed:
    - "WR-01 — plugin modunda hiçbir istek yanlış host'a gitmez (truth #4b): closed by quick task 260810-j2w (onEnvironmentReady dep + environmentReadyRef gate on currentTicketUpdated). Independently re-verified: gate present, flips only on the success path immediately after setEnvironment, never in the catch block; reject path confirmed to leave the gate closed (Test D); new grispi-context.test.tsx renders the REAL GrispiProvider (first such test in the repo) and passes; independently reproduced by disabling the gate (if (false && ...)) and confirming Test A/D go red, then restoring the tree."
    - "WR-02 — terminal link (setEnvironment mutation -> actual fetch() host) was behavior_unverified: closed by quick task 260810-jiw (9 new tests in http-handler.test.ts, 2 new tests in api.test.ts, all hardcoded host literals, never GRISPI_BASE_URLS[env] lookups). Independently re-verified by re-running the emptied-HttpHandler.setEnvironment mutation probe: 6 of the 21 tests in the two files went red with exactly the expected failure signature, then the source file was restored via git checkout and the suite re-confirmed green."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "M2 (Plan 04.1-02, still deferred) — open the plugin inside a real Grispi preprod tenant iframe and inspect DevTools -> Network for the first XHR's host; separately run `npm start` in standalone mode with only REACT_APP_DEV_TOKEN set (no REACT_APP_DEV_GRISPI_ENV) and confirm requests still go to preprod and the gsocial-test tenant loads."
    expected: "(i) Plugin-mode: first XHR host is api.grispi.net (preprod), zero .com requests. (ii) Standalone mode: requests go to api.grispi.net by default, gsocial-test tenant list loads as before."
    why_human: "Requires a real bundle + real token inside the Grispi panel iframe (or a live npm start session) and DevTools network inspection — not producible in jsdom or by static analysis. 04.1-02-SUMMARY.md still records this as 'AWAITING USER CONFIRMATION' as of this re-verification; nothing in the two closure quick-tasks touched it."
---

# Phase 04.1: Ortam Yönlendirmesi ve Prod Hazırlık Verification Report

**Phase Goal:** Plugin, yüklendiği Grispi ortamının doğru API'siyle konuşur — preprod (`.net`), prod (`.com`) ve TR prod (`.com.tr`) — ve varsayılanı yanlış ortama düşmeyecek şekilde güvenli tarafta durur.

**Verified:** 2026-08-10
**Status:** human_needed
**Re-verification:** Yes — after gap closure (previous status `gaps_found`, score 9/11; see `re_verification` frontmatter above)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — `resolveGrispiEnvironment` resolves `_grispi_env` (primary) → JWT `dev` claim (secondary) → `prod` (safe-side default) | ✓ VERIFIED | `src/lib/grispi-environment.ts:38-72`; unchanged since prior verification; `grispi-environment.test.ts` still green as part of the 399-test full-suite run |
| 2 | SC1 — `prod-tr` (hyphenated) is strictly rejected, warns once via `console.warn`, falls through the chain — never silently normalized to `prod_tr` | ✓ VERIFIED | `isGrispiEnvironment` own-property check unchanged; no `replace(/-/g` normalization in `environment.ts`/`grispi-environment.ts` |
| 3 | SC1 — malformed/non-JWT token does not crash `parseJwt`; returns `null`, falls to `prod` | ✓ VERIFIED | `parseJwt` fully wrapped in try/catch (`grispi-environment.ts:21-34`), unchanged |
| 4a | SC2 — within `bootstrapPluginInit`, `deps.setEnvironment(...)` is called before `deps.switchTicket(...)` | ✓ VERIFIED | `plugin-bootstrap.ts:78-89` — `setEnvironment` (78) precedes `switchTicket` (89) in one uninterrupted synchronous block, no `await` between; `plugin-bootstrap.test.ts` asserts `setEnvironment` < `onEnvironmentReady` < `switchTicket` via `invocationCallOrder` and passes |
| 4b | SC2 — plugin modunda **hiçbir istek** yanlış host'a gitmez (the broader claim, previously FAILED as WR-01) | ✓ VERIFIED | Independently re-verified, not taken on trust. `plugin-bootstrap.ts` gained a REQUIRED `onEnvironmentReady(): void` dep, invoked once, immediately after `setEnvironment`, in the success path only — never in the `catch` block (confirmed by reading the source: lines 78-89 vs. 90-94). `grispi-context.tsx` gained `environmentReadyRef`, gating the body (not the registration — DD-4) of `plugin.currentTicketUpdated`. Verified by (a) reading `src/contexts/__tests__/grispi-context.test.tsx` — the first test in the repo to render the REAL `GrispiProvider` (no `jest.mock("@/contexts/grispi-context")`), with Test A (pre-init event → zero `getTicket` calls), Test B (post-init event → reaches `getTicket`), Test C (bundle's own ticket routed through, ordered after `setEnvironment`), Test D (reject path → gate stays closed) all passing; (b) an independent mutation probe — patched the gate condition to `if (false && !environmentReadyRef.current)` (disabling the gate) and re-ran the test file: Test A and Test D both failed with the exact race signature (`getTicket` called once when zero expected), then `git checkout --` restored the file and the full suite (32/399) was re-confirmed green. Also independently checked the "only pre-bootstrap fetch trigger" claim: `AppContent`/`ConversationsListScreen`/`RecipientField` all gate their queries on `tenantId`/`ticket?.key`, both of which stay `null` until `bootstrapPluginInit`'s synchronous success block runs `setTenantId`/`switchTicket` — no other React Query `enabled` flag or effect issues a request independent of that state, so the SDK event handler was confirmed to be the only reachable pre-bootstrap fetch trigger. |
| 5 | SC1 (regression) — `HttpHandler`'s class-field default `baseUrl` is now the prod URL, not preprod | ✓ VERIFIED | `http-handler.ts:33` — `baseUrl: string = GRISPI_BASE_URLS[DEFAULT_ENVIRONMENT]`, unchanged; behaviorally proven by both the pre-existing `sendMultipart` default-host assertion and the new default-host control test in `http-handler.test.ts` |
| 6 | SC3 — Standalone dev mode has its own environment override; `StandaloneDevConfig.environment`, default `preprod` (`DEFAULT_DEV_ENVIRONMENT`), overridable via `REACT_APP_DEV_GRISPI_ENV` | ✓ VERIFIED | `standalone-dev.ts:18-48,79-82`, unchanged; tests cover unset→preprod, `"prod"`, `"prod_tr"`, whitespace-trim |
| 7 | SC3 — unrecognized `REACT_APP_DEV_GRISPI_ENV` value hits the allowlist and falls back to `preprod`; never leaks into `StandaloneDevConfig` | ✓ VERIFIED | `isGrispiEnvironment` gate at `standalone-dev.ts:79-82`, unchanged; tests cover `"prod-tr"`, `"staging"`, `""`, prototype-chain keys |
| 8 | SC3 — existing `gsocial-test` (preprod) standalone flow is unbroken with no env var set | ✓ VERIFIED | Test: "defaults environment to DEFAULT_DEV_ENVIRONMENT (preprod) when REACT_APP_DEV_GRISPI_ENV is unset", passes |
| 9 | SC2/SC3 — standalone branch of `grispi-context.tsx` calls `grispiAPI.setEnvironment(...)` before `switchTicket(...)`, and is genuinely unaffected by the WR-01 fix | ✓ VERIFIED | `grispi-context.tsx:163` (`setEnvironment`) precedes line 172 (`switchTicket`); the WR-01 fix only ADDS one line to this branch (`environmentReadyRef.current = true;` at 167, explicitly documented as invariant-parity-only — nothing reads the ref in this branch since there is no SDK event source in standalone mode); confirmed by source read and by the fact that `standalone-dev.test.ts` still passes unmodified as part of the 399-test suite |
| 10 | SC5 — README documents `_grispi_env`, its three values, unset-fallback behavior, TR-mandatory rule with rationale, and the hyphen trap | ✓ VERIFIED | `README.md` — `### Required setting: _grispi_env` (line 56), value table (lines 64-66), unset-fallback paragraph (line 68), `> **Important**` TR-mandatory block (line 70), hyphen-trap warning (line 72); unchanged since prior verification, independently re-confirmed by grep |
| 11 | SC4 — no environment-dependent host literal remains anywhere under `src/` outside `environment.ts` and test files | ✓ VERIFIED | `grep -rn "grispi\.\(net\|com\)" src --include="*.ts" --include="*.tsx" \| grep -v __tests__ \| grep -v environment` → empty output (re-run independently). The new WR-02 tests deliberately hardcode `api.grispi.net`/`.com`/`.com.tr` literals in `http-handler.test.ts` and `api.test.ts`, but both files live under `__tests__/` paths, so the existing `grep -v __tests__` exclusion still filters them out — confirmed by re-running the raw grep without the `__tests__` exclusion and checking that every hit is inside a `__tests__` directory (10 files, all under `__tests__/`) |
| — | Terminal link: `GrispiEnvironment` key → `HttpHandler.baseUrl` mutation → actual `fetch()` host (previously behavior_unverified as WR-02) | ✓ VERIFIED | Independently re-verified, not taken on trust. `http-handler.test.ts` gained a `describe("HttpHandler.setEnvironment -> fetch host (CORE-04 terminal link, WR-02)")` block (9 tests: 3 `send()` rows, 3 `sendMultipart()` rows, 1 default-host control, 2 switch-back/last-write-wins tests) and a new `api.test.ts` (2 tests: `GrispiAPI.setEnvironment` delegation via a real `tickets.getTicket` call on `preprod`, plus a control). All expected hosts are hardcoded literals, never `GRISPI_BASE_URLS[env]` lookups — confirmed by reading both files. Independently reproduced the claimed mutation-probe result: emptied `HttpHandler.setEnvironment`'s body, re-ran `http-handler.test.ts` + `api.test.ts`, observed 6 of 21 tests fail with the exact expected/received host mismatch signature (preprod/prod_tr rows in both `send`/`sendMultipart`, the prod→preprod switch-back, and `api.test.ts`'s treatment test), then restored via `git checkout --` and re-confirmed 21/21 green. |

**Score:** 11/11 must-haves verified (0 present-but-behavior-unverified, 0 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/grispi/client/environment.ts` | `GRISPI_BASE_URLS`, `GrispiEnvironment`, `DEFAULT_ENVIRONMENT`, `isGrispiEnvironment` | ✓ VERIFIED | Unchanged since prior verification |
| `src/lib/grispi-environment.ts` | `parseJwt`, `resolveGrispiEnvironment` | ✓ VERIFIED | Unchanged |
| `src/lib/__tests__/grispi-environment.test.ts` | SC1 behavior coverage | ✓ VERIFIED | Unchanged, still green |
| `src/lib/standalone-dev.ts` | `StandaloneDevConfig.environment`, `DEFAULT_DEV_ENVIRONMENT` | ✓ VERIFIED | Unchanged |
| `README.md` | `_grispi_env` documentation section | ✓ VERIFIED | Unchanged |
| `src/contexts/__tests__/grispi-context.test.tsx` (new, WR-01 closure) | Renders the real `GrispiProvider`, proves the WR-01 race is closed | ✓ VERIFIED | 281 lines, 4 tests (A/B/C/D), no `jest.mock("@/contexts/grispi-context")`; independently confirmed to fail (Test A/D) when the gate is disabled |
| `src/grispi/client/__tests__/api.test.ts` (new, WR-02 closure) | `GrispiAPI.setEnvironment` delegation, end-to-end through a real sub-service call | ✓ VERIFIED | 2 tests, no `GRISPI_BASE_URLS` import (non-circularity), independently confirmed via mutation probe |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `plugin-bootstrap.ts` | `resolveGrispiEnvironment` → `deps.setEnvironment` | called before `deps.switchTicket` inside `bootstrapPluginInit` | ✓ WIRED | Verified by code + passing `invocationCallOrder` test |
| `grispi-context.tsx` plugin branch | `bootstrapPluginInit({..., setEnvironment: grispiAPI.setEnvironment.bind(grispiAPI), onEnvironmentReady, switchTicket})` | dependency injection | ✓ WIRED | `grispi-context.tsx:199-211` |
| `grispi-context.tsx` standalone branch | `grispiAPI.setEnvironment(standaloneConfig.environment)` before `switchTicket`, plus `environmentReadyRef.current = true` for invariant parity | direct call, same sync block | ✓ WIRED | Confirmed by source-order read: lines 163 → 167 → 172 |
| `GrispiAPI.setEnvironment` | `HttpHandler.setEnvironment` → `HttpHandler.setBaseUrl` | delegate call chain, single shared `httpHandler` instance | ✓ WIRED (behaviorally proven) | Previously code-inspection-only (WR-02); now exercised end-to-end by `http-handler.test.ts` + `api.test.ts`, and independently mutation-probed by this re-verification |
| `plugin-bootstrap.ts`'s `onEnvironmentReady` dep | `grispi-context.tsx`'s `environmentReadyRef` | arrow closure passed as dep, flips the ref | ✓ WIRED | `grispi-context.tsx:207-209`; success-path-only, confirmed absent from the `catch` block (`plugin-bootstrap.ts:90-94`) |
| `grispi-context.tsx` plugin branch | `bootstrapPluginInit(...)` (fire-and-forget) → `plugin.currentTicketUpdated` registration | same `useEffect` body, now gated | ✓ CLOSED (previously ✗ RACE, WR-01) | `void bootstrapPluginInit(...)` is still fire-and-forget and the handler is still registered synchronously (DD-4), but the handler body now no-ops until `environmentReadyRef.current` is true — independently reproduced as load-bearing via mutation probe |

### Data-Flow Trace (Level 4)

Not applicable in the Level-4 UI-data sense — this phase's artifacts are pure resolvers and an HTTP client, not components rendering fetched data. The equivalent trace (environment key → `baseUrl` mutation → actual `fetch()` URL) is covered by the Terminal Link truth and Key Link rows above, verified behaviorally.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite green | `CI=true npm test -- --watchAll=false` | 32 suites, 399 tests, all passed (re-run independently, matches expected baseline) | ✓ PASS |
| Type check clean | `npx tsc --noEmit` | exit 0 (re-run independently) | ✓ PASS |
| SC4 static host-literal gate | `grep -rn "grispi\.\(net\|com\)" src --include="*.ts" --include="*.tsx" \| grep -v __tests__ \| grep -v environment` | empty output (re-run independently) | ✓ PASS |
| WR-01 mutation probe (this re-verification, not from SUMMARY) | Disabled the `environmentReadyRef` gate condition in `grispi-context.tsx`, ran `grispi-context.test.tsx` | Test A and Test D failed with the exact race signature (`getTicket` called once when zero expected); file restored via `git checkout --`, full suite re-confirmed 32/399 green | ✓ PASS (confirms the test is load-bearing) |
| WR-02 mutation probe (this re-verification, not from SUMMARY) | Emptied `HttpHandler.setEnvironment`'s body, ran `http-handler.test.ts` + `api.test.ts` | 6 of 21 tests failed with the exact expected/received host mismatch; file restored via `git checkout --`, both files re-confirmed 21/21 green | ✓ PASS (confirms the tests are load-bearing, not vacuous) |
| WR-03/04/05, IN-02/03 still open (regression check — must NOT have been silently closed) | Source read of `http-handler.ts` (public `setBaseUrl`), `plugin-bootstrap.ts` (`switchTicket` typed `void`, actually async, called without `await`), `plugin-bootstrap.test.ts` (`invocationCallOrder` assertions, no "no-await-between" proof), `standalone-dev.ts` (no `console.warn` on unrecognized override), `grispi-environment.ts` (`console.warn` fires on empty string too) | All five confirmed still present/unmodified, exactly as the prior report and the two quick-task plans stated they would remain | ✓ PASS (confirms deferred scope was respected) |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this project and neither plan nor either quick-task plan declares one. Step 7c: SKIPPED (no conventional or declared shell probes; this phase's "probes" are the Jest mutation probes covered under Behavioral Spot-Checks above, which this re-verification reproduced independently rather than trusting the SUMMARYs' tables).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| CORE-04 | 04.1-01, 04.1-02, quick-260810-j2w, quick-260810-jiw | Plugin talks to the correct API host per environment, safe-side default, no request ever reaches the wrong host | ✓ SATISFIED | All 11 must-haves now verified, including the two that previously blocked/qualified this (WR-01 race, WR-02 unproven terminal link). `.planning/REQUIREMENTS.md` line 83 still reads "Not started" for CORE-04 despite the checkbox at line 15 being `[x]` — re-checked independently, still stale (info-level, not a code gap; unchanged from prior verification, and untouched by both quick tasks per their explicit out-of-scope lists). |

No orphaned requirements — CORE-04 is the only requirement ID declared across the two phase plans and the two quick-task plans, and it is the only one REQUIREMENTS.md maps to Phase 04.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/grispi/client/http-handler.ts` | 33, 38-40 | `baseUrl` and `setBaseUrl` are public, unrestricted arbitrary-host setters (WR-03) | ⚠️ Warning | Undercuts the "environment.ts is the only legitimate host source" invariant for future code; confirmed still open, deliberately deferred (not in scope of either closure quick-task) |
| `src/contexts/plugin-bootstrap.ts` | 65, 89 | `switchTicket` dep typed `void` but is actually `async`; floating promise (WR-04) | ⚠️ Warning | A future sync throw in `switchTicket` would land in the outer `catch` and wipe an already-applied tenant id; confirmed still open |
| `src/contexts/__tests__/plugin-bootstrap.test.ts` | 93-117 | `invocationCallOrder` tests don't prove "no await between" (WR-05) | ⚠️ Warning | Same call-order assertions would still pass if an `await` were inserted between adjacent calls; confirmed still open (now covers `setEnvironment` < `onEnvironmentReady` < `switchTicket` too, same limitation) |
| `src/lib/standalone-dev.ts` | 79-82 | Unrecognized `REACT_APP_DEV_GRISPI_ENV` silently falls back with no `console.warn` (IN-02), unlike the plugin path | ℹ️ Info | Diagnostic asymmetry; confirmed still open |
| `src/lib/grispi-environment.ts` | 63-69 | Warning logs the raw untrusted value verbatim and fires on empty string (IN-03) | ℹ️ Info | Minor console-noise/log-hygiene issue; confirmed still open |
| `.planning/REQUIREMENTS.md` | 83 | Phase-mapping table says "Not started" for CORE-04 while the requirement checkbox (line 15) is `[x]` | ℹ️ Info | Stale documentation, not a code gap; confirmed still stale on re-check |

No `TBD`/`FIXME`/`XXX` debt markers found in any file touched by this phase or its two closure quick-tasks (re-checked independently across all 12 touched source/test files).

### Human Verification Required

1. **M2 — live preprod network check (still deferred, per 04.1-02-SUMMARY.md "AWAITING USER CONFIRMATION")**
   - **Test:** Open the plugin inside a real Grispi preprod tenant iframe and check DevTools → Network for the first XHR's host. Separately, run `npm start` in standalone mode with only `REACT_APP_DEV_TOKEN` set (no `REACT_APP_DEV_GRISPI_ENV`) and confirm requests still hit preprod and `gsocial-test` loads.
   - **Expected:** Plugin-mode first XHR host is `api.grispi.net`, zero `.com` requests. Standalone mode still defaults to preprod.
   - **Why human:** Needs a real bundle/token inside the Grispi iframe or a live local session with DevTools inspection — not reproducible in jsdom or by static analysis. Neither closure quick-task (260810-j2w, 260810-jiw) touched this item; it remains exactly as open as it was in the prior verification.

### Gaps Summary

None. Both items that blocked/qualified the previous `gaps_found` verdict are independently confirmed closed:

1. **WR-01 (was the blocking gap):** `bootstrapPluginInit` now takes a required `onEnvironmentReady()` dep, called once, immediately after `setEnvironment`, in the success path's uninterrupted synchronous block, and never in `catch`. `GrispiProvider`'s `currentTicketUpdated` handler stays registered synchronously (SDK never sees an undefined member) but its body is gated on `environmentReadyRef`, which only that success-path callback can flip. This was not accepted on the SUMMARY's word: the new `grispi-context.test.tsx` was read line-by-line, and — going further than the SUMMARY's own evidence — this re-verification independently disabled the gate condition and confirmed Test A and Test D fail with the exact race signature described in the original finding, before restoring the tree and re-confirming the full 32/399 suite green.

2. **WR-02 (was the behavior_unverified item):** The terminal link — `setEnvironment` mutating `HttpHandler.baseUrl`, and that mutation actually being read by the next `fetch()` call — now has 11 new tests across `http-handler.test.ts` and `api.test.ts`, all asserting hardcoded host literals (never a circular `GRISPI_BASE_URLS[env]` lookup). This re-verification independently reproduced the SUMMARY's claimed mutation-probe result by emptying `HttpHandler.setEnvironment`'s body and confirming 6 of 21 tests failed with the exact expected/received host mismatch, then restored the source and re-confirmed green — this is not the same probe run recorded in the quick-task SUMMARY; it is a fresh, independent repetition that reached the same result.

Everything else — the pure resolver (SC1), the strict `prod-tr`/prototype-chain allowlist, the standalone-dev override (SC3, confirmed genuinely unaffected by the WR-01 fix beyond one invariant-parity line), the static host-literal gate (SC4, re-confirmed to still correctly exclude the new test-file host literals), and the README documentation (SC5) — remains verified exactly as in the prior report.

**Deferred items unaffected by this re-verification (confirmed still open, not silently closed):** WR-03 (public `setBaseUrl`/`baseUrl`), WR-04 (`switchTicket` dep typed `void` but actually async), WR-05 (`invocationCallOrder` doesn't prove "no await between"), IN-02 (standalone missing `console.warn`), IN-03 (warn fires on empty string) — all confirmed present in the current source, exactly as both quick-task plans stated they would leave them. `.planning/REQUIREMENTS.md` line 83 is still stale ("Not started" vs. the `[x]` checkbox at line 15).

**Overall status is `human_needed`, not `passed`,** solely because M2 (the live preprod network check) remains open — a real Grispi iframe session and DevTools inspection that no automated check in this environment can substitute for. All other must-haves — including both items that previously blocked the phase — are independently verified.

---

_Verified: 2026-08-10_
_Verifier: Claude (gsd-verifier)_
