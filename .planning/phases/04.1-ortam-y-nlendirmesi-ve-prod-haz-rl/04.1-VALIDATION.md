---
phase: 04.1
slug: ortam-y-nlendirmesi-ve-prod-haz-rl
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-10
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Kaynak: `04.1-RESEARCH.md` → `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 27.5.1 (react-scripts 5.0.1 içinde bundled, `@types/jest ^27.5.2`) |
| **Config file** | CRA varsayılanı + `craco.config.js:9-17` (`moduleNameMapper`, `@/` alias) |
| **Quick run command** | `npx craco test --watchAll=false --testPathPattern=<dosya-yolu>` |
| **Full suite command** | `CI=true npm test -- --watchAll=false` |
| **Estimated runtime** | ~30 saniye (tam suite) |

---

## Sampling Rate

- **After every task commit:** `npx craco test --watchAll=false --testPathPattern=<ilgili dosya>`
- **After every plan wave:** `CI=true npm test -- --watchAll=false`
- **Before `/gsd-verify-work`:** Tam suite yeşil **+** SC4 grep kontrolü boş çıktı **+** README incelemesi tamam
- **Max feedback latency:** 30 saniye

---

## Per-Task Verification Map

> Task ID'ler PLAN.md üretildikten sonra doldurulur; aşağıdaki satırlar RESEARCH'ten gelen
> davranış→test eşlemesidir ve planner tarafından task ID'lerine bağlanmalıdır.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | CORE-04 (SC1) | T-04.1-01 | `_grispi_env` yalnızca sabit map anahtarı olarak kullanılır, URL'e interpolasyon edilmez | unit | `npx craco test --watchAll=false --testPathPattern=grispi-environment` | ❌ W0 (yeni) | ⬜ pending |
| TBD | TBD | TBD | CORE-04 (SC1) | T-04.1-01 | `prod-tr` (tire) strict-match ile eşleşmez → `console.warn` + güvenli fallback | unit | `npx craco test --watchAll=false --testPathPattern=grispi-environment` | ❌ W0 (yeni) | ⬜ pending |
| TBD | TBD | TBD | CORE-04 (SC2) | — | `setEnvironment`, `switchTicket`'tan ÖNCE çağrılır (`invocationCallOrder`) | unit | `npx craco test --watchAll=false --testPathPattern=plugin-bootstrap` | ✅ genişletilecek | ⬜ pending |
| TBD | TBD | TBD | CORE-04 (SC3) | — | Standalone dev varsayılanı `preprod`, `REACT_APP_DEV_GRISPI_ENV` override, geçersiz değer fallback | unit | `npx craco test --watchAll=false --testPathPattern=standalone-dev` | ✅ genişletilecek | ⬜ pending |
| TBD | TBD | TBD | CORE-04 (SC1 regresyon) | — | `HttpHandler` varsayılan `baseUrl` prod (`.com`) | unit | `npx craco test --watchAll=false --testPathPattern=http-handler` | ✅ `http-handler.test.ts:121` güncellenecek | ⬜ pending |
| TBD | TBD | TBD | CORE-04 (SC4) | T-04.1-02 | Ortam-bağımlı host hiçbir kaynak dosyada hardcode değil | statik (grep) | `grep -rn "grispi\.\(net\|com\)" src --include="*.ts" --include="*.tsx" \| grep -v __tests__ \| grep -v environment` → boş çıktı | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/__tests__/grispi-environment.test.ts` — YENİ; `resolveGrispiEnvironment` + `parseJwt` kapsar
- [ ] `src/grispi/client/__tests__/http-handler.test.ts:121` — `.net` → `.com` beklentisi güncellenir (ZORUNLU, aksi hâlde kırmızı)
- [ ] `src/contexts/__tests__/plugin-bootstrap.test.ts` — 4 mevcut case'e `setEnvironment: jest.fn()` eklenir (ZORUNLU; `BootstrapPluginInitDeps.setEnvironment` required alan olacağı için TS derleme hatası verir)
- [ ] `src/lib/__tests__/standalone-dev.test.ts` — `environment` alanı + `REACT_APP_DEV_GRISPI_ENV` case'leri

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| README, `_grispi_env`'i ve TR kurulumlarında elle set edilme zorunluluğunu belgeliyor | CORE-04 (SC5) | Doküman içeriği; otomatik assert edilemez (varlık kontrolü dışında) | README'de `_grispi_env` bölümü okunur; `prod` / `prod_tr` ayrımının `dev` claim'iyle yapılamadığı ve TR'de ayarın zorunlu olduğu açıkça yazılı olmalı |
| Preprod tenant'ta plugin canlı yüklenip ilk isteğin `.net` host'una gittiği doğrulanır | CORE-04 (SC2, SC3) | Gerçek bundle + gerçek token gerektirir; jsdom'da üretilemez | Grispi preprod ortamında plugin açılır; DevTools → Network ilk XHR'ın host'u `api.grispi.net` olmalı, `.com`'a giden istek olmamalı |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (`--watchAll=false` / `CI=true` her komutta)
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
