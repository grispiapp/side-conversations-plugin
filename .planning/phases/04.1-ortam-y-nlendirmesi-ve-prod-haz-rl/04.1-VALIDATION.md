---
phase: 04.1
slug: ortam-y-nlendirmesi-ve-prod-haz-rl
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-10
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Kaynak: `04.1-RESEARCH.md` → `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 27.5.1 (react-scripts 5.0.1 içinde bundled, `@types/jest ^27.5.2`) |
| **Config file** | CRA varsayılanı + `craco.config.js:9-17` (`moduleNameMapper`, `@/` alias) |
| **Quick run command** | `npx craco test --watchAll=false --testPathPattern=<dosya-yolu>` |
| **Full suite command** | `CI=true npm test -- --watchAll=false` |
| **Estimated runtime** | ~30 saniye (tam suite) |

---

## Sampling Rate

- **After every task commit:** `npx craco test --watchAll=false --testPathPattern=<ilgili dosya>`
- **After every plan wave:** `CI=true npm test -- --watchAll=false`
- **Before `/gsd-verify-work`:** Tam suite yeşil **+** SC4 grep kontrolü boş çıktı **+** README incelemesi tamam
- **Max feedback latency:** 30 saniye

---

## Per-Task Verification Map

> Task ID'ler PLAN.md üretildikten sonra dolduruldu (2026-08-10, `/gsd-plan-phase 04.1`).
> Her satır gerçek bir plan görevine bağlıdır; komutlar plan `<verify><automated>` blokları ile birebir aynıdır.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.1-01 / T1 | 04.1-01 | 1 | CORE-04 (SC1) | T-04.1-01 | `_grispi_env` yalnızca sabit map anahtarı olarak kullanılır, URL'e interpolasyon edilmez | unit | `npx craco test --watchAll=false --testPathPattern=grispi-environment` | ❌ W0 (yeni) | ⬜ pending |
| 04.1-01 / T1 | 04.1-01 | 1 | CORE-04 (SC1) | T-04.1-01 | `prod-tr` (tire) strict-match ile eşleşmez → `console.warn` + güvenli fallback | unit | `npx craco test --watchAll=false --testPathPattern=grispi-environment` | ❌ W0 (yeni) | ⬜ pending |
| 04.1-01 / T1 | 04.1-01 | 1 | CORE-04 (SC1) | T-04.1-04 | Bozuk/JWT olmayan token `parseJwt`'i çökertmez → `null` + `prod` varsayılanı | unit | `npx craco test --watchAll=false --testPathPattern=grispi-environment` | ❌ W0 (yeni) | ⬜ pending |
| 04.1-01 / T2 | 04.1-01 | 1 | CORE-04 (SC2) | — | `setEnvironment`, `switchTicket`'tan ÖNCE çağrılır (`invocationCallOrder`) | unit | `npx craco test --watchAll=false --testPathPattern=plugin-bootstrap` | ✅ genişletilecek | ⬜ pending |
| 04.1-02 / T1 | 04.1-02 | 2 | CORE-04 (SC3) | T-04.1-01 | Standalone dev varsayılanı `preprod`, `REACT_APP_DEV_GRISPI_ENV` override, geçersiz değer fallback | unit | `npx craco test --watchAll=false --testPathPattern=standalone-dev` | ✅ genişletilecek | ⬜ pending |
| 04.1-01 / T2 | 04.1-01 | 1 | CORE-04 (SC1 regresyon) | — | `HttpHandler` varsayılan `baseUrl` prod (`.com`) | unit | `npx craco test --watchAll=false --testPathPattern=http-handler` | ✅ `http-handler.test.ts:121` güncellenecek | ⬜ pending |
| 04.1-01 / T2 | 04.1-01 | 1 | CORE-04 (SC4) | T-04.1-02 | Ortam-bağımlı host hiçbir kaynak dosyada hardcode değil | statik (grep) | `grep -rn "grispi\.\(net\|com\)" src --include="*.ts" --include="*.tsx" \| grep -v __tests__ \| grep -v environment` → boş çıktı | — | ⬜ pending |
| 04.1-02 / T2 | 04.1-02 | 2 | CORE-04 (SC4) | T-04.1-02 | Aynı grep, faz kapanış tekrarı (README görevinin kapanış gate'i) | statik (grep) | aynı komut → boş çıktı | — | ⬜ pending |
| 04.1-01 / T1 | 04.1-01 | 1 | CORE-04 (SC1) | T-04.1-01 | `environment.ts`'te hiçbir şablon-literal yok (host dize birleştirmeyle inşa edilmez) | statik (grep) | `grep -c '\${' src/grispi/client/environment.ts` → `0` | ❌ W0 (yeni) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Her madde, kaynak değişikliğiyle **aynı görevde** yapılır — sonraki bir temizlik işi olarak bırakılamaz.

- [ ] `src/lib/__tests__/grispi-environment.test.ts` — YENİ; `resolveGrispiEnvironment` + `parseJwt` + `isGrispiEnvironment` kapsar → **04.1-01 / T1**
- [ ] `src/grispi/client/__tests__/http-handler.test.ts:121` — beklenti `GRISPI_BASE_URLS[DEFAULT_ENVIRONMENT]`'tan türetilir (elle host yazılmaz) (ZORUNLU, aksi hâlde kırmızı) → **04.1-01 / T2**
- [ ] `src/contexts/__tests__/plugin-bootstrap.test.ts` — 4 mevcut case'e `setEnvironment: jest.fn()` eklenir (ZORUNLU; `BootstrapPluginInitDeps.setEnvironment` required alan olacağı için TS derleme hatası verir) + `makeBundle()` yardımcısı `settings` override'ı alacak şekilde genişletilir → **04.1-01 / T2**
- [ ] `src/lib/__tests__/standalone-dev.test.ts` — `environment` alanı + `REACT_APP_DEV_GRISPI_ENV` case'leri → **04.1-02 / T1**
- [ ] `src/lib/__tests__/standalone-dev.test.ts:39-44` — **planlama sırasında tespit edilen ek kırılma:** mevcut `expect(config).toEqual({...})` TAM nesne eşitliği kontrol ediyor; `environment` alanı eklendiği anda kırılır, beklentiye `environment: DEFAULT_DEV_ENVIRONMENT` eklenmelidir → **04.1-02 / T1**

---

## Manual-Only Verifications

> `workflow.human_verify_mode: end-of-phase` olduğu için bunlar `checkpoint:human-verify` görevi DEĞİL,
> ilgili görevin `<verify><human-check>` bloğu ve planın `<verification>` bölümü olarak taşınır.

| ID | Owner Task | Behavior | Requirement | Why Manual | Test Instructions |
|----|-----------|----------|-------------|------------|-------------------|
| M1 | 04.1-02 / T2 | README, `_grispi_env`'i ve TR kurulumlarında elle set edilme zorunluluğunu belgeliyor | CORE-04 (SC5) | Doküman içeriği; otomatik assert edilemez (varlık kontrolü dışında) | README'de `### Required setting: _grispi_env` bölümü okunur; üç geçerli değer (alt çizgili `prod_tr` dâhil), ayarsız kurulumun davranışı, `prod` / `prod_tr` ayrımının `dev` claim'iyle YAPILAMADIĞI + TR'de ayarın zorunlu olduğu, ve tireli yazımın geçersizliği açıkça yazılı olmalı |
| M2 | 04.1-02 / T2 | Preprod tenant'ta plugin canlı yüklenip ilk isteğin preprod host'una gittiği doğrulanır | CORE-04 (SC2, SC3) | Gerçek bundle + gerçek token gerektirir; jsdom'da üretilemez | (i) Grispi preprod ortamında plugin açılır; DevTools → Network ilk XHR'ın host'u preprod olmalı, `.com` uzantılı istek olmamalı. (ii) `npm start` ile standalone mod (`REACT_APP_DEV_GRISPI_ENV` set EDİLMEDEN) açılır; istekler yine preprod'a gitmeli ve `gsocial-test` listesi gelmeli |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — 4/4 görevin `<verify><automated>` bloğu var
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — her görevde otomatik komut mevcut
- [x] Wave 0 covers all MISSING references — 5 madde, hepsi kaynak değişikliğiyle aynı göreve bağlandı
- [x] No watch-mode flags (`--watchAll=false` / `CI=true` her komutta)
- [x] Feedback latency < 30s — hedefli `--testPathPattern` komutları; tam suite ~30 sn
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner (`/gsd-plan-phase 04.1`, 2026-08-10) — task ID'leri bağlandı, kapsam eksiksiz
