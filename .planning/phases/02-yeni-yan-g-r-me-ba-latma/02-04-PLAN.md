---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/store/active-conversation-store.ts
  - src/store/__tests__/active-conversation-store.test.ts
  - src/store/compose-store.ts
  - src/store/__tests__/compose-store.test.ts
  - src/store/root-store.ts
  - src/components/ui/textarea.tsx
  - src/screens/components/message-field.tsx
  - src/screens/compose-screen.tsx
autonomous: true
requirements: [COMP-04, SYNC-02]

must_haves:
  truths:
    - "Temsilci mesajı gönderince optimistic bir mesaj balonu (pending) oluşur ve `POST /public/v1/tickets` atılır; publicVisible:true olduğu için alıcıya gerçek e-posta gider (COMP-04 / D-13)"
    - "POST 2xx → balon 'sent'e çözülür + yeni side ticket anahtarı ActiveConversationStore'a yazılır; POST hata → balon 'failed', mesaj metni korunur, retry AYNI POST'u yeniden atar (D-14/D-15)"
    - "Gönderim sürerken ikinci submit engellenir (senkron reentrancy guard, ilk await'ten önce — D-17)"
    - "Gönderim başarısı sonrası liste GERÇEK veriyle tazelenir (`sideConversations.load(parentKey)`), optimistic sahte-satır değil (SYNC-02 / D-16)"
  artifacts:
    - src/store/active-conversation-store.ts
    - src/components/ui/textarea.tsx
    - src/screens/components/message-field.tsx
  key_links:
    - "ComposeStore.submit → CreateTicketRequest kurar (creator=agentEmail, ts.requester=formatRequesterField(email), tu.side_conversation_parent=parentKey, ts.subject her zaman mevcut, publicVisible:true) → ActiveConversationStore.startNew → panelNavigation.openChat"
    - "ActiveConversationStore POST createTicket → resolveSent/markFailed → sideConversations.load(parentKey)"
    - "MessageField Shift+Enter → ComposeStore.submit (D-12); reentrancy guard D-17"
---

<objective>
Yazma akışının çekirdeğini teslim et (COMP-04, SYNC-02): temsilci mesajı gönderdiğinde bir side ticket oluşur, alıcıya gerçek e-posta gider ve optimistic mesaj balonu POST cevabıyla çözülür; sonrasında liste gerçek veriyle tazelenir. `ActiveConversationStore` (Faz 3'ün thread'ini bağlayacağı seam) burada kurulur; `ComposeStore` mesaj durumu + `submit` ile genişletilir; `Textarea` primitifi ve `MessageField` (ters klavye kısayolu D-12) eklenir.

Purpose: Bu, phase'in "happy path"inin mantık katmanı — gönderim, optimistic durum ve SYNC-02 refetch. Görsel chat kabuğu + Gönder butonu Plan 05'te bağlanır.
Output: `ActiveConversationStore` + `ComposeStore.submit` + `Textarea`/`MessageField`, tümü testli.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/store/active-conversation-store.ts` (YENİ): `MessageVM` interface (`{ id, direction, body, status, createdAt }`), `ActiveConversationStore` sınıfı (`ticketKey`/`recipientLabel`/`subject`/`messages`, `startNew`, `resolveSent`, `markFailed`, `retry`, private `sendCreateTicket`)
- `src/store/compose-store.ts`: `message`/`setMessage`/`submitting` alanları, `submit(...)` metodu, `isDirty` computed
- `src/store/root-store.ts`: `RootStore.activeConversation`
- `src/components/ui/textarea.tsx` (YENİ): `Textarea` primitifi
- `src/screens/components/message-field.tsx` (YENİ): `MessageField`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActiveConversationStore — optimistic mesaj yaşam döngüsü + createTicket POST + SYNC-02</name>
  <files>src/store/active-conversation-store.ts, src/store/__tests__/active-conversation-store.test.ts, src/store/root-store.ts</files>
  <read_first>
    - src/store/side-conversations-store.ts (MobX kabuk + `runInAction`; immutable array replacement dersi lines 443-450 — mesaj listesi güncellemeleri de in-place mutation DEĞİL, yeni array identity; `load(parentKey)` imzası SYNC-02 için çağrılacak)
    - src/store/root-store.ts (Plan 03'te `compose` eklendi — `activeConversation` aynı kalıpla)
    - src/grispi/client/api.ts (Plan 01 — `grispiAPI.tickets.createTicket`)
    - src/types/grispi.type.ts (Plan 01 — `CreateTicketRequest`, createTicket cevabı .key alanı)
    - src/grispi/client/http-handler.ts (`NetworkError`/`HttpError` — markFailed hata tipini ayırır; body ASLA render/log)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Pattern 4 — ActiveConversationStore seam, `MessageVM`; Common Pitfalls #5 — retry çift-oluşturma kabul edilmiş risk; Pitfall #6 — subject/recipient sunucudan geri OKUNMAZ, compose'dan echo edilir)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md (A1 — createTicket 201 cevabındaki yeni-ticket anahtarı alan adı)
  </read_first>
  <behavior>
    - Test: `startNew({recipientLabel, subject, body, request, parentKey})` → `messages` tek bir `MessageVM` içerir (`direction:"own"`, `status:"pending"`, `body`), `recipientLabel`/`subject` set olur
    - Test: startNew POST 2xx (mocked createTicket resolves .key) → ilgili mesaj `status:"sent"`, `ticketKey` cevaptaki anahtara set, `sideConversations.load(parentKey)` spy'ı ÇAĞRILIR (SYNC-02)
    - Test: startNew POST reddedilir (NetworkError) → mesaj `status:"failed"`, `body` korunur, `sideConversations.load` ÇAĞRILMAZ
    - Test: `retry(messageId)` → mesaj tekrar `pending`, createTicket yeniden çağrılır; 2xx → `sent` + `load(parentKey)`
    - Test: mesaj güncellemeleri immutable (yeni `messages` array identity — observer görebilsin)
  </behavior>
  <action>
    `active-conversation-store.ts` (YENİ) — `MessageVM` interface (`id: string` client-geçici id; `direction: "own" | "incoming"` — Faz 2 yalnız `"own"` üretir; `body: string`; `status: "pending" | "sent" | "failed"`; `createdAt: number`; opsiyonel `errorKind?: "network" | "server"` — Plan 06 copy ayrımı için). `ActiveConversationStore` — `makeAutoObservable(this)` + `constructor(rootStore)`. Alanlar: `ticketKey: string | null = null`, `recipientLabel = ""`, `subject = ""`, `messages: MessageVM[] = []`, private retry payload saklama (messageId → `{ request: CreateTicketRequest, parentKey: string }`). `startNew({ recipientLabel, subject, body, request, parentKey })`: `recipientLabel`/`subject` set (Pitfall #6 — compose'dan echo, sunucudan okunmaz); yeni `MessageVM` (`status:"pending"`) ekle (immutable array); retry payload'unu sakla; private `sendCreateTicket(messageId)` çağır. `sendCreateTicket(messageId)`: `grispiAPI.tickets.createTicket(request)` → başarı: `runInAction` içinde `resolveSent(messageId)` + `ticketKey = response.<A1 alan adı>` + `this.rootStore.sideConversations.load(parentKey)` (SYNC-02/D-16); hata: `markFailed(messageId, err instanceof NetworkError ? "network" : "server")` (error.body ASLA render/log). `resolveSent(messageId)`: ilgili mesajı immutable olarak `status:"sent"` yap. `markFailed(messageId, kind)`: `status:"failed"` + `errorKind` (body korunur). `retry(messageId)`: mesajı `pending`'e çevir + saklanan payload ile `sendCreateTicket` yeniden (D-15 aynı POST; Pitfall #5 çift-oluşturma kabul edilmiş risk — SUMMARY'ye not). `root-store.ts`'e `activeConversation: ActiveConversationStore` + instantiation. Testler `active-conversation-store.test.ts` (mock createTicket + spy `sideConversations.load`).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=active-conversation-store</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/active-conversation-store.ts` mevcut; `MessageVM` + `ActiveConversationStore` + `startNew`/`resolveSent`/`markFailed`/`retry` içeriyor
    - `grep -c "activeConversation" src/store/root-store.ts` ≥ 2 (alan + instantiation)
    - active-conversation-store testi yeşil: pending→sent + `sideConversations.load(parentKey)` çağrısı (SYNC-02), pending→failed (load çağrılmaz), retry→sent
    - Mesaj listesi güncellemeleri immutable (yeni array identity); `markFailed` `errorKind` ayrımını tutar; `error.body`/`status` hiçbir yere yazılmaz
  </acceptance_criteria>
  <done>Optimistic mesaj yaşam döngüsü + createTicket POST + SYNC-02 refetch testli; Faz 3 seam'i (`incoming` varyantı, `open`/`reply` için yer) hazır.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComposeStore.submit — reentrancy guard + payload kurulumu + handoff</name>
  <files>src/store/compose-store.ts, src/store/__tests__/compose-store.test.ts</files>
  <read_first>
    - src/store/compose-store.ts (Plan 03 — `recipientEmail`/`recipientLabel`/`subject`/arama durumu; `submit` + `message` buraya eklenir)
    - src/store/active-conversation-store.ts (Task 1 — `startNew` imzası)
    - src/store/panel-navigation-store.ts (Plan 02 — `openChat`)
    - src/lib/side-conversation.ts (Plan 01 — `formatRequesterField`)
    - src/types/grispi.type.ts (Plan 01 — `CreateTicketRequest` şekli)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Pattern 3 — reentrancy-guard submit, senkron guard ilk await'ten ÖNCE; Common Pitfalls #1 — ts.subject anahtarı ASLA omit edilmez değeri boş olabilir; #2 — publicVisible HER ZAMAN true; #3 — creator agentEmail)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md (D-10 zorunlu alan alıcı+mesaj, D-11 gövde aynen, D-17 çift-gönderim kilidi)
  </read_first>
  <behavior>
    - Test: `submit` sürerken (`submitting===true`) ikinci `submit` çağrısı ilk await'ten ÖNCE no-op (D-17 reentrancy guard)
    - Test: recipient boş veya message boş iken `submit` → gönderim başlatmaz (D-10)
    - Test: geçerli girdi ile `submit(agentEmail, parentKey)` → `activeConversation.startNew` doğru `CreateTicketRequest` ile çağrılır: `comment.publicVisible === true`, `comment.creator[0]` `{key:"us.email", value:agentEmail}`, `fields` içinde `ts.requester` = `formatRequesterField(recipientEmail)` (`:email`), `tu.side_conversation_parent` = parentKey, `ts.subject` girdisi MEVCUT (değeri boş olabilir)
    - Test: başarılı submit sonrası `panelNavigation.openChat()` çağrılır ve compose form durumu sıfırlanır
    - Test: `isDirty` — recipient/subject(prefill dışı)/message'tan herhangi biri doluysa true
  </behavior>
  <action>
    `compose-store.ts`'i genişlet: `message = ""`, `setMessage(value)`, `submitting = false`, private `submitGeneration = 0`. `submit(agentEmail: string | null, parentKey: string)`: EN BAŞTA senkron `if (this.submitting) return` (D-17, ilk await'ten önce) → `this.submitting = true`; zorunlu alan kontrolü (`recipientEmail` boş || `message.trim()` boş → `submitting=false` + return, D-10). `CreateTicketRequest` kur: `comment = { body: this.message, publicVisible: true, creator: [{ key: "us.email", value: agentEmail ?? "" }] }` (D-11 gövde aynen; D-13 publicVisible true; D-08 creator agentEmail — Pitfall #3), `fields = [ { key: "ts.subject", value: this.subject }, { key: "ts.requester", value: formatRequesterField(this.recipientEmail) }, { key: "tu.side_conversation_parent", value: parentKey } ]` (ts.subject anahtarı ASLA omit — Pitfall #1; ts.requester colon-prefix — D-07). `this.rootStore.activeConversation.startNew({ recipientLabel: this.recipientLabel, subject: this.subject, body: this.message, request, parentKey })` → `this.rootStore.panelNavigation.openChat()` → formu sıfırla (`reset()` — query/results/recipient/subject/message/subjectInitialized temizle; `submitting=false`). `get isDirty()`: `recipientEmail || message.trim() || (subject.trim() && subject !== <prefill>)` → boolean (Plan 06 dirty-guard bunu okuyacak). NOT: `agentEmail`/`parentKey` React context'ten gelir; store okuyamaz → çağıran (Plan 05 Gönder onClick / MessageField) parametre olarak geçer. Testleri `compose-store.test.ts`'e ekle (mock `activeConversation.startNew` + spy `panelNavigation.openChat`).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=compose-store</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "submitting" src/store/compose-store.ts` ≥ 2 (senkron guard + flip)
    - `grep -c "publicVisible" src/store/compose-store.ts` ≥ 1 ve değeri `true` (literal)
    - `grep -c "formatRequesterField" src/store/compose-store.ts` ≥ 1 (ts.requester `:email`)
    - `grep -c "ts.subject" src/store/compose-store.ts` ≥ 1 (girdi her zaman kurulur, omit yok)
    - compose-store testi yeşil: reentrancy guard, zorunlu-alan bloğu, doğru CreateTicketRequest, openChat + form reset, isDirty
  </acceptance_criteria>
  <done>submit reentrancy-guard'lı, doğru createTicket gövdesini kurup ActiveConversationStore'a devreder ve chat'e yönlendirir; testli.</done>
</task>

<task type="auto">
  <name>Task 3: Textarea primitifi + MessageField (Enter=yeni satır / Shift+Enter=gönder, D-12)</name>
  <files>src/components/ui/textarea.tsx, src/screens/components/message-field.tsx, src/screens/compose-screen.tsx</files>
  <read_first>
    - src/components/ui/input.tsx (birebir kalıp: `React.forwardRef` + `cn()` + `displayName` + prop-spread; Textarea aynı görsel dil, `min-h-[96px]`)
    - src/store/compose-store.ts (Task 2 — `message`/`setMessage`/`submit`)
    - src/screens/compose-screen.tsx (Plan 03 — RecipientField/SubjectField altına MessageField eklenecek)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Pattern 2 — native textarea Enter/Shift+Enter ikisi de newline; YALNIZCA Shift+Enter yakalanıp `preventDefault()`+submit; düz Enter'a dokunma; Pattern 3 — guard store içinde, iki giriş noktası da AYNI `submit`'i çağırır)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Textarea notları `min-h-[96px]`; MessageField placeholder "Mesajınızı yazın…" + kalıcı klavye ipucu "Enter: yeni satır · Shift+Enter: gönder")
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md (textarea.tsx = input.tsx exact analog; message-field.tsx = textarea usage)
  </read_first>
  <action>
    (a) `textarea.tsx` (YENİ) — `input.tsx` kalıbının `<textarea>` uyarlaması: `React.forwardRef<HTMLTextAreaElement, TextareaProps>`, `cn(...)` ile input'la aynı border/`rounded-md`/`shadow-sm`/`focus-visible:ring-1`/`disabled:opacity-50` sınıfları + `min-h-[96px]` (çok satır, içerikle büyür), `displayName="Textarea"`. `cva` GEREKMEZ (input.tsx da kullanmıyor).
    (b) `message-field.tsx` (YENİ) — `observer`-sarmalı `MessageField`. `compose = useStore().compose`. `Textarea` value `compose.message`, `onChange` → `compose.setMessage(...)`, placeholder "Mesajınızı yazın…". `onKeyDown`: `if (e.key === "Enter" && e.shiftKey) { e.preventDefault(); compose.submit(agentEmail, parentKey); }` — düz Enter'a HİÇ dokunma (native newline doğru davranış, Pattern 2). `agentEmail`/`parentKey` `useGrispi()`'den okunur (`agentEmail`, `ticket?.key`). Altında kalıcı muted ipucu "Enter: yeni satır · Shift+Enter: gönder" (D-12, her zaman görünür). `compose.submitting` iken Textarea disabled (D-17 ek kilit).
    (c) `compose-screen.tsx` — RecipientField/SubjectField altına `<MessageField />` ekle (`md` boşluk; Gönder butonu Plan 05'te).
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/textarea.tsx` mevcut; `React.forwardRef` + `Textarea.displayName` + `min-h-[96px]`
    - `src/screens/components/message-field.tsx` mevcut, `observer` sarmalı, `Textarea` kullanıyor
    - `grep -c "shiftKey" src/screens/components/message-field.tsx` ≥ 1 ve Shift+Enter dalında `preventDefault` + `compose.submit` çağrısı var (D-12)
    - Klavye ipucu metni "Shift+Enter" içeriyor (kalıcı görünür)
    - compose-screen `<MessageField />` render ediyor
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Çok satırlı mesaj alanı ters klavye kısayoluyla (D-12) çalışır; Shift+Enter store'un `submit`'ini çağırır (guard store içinde merkezi).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temsilci girdisi → createTicket gövdesi | Alıcı e-postası, konu, mesaj gövdesi `fields`/`comment`'e yazılır; parent link ayrı (güvenilir) kaynaktan |
| React context (trusted) → submit | `agentEmail` + `parentKey` yalnızca `useGrispi()`'den (SDK bağlamı) submit'e geçer |
| plugin → POST /tickets | Gönderim gerçek e-posta üretir (publicVisible:true) — harici tarafa gider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing / Tampering | `tu.side_conversation_parent` — yanlış talebe side ticket bağlama | medium | mitigate | `parentKey` submit'e YALNIZCA `useGrispi().ticket?.key`'den (trusted SDK context) geçer; hiçbir form alanından türetilmez (IDOR-benzeri önlenir) |
| T-02-05 | Spoofing | createTicket `creator` = agentEmail | medium | mitigate | agentEmail Plan 02'de güvenilir SDK/dev-kapılı kaynaktan; submit yalnızca taşır, kullanıcı-girdisiyle değiştirilemez |
| T-02-08 | Tampering | D-15 retry → at-least-once ile çift side ticket | low | accept | API'de idempotency-key yok (Pitfall #5); D-15 kilitli karar (aynı POST). Kabul edilmiş risk — SUMMARY'ye not; v2 fikri: client-generated idempotency marker |
| T-02-03 | Information Disclosure | createTicket hata gövdesi (`HttpError.body`) sızması | medium | mitigate | markFailed yalnız `errorKind` (network/server) tutar; `error.body`/`status` render/log EDİLMEZ (Faz 1 V7) |

Not: XSS (T-02-02, high) mesaj gövdesinin RENDER'ında ortaya çıkar — Plan 05'te (MessageBubble/ChatScreen) ele alınır. Bu planın yüzeyinde `high`+ tehdit yok.
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false --testPathPattern=active-conversation-store` yeşil
- `CI=true npx craco test --watchAll=false --testPathPattern=compose-store` yeşil
- `CI=true npx tsc --noEmit` hatasız
</verification>

<success_criteria>
- COMP-04 (mantık): submit → CreateTicketRequest (creator/requester/parent/subject/publicVisible doğru) → optimistic startNew → openChat
- SYNC-02: başarılı gönderim sonrası `sideConversations.load(parentKey)` gerçek refetch tetikler
- D-17 reentrancy guard, D-12 klavye kısayolu çalışır
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-04-SUMMARY.md` when done. Pitfall #5 (retry çift-oluşturma) kabul edilmiş riskini SUMMARY'ye not düş.
</output>
