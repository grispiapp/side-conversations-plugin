---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 06
type: execute
wave: 6
depends_on: [02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/screens/components/confirm-dialog.tsx
  - src/screens/compose-screen.tsx
  - src/app.tsx
  - src/screens/components/message-bubble.tsx
autonomous: false
requirements: [COMP-04, SYNC-02]

must_haves:
  truths:
    - "Dolu compose formunda geri/iptal → onaylı vazgeç dialog'u ('Vazgeçilsin mi? / Yazılanlar kaybolur'); boş formda onaysız direkt döner (D-02)"
    - "Compose açıkken temsilci aktif talebi değiştirirse: dolu taslakta uyar+onayla ('Taslağın kaybolacak'); boş taslakta sessizce kapanır (D-03)"
    - "Gönderim hatasında ağ vs sunucu ayrımı Faz 1 ErrorCard/CORE-03 diliyle tutarlı: network → 'Bağlantı sorunu · Gönderilemedi. Tekrar dene', server → jenerik 'Gönderilemedi · Tekrar dene' (D-15)"
    - "Faz-sonu UAT: tüm 5 success criteria (COMP-01..04, SYNC-02) canlı tenant'ta uçtan uca doğrulanır"
  artifacts:
    - src/screens/components/confirm-dialog.tsx
  key_links:
    - "ComposeScreen onBack → panelNavigation.requestBack(compose.isDirty) → true ise ConfirmDialog (D-02 copy) → confirmDiscardAndReturnToList"
    - "app.tsx useEffect([ticket?.key]) → panelNavigation.handleParentTicketChanged(compose.isDirty) → 'needs-confirm' ise ConfirmDialog (D-03 copy)"
---

<objective>
Happy path'i koruma katmanı ve cila ile tamamla, sonra faz-sonu UAT ile doğrula: tek bir `ConfirmDialog` bileşeni (D-02 vazgeç + D-03 parent-değişim, farklı copy/props), gönderim-hatası copy'sinin ağ/sunucu ince ayrımı (D-15) ve tüm 5 success criteria'yı canlı tenant'ta uçtan uca gösteren `checkpoint:human-verify`.

Purpose: MVP "validation + error states" dilimleri — taslak kaybı korumaları ve hata deneyimi cilası. Faz-sonu UAT, gerçek e-postanın alıcıya gittiğini (publicVisible:true) ve SYNC-02 refetch'inin gerçek veriyle çalıştığını insan gözüyle doğrular.
Output: `ConfirmDialog` + wiring (D-02/D-03) + refined failed copy (D-15) + geçen faz-sonu UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/screens/components/confirm-dialog.tsx` (YENİ): `ConfirmDialog` (overlay + card, `title`/`body`/`confirmLabel`/`cancelLabel`/`onConfirm`/`onCancel` props — D-02 ve D-03 için TEK bileşen)
- `src/screens/compose-screen.tsx`: D-02 dirty-back guard wiring + lokal dialog state
- `src/app.tsx`: D-03 parent-change useEffect bridge + lokal dialog state
- `src/screens/components/message-bubble.tsx`: D-15 network/server copy ince ayrımı
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmDialog (D-02/D-03 tek bileşen) + dirty-back + parent-change wiring</name>
  <files>src/screens/components/confirm-dialog.tsx, src/screens/compose-screen.tsx, src/app.tsx</files>
  <read_first>
    - src/screens/components/error-card.tsx (en yakın görsel analog: merkezi card + mesaj + buton(lar); `bg-card rounded-md px-6 py-6 text-center`; Button variant kullanımı)
    - src/components/ui/button.tsx (`outline` = güvenli/iptal, `destructive` = yıkıcı aksiyon; ikili buton pairing)
    - src/store/panel-navigation-store.ts (Plan 02 — `requestBack(isDirty)`, `confirmDiscardAndReturnToList`, `handleParentTicketChanged(isDirty)` dönüş union'ı)
    - src/store/compose-store.ts (Plan 04 — `isDirty` computed; `reset()`)
    - src/screens/compose-screen.tsx (Plan 03/04/05 — `ScreenHeader onBack` şu an `requestBack(false)`; gerçek `isDirty` bağlanacak)
    - src/app.tsx (Plan 02 — screen-swap; `useGrispi().ticket?.key` değişimini köprüleyecek useEffect eklenecek)
    - src/screens/conversations-list-screen.tsx (`useEffect([ticket?.key])` köprüleme deseni lines 23-28 — app.tsx'e aynı desen uygulanır)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Copywriting: D-02 başlık "Vazgeçilsin mi?" body "Yazılanlar kaybolur." butonlar "İptal"(outline)/"Vazgeç"(destructive); D-03 başlık "Taslağın kaybolacak" body "Yeni görüşme taslağın var; talebi değiştirirsen kaybolur." butonlar "Kalsın"(outline)/"Vazgeç ve devam et"(destructive); ConfirmDialog `fixed inset-0 bg-black/40` overlay + merkezi `bg-card rounded-lg shadow-lg p-6` card; TEK bileşen iki varyant)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md (D-02 dolu-form onaylı / boş-form direkt; D-03 dolu-taslak uyar+onayla / boş-taslak sessizce)
  </read_first>
  <action>
    (a) `confirm-dialog.tsx` (YENİ) — `ConfirmDialog` props: `title`, `body`, `confirmLabel`, `cancelLabel`, `onConfirm`, `onCancel`. `fixed inset-0 bg-black/40` overlay + merkezi `bg-card rounded-lg shadow-lg p-6` card: başlık (Heading), body (Body), sağa-hizalı iki buton — cancel `variant="outline"` (güvenli), confirm `variant="destructive"` (asla bare "Tamam"/"OK"). D-02 VE D-03 için AYNI bileşen, farklı copy/props (CONTEXT açık talimatı).
    (b) `compose-screen.tsx` — lokal `useState` dialog görünürlüğü ekle. `ScreenHeader onBack`'i güncelle: `if (panelNav.requestBack(compose.isDirty)) setDiscardOpen(true)` (dirty ise dialog aç, D-02; değilse requestBack zaten listeye döndü). Dialog açıkken `<ConfirmDialog title="Vazgeçilsin mi?" body="Yazılanlar kaybolur." cancelLabel="İptal" confirmLabel="Vazgeç" onCancel={() => setDiscardOpen(false)} onConfirm={() => { compose.reset(); panelNav.confirmDiscardAndReturnToList(); setDiscardOpen(false); }} />`.
    (c) `app.tsx` — `useGrispi().ticket?.key` değişimini köprüleyen `useEffect` ekle (conversations-list-screen'deki `[ticket?.key]` deseniyle): `const result = panelNav.handleParentTicketChanged(compose.isDirty)`; `result === "needs-confirm"` ise lokal `useState` ile D-03 dialog'unu aç. Dialog: `<ConfirmDialog title="Taslağın kaybolacak" body="Yeni görüşme taslağın var; talebi değiştirirsen kaybolur." cancelLabel="Kalsın" confirmLabel="Vazgeç ve devam et" onCancel={...stay in compose, revert ticket switch akışı...} onConfirm={() => { compose.reset(); panelNav.confirmDiscardAndReturnToList(); ...yeni talebin listesi zaten yüklenecek... }} />`. Köprü component seviyesinde kalmalı (MobX store React context'i okuyamaz — RESEARCH Pattern 5). NOT: `useStore()`/`useGrispi()` provider içinde çağrılmalı (Plan 02'deki iç observer component).
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/components/confirm-dialog.tsx` mevcut; tek `ConfirmDialog` bileşeni title/body/confirmLabel/cancelLabel/onConfirm/onCancel props alıyor (overlay + card)
    - `grep -c "Vazgeçilsin mi" src/screens/compose-screen.tsx` ≥ 1 (D-02 copy) ve onBack `requestBack(compose.isDirty)` çağırıyor
    - `grep -c "Taslağın kaybolacak" src/app.tsx` ≥ 1 (D-03 copy) ve `handleParentTicketChanged` `[ticket?.key]` useEffect'inden çağrılıyor
    - Boş formda geri → dialog AÇILMAZ, direkt liste (D-02); boş taslakta parent değişimi → sessizce kapanır (D-03)
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Tek ConfirmDialog iki call-site'ta (D-02 dirty-back, D-03 parent-change) doğru copy'lerle çalışır; boş durumlarda dialog atlanır.</done>
</task>

<task type="auto">
  <name>Task 2: MessageBubble failed copy — ağ vs sunucu ince ayrımı (D-15)</name>
  <files>src/screens/components/message-bubble.tsx</files>
  <read_first>
    - src/screens/components/message-bubble.tsx (Plan 05 — failed durumu şu an tek jenerik copy "Gönderilemedi · Tekrar dene"; `message.errorKind` alanı Plan 04'te set ediliyor)
    - src/screens/components/error-card.tsx (Faz 1 CORE-03 network/server copy ayrımı: NetworkError → "Bağlantı sorunu…", diğer → jenerik; aynı dil MessageBubble'a taşınır)
    - src/store/active-conversation-store.ts (Plan 04 — `markFailed(messageId, "network" | "server")` → `errorKind`)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Copywriting Bubble-failed: network → "Bağlantı sorunu · Gönderilemedi. Tekrar dene"; server/HTTP (null/untyped dahil, WR-05) → "Gönderilemedi · Tekrar dene" jenerik; "Tekrar dene" aynı POST'u yeniden atar, mesaj metni korunur)
  </read_first>
  <action>
    `message-bubble.tsx` failed durumundaki copy'yi `message.errorKind`'a göre ayır: `errorKind === "network"` → "Bağlantı sorunu · Gönderilemedi. Tekrar dene"; aksi (server/null/untyped — WR-05 precedent) → jenerik "Gönderilemedi · Tekrar dene". `ExclamationTriangleIcon` prefix + tıklanabilir "Tekrar dene" aksiyonu (retry) korunur; mesaj metni asla temizlenmez (D-15). Hata detayı (`HttpError.body`/`status`) render EDİLMEZ — yalnız `errorKind` kategorisi (V7).
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Bağlantı sorunu" src/screens/components/message-bubble.tsx` ≥ 1 (network dalı)
    - `grep -c "Gönderilemedi" src/screens/components/message-bubble.tsx` ≥ 1 (server/jenerik dalı)
    - Copy `message.errorKind`'a göre dallanıyor; retry aksiyonu + korunan mesaj metni değişmedi
    - `grep -c "dangerouslySetInnerHTML" src/screens/components/message-bubble.tsx` == 0 (T-02-02 regresyon yok)
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Gönderim-hatası copy'si ağ/sunucu ayrımıyla Faz 1 CORE-03 diline uygun; hata detayı sızmıyor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Faz-sonu UAT — 5 success criteria canlı doğrulama</name>
  <files></files>
  <read_first>
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md (D-01..D-17 — beklenen davranışlar)
    - .planning/ROADMAP.md (Phase 2 — 5 Success Criteria)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-CONTEXT.md (D-16/17/18 — standalone dev modu + gerçek token ile canlı UAT düzeni; test parent TICKET-563)
    - src/lib/standalone-dev.ts (standalone dev aktivasyonu + REACT_APP_DEV_AGENT_EMAIL)
  </read_first>
  <what-built>
    Faz 2'nin tamamı: "+"/boş-CTA ile compose, alıcı autocomplete + serbest e-posta, prefilled/düzenlenebilir konu, mesaj + Gönder → side ticket + gerçek e-posta, optimistic chat balonu, SYNC-02 refetch, vazgeç/parent korumaları, hata/retry. Executor `npm start`'ı standalone dev modunda (gerçek token + REACT_APP_DEV_AGENT_EMAIL) hazır bırakır.
  </what-built>
  <how-to-verify>
    Davut, standalone dev modunda (localhost, gerçek gsocial-test token, parent `TICKET-563`) şunları test eder:
    1. **COMP-01:** Liste header "+" VE boş-durum CTA'sı compose ekranını açar; geri oku listeye döner (boş formda onaysız).
    2. **COMP-02:** Alıcı alanına 3+ karakter → ~300ms sonra müşteri sonuçları; isim+e-posta görünür; geçerli-ama-kayıtsız bir e-posta yazınca "✉ … adresini kullan" satırı belirir ve seçilebilir; geçersiz formatta uyarı + "kullan" satırı yok.
    3. **COMP-03:** Compose açılışında konu `[TICKET-563] <başlık>` dolu gelir, düzenlenebilir, üzerine yazılmaz.
    4. **COMP-04:** Alıcı + mesaj dolu → Gönder → optimistic balonla chat'e düşülür (sağ-altta spinner), POST çözülünce balon "sent"; **gerçek e-posta alıcıya gitti mi** (publicVisible:true — alıcı gelen kutusunu ya da Grispi'de oluşan side ticket'ı kontrol et).
    5. **SYNC-02:** Chat'ten listeye dönünce yeni görüşme GERÇEK veriyle (doğru rozet/zaman) görünür — sahte optimistic satır değil.
    6. **D-02:** Dolu formda geri → "Vazgeçilsin mi?" dialog'u; İptal kalır, Vazgeç listeye döner.
    7. **D-03:** Compose'da dolu taslakla aktif talebi değiştir → "Taslağın kaybolacak" dialog'u; boş taslakta sessizce kapanma.
    8. **D-15 (opsiyonel):** Ağı kısıtlayıp Gönder → balon "failed" + "Tekrar dene"; tekrar dene çalışır (Pitfall #5 çift-oluşturma riskini gözlemle — kabul edilmiş).
  </how-to-verify>
  <resume-signal>Her madde için "geçti/sorunlu" belirt. Sorun varsa açıklama ekle — gap-closure planı (`/gsd-plan-phase --gaps`) ile ele alınır. Hepsi geçtiyse "onaylandı" ile faz tamam.</resume-signal>
  <action>
    Executor: uygulamayı standalone dev modunda çalışır bırak (`npm start`, gerçek gsocial-test token + `REACT_APP_DEV_AGENT_EMAIL`, parent `TICKET-563`) ve `how-to-verify`'daki 8 maddelik UAT senaryosunu Davut'a sun. Davut madde-madde test edip sonuçları bildirir; sonuçları (geçti/sorunlu + notlar) SUMMARY'ye kaydet. Sorunlu maddeler gap-closure planına girer.
  </action>
  <verify>
    <human-check>Davut 8 UAT maddesini canlı test etti; özellikle COMP-04'te alıcıya gerçek e-posta gittiği (publicVisible:true) ve SYNC-02'de listenin gerçek refetch ile tazelendiği doğrulandı.</human-check>
  </verify>
  <done>5 phase success criteria (COMP-01..04, SYNC-02) + D-02/D-03/D-15 canlı UAT'ta doğrulandı; sonuçlar SUMMARY'ye kaydedildi.</done>
  <acceptance_criteria>
    - 8 UAT maddesinin her biri için açık geçti/sorunlu kaydı alınmış olmalı
    - Özellikle COMP-04 maddesinde alıcıya GERÇEK e-posta gittiği doğrulanmış olmalı (publicVisible:true davranışının kanıtı)
    - SYNC-02'de listenin gerçek refetch ile (optimistic sahte-satır değil) tazelendiği doğrulanmış olmalı
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK aktif-talep değişim event'i → compose taslak koruması | Parent değişimi client'ta tüketilir; taslak yanlış parent'a bağlanmadan korunur (D-03) |
| failed-bubble copy → kullanıcı | Hata kategorisi gösterilir, ham hata detayı gösterilmez |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | failed-bubble copy (network/server) | medium | mitigate | Yalnız `errorKind` kategorisi render edilir; `HttpError.body`/`status` asla UI/console'a (Faz 1 V7) |
| T-02-01 | Tampering | D-03 taslak koruması — parent değişince yanlış talebe bağlı taslak | medium | mitigate | `handleParentTicketChanged` dolu taslakta gönderimi durdurur (onay ister); onaysız yeni parent'a geçilmez |
| T-02-02 | Tampering / Information Disclosure | MessageBubble copy düzenlemesi XSS regresyonu | high | mitigate | Task 2 yalnız statik Türkçe copy ekler; `dangerouslySetInnerHTML` regen == 0 acceptance ile regresyon önlenir |

UAT notu: Faz-sonu UAT, güvenlik-kritik davranışı (publicVisible:true → gerçek harici e-posta) insan gözüyle doğrular — plugin'in çekirdek gizlilik/gönderim sözleşmesinin canlı kanıtı.
</threat_model>

<verification>
- `CI=true npx tsc --noEmit` hatasız
- `CI=true npm test -- --watchAll=false` tüm suite yeşil (regresyon yok)
- `grep -c "dangerouslySetInnerHTML" src/screens/components/message-bubble.tsx` == 0
- Faz-sonu UAT'ın 8 maddesi geçti (COMP-01..04 + SYNC-02 + D-02/D-03/D-15)
</verification>

<success_criteria>
- D-02/D-03 taslak-kayıp korumaları çalışır (dolu → dialog, boş → sessiz)
- D-15 hata copy'si ağ/sunucu ayrımıyla tutarlı
- 5 phase success criteria (COMP-01..04, SYNC-02) canlı UAT'ta geçti
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-06-SUMMARY.md` when done. UAT sonuçlarını (madde-madde geçti/sorunlu) SUMMARY'ye kaydet.
</output>
