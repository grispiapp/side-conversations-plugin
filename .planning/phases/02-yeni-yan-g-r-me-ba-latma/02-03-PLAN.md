---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/store/compose-store.ts
  - src/store/__tests__/compose-store.test.ts
  - src/store/root-store.ts
  - src/screens/components/recipient-field.tsx
  - src/screens/components/subject-field.tsx
  - src/screens/compose-screen.tsx
autonomous: true
requirements: [COMP-02, COMP-03]

must_haves:
  truths:
    - "Alıcı alanı 3+ karakterde ~300ms debounce ile müşteri araması yapar; generation-guard eski/yavaş cevabın yeni sonucu ezmesini önler (COMP-02 / D-04)"
    - "Geçerli ve eşleşmeyen bir e-posta yazılınca sonuçların altında '✉ <e-posta> adresini kullan' satırı belirir; tıklanınca alıcı olur (D-05)"
    - "Geçersiz formatlı serbest e-postada 'kullan' satırı ÇIKMAZ + inline uyarı gösterilir (D-06)"
    - "Konu alanı compose açılışında bir kez `[<TALEP_ANAHTARI>] <Talep Başlığı>` ile dolar, düzenlenebilir, otomatik üzerine yazılmaz (COMP-03 / D-08/D-09); boşaltılırsa non-blocking inline uyarı (D-10)"
  artifacts:
    - src/store/compose-store.ts
    - src/screens/components/recipient-field.tsx
    - src/screens/components/subject-field.tsx
  key_links:
    - "RecipientField → ComposeStore.setQuery → (debounce+generation-guard) → grispiAPI.customers.search"
    - "ComposeStore.selectRecipient → recipientEmail (formatRequesterField'a girecek) + recipientLabel (chat header'da kullanılacak)"
    - "ComposeScreen mount → ComposeStore.initSubject(formatPrefillSubject(ticket.key, ticket.subject)) bir kez (D-09)"
---

<objective>
Compose formunun ilk iki alanını teslim et: **alıcı otomatik-tamamlama** (COMP-02) ve **konu ön-doldurma** (COMP-03). `ComposeStore` bu planda oluşturulur — Faz 1'in generation-guard desenini debounce'lu müşteri aramasına uyarlar ve form durumunu (recipient, subject) tutar. Kullanıcıya görünür dilim artışı: temsilci compose'da isim/e-posta ile arayıp bir alıcı seçebilir (kayıtlı müşteri ya da serbest e-posta), ve talepten önceden dolu, düzenlenebilir bir konu görür.

Purpose: Mesaj + gönderim (Plan 04) bu form durumunun üstüne oturur; recipient/subject olmadan createTicket gövdesi kurulamaz.
Output: `ComposeStore` (arama + form state) + `RecipientField` (5 durumlu dropdown) + `SubjectField` (prefill).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/store/compose-store.ts` (YENİ): `ComposeStore` sınıfı, `CustomerVM`/arama VM tipi, alanlar `query`/`searchStatus`/`results`/`recipientEmail`/`recipientLabel`/`subject`/`subjectInitialized`, metotlar `setQuery`/`runSearch`/`selectRecipient`/`selectFreeEmail`/`setSubject`/`initSubject`, computed `showFreeEmailRow`
- `src/store/root-store.ts`: `RootStore.compose`
- `src/screens/components/recipient-field.tsx` (YENİ): `RecipientField`
- `src/screens/components/subject-field.tsx` (YENİ): `SubjectField`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComposeStore — debounce'lu/generation-guard'lı arama + form durumu</name>
  <files>src/store/compose-store.ts, src/store/__tests__/compose-store.test.ts, src/store/root-store.ts</files>
  <read_first>
    - src/store/side-conversations-store.ts (generation-guard deseni lines 232-300: `const gen = ++this.generation; ...await...; if (gen !== this.generation) return;`; error typing `err instanceof NetworkError || err instanceof HttpError ? err : null`; MobX kabuk lines 205-222; immutable array replacement dersi lines 443-450)
    - src/store/root-store.ts (Plan 02'de eklenen `panelNavigation` — `compose` aynı kalıpla eklenir)
    - src/lib/side-conversation.ts (Plan 01 — `isValidEmail`; free-email satır görünürlüğü buna dayanır)
    - src/grispi/client/api.ts (Plan 01 — `grispiAPI.customers.search`)
    - src/types/grispi.type.ts (Plan 01 probe — `Customer`/`CustomerSearchResponse` alan adları)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Pattern 1 — generation-guard'lı debounce, TAM adaptasyon; Anti-Patterns — debounce tek başına yetmez / iki ayrı regex yok; Validation Architecture — `jest.useFakeTimers()` ilk kez kullanılıyor, `afterEach` ile `jest.useRealTimers()` temizliği)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md (A2/A3 doğrulanmış zarf + kayıt alan adları)
  </read_first>
  <behavior>
    - Test: `setQuery("ab")` (<3 char) → `searchStatus === "idle"`, `results` boş, istek atılmaz
    - Test: `setQuery("acme")` → 300ms debounce sonrası tam bir `grispiAPI.customers.search` çağrısı (fake timers ile advance)
    - Test: hızlı `setQuery("acme")` sonra `setQuery("acme corp")` — eski aramanın geç gelen cevabı generation-guard ile YOK sayılır (yalnız son sorgunun sonucu `results`'a yazılır)
    - Test: arama 0 sonuç → `searchStatus === "no-results"`
    - Test: arama hata (NetworkError/HttpError) → `searchStatus === "no-results"` (jenerik, CORE-03 diliyle; error.body sızmaz)
    - Test: `showFreeEmailRow` — `query` geçerli e-posta (isValidEmail true) ve sonuçlarda birebir eşleşme yoksa `true`; geçersiz e-postada `false` (D-05/D-06)
    - Test: `selectRecipient(vm)` → `recipientEmail`/`recipientLabel` set olur (isim varsa isim, yoksa e-posta — D-06 fallback)
    - Test: `selectFreeEmail("x@y.com")` → `recipientEmail === "x@y.com"`, `recipientLabel === "x@y.com"`
    - Test: `initSubject("[K] T")` bir kez set eder; ikinci çağrı üzerine YAZMAZ (`subjectInitialized` guard, D-09); `setSubject` her zaman serbest günceller
  </behavior>
  <action>
    `compose-store.ts` (YENİ) — `ComposeStore` sınıfı, `makeAutoObservable(this)` + `constructor(rootStore: RootStore)` (side-conversations-store kabuğu). Arama alanları: `query = ""`, `searchStatus: "idle" | "loading" | "results" | "no-results" | "invalid" = "idle"`, `results: CustomerVM[] = []`, private `searchGeneration = 0`, private `debounceHandle`. `setQuery(value)`: `query=value`, önceki debounce'u temizle; `value.trim().length < 3` ise `searchStatus="idle"`+`results=[]`+return; aksi halde 300ms `setTimeout(() => this.runSearch(value), 300)`. `runSearch(term)`: `const gen = ++this.searchGeneration` → `runInAction` `searchStatus="loading"` → `grispiAPI.customers.search({ searchTerm: term, size: 10, page: 0 })` → `if (gen !== this.searchGeneration) return` (stale guard) → sonuçları `CustomerVM`'e map et (Plan 01 probe alan adlarıyla), `searchStatus = results.length ? "results" : "no-results"`; catch'te de generation-guard + jenerik `"no-results"` (error.body ASLA render/log). Form alanları: `recipientEmail=""`, `recipientLabel=""`, `subject=""`, private `subjectInitialized=false`. `selectRecipient(vm)`: `recipientEmail=vm.email`, `recipientLabel = vm.name || vm.email` (D-06 fallback). `selectFreeEmail(email)`: ikisini de `email` yap. computed `get showFreeEmailRow()`: `isValidEmail(this.query)` && sonuçlarda `email === this.query.trim()` yok → true. `setSubject(value)`: `subject=value`. `initSubject(value)`: `if (this.subjectInitialized) return; this.subject=value; this.subjectInitialized=true` (D-09). `CustomerVM` tipi: `{ id, name: string | null, email: string }` (probe alan adlarından türet). `root-store.ts`'e `compose: ComposeStore` + `new ComposeStore(this)`. Testler `compose-store.test.ts`'e (behavior case'leri), `jest.mock("@/grispi/client/api", ...)` + `jest.useFakeTimers()`/`afterEach(jest.useRealTimers)` ile.
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=compose-store</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/compose-store.ts` mevcut; `ComposeStore` + `setQuery`/`runSearch`/`selectRecipient`/`selectFreeEmail`/`setSubject`/`initSubject`/`showFreeEmailRow` içeriyor
    - `grep -c "searchGeneration" src/store/compose-store.ts` ≥ 2 (generation-guard uygulanmış: increment + karşılaştırma)
    - `grep -c "compose" src/store/root-store.ts` ≥ 2 (alan + instantiation)
    - compose-store testi yeşil (fake-timers debounce + generation-guard stale-race + showFreeEmailRow + initSubject-once dahil)
    - error dalı jenerik `no-results` set eder; `error.body`/`status` hiçbir yere yazılmaz (T-02-03)
  </acceptance_criteria>
  <done>Arama (debounce+generation-guard) ve form durumu deterministik ve testli; root-store'a bağlı.</done>
</task>

<task type="auto">
  <name>Task 2: RecipientField (5 durumlu dropdown) + SubjectField (prefill)</name>
  <files>src/screens/components/recipient-field.tsx, src/screens/components/subject-field.tsx</files>
  <read_first>
    - src/store/compose-store.ts (Task 1 — `query`/`searchStatus`/`results`/`showFreeEmailRow`/`setQuery`/`selectRecipient`/`selectFreeEmail`/`subject`/`setSubject`)
    - src/components/ui/input.tsx (metin girişi primitifi — RecipientField ve SubjectField `Input` kullanır)
    - src/screens/conversations-list-screen.tsx (observer + `useStore()` tek-yönlü bind kalıbı; store durumuna göre koşullu render lines 41-69)
    - src/screens/components/conversation-row.tsx (isim + `font-mono` e-posta yerleşimi; recipient satırının görsel yoğunluğu analog)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Recipient dropdown states 1-5: Idle/Loading/Results/Free-email/Invalid; Copywriting Contract — placeholder "İsim veya e-posta ile ara…", "Aranıyor…", "Sonuç bulunamadı", "{EnvelopeClosedIcon} <e-posta> adresini kullan", "Geçerli bir e-posta adresi girin."; SubjectField prefill/empty-warning "Konu boş — e-posta konusuz gönderilecek.")
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md (RecipientField — no combobox analog, plain `cn()`-styled `<div>` dropdown, Radix Popover REDDEDİLDİ; SubjectField — düz `Input` kullanımı)
  </read_first>
  <action>
    (a) `recipient-field.tsx` (YENİ) — `observer`-sarmalı `RecipientField`. `compose = useStore().compose`. `Input` (`onChange={e => compose.setQuery(e.target.value)}`, value `compose.query`, placeholder "İsim veya e-posta ile ara…"). Altında absolutely-positioned `bg-card border rounded-md shadow` dropdown paneli (genişlik input'la aynı, taşma yok — 372px kısıtı). Durumlar (UI-SPEC state machine): `query.trim().length < 3` → panel yok (Idle); `searchStatus==="loading"` → "Aranıyor…" (muted, Label); `searchStatus==="results"` → her satır isim (Body 400) + e-posta (`font-mono` Label muted), isim yoksa yalnız e-posta (D-06 fallback); satır `onClick` → `compose.selectRecipient(vm)`; `searchStatus==="no-results" && !showFreeEmailRow && !isValidEmail(query)` → geçersizde "Geçerli bir e-posta adresi girin." (destructive Label, D-06), aksi boş sonuçta "Sonuç bulunamadı"; `compose.showFreeEmailRow` → sonuçların ALTINDA `border-t` ile ayrılmış, `EnvelopeClosedIcon` + "`<query>` adresini kullan" (primary renk, D-05), `onClick` → `compose.selectFreeEmail(compose.query.trim())`. Seçili alıcı varsa (`recipientLabel`) input yerine seçili göster ya da input'un yanında rozet — planner discretion (UI-SPEC Copywriting'e sadık). Sonuç satırları klavye-gezinilebilir (ok tuşları + Enter) baseline a11y.
    (b) `subject-field.tsx` (YENİ) — `observer`-sarmalı `SubjectField`. `Input` value `compose.subject`, `onChange` → `compose.setSubject(...)` (serbest düzenlenebilir, D-08). Prefill'in kendisi ComposeScreen mount'unda `initSubject` ile yapılır (Task 3) — bu component yalnızca gösterir/düzenler. `compose.subject.trim() === ""` ise altında non-blocking muted uyarı "Konu boş — e-posta konusuz gönderilecek." (D-10 — Gönder'i engellemez).
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/components/recipient-field.tsx` ve `src/screens/components/subject-field.tsx` mevcut, ikisi de `observer` sarmalı ve `useStore().compose`'u okuyor
    - RecipientField beş durumu da render ediyor: idle (panel yok) / "Aranıyor…" / sonuç satırları / "kullan" satırı (EnvelopeClosedIcon) / "Geçerli bir e-posta adresi girin."
    - `grep -c "adresini kullan" src/screens/components/recipient-field.tsx` ≥ 1 (D-05 free-email satırı)
    - SubjectField `compose.subject`'i düzenlenebilir gösterir + boş-uyarı metni içerir
    - `CI=true npx tsc --noEmit` hatasız (component'ler store'a doğru bağlı)
  </acceptance_criteria>
  <done>Alıcı dropdown'u 5 durumu ve serbest-e-posta akışını (D-04/05/06/07) render ediyor; konu alanı düzenlenebilir + boş-uyarılı.</done>
</task>

<task type="auto">
  <name>Task 3: ComposeScreen'e alan yerleşimi + konu prefill bağlama (D-08/D-09)</name>
  <files>src/screens/compose-screen.tsx</files>
  <read_first>
    - src/screens/compose-screen.tsx (Plan 02 boş kabuk — `ScreenContent` içindeki `flex flex-col gap-2 p-4` div'e alanlar yerleşecek)
    - src/screens/components/recipient-field.tsx, src/screens/components/subject-field.tsx (Task 2)
    - src/store/compose-store.ts (Task 1 — `initSubject`)
    - src/lib/side-conversation.ts (Plan 01 — `formatPrefillSubject`)
    - src/contexts/grispi-context.tsx (aktif `ticket` — `ticket.key` + başlık kaynağı; ConversationsListScreen'deki `useGrispi().ticket` + `useEffect([ticket?.key])` köprüleme deseni)
    - src/screens/conversations-list-screen.tsx (mount-effect köprüleme deseni lines 23-28)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Compose screen anatomy — RecipientField → SubjectField → (MessageField Plan 04) → Gönder; `md` 16px boşluklar)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md (talep başlığı kaynağı: `advancedSearch` özetinde `summary.subject` var ama tam ticket'ta `ts.subject` YOK — parent'ın başlığı için doğru kaynak neyse ona bak; Pitfall #6)
  </read_first>
  <action>
    `compose-screen.tsx`'i güncelle: `ScreenContent` içine sırayla `<RecipientField />` ve `<SubjectField />` yerleştir (`md`/16px dikey ritim; mesaj alanı + Gönder butonu Plan 04'te eklenecek — yer bırak). Mount'ta konu prefill'ini bir kez tetikle: `useEffect(() => { if (ticket?.key) compose.initSubject(formatPrefillSubject(ticket.key, <parent talep başlığı>)); }, [ticket?.key])` (ConversationsListScreen'in `[ticket?.key]` effect köprüleme deseniyle aynı; `initSubject` `subjectInitialized` guard'ıyla üzerine yazmayı önler — D-09). Parent talep başlığının kaynağı: `useGrispi().ticket`'ten türet (tam ticket'ta `ts.subject` yok — Pitfall #6; başlık için ticket objesinde mevcut olan alanı kullan, yoksa yalnızca `[<KEY>]` ile prefill ve boş-başlık toleransı). `compose = useStore().compose`, `ticket = useGrispi().ticket`.
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - compose-screen `<RecipientField />` ve `<SubjectField />` render ediyor
    - `grep -c "initSubject" src/screens/compose-screen.tsx` ≥ 1 ve `formatPrefillSubject` import edilip çağrılıyor
    - Prefill mount effect'i `[ticket?.key]` bağımlılığıyla bir kez tetikleniyor (üzerine-yazma yok — D-09)
    - `CI=true npx tsc --noEmit` hatasız
    - observable (Plan 05 UAT): compose açılınca konu `[<TALEP_ANAHTARI>] <Başlık>` dolu gelir, düzenlenebilir; temsilci alıcı arayıp seçebilir (COMP-02/COMP-03)
  </acceptance_criteria>
  <done>Compose formu recipient + subject alanlarını gösteriyor; konu talep anahtarı+başlığıyla bir kez prefill ediliyor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temsilci girdisi → alıcı/konu | Serbest metin (arama sorgusu, serbest e-posta, konu) client'ta işlenir; alıcı e-postası `ts.requester`'a, konu `ts.subject`'e gidecek |
| plugin → /customers/search | Arama terimi Grispi API'ye query param olarak gider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | SubjectField (`<Input type="text">`) — e-posta header injection karakterleri | low | mitigate | Tek-satır `<input type="text">` doğası gereği çok-satır girişini engeller (defense-in-depth); asıl sanitizasyon Grispi backend sorumluluğu |
| T-02-06 | Information Disclosure | Konu prefill'i iç talep anahtarını (D-08) dış e-posta konusuna taşır | low | accept | D-08 kilitli; temsilci konuyu düzenleyebilir (kontrolünde) |
| T-02-07 | Repudiation | Serbest e-posta girişi → arbitrer adrese görüşme | medium | mitigate | Tek-kaynak `isValidEmail` UX kapısı (D-06); geçersiz formatta "kullan" satırı çıkmaz + Gönder engellenir (Plan 04); tek alıcı |
| T-02-03 | Information Disclosure | Arama hata cevabı (`HttpError.body`) UI'ya sızması | medium | mitigate | Arama catch dalı jenerik `no-results` set eder; `error.body`/`status` asla render/log edilmez (Faz 1 V7) |

Not: `high`+ severity tehdit yok → block-on high tetiklenmez.
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false --testPathPattern=compose-store` yeşil
- `CI=true npx tsc --noEmit` hatasız
</verification>

<success_criteria>
- COMP-02: alıcı alanı müşteri aramasıyla otomatik tamamlanır; geçerli serbest e-posta "kullan" satırıyla girilebilir; geçersizde uyarı
- COMP-03: konu `[<KEY>] <Başlık>` ile bir kez prefill, düzenlenebilir, üzerine yazılmaz
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-03-SUMMARY.md` when done.
</output>
