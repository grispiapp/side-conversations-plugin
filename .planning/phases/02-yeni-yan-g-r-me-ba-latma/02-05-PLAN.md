---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - src/screens/components/message-bubble.tsx
  - src/screens/chat-screen.tsx
  - src/screens/compose-screen.tsx
autonomous: true
requirements: [COMP-04]

must_haves:
  truths:
    - "Gönderim sonrası temsilci minimal chat görünümüne düşer: başlık (alıcı · konu) + gönderdiği mesaj balonu; balon pending'de sağ-altta optimistic spinner gösterir, sent'te spinner kaybolur (COMP-04 / D-14)"
    - "Hata durumunda balonda mesaj metni korunur + 'Gönderilemedi · Tekrar dene' aksiyonu; tıklanınca ActiveConversationStore.retry ile aynı POST yeniden atılır (D-15)"
    - "Chat başlığı sunucudan OKUNMAZ — compose'da bilinen recipientLabel/subject client-side echo'sudur (Pitfall #6)"
    - "Gönder butonu; alıcı seçili değilse VEYA mesaj boşsa VEYA alıcı alanı geçersiz-e-posta uyarısındaysa devre dışıdır; konu boşluğu Gönder'i engellemez (D-06/D-10)"
  artifacts:
    - src/screens/components/message-bubble.tsx
    - src/screens/chat-screen.tsx
  key_links:
    - "ChatScreen → ActiveConversationStore.messages'ı MessageBubble listesi olarak render eder; başlık recipientLabel·subject"
    - "ComposeScreen Gönder → compose.submit(useGrispi().agentEmail, useGrispi().ticket.key) → panelNavigation.openChat"
    - "MessageBubble failed → ActiveConversationStore.retry(messageId)"
---

<objective>
Happy path'i uçtan uca bağla (COMP-04): gönderilen mesajın görsel karşılığı olan `MessageBubble` (optimistic pending/sent/failed durum makinesi, D-14/D-15), inilecek minimal `ChatScreen` kabuğu ve compose'daki `Gönder` butonu. Bu plandan sonra temsilci gerçekten: alıcı seçer → konu/mesaj yazar → Gönder'e basar → side ticket oluşur, alıcıya e-posta gider → optimistic balonla chat'e düşer → balon POST cevabıyla çözülür.

Purpose: Plan 04'ün mantık katmanını kullanıcıya görünür hale getiren görsel dilim — phase'in çekirdek user story'sinin tamamlandığı nokta. Güvenlik açısından kritik: harici/güvenilmeyen mesaj gövdesi ilk kez RENDER edilir (XSS yüzeyi).
Output: `MessageBubble` + `ChatScreen` (gerçek) + `Gönder` butonu; tam happy path çalışır.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/screens/components/message-bubble.tsx` (YENİ): `MessageBubble` + `bubbleVariants` (cva `direction` varyantı)
- `src/screens/chat-screen.tsx`: gerçek minimal chat kabuğu (Plan 02 stub'ının yerini alır)
- `src/screens/compose-screen.tsx`: `Gönder` butonu + submit wiring
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: MessageBubble — cva own varyantı + pending/sent/failed durum makinesi (D-14/D-15)</name>
  <files>src/screens/components/message-bubble.tsx</files>
  <read_first>
    - src/components/ui/badge.tsx (cva + `VariantProps` + `cn()` export şekli — MessageBubble'ın `direction` varyantı bu kalıpta)
    - src/components/ui/button.tsx (cva ikinci referansı; variant/size şekli)
    - src/store/active-conversation-store.ts (Plan 04 — `MessageVM` {id, direction, body, status, createdAt, errorKind}; `retry(messageId)`)
    - src/screens/components/error-card.tsx (hata görsel dili: `ExclamationTriangleIcon` + destructive renk + Türkçe copy; error.body ASLA render — V7)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Bubble state colors tablosu: pending `bg-primary` + `ReloadIcon animate-spin` sağ-alt; sent spinner kaybolur; failed mesaj metni kalır + `ExclamationTriangleIcon` + "Gönderilemedi · Tekrar dene"; Assumption: emoji glyph yerine icon component; own varyant `ml-auto bg-primary text-primary-foreground rounded-lg` + `rounded-br-sm`)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Don't Hand-Roll — yorum gövdesi React text interpolation ile render, ham-HTML enjeksiyonu YASAK, Faz 1 T-01; Security Domain — XSS mitigation)
  </read_first>
  <action>
    `message-bubble.tsx` (YENİ) — `badge.tsx` kalıbında `bubbleVariants = cva("rounded-lg px-3 py-2 text-sm max-w-[85%]", { variants: { direction: { own: "ml-auto bg-primary text-primary-foreground rounded-br-sm", incoming: "mr-auto bg-card" } }, defaultVariants: { direction: "own" } })` (incoming = Faz 3, bu fazda kullanılmaz — seam). `MessageBubble` prop: `message: MessageVM` + `onRetry: (id: string) => void`. Mesaj gövdesini React child interpolation ile render et (`{message.body}`) — ham-HTML enjeksiyon prop'u KULLANMA (harici/güvenilmeyen içerik asla HTML olarak yorumlanmaz, Faz 1 T-01). Durumlar: `status==="pending"` → balonun sağ-altında `ReloadIcon` (`animate-spin`, ~12px, `text-primary-foreground/70`); `status==="sent"` → spinner yok (ek checkmark yok — icon bütçesi minimal); `status==="failed"` → balon metni KORUNUR (renk kırmızıya çevrilmez, D-15), altında aynı container'da `ExclamationTriangleIcon` + tıklanabilir "Gönderilemedi · Tekrar dene" satırı → `onClick` `onRetry(message.id)` (D-15). Copy bu planda temel formda ("Gönderilemedi · Tekrar dene"); network/server ince ayrımı Plan 06'da `errorKind`'a göre eklenir.
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/components/message-bubble.tsx` mevcut; `bubbleVariants` (cva) + `MessageBubble` export ediyor
    - `grep -c "dangerouslySetInnerHTML" src/screens/components/message-bubble.tsx` == 0 (T-02-02 XSS — mesaj gövdesi yalnızca React text interpolation ile render edilir)
    - `grep -c "animate-spin" src/screens/components/message-bubble.tsx` ≥ 1 (pending optimistic spinner, D-14)
    - `grep -c "Tekrar dene" src/screens/components/message-bubble.tsx` ≥ 1 (failed retry aksiyonu, D-15)
    - Üç durum (pending/sent/failed) ayrı render edilir; failed'de mesaj metni korunur (kırmızıya boyanmaz)
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Optimistic balon üç durumu (D-14/D-15) render eder, gövde XSS-güvenli interpolation'la basılır, failed retry'ı tetikler.</done>
</task>

<task type="auto">
  <name>Task 2: ChatScreen — gerçek minimal kabuk (başlık alıcı·konu + balon listesi)</name>
  <files>src/screens/chat-screen.tsx</files>
  <read_first>
    - src/screens/chat-screen.tsx (Plan 02 stub — gerçek içerikle değişecek)
    - src/screens/conversations-list-screen.tsx (Screen/ScreenHeader/ScreenContent + observer + store-driven koşullu render kalıbı)
    - src/store/active-conversation-store.ts (Plan 04 — `messages`/`recipientLabel`/`subject`/`retry`)
    - src/store/panel-navigation-store.ts (Plan 02 — `confirmDiscardAndReturnToList` / listeye dönüş)
    - src/screens/components/message-bubble.tsx (Task 1)
    - src/screens/components/conversation-row.tsx (`·` meta-separatör + `line-clamp-1` truncation konvansiyonu — chat header aynı)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Chat screen anatomy: ScreenHeader onBack→list, başlık "{recipient} · {subject}" truncated; ScreenContent `lg` 24px top padding; bubble list container GENERIC — Faz 3 tam thread'i buraya asacak, "tam bir çocuk" hardcode etme; Pitfall #6 — başlık sunucudan okunmaz)
  </read_first>
  <action>
    `chat-screen.tsx`'i gerçek minimal kabukla değiştir: `observer`-sarmalı `ChatScreen`. `activeConversation = useStore().activeConversation`, `panelNav = useStore().panelNavigation`. `Screen` > `ScreenHeader` (`onBack` → listeye dönüş: `panelNav`'ın liste-dönüş metodu; başlık `{recipientLabel} · {subject}`, `line-clamp-1` truncated — `·` separatör konvansiyonu ConversationRow ile aynı; başlık ActiveConversationStore'dan, sunucudan DEĞİL — Pitfall #6) > `ScreenContent` `lg`/24px top padding içinde GENERIC dikey flex liste: `activeConversation.messages.map(m => <MessageBubble key={m.id} message={m} onRetry={id => activeConversation.retry(id)} />)`. Container "tam bir çocuk" değil, liste (Faz 3 gelen mesajları buraya ekleyecek — seam korunur). Listeye dönerken SYNC-02 refetch zaten Plan 04 submit'inde `sideConversations.load` ile tetiklendi; back yalnızca `panelNavigation.screen`'i `list`'e çeker.
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - chat-screen `observer` sarmalı; `activeConversation.messages`'ı `MessageBubble` listesi olarak render ediyor
    - Başlık `recipientLabel` + `·` + `subject` gösteriyor, `line-clamp-1` truncated; sunucudan yeniden fetch YOK (`grep -c "getTicket" src/screens/chat-screen.tsx` == 0 — Pitfall #6)
    - ScreenHeader `onBack` listeye döndürüyor (panelNavigation)
    - Bubble list container generic bir liste (tek-çocuk hardcode değil — Faz 3 seam)
    - `CI=true npx tsc --noEmit` hatasız
  </acceptance_criteria>
  <done>Gerçek minimal chat kabuğu: başlık client-echo (alıcı·konu), balon listesi, listeye dönüş; Faz 3 thread'inin bağlanacağı generic liste seam'i.</done>
</task>

<task type="auto">
  <name>Task 3: ComposeScreen Gönder butonu + submit wiring (happy path tamamlanır)</name>
  <files>src/screens/compose-screen.tsx</files>
  <read_first>
    - src/screens/compose-screen.tsx (Plan 03/04 — RecipientField/SubjectField/MessageField yerleşik; Gönder butonu eklenecek)
    - src/store/compose-store.ts (Plan 04 — `submit(agentEmail, parentKey)`, `submitting`, `recipientEmail`, `message`, `showFreeEmailRow`/arama durumu — disabled mantığı için)
    - src/contexts/grispi-context.tsx (`useGrispi()` — `agentEmail`, `ticket?.key`; submit'e geçilecek trusted değerler)
    - src/components/ui/button.tsx (`default` (primary) variant, full-width; `disabled` prop)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Compose anatomy — "Gönder" full-width primary, ekran altına pinned bottom action bar (`border-t`, `bg-card`, sticky); Copywriting — disabled koşulları: alıcı seçili değil VEYA mesaj boş VEYA alıcı geçersiz-e-posta uyarısında; konu boşluğu engellemez D-10)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md (D-10 zorunlu alıcı+mesaj, D-12 açık Gönder butonu, D-17 çift-gönderim kilidi)
  </read_first>
  <action>
    `compose-screen.tsx`'e alanların altına full-width `Button` (`default` variant, "Gönder") ekle — UI-SPEC'e göre ekran altına pinned bottom action bar (`border-t bg-card`, sticky within screen, textarea büyürken erişilebilir kalır). `onClick={() => compose.submit(agentEmail, ticket?.key ?? "")}` — `agentEmail`/`ticket` `useGrispi()`'den (Plan 04'te submit imzası bu iki trusted değeri parametre alıyor). `disabled`: `!compose.recipientEmail || compose.message.trim() === "" || compose.submitting || <alıcı alanı geçersiz-e-posta durumundaysa>` (D-06/D-10/D-17). Konu boşluğu disabled'a KATILMAZ (D-10). MessageField'ın Shift+Enter'ı da aynı `compose.submit`'i çağırdığı için guard store içinde merkezi (Plan 04 Pattern 3) — buton yalnızca ikinci giriş noktası.
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - compose-screen full-width "Gönder" `Button` render ediyor, `onClick` → `compose.submit(agentEmail, ticket?.key)` (trusted context değerleriyle)
    - `grep -c "Gönder" src/screens/compose-screen.tsx` ≥ 1
    - Buton `disabled` koşulu: alıcı yok VEYA mesaj boş VEYA submitting VEYA geçersiz-e-posta durumu (konu boşluğu HARİÇ — D-10)
    - `CI=true npx tsc --noEmit` hatasız
    - observable (Plan 06 UAT): alıcı+mesaj dolu → Gönder aktif → basınca side ticket oluşur, alıcıya e-posta gider, temsilci optimistic balonla chat'e düşer, POST çözülünce balon sent olur, listeye dönünce yeni görüşme gerçek veriyle görünür (COMP-04 + SYNC-02 uçtan uca)
  </acceptance_criteria>
  <done>Gönder butonu submit'i trusted context değerleriyle çağırır; happy path uçtan uca çalışır.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActiveConversationStore (güvenilmeyen mesaj gövdesi) → DOM render | Mesaj gövdesi (temsilci + Faz 3'te harici taraf) ilk kez ekrana basılır — XSS yüzeyi |
| React context (trusted) → Gönder → submit | `agentEmail`/`parentKey` yalnızca `useGrispi()`'den submit'e geçer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering / Information Disclosure | MessageBubble / ChatScreen — mesaj gövdesinin HTML olarak render'ı (XSS) | high | mitigate | Mesaj gövdesi YALNIZCA React text interpolation ile render edilir; ham-HTML enjeksiyon API'si kullanılmaz (Faz 1 T-01 kararının Faz 2'ye taşınması). Acceptance: `dangerouslySetInnerHTML` grep == 0 |
| T-02-03 | Information Disclosure | Failed-bubble copy'sinin `HttpError.body`/`status` sızdırması | medium | mitigate | Copy jenerik ("Gönderilemedi · Tekrar dene"); `errorKind` (network/server) dışında hata detayı render/log EDİLMEZ (V7) |
| T-02-01 | Spoofing / Tampering | Gönder → submit parent linkage | medium | mitigate | `parentKey` `useGrispi().ticket?.key`'den (trusted); form alanından türetilmez |

Not: T-02-02 `high` severity → block-on high kapsamında. Mitigation zorunlu ve acceptance grep gate'iyle doğrulanır (`dangerouslySetInnerHTML` == 0). Plan checker bu planı XSS mitigation'ı olmadan geçirmemeli.
</threat_model>

<verification>
- `CI=true npx tsc --noEmit` hatasız
- `grep -c "dangerouslySetInnerHTML" src/screens/components/message-bubble.tsx` == 0 ve aynı kontrol `src/screens/chat-screen.tsx` için 0
- Plan 04 store testleri hâlâ yeşil (regresyon yok): `CI=true npm test -- --watchAll=false`
</verification>

<success_criteria>
- COMP-04 (görsel + uçtan uca): temsilci Gönder ile side ticket oluşturur, alıcıya e-posta gider, optimistic balonla chat'e düşer, balon çözülür
- MessageBubble üç durumu (pending/sent/failed + retry) XSS-güvenli render eder
- ChatScreen başlığı client-echo (Pitfall #6), balon listesi Faz 3 seam'iyle generic
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-05-SUMMARY.md` when done.
</output>
