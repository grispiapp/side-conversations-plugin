---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/standalone-dev.ts
  - src/contexts/grispi-context.tsx
  - src/contexts/plugin-bootstrap.ts
  - src/contexts/__tests__/plugin-bootstrap.test.ts
  - src/store/panel-navigation-store.ts
  - src/store/__tests__/panel-navigation-store.test.ts
  - src/store/root-store.ts
  - src/app.tsx
  - src/screens/compose-screen.tsx
  - src/screens/chat-screen.tsx
  - src/screens/components/empty-state.tsx
  - src/screens/conversations-list-screen.tsx
autonomous: true
requirements: [COMP-01, COMP-04]
user_setup:
  - service: grispi-standalone-dev
    why: "Standalone dev modunda (localhost UAT) agent kimliği SDK'dan gelmez; createTicket'in creator alanı için REACT_APP_DEV_AGENT_EMAIL fallback'i gerekir (RESEARCH Pitfall #3)."
    env_vars:
      - name: REACT_APP_DEV_AGENT_EMAIL
        source: ".env.development.local — Davut'un Grispi hesabının e-postası (ör. davutkmbr@gmail.com); yalnızca NODE_ENV=development'ta okunur"

must_haves:
  truths:
    - "Temsilci liste header'ındaki '+' ve boş-durum CTA'sı ile compose ekranını açar; ScreenHeader geri oku ile listeye döner (COMP-01 / D-01)"
    - "Plugin modunda `bundle.context.agent.email`, standalone dev modunda `REACT_APP_DEV_AGENT_EMAIL`, `GrispiContext.agentEmail`'e yazılır (createTicket creator'ının kaynağı — D-13)"
    - "PanelNavigationStore `list`/`compose`/`chat` arası geçişi ve D-02/D-03 dirty-guard dönüşlerini deterministik yönetir"
  artifacts:
    - src/store/panel-navigation-store.ts
    - src/screens/compose-screen.tsx
    - src/screens/chat-screen.tsx
  key_links:
    - "app.tsx `panelNavigation.screen`'e göre list/compose/chat koşullu render eder (observer)"
    - "empty-state.tsx + conversations-list-screen.tsx '+' → `panelNavigation.openCompose()`"
    - "plugin-bootstrap.ts `bundle.context.agent?.email` → `deps.setAgentEmail(...)` → grispi-context `agentEmail` state"
---

<objective>
İki HIGH-confidence RESEARCH boşluğunu kapat ve COMP-01'i teslim et: (1) **navigasyon durum makinesi** (bugün `app.tsx` sabit `<ConversationsListScreen/>` render ediyor — list/compose/chat geçişi HİÇ yok) `PanelNavigationStore` ile ilk kez kurulur; (2) **agent kimliği** (`bundle.context.agent.email` hiç tüketilmiyor) `GrispiContext.agentEmail`'e bağlanır + standalone-dev fallback env var eklenir. Bu planın kullanıcıya görünür dilimi: temsilci "+"/boş-CTA ile compose kabuğunu açar ve geri oku ile listeye döner.

Purpose: Her sonraki dilim (alıcı/konu/mesaj/gönder) bu iki seam üzerine oturur — createTicket'in zorunlu `creator` alanı `agentEmail` olmadan çalışamaz, ekran geçişi olmadan compose görünemez.
Output: Çalışan navigasyon seam'i + agent-email kaynağı + boş compose ekran kabuğu (COMP-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli):
- `src/lib/standalone-dev.ts`: `StandaloneDevConfig.agentEmail` alanı, `REACT_APP_DEV_AGENT_EMAIL` env okuması, `DEFAULT_DEV_AGENT_EMAIL` sabiti
- `src/contexts/grispi-context.tsx`: `GrispiContextType.agentEmail`, `agentEmail` state
- `src/contexts/plugin-bootstrap.ts`: `BootstrapPluginInitDeps.setAgentEmail`
- `src/store/panel-navigation-store.ts` (YENİ): `PanelScreen` tipi, `PanelNavigationStore` sınıfı (`openCompose`, `requestBack`, `confirmDiscardAndReturnToList`, `handleParentTicketChanged`, `openChat`)
- `src/store/root-store.ts`: `RootStore.panelNavigation`
- `src/screens/compose-screen.tsx` (YENİ): `ComposeScreen` (kabuk)
- `src/screens/chat-screen.tsx` (YENİ): `ChatScreen` (kabuk stub)
- `src/app.tsx`: `panelNavigation.screen`'e göre koşullu render
- `src/screens/components/empty-state.tsx`: enabled "+" CTA
- `src/screens/conversations-list-screen.tsx`: header "+" butonu
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Agent kimliği — GrispiContext.agentEmail + plugin-bootstrap + standalone-dev fallback</name>
  <files>src/lib/standalone-dev.ts, src/contexts/grispi-context.tsx, src/contexts/plugin-bootstrap.ts, src/contexts/__tests__/plugin-bootstrap.test.ts</files>
  <read_first>
    - src/contexts/grispi-context.tsx (mevcut `ticket`/`settings`/`loading` state kalıbı + `<GrispiContext.Provider value={{...}}>` threading; standalone branch lines 130-145, plugin branch bootstrapPluginInit çağrısı lines 169-175)
    - src/contexts/plugin-bootstrap.ts (mevcut `BootstrapPluginInitDeps` + `bootstrapPluginInit` — `deps.setSettings`/`deps.setLoading` kalıbı; `bundle.context` tüketimi lines 45-49)
    - src/lib/standalone-dev.ts (mevcut `StandaloneDevConfig` + `resolveStandaloneDevConfig` — `.trim()` + fallback stili, `DEFAULT_DEV_*` sabitleri)
    - src/types/grispi.type.ts (mevcut `Agent { id, fullName, email, phone }` ve `Context.agent` tipleri — zaten tanımlı, tüketilmiyor)
    - src/contexts/__tests__/plugin-bootstrap.test.ts (mevcut inject-fake-deps test konvansiyonu — her dep için fake enjekte, çağrıları assert)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Common Pitfalls #3 — creator zorunlu, agentEmail hiçbir yerde tutulmuyor; Environment Availability — standalone'da SDK agent yok)
  </read_first>
  <action>
    (a) `standalone-dev.ts`: `StandaloneDevConfig` interface'ine `agentEmail: string` ekle; `DEFAULT_DEV_AGENT_EMAIL` sabiti tanımla (Davut'un test hesabı e-postası, ör. `"davutkmbr@gmail.com"`); `resolveStandaloneDevConfig`'te `env.REACT_APP_DEV_AGENT_EMAIL?.trim() || DEFAULT_DEV_AGENT_EMAIL` ile `agentEmail`'i çöz (mevcut `tenantId` çözümüyle aynı `.trim()`+fallback stili) ve dönen objeye ekle.
    (b) `grispi-context.tsx`: `GrispiContextType`'a `agentEmail: string | null` ekle; `ticket`/`settings` yanında `const [agentEmail, setAgentEmail] = useState<string | null>(null)` ekle; standalone branch'te `setAgentEmail(standaloneConfig.agentEmail)`; `<GrispiContext.Provider value={{...}}>` objesine `agentEmail`'i thread et (mevcut `settings`/`loading` gibi).
    (c) `plugin-bootstrap.ts`: `BootstrapPluginInitDeps`'e `setAgentEmail(email: string | null): void` ekle; `bootstrapPluginInit` try bloğunda `deps.setSettings(...)` yanına `deps.setAgentEmail(bundle.context.agent?.email ?? null)` ekle (mevcut defensive optional-chaining stiliyle). grispi-context'in plugin branch'inde `bootstrapPluginInit({..., setAgentEmail})` çağrısına `setAgentEmail`'i geç.
    (d) `plugin-bootstrap.test.ts`: mevcut testlere `setAgentEmail` fake'ini ekle ve başarılı bootstrap'ta `bundle.context.agent.email` ile çağrıldığını, agent yoksa `null` ile çağrıldığını assert et (mevcut inject-fake-deps kalıbı).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=plugin-bootstrap</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "agentEmail" src/contexts/grispi-context.tsx` ≥ 2 (tip + state + provider threading)
    - `grep -c "setAgentEmail" src/contexts/plugin-bootstrap.ts` ≥ 2 (dep tipi + çağrı)
    - `grep -c "REACT_APP_DEV_AGENT_EMAIL" src/lib/standalone-dev.ts` ≥ 1 ve `agentEmail` `StandaloneDevConfig`'te
    - plugin-bootstrap testi yeşil: başarılı bootstrap'ta `setAgentEmail`, `bundle.context.agent.email` ile; agent yoksa `null` ile çağrılıyor
    - observable: standalone dev modunda `useGrispi().agentEmail` non-null (env fallback ile) — createTicket creator'ı Plan 04'te bunu okuyabilir
  </acceptance_criteria>
  <done>agentEmail, hem plugin (bundle) hem standalone (env fallback) yolunda GrispiContext'e yazılıyor; test yeşil.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PanelNavigationStore — list/compose/chat durum makinesi + D-02/D-03 dirty-guard</name>
  <files>src/store/panel-navigation-store.ts, src/store/__tests__/panel-navigation-store.test.ts, src/store/root-store.ts</files>
  <read_first>
    - src/store/side-conversations-store.ts (MobX kabuk kalıbı: `makeAutoObservable(this)` + `constructor(rootStore: RootStore)` + `this.rootStore = rootStore`; generation-guard BURADA GEREKMEZ — bu store'da eşzamanlı fetch yok)
    - src/store/root-store.ts (mevcut store composition — her store `rootStore` alır; yeni `panelNavigation` alanı eklenir)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Pattern 5 — önerilen concrete implementation; `handleParentTicketChanged` dönüş union'ı `"closed-silently" | "needs-confirm" | "no-op"`)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md (D-01 screen-swap, D-02 dolu-formda-onaylı-vazgeç/boş-formda-direkt, D-03 parent değişince taslak koruması)
  </read_first>
  <behavior>
    - Test: başlangıç `screen === "list"`
    - Test: `openCompose()` → `screen === "compose"`
    - Test: `openChat()` → `screen === "chat"`
    - Test: `requestBack(false)` → `false` döner ve `screen === "list"` (boş form direkt döner, D-02)
    - Test: `requestBack(true)` → `true` döner ve `screen` DEĞİŞMEZ (dolu form confirm gerektirir, D-02)
    - Test: `confirmDiscardAndReturnToList()` → `screen === "list"`
    - Test: compose'dayken `handleParentTicketChanged(false)` → `"closed-silently"` ve `screen === "list"` (D-03 boş taslak sessizce kapanır)
    - Test: compose'dayken `handleParentTicketChanged(true)` → `"needs-confirm"` ve `screen === "compose"` (kalır)
    - Test: compose'da DEĞİLKEN `handleParentTicketChanged(anything)` → `"no-op"` ve `screen` değişmez
  </behavior>
  <action>
    `panel-navigation-store.ts` (YENİ) oluştur: `type PanelScreen = "list" | "compose" | "chat"`; `PanelNavigationStore` sınıfı — `makeAutoObservable(this)` + `constructor(rootStore: RootStore)` (side-conversations-store kabuğuyla aynı). Alan: `screen: PanelScreen = "list"`. Metotlar: `openCompose()` → `screen="compose"`; `openChat()` → `screen="chat"`; `requestBack(isDirty: boolean): boolean` → `isDirty` ise `true` döndür (çağıran ConfirmDialog açar, D-02) ve screen'i DEĞİŞTİRME, değilse `screen="list"` yapıp `false` döndür; `confirmDiscardAndReturnToList()` → `screen="list"`; `handleParentTicketChanged(isDirty: boolean): "closed-silently" | "needs-confirm" | "no-op"` → `screen !== "compose"` ise `"no-op"`; dirty değilse `screen="list"` + `"closed-silently"`; dirty ise `"needs-confirm"` (screen kalır, çağıran D-03 copy'siyle dialog açar). `root-store.ts`'e `panelNavigation: PanelNavigationStore` alanı + constructor'da `new PanelNavigationStore(this)` ekle. Testleri behavior bloğundaki case'lerle `panel-navigation-store.test.ts`'e yaz (RootStore üzerinden veya doğrudan `new PanelNavigationStore({} as RootStore)` ile).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=panel-navigation-store</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/panel-navigation-store.ts` mevcut; `PanelNavigationStore` + `openCompose`/`requestBack`/`confirmDiscardAndReturnToList`/`handleParentTicketChanged`/`openChat` içeriyor
    - `grep -c "panelNavigation" src/store/root-store.ts` ≥ 2 (alan + instantiation)
    - panel-navigation-store testi yeşil (behavior bloğundaki 9 case dahil — özellikle `requestBack(true)` screen'i değiştirmiyor, `handleParentTicketChanged` üç dalı doğru)
  </acceptance_criteria>
  <done>Navigasyon durum makinesi ve dirty-guard dönüşleri deterministik ve testli; root-store'a bağlı.</done>
</task>

<task type="auto">
  <name>Task 3: app.tsx screen-swap + ComposeScreen/ChatScreen kabukları + "+" CTA enable (COMP-01)</name>
  <files>src/app.tsx, src/screens/compose-screen.tsx, src/screens/chat-screen.tsx, src/screens/components/empty-state.tsx, src/screens/conversations-list-screen.tsx</files>
  <read_first>
    - src/app.tsx (mevcut sabit `<ConversationsListScreen/>` render; provider nesting `StoreProvider > GrispiProvider`)
    - src/screens/conversations-list-screen.tsx (mevcut `observer` + `useStore()` kullanımı; ScreenHeader/ScreenTitle; header'a "+" eklenecek yer, lines 35-38)
    - src/components/ui/screen.tsx (Screen/ScreenHeader/ScreenContent; `ScreenHeader` `onBack` prop'u geri okunu render eder — D-01/D-02 mekanizması; yeni header component GEREKMEZ)
    - src/screens/components/empty-state.tsx (mevcut `disabled`/`aria-label="Çok yakında"`/`title="Çok yakında"` "+" Button — Faz 2 yalnızca `disabled` kaldırır + onClick bağlar, layout değişmez D-01)
    - src/store/panel-navigation-store.ts (Task 2 — `openCompose`/`requestBack`/`screen`)
    - src/contexts/store-context.tsx (`useStore()` StoreProvider içinde çağrılmalı)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-UI-SPEC.md (Copywriting Contract — compose header "Yeni Görüşme"; "+" aria-label "Yeni görüşme başlat"; Layout & Component Notes — ComposeScreen anatomy)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md (app.tsx conditional-render + observer wrap; ComposeScreen/ChatScreen analogları)
  </read_first>
  <!-- planner-discipline-allow: disabled -->
  <!-- planner-discipline-allow: Çok yakında -->
  <action>
    (a) `compose-screen.tsx` (YENİ) — `observer`-sarmalı `ComposeScreen`: `Screen` > `ScreenHeader` (`onBack={() => { if (!panelNav.requestBack(false)) return; }}`, başlık `ScreenTitle`="Yeni Görüşme") > `ScreenContent` içinde `flex h-full flex-col gap-2 p-4` bir `<div>` (bu fazda BOŞ kabuk — form alanları Plan 03'te eklenecek; alanlar için yer bırak). `panelNav = useStore().panelNavigation`. Şimdilik `isDirty` hep `false` (form yok) — Plan 05 dirty-guard'ı bağlayacak.
    (b) `chat-screen.tsx` (YENİ) — `observer`-sarmalı minimal `ChatScreen` STUB: `Screen` > `ScreenHeader` (`onBack={() => panelNav.confirmDiscardAndReturnToList()}` yani listeye dön, başlık placeholder) > `ScreenContent`. Faz 2 sonraki planlarında (04/05) gerçek balon/başlık gelecek; şimdilik `screen==="chat"` render edilince crash olmaması için var.
    (c) `app.tsx` — `App`'i `observer` ile sar; `useStore().panelNavigation.screen`'i oku; `StoreProvider > GrispiProvider` içinde `screen==="list" && <ConversationsListScreen/>`, `screen==="compose" && <ComposeScreen/>`, `screen==="chat" && <ChatScreen/>` koşullu render + `<DevTicketSwitcher/>` korunur. NOT: `useStore()` provider'ların İÇİNDE çağrılmalı → App'i sarmalayan iç `observer` component'e böl (bugünkü provider-then-content nesting korunarak).
    (d) `empty-state.tsx` — "+" Button'dan `disabled`/`aria-label="Çok yakında"`/`title="Çok yakında"` kaldır; `onClick={() => panelNav.openCompose()}` bağla; `aria-label="Yeni görüşme başlat"`; aynı `Button` element/className kalır (layout zıplaması yok, D-01). `EmptyState`'e `panelNav` erişimi için `useStore()` (component `observer` değilse de `openCompose` çağrısı yeterli) — ya `EmptyState` içinde `useStore()` çağır ya da onClick prop olarak yukarıdan geç (planner discretion: EmptyState zaten liste ekranı altında; `useStore()` doğrudan çağrılabilir).
    (e) `conversations-list-screen.tsx` — `ScreenHeader`'a bir "+" `Button` ekle (`size="icon"`, `PlusIcon`, `aria-label="Yeni görüşme başlat"`, `onClick={() => store... }` yerine `panelNavigation.openCompose()`); `useStore().panelNavigation` erişimini ekle. Header'daki mevcut `ScreenTitle` korunur; "+" görsel olarak header'a yerleşir (UI-SPEC — D-01, layout zıplaması yok).
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/compose-screen.tsx` ve `src/screens/chat-screen.tsx` mevcut; ikisi de `observer` ile sarılı ve Screen/ScreenHeader/ScreenContent kullanıyor
    - `grep -c "panelNavigation.screen" src/app.tsx` ≥ 1 ve app.tsx `observer` ile sarılı (ComposeScreen + ChatScreen + ConversationsListScreen koşullu render)
    - `grep -c "disabled" src/screens/components/empty-state.tsx` == 0 (CTA artık devre dışı değil) ve `openCompose` çağrısı bağlı
    - `grep -c "Çok yakında" src/screens/components/empty-state.tsx` == 0 (tooltip kaldırıldı)
    - conversations-list-screen header'ında "+" butonu var ve `openCompose` çağırıyor
    - `CI=true npx tsc --noEmit` hatasız
    - observable (Plan 05 UAT'ta doğrulanacak): "+"/boş-CTA → compose kabuğu açılır; geri oku → liste döner (COMP-01)
  </acceptance_criteria>
  <done>Navigasyon seam'i çalışır; temsilci "+"/boş-CTA ile compose kabuğunu açıp geri dönebilir (COMP-01); typecheck yeşil.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grispi SDK bridge → GrispiContext | `bundle.context.agent.email` güvenilir SDK bağlamından okunur (temsilci kimliği) |
| dev env var → GrispiContext (yalnız NODE_ENV=development) | `REACT_APP_DEV_AGENT_EMAIL` standalone dev fallback'i |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Spoofing | createTicket `creator` (`us.email`) kaynağı — agentEmail | medium | mitigate | `agentEmail` YALNIZCA güvenilir `bundle.context.agent.email`'den (plugin) ya da `REACT_APP_DEV_AGENT_EMAIL`'den (yalnız `NODE_ENV=development` kapısı, `standalone-dev.ts` aktivasyon kuralı) gelir; kullanıcı-girdisi creator'ı etkileyemez |
| T-02-09 | Elevation of Privilege | standalone dev fallback'in prod'a sızması | low | mitigate | `resolveStandaloneDevConfig` `env.NODE_ENV !== "development"` ise `null` döner (mevcut sıfır-prod-risk aktivasyon kuralı); agentEmail fallback aynı kapının arkasında |

Not: `high`+ severity tehdit yok → block-on high tetiklenmez.
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false --testPathPattern=plugin-bootstrap` yeşil
- `CI=true npx craco test --watchAll=false --testPathPattern=panel-navigation-store` yeşil
- `CI=true npx tsc --noEmit` hatasız
</verification>

<success_criteria>
- COMP-01: temsilci "+"/boş-CTA ile compose ekranını açar, geri oku ile listeye döner
- `agentEmail` plugin + standalone yollarında GrispiContext'te mevcut (createTicket creator kaynağı hazır)
- Navigasyon durum makinesi + dirty-guard dönüşleri testli
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-02-SUMMARY.md` when done.
</output>
