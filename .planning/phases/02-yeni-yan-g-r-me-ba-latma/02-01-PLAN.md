---
phase: 02-yeni-yan-g-r-me-ba-latma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/grispi.type.ts
  - src/lib/side-conversation.ts
  - src/lib/__tests__/side-conversation.test.ts
  - src/grispi/client/tickets.ts
  - src/grispi/client/customers.ts
  - src/grispi/client/api.ts
autonomous: false
requirements: [COMP-02, COMP-04]
user_setup:
  - service: grispi-live-tenant
    why: "Task 1 canlı probe, gerçek tenant'a bir POST /tickets ve bir GET /customers/search atar (A1-A5 doğrulaması). Faz 1'de kurulan .env.development.local (REACT_APP_DEV_TOKEN) + gsocial-test tenant erişimi yeterli — yeni kurulum yok."
    env_vars:
      - name: REACT_APP_DEV_TOKEN
        source: ".env.development.local (Faz 1'de mevcut) — canlı gsocial-test token'ı"

must_haves:
  truths:
    - "createTicket gövdesi canlı tenant'ta 201 döner ve dönen gövde yeni side ticket anahtarını (.key) taşır (A1 CONFIRMED)"
    - "customers.search çağrısı canlı tenant'ta 200 döner; zarf ve müşteri kaydı alan adları (isim + e-posta) parse edilebilir (A2/A3 CONFIRMED)"
    - "formatRequesterField(email) `:email` üretir (D-07); formatPrefillSubject(key,title) `[key] title` üretir (D-08); isValidEmail geçerli ve geçersiz e-postayı ayırır (D-06)"
  artifacts:
    - src/grispi/client/customers.ts
    - src/grispi/client/tickets.ts
    - src/lib/side-conversation.ts
    - src/types/grispi.type.ts
  key_links:
    - "grispiAPI.customers → Customers.search (api.ts facade wiring)"
    - "Tickets.createTicket gövdesi: comment.creator=[{key:'us.email',value:<agentEmail>}], fields ts.subject (her zaman mevcut), ts.requester=`:email`, tu.side_conversation_parent=<parentKey>, publicVisible:true (D-13)"
---

## Phase Goal (User Story)

> ROADMAP Phase 2 `**Goal:**` satırından türetildi (invented değil — yeniden biçimlendirildi).

**As a** Grispi temsilcisi (talep sahibini dahil etmeden harici taraflarla yazışmak isteyen),
**I want to** listeden "+" ile alıcı seçip konu ve mesaj yazarak yeni bir yan görüşme başlatmak,
**so that** bir side ticket oluşsun, alıcıya gerçek bir e-posta gitsin (talep sahibi görmeden) ve doğrudan o görüşmenin ekranına düşeyim.

Bu plan (01) o dilimin **temelini** kurar: canlı API şekillerini doğrular ve yazma/arama istemcileri + saf yardımcı fonksiyonları inşa eder. Kullanıcıya görünür ilk yetenek (compose'u açma) Plan 02'de gelir.

<objective>
Faz 2'nin yazma akışının temelini kur: (1) canlı-probe checkpoint'i ile `POST /public/v1/tickets` ve `GET /public/v1/customers/search`'ün gerçek cevap şekillerini doğrula (Assumptions Log A1–A5), (2) doğrulanmış şekle göre `grispi.type.ts` tiplerini yaz, (3) `Tickets.createTicket` (COMP-04) + `Customers.search` (COMP-02) istemcilerini ekle, (4) `formatRequesterField`/`formatPrefillSubject`/`isValidEmail` saf yardımcılarını TDD ile ekle.

Purpose: RESEARCH.md'nin Primary recommendation'ı — createTicket/RecipientField'ı **varsayılan** şekiller üzerine kurmayı önler; A1–A5 gerçek tenant'ta CONFIRMED/CORRECTED'e çevrilmeden hiçbir kod bu şekillere bağlanmaz. Faz 1'in Plan 01-02/Task 1 probe deseninin birebir tekrarı.
Output: Doğrulanmış tipler + iki yeni/genişletilmiş API istemcisi + üç saf yardımcı fonksiyon (testli).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-CONTEXT.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md
@.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md
@.planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md
</context>

<artifacts_produced>
Bu planın ürettiği/eklediği semboller (drift doğrulaması bunları YENİ kabul etmeli, mevcut kod sanmamalı):
- `src/types/grispi.type.ts`: `CreateTicketRequest`, `CreateTicketResponse` (veya doğrulanmış `Ticket` alias'ı), `Customer`, `CustomerSearchResponse` interface'leri
- `src/grispi/client/tickets.ts`: `Tickets.createTicket(body)` metodu
- `src/grispi/client/customers.ts` (YENİ dosya): `Customers` sınıfı, `Customers.search(params)` metodu
- `src/grispi/client/api.ts`: `GrispiAPI.customers` alanı + `grispiAPI.customers` erişimi
- `src/lib/side-conversation.ts`: `formatRequesterField`, `formatPrefillSubject`, `isValidEmail` saf fonksiyonları
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Canlı API probe — createTicket + customers.search şekillerini doğrula (A1–A5)</name>
  <files>src/types/grispi.type.ts</files>
  <read_first>
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md (Faz 1'in canlı-doğrulanmış sözleşmesi; `ts.requester` `:email` colon-prefix, `publicVisible:true`=public→e-posta, test parent `TICKET-563`)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Assumptions Log A1–A5, Common Pitfalls #1/#2/#3/#7, Open Questions #2/#3)
    - src/grispi/client/http-handler.ts (baseUrl `https://api.grispi.net`, `Content-Type: application/json`)
    - src/grispi/client/authentication.ts (header adları: `tenantId`, `Authorization: Bearer <token>`)
  </read_first>
  <what-built>
    Bu bir "yazma yapılmadan önce gerçeği doğrula" checkpoint'idir — Faz 1'in Plan 01-02/Task 1'inin aynısı. Executor, canlı tenant'a (gsocial-test) atılacak üç curl komutunu hazırlar ve human'a sunar; human token'la çalıştırıp ham JSON'ı yapıştırır. Hedef: A1–A5'i CONFIRMED/CORRECTED'e çevirmek. HİÇBİR istemci kodu (Task 3) bu doğrulama tamamlanmadan yazılmaz.
  </what-built>
  <how-to-verify>
    Executor, aşağıdaki üç probe'u tam curl komutu olarak üretsin (baseUrl `https://api.grispi.net`, header'lar: `tenantId: gsocial-test`, `Authorization: Bearer $REACT_APP_DEV_TOKEN`, `Content-Type: application/json`). Human bunları çalıştırıp cevabı yapıştırır:

    1. **createTicket 201 gövdesi (A1):** `POST /public/v1/tickets` gövdesi: `comment` = `{ body: "[TEST] Faz2 probe", publicVisible: true, creator: [{ key: "us.email", value: "<Davut'un agent e-postası, ör. davutkmbr@gmail.com>" }] }`, `fields` = `[ { key: "ts.subject", value: "[TICKET-563] Faz2 probe" }, { key: "ts.requester", value: ":faz2-probe-alici@example.com" }, { key: "tu.side_conversation_parent", value: "TICKET-563" } ]`.
       → SORU A1: Cevap 201 mi? Dönen gövdede yeni ticket anahtarı hangi alanda (`key`?)? `channel` ne geldi (Open Question #2)?
    2. **Boş subject (Pitfall #1):** (1) ile aynı, yalnızca `ts.subject` girdisinin `value`'su `""`.
       → SORU: 2xx mi yoksa 400 mü? (Değeri boş ama anahtar mevcut olduğunda kabul ediliyor mu?)
    3. **customers.search zarfı (A2/A3):** `GET /public/v1/customers/search?searchTerm=<tenant'ta var olan bir isim/parça>&orderBy=fullName&size=10&page=0`.
       → SORU A2: Zarf `{ content: [...] }` mı (advancedSearch gibi) yoksa düz dizi mi? SORU A3: Kayıt objesinde isim alanı adı ne (`fullName`?), e-posta alanı adı ne (`email` / `emails[]` / `primaryEmail`?)?

    Ek doğrulama: A4 (kayıtsız `:email` requester ile ticket'ın yine oluşup oluşmadığı — probe 1 zaten kayıtsız bir e-posta kullanıyor, oluşan ticket'ın requester'ı `/users/{id}.primaryEmail` ile kontrol edilebilir) ve A5 (creator'a yazılan agent e-postasının Grispi tarafında doğru kullanıcıya bağlandığı).
  </how-to-verify>
  <resume-signal>Üç probe'un ham JSON cevabını yapıştır ve A1–A5 için CONFIRMED/CORRECTED durumunu belirt. "Onaylandı" + JSON ile devam sinyali ver; sorun varsa (ör. `ts.subject` boşta 400) not düş — Task 3 tipleri buna göre yazılacak.</resume-signal>
  <action>
    Executor: yukarıdaki `how-to-verify` bölümündeki üç probe'u tam curl komutu olarak üret (baseUrl `https://api.grispi.net`, header'lar `tenantId: gsocial-test` + `Authorization: Bearer $REACT_APP_DEV_TOKEN` + `Content-Type: application/json`) ve human'a sun. Human token'la çalıştırıp ham JSON'ı yapıştırdıktan sonra A1–A5 verdict'lerini (CONFIRMED/CORRECTED + gözlenen şekil) SUMMARY'ye kaydet. Tip güncellemesi ve istemci kodu Task 3'te yapılır; bu checkpoint onaylanmadan hiçbir yazma kodu yazılmaz (varsayıma düşülmez).
  </action>
  <verify>
    <human-check>Human üç probe'un ham JSON cevabını yapıştırdı; A1 (createTicket 201 + yeni-ticket anahtar alanı), A2/A3 (customers.search zarfı + kayıt alan adları) ve boş-subject davranışı için CONFIRMED/CORRECTED belirtildi.</human-check>
  </verify>
  <done>A1–A5 canlı tenant'ta doğrulandı; probe JSON'ı ve verdict'ler SUMMARY'ye kaydedildi — Task 3 bunlara yaslanacak.</done>
  <acceptance_criteria>
    - A1–A5'in her biri için human'dan CONFIRMED veya CORRECTED (düzeltilmiş şekil) yanıtı alınmış olmalı
    - createTicket 201 cevabındaki yeni-ticket-anahtarı alan adı netleşmiş olmalı (ChatScreen'in Faz'daki yönlendirmesi buna bağlı — A1)
    - customers.search zarf tipi (content-sarmalı mı düz dizi mi) ve müşteri kaydının isim/e-posta alan adları netleşmiş olmalı (A2/A3)
    - Boş `ts.subject` değerinin kabul edilip edilmediği netleşmiş olmalı (Pitfall #1)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Saf yardımcı fonksiyonlar — formatRequesterField / formatPrefillSubject / isValidEmail</name>
  <files>src/lib/side-conversation.ts, src/lib/__tests__/side-conversation.test.ts</files>
  <read_first>
    - src/lib/side-conversation.ts (mevcut tek export `SIDE_CONVERSATION_PARENT_FIELD_KEY` + doc-comment stili — WHY yazan, D-XX/probe atıfı veren konvansiyon)
    - src/lib/__tests__/side-conversation.test.ts (mevcut test dosyası — yeni `describe` blokları buraya eklenir, YENİ dosya açılmaz)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Code Examples bölümü fonksiyon gövdelerini verir; Don't Hand-Roll — tek-kaynak isValidEmail, aşırı-sıkı RFC 5322 regex YASAK)
    - .planning/phases/01-temel-ve-salt-okunur-g-r-me-listesi/01-02-probe-findings.md (EK BULGULAR #3 — `ts.requester` `:email` formatı)
  </read_first>
  <behavior>
    - Test: `formatRequesterField("a@b.com")` → `":a@b.com"` (colon-prefix, D-07)
    - Test: `formatPrefillSubject("DESTEK-1042", "Kargo sorunu")` → `"[DESTEK-1042] Kargo sorunu"` (D-08)
    - Test: `formatPrefillSubject("X-1", "")` boşluk-trim'li tutarlı çıktı verir (kenar durum)
    - Test: `isValidEmail("a@b.com")` → true; `isValidEmail("bos")`/`isValidEmail("a@b")`/`isValidEmail("")`/`isValidEmail("a @b.com")` → false (D-06)
    - Test: `isValidEmail("  a@b.com  ")` → true (trim uygulanır)
  </behavior>
  <action>
    `side-conversation.ts`'e üç saf fonksiyon export'u ekle, her birine WHY-odaklı doc-comment + ilgili D-XX/probe atfı (mevcut `SIDE_CONVERSATION_PARENT_FIELD_KEY` stiliyle). `formatRequesterField(email: string): string` → `":" + email` döndürür (D-07 / 01-02-probe EK BULGULAR #3: yeni/var olan kullanıcıyı e-postayla bağlamak için `ts.requester` değeri baştan iki noktalı). `formatPrefillSubject(ticketKey: string, ticketTitle: string): string` → `` `[${ticketKey}] ${ticketTitle}` `` (trim'li) (D-08). `isValidEmail(value: string): boolean` → basit tek-kaynak kontrol `^[^\s@]+@[^\s@]+\.[^\s@]+$` benzeri, `value.trim()` üzerinde (D-06; Don't Hand-Roll: sunucu gerçek doğrulamayı yapar, client-side yalnızca UX — aşırı sıkı regex geçerli adresi reddetmemeli). Testleri mevcut `side-conversation.test.ts`'e yeni `describe` blokları olarak ekle (behavior bloğundaki case'ler).
  </action>
  <verify>
    <automated>CI=true npx craco test --watchAll=false --testPathPattern=side-conversation</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function formatRequesterField" src/lib/side-conversation.ts` ≥ 1
    - `grep -c "export function formatPrefillSubject" src/lib/side-conversation.ts` ≥ 1
    - `grep -c "export function isValidEmail" src/lib/side-conversation.ts` ≥ 1
    - side-conversation testleri yeşil (behavior bloğundaki tüm case'ler dahil)
    - Mevcut `SIDE_CONVERSATION_PARENT_FIELD_KEY` export'u ve testi bozulmamış
  </acceptance_criteria>
  <done>Üç saf fonksiyon export edilmiş, doc-comment'li ve testli; tek yeni test dosyası açılmadı (mevcut dosyaya eklendi).</done>
</task>

<task type="auto">
  <name>Task 3: Yazma/arama istemcileri — createTicket + Customers.search + facade wiring (probe-doğrulanmış tiplerle)</name>
  <files>src/types/grispi.type.ts, src/grispi/client/tickets.ts, src/grispi/client/customers.ts, src/grispi/client/api.ts</files>
  <read_first>
    - Task 1 probe çıktısı (canlı JSON) — tipler ve `createTicket` gövdesi buna göre yazılır; varsayıma DÜŞÜLMEZ
    - src/grispi/client/tickets.ts (mevcut `advancedSearch` POST kalıbı — `this.http.send<T>(url, { method, cache:"no-cache", headers:this.auth.headers, body:JSON.stringify(...) })`; ayrıca lines 29-34 phase-scoping doc-comment'i createTicket eklenince güncellenmeli)
    - src/grispi/client/users.ts (yeni `customers.ts` bunun birebir kalıbı — constructor-injection `http`/`auth`, doc-comment ile endpoint'in undokumente olduğunu + probe doğrulamasını belirtme)
    - src/grispi/client/api.ts (mevcut `tickets`/`users` triplet'i — `customers` için aynı üç satır: alan + constructor + instantiation)
    - src/types/grispi.type.ts (mevcut `AdvancedSearch*`/`SideTicketSummary`/`GrispiUserProfile` tip kalıbı + undokumente-şekil doc-comment konvansiyonu)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-PATTERNS.md (tickets/customers/api analogları, satır atıfları)
    - .planning/phases/02-yeni-yan-g-r-me-ba-latma/02-RESEARCH.md (Common Pitfalls #1/#2/#3 — ts.subject anahtarı ASLA omit edilmez, publicVisible HER ZAMAN true, creator zorunlu)
  </read_first>
  <action>
    (a) `grispi.type.ts`'e Task 1 probe'undan doğrulanmış tipleri ekle: `CreateTicketRequest` (`comment: { body: string; publicVisible: true; creator: [{ key: "us.email"; value: string }] }`, `fields: Array<{ key: string; value: string }>`), createTicket cevabı için tip (probe A1'e göre `.key` taşıyan obje — mevcut `Ticket` yeterliyse onu kullan, değilse `CreateTicketResponse` tanımla), `Customer` (probe A3 alan adlarıyla — isim + e-posta), `CustomerSearchResponse` (probe A2 zarfına göre — `content`-sarmalı ise advancedSearch benzeri, düz dizi ise ona göre). Her yeni tipe "CONFIRMED live (Plan 02/Task 1 probe)" doc-comment'i ekle.
    (b) `tickets.ts`'e `async createTicket(body: CreateTicketRequest)` ekle: `this.http.send<...>("public/v1/tickets", { method: "POST", cache: "no-cache", headers: this.auth.headers, body: JSON.stringify(body) })` — `advancedSearch` kalıbının birebir aynısı. Body'i çağıran (Plan 04 ComposeStore) kurar; bu metot yalnızca gönderir. Sınıf üstündeki phase-scoping doc-comment'ini (createTicket artık Faz 2'de eklendiği için) güncelle. Content-Type multipart DEĞİL, `application/json` (Open Question #1 — bu fazda dosya eki yok).
    (c) `customers.ts` (YENİ) — `users.ts` kalıbında `Customers` sınıfı: `async search(params: { searchTerm: string; size: number; page: number })` → `URLSearchParams` ile `searchTerm`, `orderBy=fullName`, `size`, `page` (0-indexli) query'si kurup `this.http.send<CustomerSearchResponse>("public/v1/customers/search?" + query, { method: "GET", cache: "no-cache", headers: this.auth.headers })`. `size=10` başlangıç değeri (Open Question #3 — 372px panel için 100 aşırı; discretion planner'a bırakıldı → 10). Doc-comment: endpoint public spec'te response şeması yok, zarf Plan 02/Task 1 probe'unda doğrulandı; CONTEXT.md canonical_refs atfı.
    (d) `api.ts`'e `Customers` import'u + `readonly customers: Customers` alanı + constructor'da `this.customers = new Customers(this.httpHandler, this.authentication)` — mevcut `tickets`/`users` triplet'iyle simetrik.
  </action>
  <verify>
    <automated>CI=true npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async createTicket" src/grispi/client/tickets.ts` ≥ 1
    - `grep -c "public/v1/tickets" src/grispi/client/tickets.ts` ≥ 1 ve createTicket `method: "POST"` kullanıyor
    - `src/grispi/client/customers.ts` mevcut ve `class Customers` + `async search` + `public/v1/customers/search` içeriyor
    - `grep -c "customers" src/grispi/client/api.ts` ≥ 2 (import/alan/instantiation) ve `grispiAPI.customers` erişilebilir
    - `CI=true npx tsc --noEmit` hatasız geçer (tipler Task 1 probe şekliyle tutarlı, `any` kaçağı yok)
    - createTicket body tipinde `publicVisible: true` (literal true) ve `creator` zorunlu; `ts.subject` girdisi çağıran tarafça her zaman verilebilir biçimde tiplenmiş (omit gerektirmez)
  </acceptance_criteria>
  <done>createTicket + Customers.search istemcileri probe-doğrulanmış tiplerle eklendi ve facade'a bağlandı; typecheck yeşil.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → Grispi Public API | Tüm yazma/arama istekleri buradan geçer; canlı bundle token'ı taşınır |
| temsilci girdisi → createTicket gövdesi | Alıcı e-postası (serbest metin) ve subject temsilciden gelir, `fields`'e yazılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Information Disclosure | createTicket `ts.subject` (D-08 — iç talep anahtarı dış e-posta konusuna gider) | low | accept | D-08 kilitli karar; konu düzenlenebilir (temsilci kontrolünde). İstemci yalnızca gövdeyi taşır; ek maruziyet yok |
| T-02-07 | Repudiation | serbest e-posta → `ts.requester` (kayıtsız adrese e-posta) | medium | mitigate | `isValidEmail` client-side UX kapısı (Task 2) + tek alıcı; asıl doğrulama sunucuda (`ts.requester`). İstemci arbitrer adrese yazmayı kolaylaştırmaz, format kontrolü ekler |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Bu fazda YENİ paket kurulmuyor (RESEARCH.md Package Legitimacy Audit) — tedarik-zinciri yüzeyi yok; SLOP/SUS paket yok |

Not: Bu planın yüzeyinde `high`+ severity tehdit yok → block-on high tetiklenmez. XSS (T-02-02, high) render katmanında; Plan 04'te ele alınır.
</threat_model>

<verification>
- `CI=true npx craco test --watchAll=false --testPathPattern=side-conversation` yeşil
- `CI=true npx tsc --noEmit` hatasız
- Task 1 probe çıktısı (A1–A5 verdict'leri) SUMMARY'ye kaydedilmiş; Plan 04'ün createTicket body kurulumu bu doğrulanmış şekle yaslanacak
</verification>

<success_criteria>
- A1–A5 canlı tenant'ta CONFIRMED/CORRECTED (varsayımdan kod inşa edilmedi)
- `Tickets.createTicket` + `Customers.search` + `grispiAPI.customers` mevcut, typecheck yeşil
- `formatRequesterField`/`formatPrefillSubject`/`isValidEmail` export'lu ve testli
</success_criteria>

<output>
Create `.planning/phases/02-yeni-yan-g-r-me-ba-latma/02-01-SUMMARY.md` when done. Probe çıktısını (A1–A5 verdict'leri + ham JSON özeti) SUMMARY'ye ekle — Plan 04 buna bağımlı.
</output>
