# The build/ directory is committed to this repo and IS the deploy
# (grispi.app serves it directly). GENERATE_SOURCEMAP=false keeps the JS
# source map (~3.4 MB) out of every rebuild commit — git keeps it forever.
GENERATE_SOURCEMAP=false
